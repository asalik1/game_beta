# Emberfall — Balance & Pacing Round Log

The chronological record of every balance/pacing round: the trigger, the numbers moved, and the experiments later superseded. Moved out of DESIGN.md 2026-07-06 — **DESIGN.md keeps only the distilled standing rules** (its "Pacing & balance — standing rules" section); this file keeps the full history. **New rounds get logged here, at the TOP of the tuning-round list**; when a round produces a durable rule, promote the rule (not the narrative) to DESIGN.md.

## Tuning rounds (newest first)

- **Resonance band leans — Hunger / Constancy (2026-07-09, player-directed):** resonance was almost pure narrative (dialogue gating, the ±10% band haggle, one-time shrine payouts, idle emotes — the keystone layer stays deferred to Act 2), so it gained an everyday mechanical layer: a **conviction-scaled passive rider** — strength ramps with |resonance| from the band line (±25, zero through the neutral band — its emptiness is the pull) to full at ±100, flavor by sign. **Virtue = Constancy**: health potions mend up to +25% deeper (`RES_CONSTANCY_HEAL_MAX`; action-gated sustain per the safety→skill doctrine, never free DR), stacking with the existing steady 0.9 haggle so steady owns the spend side. **Temptation = Hunger**: up to +10% damage to mobs below 25% HP (`RES_HUNGER_EXEC_MAX`/`_AT` — mobs ONLY, player ruling: boss execute windows stay design-owned; implemented beside the assassin's Coup de Grâce in `hit_enemy`) and up to +15% kill gold (`RES_HUNGER_GOLD_MAX`, applied where kill gold is rolled in `game_flow._kill_gold` — mob and boss piles both; greed still applies later at pickup), so tempted owns the earn side — mirrored economic identities, no correct band. Authored-content check: ~276 resonance-moving options exist; a committed player saturates ±100 around end of ch2, a waverer never does, and every source is one-time so conviction can't be farmed. The inline 0.6 potion heal moved to `Balance.POTION_HEAL_FRAC` in passing. Surfaced per the visible-numbers rule: Stats-tab "Shard lean" row + the HUD shard popover both print the live rider values. **Benchmark hygiene:** dps_bench pins `resonance = 0` per case and autotest gained `_test_res_lean` (3d13b: ramp shape, band-line onset, side exclusivity, kill-gold plumbing). Companion piece (same session, built): **GOLD RUSH** — greed stays permanently sourceless and becomes the farm-event stat via a rare charged coin from PAYING trash kills (`GOLDRUSH_DROP_CHANCE` 0.01 ≈ 1/replay run) that surges greed +30% (`GOLDRUSH_GREED`) for 150s (`GOLDRUSH_DUR`) on touch — auto-trigger via a new Pickup flavor (oversized HDR coin, coin magnet, never a bag item, refresh-don't-stack), drop-only never shop-sold, buff-bar chip + live Stats-tab greed row (`current_greed()` is now the single consumption accessor for gold gain and the chest-odds nudge). Renamed from the planned "Gilded" to avoid colliding with the weekly "Gilded Blood" modifier (that one scales kill gold at the drop; this rides the greed stat at the gain — they stack, deliberately, as the weekly's farm fantasy). Expected impact ~8% uptime × ~30% ≈ +2-3% run income — a slot-machine beat, not an economy dial; econ_audit unchanged (it doesn't model the coin). Autotest 3d13c covers the sourceless baseline, the window pay bump, and the touch trigger (incl. no bag slot). **Same-day follow-up (player review):** (1) Hunger's execute now bites only prey that PAYS (`gold_value > 0`) — boss summons, spawner sprouts and event mood spawns all zero it by convention, so a boss's adds die at the fight's own pace and the exclusion is self-maintaining for future summons. (2) **Measured Hunger-vs-haggle** (econ_audit replay runs): kill gold (mob+boss piles) is 63%→86% of replay income ch1→ch7, so full-conviction Hunger pays +9.4%→+13.0% of income per run — versus the band haggle moving 10% of SPEND each way (steady 0.9 saves it, tempted 1.1 pays it). Net vs neutral: tempted = `+~12% income − 10% spend`, steady = `+10% spend` → **tie when merchant spend ≈ 47% (ch1) → 65% (ch7) of income; heavy spenders (gamble-pity chasers, potion stockers — the progression phase, where build step ≈ 2-4 replays implies spend≈income) do better STEADY; hoarding farmers do better TEMPTED, and the late-game gold-bloat watch item pushes endgame toward tempted.** Ruled healthy for now — the pre-existing ±10% haggle is the counterweight that keeps Hunger from dominating, and the split maps to identity (virtue realizes value through the world's merchants, temptation hoards raw coin). Known asymmetry, logged not fixed: the haggle is FLAT from the band line while Hunger ramps with conviction, so a shallow-tempted player (-25..-40) is economically behind everyone; if it grates in play, the candidate fix is conviction-scaling the haggle too. a top-to-bottom review (economy/progression, class kits/talents, combat systems) rated the game **MID overall** — the authored numbers read hardcore (boss hits 30%→74% of squishy HP, TTK 40-60s) but two SYSTEMS quietly pulled real difficulty down: the shared 0.6s hurt_cd (capped ALL incoming at ~1.67 hits/s, let a chip hit eat any boss telegraph, made plate facetank whole rooms) and a zero-cost death loop (bosses pure attrition). The review also verified clean: no economy exploit loops, no ult-combo/perma-iframe, mana healthy, theme identity split holds, XP budgets accurate, realistic-kit ladder (B+Lv2 ≈ 52% of BiS power) keeps the BiS shape. Fixes landed this round: (1) **HEAVY hits pierce the hurt_cd gate** — `take_damage(heavy)` + `hurt_was_heavy`: telegraphs (`telegraph`/`telegraph_safe`) are heavy and pierce a chip-armed gate; heavy-vs-heavy still gates (no overlapping-tile double-taps); deliberate i-frames (Tumble, dash-strike, Judgment leap, Death Mark execution) mark the gate heavy so a TIMED dodge still dodges telegraphs — earned, not passive. Kills the generalized Vess-wail cheese; pack chip stays readable. Four-way gate matrix added to autotest. (2) **DEATH GOLD TITHE** (`DEATH_GOLD_TITHE` 0.10): dying costs 10% of carried gold, shown as a "-N gold" floater — attrition now has a price; respawn/boss-reset unchanged. (3) **BULWARK/AEGIS guard_buff TRIMMED** (free-stat purge, same doctrine as the shadow/plate-DR rounds): the 2.5s refresh-on-cast guard on ≤0.8s basics was a permanent +70-90 free resistance that double-fed plate's res-scaled DR and res→damage; values cut to Cleave 30 / Bash 40 / Whirlwind 35 / Aegis 25 (mechanic kept — it's the identity). (4) **HUNT ARCHER: EVERY 4th SHOT CRITS** (`HUNT_RHYTHM_SHOTS` 4, player-picked Jhin rhythm): the flat +25% CAP-EXEMPT crit on the 0.36s Quick Shot (≈60% effective crit, gear crit redundant) is deleted; a rhythm counter arms the existing `next_crit` guaranteed-crit path on every 4th hunt shot, with a white-hot muzzle tell. Benched at **3135 DPS** — inside the old 2815-3175 band, power-neutral: the crit is now earned rhythm + built gear. S-weapon parity RULED INTENTIONAL (player, same day): plate legendaries deal more because plate scales less from damage stats — the legendary is their scaling compensation, not a bug. Deferred from the review (flagged, not fixed): late-game gold bloat (faucets scale ×5.68, sinks flat), Serpent's Due trivial +40%, dominated cd-micro talent cells, TIERLIST re-baseline. Full `test.bat` green on the combined tree.
- **Economy pacing batch — potion investment, gamble pity machine, C sockets (2026-07-09, player-approved spec):** three linked economy moves. (1) **POTIONS BECOME AN INVESTMENT** (player's own design): the old system handed potions out — start with 2, every boss kill restocked to `BOSS_KILL_POTION_FLOOR` 2 — so the 25g potion was a merchant line nobody needed. Now stock is **BOUGHT, period**: heroes start dry (`player.potions` 0), the boss-kill restock is deleted, and nothing re-grants on death/retry (bought stock carries across chapters — it's owned property, not run state). **Teaching exception, anti-farm:** entering ch1/2/3 grants ONE free health potion in a separate `potions_free` counter that **EXPIRES on leaving the chapter** — `switch_chapter` sets it absolutely (1 in ch1-3, 0 elsewhere), so revisits can't stack freebies; it's drunk FIRST, never sellable, and rides the save with its chapter (load re-grants, then the save's value overwrites — drunk stays drunk). **Price scales with progression** (`Balance.potion_price`: 25g base × `POTION_PRICE_MULT` 1.0/1.4/1.8/2.4/3.2/4.0/5.0 ch1-7 → 25/35/45/60/80/100/125g — potions heal % HP so value scales; the mult tracks the measured g/min climb, holding farm-time-per-potion at ~30-40s); SELL stays on the flat ch1 base (no buy-cheap-early/sell-late arbitrage). **Per-room use cap goes chapter-banded** (`Balance.potion_slots` replaces `POTION_SLOTS_BY_ACT` — act 1 spans 7 chapters, one flat cap couldn't ramp): ch1-2 = 1, ch3-4 = 2, ch5-7 = 3, act 2 = 4 / act 3 = 5 latent. (2) **GAMBLE BECOMES THE BOSS-BAND PITY MACHINE:** the round-51 gamble priced at the GENERAL band's cap grade (×0.8) but paid a general-band roll — in ch7 that's ~10.4k for a 94%-C table whose items sell for ~13g; a dead feature. It now **rolls the chapter's BOSS table** (`CHAPTER_BOSS_WEIGHTS` — ch7 B70/A30, the piece you can't farm out of chests) and is priced against what it pays: `cost = Σ(weight_g × farm_price(g)) × GAMBLE_DISCOUNT 0.8` (boss-table-weighted expected farm cost, the honest round-51 machinery) → ch2 ≈ 1.3k (E100), ch5 ≈ 7.4k (C75/B25), ch7 ≈ 13.3k (B70/A30) before haggle. The dead `Balance.GAMBLE_COST` const (wood/silver/gold — nothing read it) is deleted; econ_audit's `_sinks` and DESIGN.md now quote the real formula. (3) **C-GRADE GEAR GETS 1 SOCKET** (sockets extend down a tier): `GEM_SLOTS` C 0→1 (regular-only; the A+ special slot is untouched), `GEM_LEVEL_LIMIT` C 0→**2** (exactly what ch4-5 drop: act-1 floor Lv1, elite/first-clear Lv2), and reforge "add socket" admits C (cap 1+1=2). Rationale: regular gems start dropping in ch4, but ch4-5 are C-BAND chapters — under B-gated sockets the new currency idled for two chapters. Old saves need no migration (`gem_slots` is stamped per-item at roll time; `_fix_item` tolerates legacy C items at 0 slots — they just predate the socket). Autotests updated: typed-slot test covers C (regular fits, special refused), reforge test covers the C socket cap, gamble test asserts the boss-band grade + formula price. Full `test_quick` green; codex (gems card, consumables card) and HUD potion tooltip updated in the same pass.
- **Mob distribution + skirmisher pass (2026-07-09, player-directed; commit d9310b2):** the mob-tier version of the boss audit — "some rooms melee-majority, some ranged-majority, the spread controlled so neither dominates" — three axes, all measured with headless probes. (1) **ROOM BANDING:** chapter totals ramped sanely (17%→31% ranged) but the per-room texture didn't — ~3 big ALL-MELEE rooms every chapter (always the openers + a side farm) and only THREE rooms game-wide crossed 50% ranged (all 2-mob flavor closets). Every chapter's combat rooms are now dealt across bands: **~3 melee-heavy** (openers keep their teaching role) / **mixed 25–45% core** / **2–3 ranged-heavy 55–67% "artillery rooms"** (Witchlight Fen 64, Inner Ward 60, Howling Fields 63, Sanctified Ruins 67, Endless Procession 64, Widow's Row 63, Foundry Concourse 64, Sermon Road 57, Processional Ice 67, Hymn Road 58, Root Vault 58, Nursery 56, Screaming Heights 58, Hollow Relay 56). Re-authored ~5 rooms/chapter by kind-swapping existing spawn rows — positions and pack structure preserved. **Root cause fixed:** ch3–7 each had exactly ONE native ranged kind (vale_mourner / forge_acolyte / hushcaller / bloom_acolyte / static_caller), so a ranged room meant five identical robed statues; variety now comes from **cross-chapter imports at level overrides** (cultist/stormcult/plague_chanter/null_acolyte travel — sprites already exist, the `[kind,x,y,pack,level]` param already supported it). (2) **SKIRMISHER KIT** — the missing ranged archetype: every ranged mob in the game was a speed-90–105 turret whose 0.8× backpedal a 250-speed player just walks down. New `skirmish` trait: **full-speed backpedal on a strafing arc** inside `MOB_SKIRMISH_KEEP` (280) — it genuinely kites, and CORNERING it against a wall is the earned counterplay (codex TRAIT_DESC teaches exactly that). One new cross-chapter kind **wildkin_ranger** (the unused elf_ranger sprite, base L8, travels like the cultist) + **beastkin_howler retagged** (speed 150, already the mobile one). They debut in ch2's Howling Fields (now THE beastkin skirmish line) and reappear in the overgrowth (ch6) and steppe (ch7). (3) **XP-BUDGET INTEGRITY** (caught mid-pass by a before/after probe): imports ride `reward_m` off a LOW base level and overpaid 3–4× vs the natives they displaced — measured **+24–36% chapter trash XP** in ch1/ch5/ch6/ch7, violating the authored-budget doctrine. Spawn rows gained an **optional 6th param = authored XP override** (game_world; same spirit as boss summons paying zero), and every import is pinned to the displaced kind's at-level XP. Re-measured: ch1 exactly neutral, ch3–7 within ±3%. (4) **CH3 STAT LIFT:** the L16–19 band was INVERTED vs ch2's optional-terrain heavies (authored bases regressed — void_husk 260hp@L15 → gravewalker 120hp@L16; vale_mourner@18 hit exactly as hard as tomb_warden@11). Lifted onto the continuing curve: gravewalker 120/24→**185/29**, barrow_wight 155/27→**205/32**, vale_mourner 125/30→**160/35** (ranged support stays lighter), casket_creeper 140/33→**180/37**; XP untouched (budgeted). **Kept deliberately:** the late-trash damage creep (~13% of squishy HP at ch1 → ~27% at ch7 — a 4-pack alpha ≈ full HP) matches the bullet-hell density doctrine; logged as a watch item. Full `test.bat` green (all chapters end-to-end on the new tables); skirmisher kite-feel (keep 280 / strafe 0.45) flagged for a by-taste dev-mode eyeball.
- **Endgame balance arc — L100 stress-test to shippable (2026-07-09, player-directed; commits 6048ecc / 469d927 / a4bc5ec + an uncommitted boss-anti-kite pass):** a long session that started from "what does a maxed L100 character vs a L100 boss look like" and cascaded into five linked passes. The DPS bench gained `--level / --grade / --gemlvl / --boss` (arbitrary scenarios) and `--defense` (an EHP + damage-taken-per-attack readout), and confirmed attributes were always allocated (all-in-primary). (1) **ENDGAME SCALING (the core fix):** a L100 boss had **184× HP** (30-min fights) and **32× damage** (one-shot every class, tanks included, by 4–16×) because boss HP/dmg grew exponentially per-level (~8%/lvl HP, 5.5%/lvl dmg) and compounded over the 59-level gap. Recalibrated to TRACK THE PLAYER CURVES over that range: `BOSS_HP_GROWTH` 0.018 (tracks player DPS → level-invariant TTK) and `BOSS_DMG_GROWTH` 0.055→0.015 (tracks player EHP → level-invariant hit-danger); folded the double-counting boss gem ramps into the growth rates. **Bosses-only; native-level play (d=0) is untouched.** L100 Cyrraeth: 23.9M→372k HP (~32s TTK = L40), 22,877→1,727 dmg (plate survives, squishy dodges — the native-L40 dynamic). Reframed the old "+10 = 1.7× wall": that WAS the bug that compounded into one-shots; scaled bosses now stay at parity, and an intentional Nightmare +N would be a separate explicit multiplier. Updated the two autotest assertions that encoded the old model. (2) **DISTRIBUTION (L100 ladder made to match the L40 shape):** plate res→damage linear+cap → **LOG curve** (diminishing, breaks "tanky AND top-DPS"); **greed gem PURGED from the bench presets** — of the 5 specials only dmg_pct/combo/cdr add damage, so max-DPS is always those three + lifesteal, and cdr weakly dominates greed for every class (warlock jumped +36–43%); mage Firebolt **mp 4→2** (S-gear regen makes cdr affordable → caster cadence finally scales); **S set bonuses became the endgame DPS dial** (S-gear only, inert at L40 — glass cannons get offense, plate a small lift); **`CLEAVE_FLOOR_CD`** (cdr can't pierce it except in Berserk — "plate hits hard, not fast", endgame-only). Warrior's ult already ignored cdr (all ults do). (3) **LATE-BOSS DAMAGE RAMP (469d927):** playtest found even hard-to-dodge chip hits were ~90% of a squishy's HP at endgame. A blunt global `BOSS_DMG_MULT` halving was tried and **reverted** (it made already-gentle EARLY bosses trivial — fangmaw 26%→13%). Audited all 27 bosses' hit-% vs a level-appropriate squishy (`--defense`) — the problem was a CLUSTER of ch5-7 near-one-shotters, not a baseline issue. Trimmed `base_dmg` on the 10 offenders only (ch1-4 untouched): ashpriest 150→125, whitepelt 160→135, icebound 190→140, sleepkeeper 230→150, auroch 250→178, gardener 270→185, curetwisted 330→205, veyx 350→205, echo 375→260, stormmouth 460→275. Clean monotonic ramp — worst hit ~30% (intro) → ~74% (finale), NOTHING one-shots; Nightmare ×2 restores the lethal tuning. (4) **TENACITY GEM (469d927):** the DPS-dead greed gem repurposed → Tenacity (`flat_dr`, ~13% DR at Lv10, one per character) — the squishy survival lever. The greed STAT survives but is now sourceless (a temp farm-event buff, "Gilded" surge, is the planned new source — see [[resonance-greed-mechanical-layer]]). (5) **PLATE DR EARNED BY RESISTANCE (a4bc5ec):** plate's flat 15% DR made early plate near-untouchable (8–15%/hit, facetank window widest at the start). Now the DR **ramps 0 → `PLATE_DR_MAX` (0.15)** as the class's SIGNATURE res climbs to `PLATE_DR_FULL_RES` (130) — warrior scales on physres, paladin on magres, computed in recalc from built res (gear-gated, diminishing, no flat double-dip). Early plate blocks ~9% and must respect mechanics; endgame ~12–13%, full 15% is a res-invested tank reward. Made the two tanks MIRROR-image: warrior stays phys-lean (80/45, wards steel), paladin flipped **magic-lean (60/60→50/75, wards spell)** — each tankiest vs its signature damage type. (6) **BOSS ANTI-KITE DENSITY (uncommitted):** re-audited melee-vs-ranged fairness by READING the code (an earlier grep-signal audit falsely flagged ~6 bosses — the r51 "kite answer" pass + caster/artillery/anti-turret designs already handle most). The real hole was **ranged threat DENSITY**: three bosses gave a kiter free real-estate — fangmaw & cinderhide (charge-only, zero projectiles between charges) and sexton (single floor tell every 4.5s). Fixes: fangmaw gets a **ground-rake fissure** that races out past you along your lane; cinderhide's **ember rain ungated** (was enrage-only <30% HP → now the whole fight); sexton's floor **single → 3-tell cluster** (reposition, not sidestep-one-circle). 🟡/🟢 bosses (fans, ring/nova casters) untouched. All passes: full `test.bat` green; path-scoped around a sibling agent's UI commits.
- **Gem-system overhaul + shadow-assassin rework (2026-07-08, player-directed; commit 6bb2fee; supersedes the previous round's "4× Ruby/ATK% + Haste-to-cap" build assumptions):** two linked passes off the DPS bench. **Gems** — the optimizer had collapsed to "all Ruby" degeneracy, so the socket economy was re-founded on **typed slots**: A/S gear unlocks ONE dedicated **special** slot (special gems only — `cdr`/`lifesteal`/`combo`/`greed`/`dmg_pct`), B gear stays regular-only, and you may equip **one special gem per STAT across all four pieces** (kills 4×Haste stacking). `crit_dmg` removed from gear entirely (gem-only path); Sunstone flipped from crit-only `crit_dmg` to a universal **`dmg_pct`** "damage increase" special (rides `atk_pct`'s multiplier, so any crit level wants it). **Ruby (atk) is now FLAT** (`atk_flat`, +11.4 atk/gem at Lv6, non-scaling) instead of `atk_pct` — stacking regulars can't runaway; it's early-game filler while the special slot is the endgame chase. Plate classes (warrior/paladin) gained a small **capped resistance→damage** kit passive (`PLATE_RES_DMG_SCALE`/`_CAP` 0.30) so they lift but never top the charts. Set bonuses re-cut role-weakness (tanks offense; squishies VIT+res+critres, no evasion). **Shadow assassin** (all shadow-only — poison 2754 / blood 2858 verified unchanged): the spec handed you ~45% *free* crit just for picking it (per-ability `crit_bonus`), saturating your best secondary stat so crit gear was redundant — and its signature "stunned/slowed prey always crits" (opportunist) was **dead on every boss** (bosses convert stun/slow to concussion, never setting the state). Fixes: **a1** opportunist → "**marked/EXPOSED prey always crits**" (keys on `vuln_time`, which bosses honor), free crit deleted, +5% **combo** instead (folds into the combo stat via a new theme-passive aggregation in `recalc`); **a2** Phantom Step free crit deleted, `kill_refund` now fires on **ANY kill within a 2s window** after the dash (`PHANTOM_REFUND_WINDOW`; the Fan/ult-stab that actually kills — resolves in `hit_enemy` — not just the dash's own hit), making it the kit's real mobility button; **a3** Fan `crit_bonus` 0.15→0.10; **s31** Phantom `crit_dmg` 3%→2%/pt; **Death Mark vuln is now per-theme** (`enemy.vuln_mult`) — shadow's ult a leaner **+40%** (its power is the marked-window crits, not the amp), poison/blood keep +50%. Crit is now **earned** (mark window) + **built** (gear/gems), never a passive floor. **Measured**: shadow boss DPS **3805 → 3574 mean** (new `dps_bench_rep.ps1` — fans N parallel copies of ONE case, each an independent RNG stream via `--rep=N`, reports mean/min/max/spread), on the player's accepted ~3500 target. The ~25% lead over the pack that remains is the **fan-spam-on-a-single-boss** sim tax the player explicitly accepted: `--knifeprobe` proved the 5-blade Fan lands **~4.1 of 5** on one boss regardless of range (the assassin dashes/teleports to point-blank; `--standoff` sweep flat 3745-3772) — real boss fights don't let you camp there. **Watch item (deferred):** hunt archer still stacks the same cap-exempt free crit — fine on the numbers now (~2815 boss), revisit if it drifts. Full suite green (both chapters). Committed path-scoped alongside a sibling agent's HUD-hide + vargoth-sprite work (left untouched, incl. the mixed `menus.gd`).
- **Optimization re-baseline — bench flips to optimal builds (2026-07-08, no balance changes):** the DPS bench presets moved from *save-copies* to the derived **max-DPS build per variant** (attributes all-in-primary; regular gems 4× Ruby/ATK% universally — CritDmg only wins above ~50% eff crit, which no variant reaches; special gem Haste-to-cap for free-basic classes (warrior/paladin/assassin) and Combo for the mana-bound (archer/mage/warlock) whose cooldown abilities starve under Haste; talents = best-DPS cell per row, incl. Serpent's Due for Venom and Killing Frost for Ice). **Structural finding (no fix made, logged as a watch item):** the **manaless assassin** is the only class that pairs Haste-to-cap with full Ruby at ZERO mana tax, so under optimal builds it takes boss #1/#3/#4 (Shadow 5550 / Blood 4806 / Poison 4078) and pack #2/#4/#5 — the round-49 hand-authored target ladder **no longer holds** (Poison boss #13→#4, Venom #12→#6, Fire #11→#14; the Fire~Poison~Venom tie broken). TIERLIST.md rewritten: frame documents the optimized builds (§1/§8), §2 is now a MEASURED re-baseline (not a hand-authored contract), §3 re-graded (both assassins A+, Poison the big riser), §4/§5 carry the verified numbers, §10 logs the assassin-dominance finding + "old ladder retired." Verified by re-running both boards (the pass's pasted numbers weren't saved to disk, so they were re-measured before writing — reproduced within ±5%). Reports are UTF-16 (decode, don't ASCII-grep). No kit/tuning numbers touched — pure measurement + documentation.
- **Round 51b — dormant legendary passives + awakening (2026-07-07, player ruling on round-51's flagged S-weapon question):** S weapons DO drop and sell, but the round-51 flag was real — `roll_item_of` stamped the signature passive live, so a looted legendary was fully active with no gate. Fix: a dropped/bought S weapon now carries `passive_dormant: true` (keeps its NAME + top S stats — "I found it but it sleeps"), and its passive grants NOTHING until the character completes a short per-class **awakening quest** that sets `s_awakened_<cls>`. Single gate: **every kit/effect reads the passive through `Player.s_passive()`**, which returns "" for a dormant weapon whose class flag is unset — so no per-effect edits were needed. The flag persists across chapters (added to `KEPT_FLAG_PREFIXES`) and saves. `describe()`/`diff_text()` gained an `awakened` arg and show "★ <Passive> — LOCKED (awakening)" vs active; codex says legendaries sleep until awoken. Legacy legendaries without the dormant marker (old saves / direct grants) stay active — no retroactive nerf. Dev-panel toggle "Awaken S passive (this class)" exercises it until the quests exist (a separate content task sets `s_awakened_<cls>` on completion). Pricing UNCHANGED (`S_WEAPON_DROP_WEIGHT` 0.5 / ~2x — one-line flip). Also folded in: **boss BAG drops made rarer** — a separate flat `BOSS_BAG_DROP_CHANCE` 0.15/boss (grade = loot_cap) instead of the per-gear-grade roll that felt spammy (~1 B bag/run → ~0.45); dupes still cash at 1g. All green (test_quick + test.bat).
- **Mob mechanic REDESIGN — escalation curve (2026-07-07, playtest: "reusing same mechanics different look... introduce new mechanics or a spin on an existing one"):** the first mob-trait pass was rejected as generic archetype reskinning that ALSO violated the bible's "mechanics escalate across the chapter" rule (6 flat traits spread uniformly ch1-7). Replaced with a real vocabulary where each is a DECISION and most reuse an existing system, distributed as one new headline mechanic per chapter (earlier verbs recede to baseline): **ch1** pounce (OVERSHOOT gap-closer — sidestep the crouch and it sails past, whiff-punishable), web (root-shot: projectile `root_dur` denies the dodge for a pounce combo), channel_heal (interruptible — heals only while channeling a visible beam; damage breaks it); **ch2** warded (a GUARD you SHATTER, not nibble — 65% chip DR until a CRIT, a heavy single hit >=12% max HP, OR any status breaks it FOR GOOD; player-flagged: status-only would wall the 7 no-application theme variants — Shadow/Wind/Fury/Bulwark/Storm/Wrath/Pact — into tedium, so every build cracks it via crit/burst/control instead); **ch3** bloat (death → blight hazard pool), martyr (death-wail heals+enrages the pack → kill it LAST); **ch4** reflect (telegraphed forge-shield bounces 50% back → pause fire), sower (lava trail via `_add_hazard`); **ch5** frost_aura (moving chill aura — new `player.apply_chill` slow), snare (telegraph freeze patch, `telegraph()` gained a freeze rider); **ch6** spawner (sprouts capped weak adds), tether (linked pair — the bond BURNS the player who crosses it, and one dying FULL-HEALS the twin so you must burst both together; paired at room build); **ch7** blinker (untargetable flicker gap-close), counter (raised guard staggers the player who strikes it — wait it out). All 37 mobs retagged on the curve; mend/frenzy/swift kept as baseline modifiers. Presence pass (HP x2 / dmg x1.2 / +15% density) unchanged. Runtime-verified 9/9 via `game/shot_mobs.tscn` (the shot rig doubles as a mechanic selftest, independent of the suite); codex shows each kind's traits so counters are learnable; autotest coverage guard keeps every mob tagged with a real trait. Numbers are one knob each in Balance — dial in playtest. (Verification was briefly blocked by a parallel agent's shop-economy refactor leaving autotest.gd non-compiling; ran once it settled.)
- **Round 51 — FARM-COST pricing + act-keyed loot framework (2026-07-07, player design spec; supersedes round 50's level ladder):** the shop was re-founded on one principle — **buying must never beat farming; the merchant is a convenience/pity option.** A gear buy now costs ~the gold you'd earn farming one yourself + a 5% tax: `price = (first_run_gold + (N_runs-1)*replay_gold) * FARM_TAX`, `N_runs = ceil((1/boss_drop_chance) / BOSSES_PER_RUN)`, `BOSSES_PER_RUN = 3` (**verified**: econ_audit reports exactly 3 bosses in every chapter ch1-7). Per-chapter run gold is measured by `econ_audit.gd` and baked into `Balance.CHAPTER_ECON` (re-run + update when rewards move). Because gold/run already scales with level, this scales the shop automatically — **round 50's SHOP_PRICE_PER_LEVEL ladder is retired.** N_runs: B(1/3)→1 · A ch7(1/10)→4 · A act2(1/8)→3 · **S act2(1/25)→9** (÷3 bosses, not 25) · S act3(1/15)→5. Resulting Act-1 shop **B buy prices** (pre-haggle): ch2 1821 · ch3 3091 · ch4 3585 · ch5 4219 · ch6 5109 · ch7 6933 — a single B is now ~1 replay-run of gold, climbing as farming pays more. Commodity grades (C and below, the shop's 2/3 filler) stay cheap-flat (`Items.price × SHOP_BUY_MARKUP 2` ≈ 44-58g junk). Latent Act-2/3 (at ch7 gold as a stand-in): A act2 ~18.9k · **S act2 ~54.9k · S-WEAPON act2 ~103k**. **Act-keyed loot framework** (Act 2/3 latent until those chapters ship, via `Story.act_of`): loot_cap **A→B for ch2-7** (ch1 stays C) — Act-1 chests/bags/spoils cap B; **A in all of Act 1 comes ONLY from ch7 boss gear at 1/10.** New **BOSS GEAR + BAG drop channel** on every boss (on top of gems/gold/spoils): Act1 ch1-6 B@1/3, ch7 +A@1/10; two independent rolls (one gear, one bag). Shop stock grade rolls the **act appearance table** (Act1 B@1/3 over C/D filler; Act2 floor B, A@1/5, S@1/10; Act3 floor A, S@1/5), clamped to loot_cap. **Gems**: elite/boss drop level is now act-floored (Act1 L1 / Act2 L2 / Act3 L5), replacing the gem_lv2_chance ramp; first-clear bundle rolls +1 level; a new **shop gem shelf** sells loose gems at farm-cost (Act1 L1 ~92-333g by chapter — a fraction of gear). **S-tier WEAPONS drop at half rate** (`S_WEAPON_DROP_WEIGHT 0.5`, S-only — they carry the endgame passives), doubling their farm N and price (S-weapon act2 ~103k vs S-armor ~55k); applied at the slot roll for grade S only, so A/B weapon drops are unchanged. **Upgrade curve** rebuilt steep: `UPGRADE_BASE 12 × GRADE_FACTOR × (1+plus)`, doubling per tier — C step 24 / B 48 / A 96 / **S 192** (S is 8× C); totals to +3: C 144 · B 288 · A 576 · S 1152. **SELL basis** (design fork): buy = farm-cost (huge), but SELL = `MERCHANT_SELL_FRACTION 0.45 × INTRINSIC` (the small `Items.price` / `gem_gold_value` curve), NOT the inflated buy — a lucky S sells for ~31g, never ~30k (no sell-spiral). Gear C/B/A/S sell 13/17/23/31; gems Lv1-4 sell 13/40/121/364. **Bags always cash out for exactly 1g** (anti-exploit — never the 0.45 formula, since bosses now drop bags). Quest keepsakes remain unsellable. Codex updated (boss gear/bag drops, merchant gem shelf). All green (test_quick + test.bat). **Flagged design point**: a random-drop S weapon and the quest-gated class legendary are the SAME object (`roll_item_of` at grade S auto-applies the S_GEAR name + passive) — the half-rate/price applies to those legendaries; a separate quest-only gate is NOT enforced in the roll.
- **Mob presence + identity traits (2026-07-07, playtest: "mobs feel weak — I one/two-tap ch3 mobs; give each kind a unique characteristic"):** trash was a stat-block with one shared brain (walk at player, bite/shoot). Two changes. **Presence pass** (global, rides `enemy_stats_at`, non-boss only so the codex stays honest): `MOB_HP_MULT` 2.0 (~1-2 taps -> ~4 hits), `MOB_DMG_MULT` 1.2 (+20%), and `+15% density` (`MOB_DENSITY_EXTRA` — seeded per room, each authored spawn may bring a jittered twin; boss arenas exempt). Elites inherit the fatter HP (they multiply the scaled pool) — intended. **Identity traits** (data in each kind's ENEMIES "traits"; behavior in enemy.gd; one vocabulary tuned in Balance): `lunge` (telegraphed pounce gap-closer, purple crouch tell), `evasive` (weaves sideways as it closes, on top of its eva stat), `mend` (self-heals a % max HP/s — undead durability), `healer` (pulses a heal to nearby wounded allies + self; wears a GREEN RING = kill-priority, same language as the elite gold ring), `frenzy` (below 40% HP: +35% speed, +30% dmg), `swift` (the no-gap-closer melee speed comp, +18%). Assignments — base: wolf lunge / spider evasive / cultist healer / skeleton frenzy+swift / zombie mend; ch3: gravewalker mend / barrow_wight frenzy+swift / vale_mourner healer / casket_creeper lunge+evasive. Codex shows each kind's traits so the counter is learnable. Screenshot-verified (`game/shot_mobs.tscn`). ch4-7 mobs are untagged (the system reads "traits" or none) — tag them in the next content pass. Numbers (2.0 HP / 1.2 dmg / 0.15 density) are the player's ask, one knob each — dial in playtest.
- **Round 50 — merchant economy: level-scaled prices + shop overhaul (2026-07-07, player-reported: "by Ch3 I have ~4000 gold and shop items cost ~200"):** the merchant went stale because shop prices were **level-independent** — `Items.price` depends only on grade + `plus`, so an A +0 item was ~104g whether in Ch1 or Ch7 — while mob/boss gold climbs `REWARD_PER_LEVEL` 0.12/level. `econ_audit.gd` confirmed the split: replay income runs Ch1 1511g → Ch3 2470g → Ch5 3346g → Ch7 5787g against frozen ~100-140g gear. **Fix — a merchant price LADDER keyed to player level** (`Balance.merchant_price_mult(level) = 1 + SHOP_PRICE_PER_LEVEL·(level-1)`, `SHOP_PRICE_PER_LEVEL` **0.11** — deliberately just UNDER income's 0.12 so purchasing power creeps up but a merchant is always a real spend). Applied to **gear buy** (`Items.price · SHOP_BUY_MARKUP 2.0 · mult · haggle`), **upgrade**, **gamble** (scaled at source in `game_base.gamble_cost` so display + charge agree), and the **reforge bench**. Consumables and the health potion stay FLAT staples (commodities shouldn't inflate; `POTION_PRICE` 25 now a knob). Resulting A/S +0 buy prices: L7 ~173/232 · L19 (Ch3) ~310/417 · L31 ~447/602 · L39 ~539/725 — a Ch3 S upgrade is now ~17% of a run, a full shopping spree drains a multi-run hoard. **Shop widened**: rolled-gear count now `SHOP_STOCK_BY_TIER` (wood 3 / silver 4 / gold 5), and two new alchemist consumables — **Elixir of Warding** (`ELIXIR_WARD_AMT` -25% incoming for `ELIXIR_WARD_DUR` 20s, rides the existing `dr_` system, 110g) and **Draught of Renewal** (`RENEWAL_HEAL_FRAC` +50% max-HP burst heal, stocks past the 5-potion cap, 90g). **Selling extended** beyond gear: **gems** (`gem_gold_value(lvl)` = 30·3^(lvl-1), on the gem-combine curve, NOT player level) and **merchant consumables** now sell, all at `MERCHANT_SELL_FRACTION` **0.45** of market (harmonized down from gear's old 0.5) — always well under the 2.0x buy markup, no arbitrage. **Quest keepsakes can never sell**: the sell filter admits only ids present in `CONSUMABLE_PRICES`, which excludes `kind:"quest"` items and elite utility (Stone/Palimpsest). Codex consumables list + autotest coverage (`_test_merchant_economy`) updated. Open question for the user (flagged, not guessed): `econ_audit.gd`'s chest-gear valuation still uses a level-independent `/2` sell floor — left as-is (it's a labeled estimate and income conclusions are unaffected), but it now slightly under-reads high-chapter sell value.
- **Ashpriest verdict readability (2026-07-07, playtest: "ashpriest one shots me and idk why"):** the paired sub-50% VERDICT was a hidden trap — the callout announced only the FIRST half ("GUILTY: THE WEST") while both halves' tiles spawned simultaneously and IDENTICALLY, so the "shelter" detonated 0.5s after you reached it, and the callout hung over the often-off-screen boss using compass words in a featureless arena. Fixes: (1) callout is PLAYER-anchored and names BOTH waves when paired ("GUILTY: THE WEST — THEN THE EAST"); (2) the judged half is WASHED in verdict light for its whole fuse (`_half_wash` Polygon2D, z -7) — direction needs no compass; (3) the paired second wave burns visibly DARKER (muted tiles + dim wash) until the first lands, then its wash FLARES — the eye is handed the handoff, and the intended counterplay (step into the ashes of the first half during the 0.5s gap) is finally discoverable; (4) composition rule, same as Vess's ring: the enrage rain (3 personal rings / 3s) holds while a verdict is airborne; (5) the Sons intercept order is player-anchored too. Verified by screenshot rig `game/shot_verdict.tscn` (single / paired / handoff). Untouched, same taste calls as Vess: tile damage (dmg x1.2, lethal-adjacent upscaled) and Sons pacing.
- **Bag & risk-event playtest batch (2026-07-07, player-reported):** five fixes from
  one session. (1) **Cursed chest offer moved to the DOOR** — it used to wait in the
  room, so clearing first made the bargain free (buff hits only the LIVING pack) and
  claiming after the purge paid NOTHING (the payout rides the room-clear event, which
  had already fired). Now it materializes ahead of the player on FIRST entry, gives
  `CURSE_OFFER_WINDOW` (10s, pause-respecting) to decide, then withdraws — acceptance
  always meets a full room. Same seeded roll → same rooms carry it. (2) **Door seals
  no longer arm INTO the player** ("entering a room sometimes takes seconds of holding
  the key"): a slow doorway crossing left the body inside the freshly-armed seal
  collider, grinding against depenetration; the seal now parks until the player is
  clear of its band (TILE·2.4). (3) **Early gem faucet trimmed**: the per-elite gem
  guarantee starts at `ELITE_GEM_SURE_LEVEL` 12; below it the drop is
  `ELITE_GEM_EARLY_CHANCE` 0.45 — chapter-1 bags stop drowning in unsocketable gems.
  Gem quality ramps untouched. (4) **Bags +5 slots every tier** (F 35 … S 120) and
  **consumables stack by id** (one slot per type, like gem stacks, xN in the bag UI).
  (5) **Potion loadout — a per-ROOM budget** (v2, player-clarified same day: the cap
  is on potions USED per fight, not carried): the loadout holds `POTION_SLOTS_BY_ACT`
  slots (1 / 3 / 5 by act), each slot a potion TYPE with duplicates welcome —
  unassigned slots default to health, so an untouched loadout is pure health potions.
  Entering a room refills the budget; every Q-drink spends a slot; a spent loadout
  locks Q until the next room ("No potions left this room"). SHIFT-click a potion in
  the inventory to slot it, CTRL-click to unslot; `potion_next` (R) cycles the active
  type among slots still budgeted; the HUD Q-box shows the active type and its
  remaining room uses (▸N). Bag CARRYING is uncapped (stacks) — the fight is not.
  Act 1's single slot means the choice is simply WHICH potion you bring. Vess's Silence could play out entirely INVISIBLY — the callout anchored to the (often off-screen) boss, the quiet circle spawned in a random direction at z -6 under scenery/behind camera clamp, and the inverse telegraph drew only the SAFETY: an unseen circle made the arena look perfectly safe for 2s before a x2 arena-wide wail. Fixed in `telegraph_safe` itself so all four safe-spot bosses inherit it (Vess / Varo / Serane / Cyrraeth): (1) **player-anchored callout** + (2) **rising audio timer** (new `keen` sfx — detuned banshee glides over a 2s swelling noise, the swell IS the clock; opts "callout"/"sfx", Vess passes both); (3) **danger is DRAWN**: a red HDR edge-vignette floods the screen rim over the window (`hud.danger_ramp`/`danger_end`; green blink sheltered, clean fade caught) — needed a new WHITE `dangerrim` texture because the ambient vignette is black and modulate can't tint black (screenshot rig caught the first draft showing nothing); (4) **light beacons** rise from every circle (lootbeam texture, z 5) so shelter reads over ground clutter — decoy beacons flicker with their lie, keeping the enrage tell consistent across both reads; (5) **Vess's circle placement is view-biased** (`_quiet_spot`: 8 candidate headings, first unclamped + on-camera wins) — walls + camera clamp can no longer hold the exam off-screen; (6) **composition rule**: the 12-bolt ring holds while a Silence is airborne (running to shelter through a bolt wall was a forced trade, not a test). Verified by screenshot rig `game/shot_silence.tscn` (early / deep / enraged-decoys). Deliberately NOT changed (player's taste calls): the wail's x2 lethality, and the hurt_cd chip-hit wail-skip cheese.
- **Round 49 — first measured dps_bench round (2026-07-07, player-directed):** the new
  `dps_bench.bat` harness (L40, full A + Lv6 gems, mono specs, playstyle-constrained
  rotations, 180s vs an immortal average-L40-boss dummy) replaced grade guesswork with
  numbers, and the first table drove a broad pass. **Plate cadence tax** (player rule:
  "plate hits HARD, not fast" — warrior/paladin topped the chart at ~15 hits/s):
  Cleave cd 0.45→0.74 with **Berserk restoring the old 0.45 cadence** for its window
  (`Balance.BERSERK_CLEAVE_CD` — the ult is a tempo steroid now); Judgment 0.5→0.8
  flat (Holy included, which alone dropped Holy from 3098 to 2074 — intended: that
  sustain shouldn't also out-damage midfield). **Archer +5% base ATK** (11.1/2.91 →
  11.66/3.06, partially reverting round 47). **Warlock trailed everywhere** (bench
  cleared it of mana problems: min MP ~230 of 380): round-45's −10% bolt tax reverted,
  Shadowbolt cd 0.55→0.5, wither deepened 0.06→0.08/stack (cap +48%→+64%) — all three
  variants moved from ranks 14-17 to midfield. **DoT-spec floor lifted**: toxin
  0.08→0.12/stack (poison assassin 2566→3105, venom archer also rides it), poison's
  bloom blade 0.21→0.29, venom a1 dmg_mult 1.15 + dot 0.45→0.65. **Storm's
  single-target floor**: with no second target the fork now arcs BACK into the same
  body at 35% (`ric_back`, projectile.gd) — 1755→2355; packs keep the full leap.
  **Blood↔Shadow flip** (player call: blood at full HP shouldn't beat shadow, since
  blood_amp is the low-HP payoff): blood echo 0.45/0.30→0.35/0.22; shadow crit_bonus
  0.15→0.20 (a1+a3) and the five-knife fan converges (spread 0.22→0.15) — shadow
  4286 > blood 3521, with blood still closing the gap the deeper the pilot bleeds.
  Found along the way: **mage is mana-bound past ~2min** of pure Firebolt spam
  (all variants hit 0 MP; Meteor sat unaffordable ~48s of 180s) — Frost Nova's
  missing-mana refund is load-bearing in long fights, not optional utility.
- **Round 49b — the target ladder (2026-07-07, same day, player-specified):** the
  player pinned the whole spread — *Shadow > Hunt > Wind > Curse ~ Fury >= Wrath ~
  Void > Fire > Poison >= Venom > Storm ~ Pact; floor: Bulwark ~ Aegis ~ Holy* — and
  the round tuned to it, settling several 49a values. Final deltas from 49a: shadow
  a3 crit_bonus 0.20→0.15 (slight top-end trim, a1 keeps 0.20); Hunt a1 dmg_mult 1.2
  + crit_bonus 0.25, a2 narrow dmg_mult 1.2 (hunt 3294→~3950, now the clear #2); wind
  a1 echo 0.35→0.20 (stays under hunt); curse a1 dot 0.65 / hex-variant dot 0.55
  (curse ~ fury ✓); CRUSH settles 0.25→0.28 (void ~ wrath, both under fury); pact
  bolt 1.15→1.08 (down to storm's band); fire a1 dot 0.35→0.60 + Meteor burn_mult
  1.6→2.0; poison bloom settles at 0.24; venom a2 arrows dmg_mult 1.5→1.7; storm
  ric_back 0.35→0.50 (storm ~2600 ~ pact). Final 180s board: shadow 4189, hunt 3972,
  wind 3604, blood 3579, fury 3416, ice 3230, curse 3190, void 3080, wrath 3021,
  poison ~2830, fire ~2800 (tied), venom 2728, storm ~2620, pact ~2650, holy 2118,
  bulwark 1844, aegis 1703. Harness upgrades that made it cheap: **parallel fan-out**
  (six Godot processes, one class each — a full 18-case sweep in ~1/6 the wall time,
  `tools/dps_bench_fan.ps1`; --fixed-fps keeps results CPU-independent), and the
  bench mage now casts an **emergency Frost Nova below 55 MP** (never on cooldown —
  the refill costs zero bolt casts and keeps Meteor cadence; without it fire is
  hard-capped by starvation and the ladder's "Fire > Poison" is unreachable).
  Measurement note: run-to-run variance is ~±5% (physics iteration order perturbs
  crit/echo rolls even with fixed seeds) — treat sub-5% gaps as ties; the ladder's
  `~` groups land inside that band by design.
- **Round 49c — the AoE board (2026-07-07, same day, player-specified):** the bench
  grew an **--aoe mode** (three boss pillars shoulder-to-shoulder + five 1200-HP adds
  every 10s that really DIE — kill-triggered effects live; pooled EFFECTIVE damage,
  overkill never counts) and the player pinned the pack ladder: *Fire king > Pact 2nd
  > Shadow ~ Storm 3rd > Fury ~ Wrath 4th; Blood < Shadow; Wind ≤ Storm; Curse ≤ Wind
  but ≥ Ice; Void ≤ Storm ~ Ice; Venom/Poison/Ice above the floor, below Wind; floor
  Holy/Bulwark/Aegis.* Tuning used AoE-pure knobs so the 49b boss ladder never moved
  (verified by an ST regression sweep). Structural fixes first: **DASH_RIDER_CAP 2**
  (the dash-stab rider was landing full stabs on EVERY body in the 150px corridor —
  blood benched 9087, #1 overall; bosses with one victim never notice) and
  **`pierce_cap` fx** (projectile.gd: capped pierce stops after N bodies — venom
  arrows pierce 3; blood knives and void bolts LOST pierce to hold their slots;
  unlimited pierce is now a grant, not a default). Numbers: fire a1 splash 0.45;
  **Dark Pact blast dmg_mult 1.5→3.5** (AoE-pure — it's not in the boss rotation;
  boss-floor, pack-#2 is the intended inversion); storm splash 0.45 a1 / 0.55 a2 /
  0.75 ult; wind a1 gains splash 0.05 (0.15 first — ~3× hot, +17%); hex trimmed for
  ALL warlocks (radius 140→120, cast hit 0.5→0.35) with pact compensated; curse
  hex-variant dot →0.25 + hex_boom →0.3; void ult radius_mult and a2 crush flag
  dropped; whirlwind 0.9→1.0 with fury cyclone echo 0.55/radius 1.5; wrath
  consecration dot 0.55; holy consecration dmg_mult 0.7 (sustain trades fire for
  mending on BOTH boards now); venom ult dot 0.65; poison bloom mist parametrized
  (`bloom_dps` 0.32). Harness fixes: melee pinned in AoE mode (add waves physically
  bulldozed a stationary warrior out of reach — first bench read 83 dps of swung
  air); mage AoE rotation casts Nova on cooldown. Final pack board: fire 6256, pact
  5615, shadow 5373, storm 5208, wind 5038, curse 4836, void 4823, blood 4775,
  poison 4576, wrath 4534, ice 4423, fury 4180, hunt 3739 (14/95 adds — the boss
  spec on the wrong board, as designed), earth 3656, venom ~3030, holy ~2950, aegis
  2674, bulwark 2367. TIERLIST.md rewritten as the measured first edition (both
  boards + both ladders as the standing contract).
- **Round 49d — HOBBLED: the slow conversion (2026-07-07, same day):** the last
  systemic hole in the CC-immunity doctrine: stuns that fail on bosses convert to
  CONCUSSION damage and ice earned brittle, but a failed SLOW converted to NOTHING —
  every control theme (venom/poison/ice/void/earth, plus the mage novas) silently
  paid budget for a dead rider at boss doors, and venom paid it on all four slots
  (the weakest combined package in the game: bottom-half on BOTH boards, the only
  offensive spec with no home axis). Fix, the concussion move for slows:
  **HOBBLED** — a slow that fails on a CC-immune boss scuffs its footing instead;
  the boss takes **+4% damage from the player for 2.5s** (`HOBBLE_MULT`/
  `HOBBLE_DUR`), refreshed per failed slow, enemy-side like EXPOSED so DoT ticks
  benefit; "HOBBLED" floater on the fresh mark (throttled), codex fx_text teaches
  it on every slow line. Guardrails so the 49b ladder held: poison's bloom
  0.24→0.22 (its hobble gain would have pushed it past Fire) and **CRUSH_MULT
  0.28→0.22** (Void's slow rides every bolt — hobble handed it a free ride past
  wrath ST and storm AoE; the crush trim re-seats it at wrath's shoulder / a
  statistical tie with storm). Measured outcome: venom ST 2690→~2790 (the intended
  winner — tied with poison now, which "Poison ≥ Venom" permits), Fire nominally
  above the pair, ice/earth up a few % where their slows used to evaporate.
  Boards re-verified in both modes; the bench dummy honors the mark.
- **Soft-knee cap doctrine (2026-07-06, player-designed):** caps redefined game-wide — a cap is NEVER a dead stop: full value to the cap, ~1/10 conversion beyond (`Balance.soft_cap`, `SOFT_CAP_RATE` 0.1). "There is no such thing as an actual full cap where a stat becomes useless." New/changed values: **Crit knee 35%** (was linear-to-70% + 95% asymptote — far too generous; themed crit_bonus rides the same knee), **Evasion 50%** (was hard 60%), Haste 40%, Lifesteal 35%, Combo 30% (recalc knees replace the hard clamps), **Greed 40%** (was 50%-knee) and greed's chest-drop bonus now applies from the FIRST point (the 30% threshold is gone; still bounded +10%), **resistance damage-reduction knee'd at 80%** (max ~82%; curve itself unchanged below it). **Ults ignore Haste for EVERY class** ("there's a reason they're called ults") — authored ult-cd talents still apply; the assassin's Death Mark stays fully fixed (round 38). **Lifesteal surges are ADDITIVE and share the knee**: `current_lifesteal` = knee(stat + berserk/pact/stab-surge) — a 2%-lifesteal assassin proccing the 26% surge drains at 28%, and no temp window smuggles past the cap. **Gem LEVEL limits by gear grade** (`GEM_LEVEL_LIMIT`): B ≤ Lv3, A ≤ Lv6, S ≤ Lv10 — deep gems need endgame vessels, which also hard-bounds Act 1 gem power (4 sockets × Lv3 max). Legacy assertions updated: the old crit-curve and combo-60%-cap tests re-anchored. Amendment, same day: **crit's knee diminishes at 1/5** (`CRIT_SOFT_RATE` 0.2, others stay 1/10) — crit is a payoff stat; the harsh knees are for the system-breakers ("too much combo, u start spamming every ability with barely any issue"). Second amendment: **theme bonuses are CAP-EXEMPT** — a theme's crit_bonus (and theme-line talents like Nightfall) is added ABOVE the knee at full value (`Stats.effective_crit`: knee the built stat, add the themed edge raw, then enemy crit-res shaves the total — their stat, not a cap). The crit-theme tax is GONE: a 28%-crit archer's Hunt hit crits at a full 43%. Themes are earned, timed, ability-scoped power — exactly what caps shouldn't punish.
- **Sub-1 attribute conversion (2026-07-06, player-caught):** the player spotted that primary-attribute→ATK conversion at 1.2 was WRONG-WAY ("100 STR should convert to 90 ATK and 120 HP") — and the math audit confirmed worse: with the v2 budgets (24 base pts/set), gear attribute mains were injecting +13–19% ATK at full B and ~+35% at S beyond the calibrated envelope, compounding with grade progression while every boss is pinned to the player's ~5.5%/level curve (round 13) — Acts 2–3 would have silently flattened. Fix, both levers: (1) every class's PRIMARY converts at **0.9 atk** (was 1.2; paladin 1.0), warrior/paladin STR hp **2.0/3.0 → 1.2** per the player's exact ratio; (2) `SLOT_MAIN_BUDGET` **24 → 12.5** total (weapon 5 / armor 3 / charm 2.5 / boots 2), sized so the L42 full-B benchmark lands within ~1–2% of the pre-redesign envelope (archer: old ≈200 ATK, new ≈197.5). Known side effect: the plate classes lose ~6–9% HP pool at 42 (the STR-hp cut also hits allocated points); VIT is the recovery lever (conversions untouched: warrior 7/paladin 6 hp/pt) — bump VIT/hp_lvl if the plate playtest reads squishy, never re-inflate STR. attr_help threshold moved (0.9 still reads "greatly increases ATK"). Follow-up ruling, same day: **attribute IDENTITIES are uniform across classes** — STR = ATK + HP (1.2/pt, unified for off-class STR too, was 2.0), AGI = ATK + CRIT, INT = ATK + CRIT (+ mana; paladin's hybrid INT gains crit 0.0005) — the AGI secondaries that paid evasion (warrior/mage/paladin/warlock) pay crit now, and the assassin's AGI eva rider is gone (his evasion identity lives in the base-kit 15%). Primary crit rate 0.0008/pt ≈ +4.9% crit at L42 for AGI/INT classes. Final identity table (third ruling, same day): **VIT = HP first + ALL THREE resists as secondaries** (phys/mag/crit res — the one-stop bulk stat); **INT gives NO mana** (pools live in class bases; INT-stacking must not also solve sustain — watch mage/warlock mana at the bench: full INT investment used to pay ~100-120 MP at 42, the recovery lever is base `mp_lvl`, never the attribute); **AGI and INT are twins** — both ATK + CRIT, AGI leans crit (0.0008/pt) while INT leans a small DEX (0.0006 crit + 0.15 dex/pt); off-class INT everywhere reshaped to the same tiny-rate shape (0.2 atk / 0.0002 crit / 0.06 dex, replacing the old mp/magres grab-bag).
- **Itemization doctrine v2 (2026-07-06, player-designed — supersedes parts of the same-day pass below):** the player tightened the gear model into its WoW-shaped final form. (1) **Attribute mains**: every piece's main is the class PRIMARY attribute (STR/AGI/INT), guaranteed and class-matched, slot-budgeted (weapon 8 > armor 6 > charm 5.5 > boots 4.5 base pts × grade × shape mult), converting through `ATTR_SCALE` like allocated talent points — replaces the old slot mains (weapon ATK / armor HP / boots Speed / charm Haste). (2) **Movement speed is SOVEREIGN** — removed from EVERYTHING itemized (boots main, sub pool, Kunai/Mail shapes, S boots, archer 4pc set); only terrain (`hazard_speed`) and abilities (`theme_speed`) may touch it: "mov speed is king in a game where dodging is life or death." recalc's speed term now reads class base only. (3) **Class lock**: rolled gear carries `cls`; `equip()` refuses other classes' gear (legacy unclassed items pass). (4) **S exemption REVOKED** (the earlier pass had sanctioned S legendary specials): all cdr/lifesteal/combo/speed purged from S pieces and SET_BONUSES with same-budget legal swaps (e.g. Bloodpact combo→crit_dmg 0.25, Archmage 2pc cdr→magpen 8, Hawk 4pc speed→dex 8) — S differs by MAGNITUDE + weapon passive + 3 sockets, never stat types. (5) **Tree purge**: 8 nodes granted combo/lifesteal (Battle Rhythm, Chain Reflex, Quick Hands, Fervor, Arcane Echo → crit_dmg/DEX; Bloodletter→"Iron Veins", Lightmender, Blood Tithe → +4% max HP) — talents can never buy specials or speed. (6) **VIT joins the sub pool** (3.0/roll). Legacy saves migrate on load (`_fix_item` strips banned stats; old ATK/HP mains still count). Weapon-shape identity test re-anchored to attribute budgets (Claymore STR > Fang AGI).
- **Gear-stat doctrine + act caps + boss gem ramp (2026-07-06, player-designed):** three rulings in one pass. (1) **Act loot ceilings corrected to the player's model** — code had ch2–ch7 all capped at A (a round-4 relic from when ch2 was the endgame); now Act 1 = F→B, Act 2 = A, Act 3 = S, clamped centrally in `loot_cap()` so content modules never carry the rule. (2) **Special stats (Haste/Lifesteal/Combo/Greed) became GEM-ONLY**: removed from the random sub pool AND shape personalities (charm main Haste→Crit 0.05; Bow cdr→DEX "true aim"; Wand combo→crit; Tome lifesteal→MP; Striders cdr→EVA "elusive"), one special gem per item (`embed_gem_into` guard), hard caps in recalc — Haste 40% (was implicit 45%: the round-30 dash floor moves 1.49→1.62s at full build), Lifesteal 35%, Combo 30%. S legendaries keep their small authored specials as the sanctioned quest-gated exception; the caps guard the ceiling. Anti-degeneracy rationale, verbatim: no builds above 30% combo / 35% lifesteal / 40% haste; a late-game cap-lift by level (~L80) is noted for later. Supersedes round 43's B-gate (nothing rolls them at ANY grade now). Facts established along the way: duplicate substats were already impossible (draw-without-replacement) and gear carries no attribute stats at all — class fit is arsenal + pen-gating, so "a dagger with INT" can't exist. (3) **Boss gem-expectation ramp**: the TIERLIST benchmark was gemless but a real L40 player carries ~4 sockets of Lv3 gems (B-cap world), so UPSCALED bosses gain +1.2%/lvl HP and +0.6%/lvl dmg for levels above max(L32, their anchor) inside `enemy_stats_at` (anchors stay authored-exact — the ch5 selftest caught the first draft inflating Mother Halla's L33 anchor) (+~10%/+5% at L40, growing through Act 2) — round 45's "budget what the player actually has", extended to gems. TIERLIST frame re-anchored to "full B + four Lv3 gems" (numerically ≈ the old gemless-A frame, so historic grades stay comparable). All of it needs the difficulty-tuning playtest.
- **Reward calibration pass (2026-07-06, player-directed):** the player named the target: early progression SMOOTH, later farming WANT-driven (build + exploration), sweet spot between "too generous kills farming" and "too stingy kills players". Built the instrument first — `game/econ_audit.gd` prints every chapter's actual payout (first run vs replay, per faucet). Findings: replay already out-earns first-run per minute (~1.5×, same faucets in less time) so farming was viable but the first clear had NO legible beat beyond XP/boss-gems; gems/min was FLAT across chapters (~0.5) so clubbing Chapter 1 was the rational socket farm; ch2 is a reward hole (legacy chain, zero exploration faucets, 42 g/min vs ch3's 77). Changes: (1) **first-clear beat** — `FIRST_CLEAR_GOLD` 150 × level-mult in hand (≈15–25% of the run's own gold) + a mailed spoils package (one act-cap gear roll + Lv2 gem), once per character per chapter; (2) **gem quality chases the frontier** — the guaranteed-gem Lv2 chance is now `gem_lv2_chance(level)`: 35% floor ≤L10, +1%/level, 65% cap (count stays flat; quality is why you farm at-level when no-downscaling makes old content safe); (3) **hidden caches** — ~25% of dead ends bury a chest that only glints awake within ~150px (`Chest.bury()`), counts as a results-card secret. Full model (faucet jobs, sink sizing in farm-minutes, endless-mode rule) promoted to DESIGN.md "Reward economy — calibration doctrine". Ch2's hole deliberately NOT patched with numbers — it needs the graph retrofit.
- **Grit + Conviction (round 48, in working tree):** the two plate classes lose Second Wind (rounds 22/24 keys REMOVED) — kiting-shaped sustain was anti-identity on the classes that live in the grind. Warrior replacement **GRIT**: every enemy blow TAKEN adds +0.6% max-HP/s regen, 6-stack cap, 6s window refreshed per hit — facetanking feeds the juggernaut, kiting starves him. Attacker-less damage (hazards/telegraphs) builds nothing (no camping a campfire), and hurt_cd already rate-limits the ramp. Paladin becomes a **STANCE knight** — no true ult: **Conviction** (8s cd, 0 mp) swaps HOLY (−20% damage, +1% max-HP mend per blow landed, AoE at a third) ↔ RETRIBUTION (+25% damage, no stance mend). Entering Retribution casts Chains of Wrath at 0.5 scale (theme fx intact — the three Conviction theme ults ride the swap); returning to Holy pays a 10% max-HP blessing. Sustain and damage are never simultaneous — reading WHEN to be which is the skill; the reward curve lives in Retribution uptime. Also: **Judgment's leap is now a RIDER on its own 5s cd** (`JUDGMENT_LEAP_CD`) — ranged pilots were leaping (and landing-i-framing) on every 0.5s cast for ~90% immunity; the swing stays spammable, the leap + its guard is the once-in-a-while answer (same pattern as warrior Charge).
- **~1200 parity + earned surge (rounds 46–47):** boss-dps bench pinned at ~1200: archer base atk −7.5% (was ~1298); assassin Shadow Dash cd 2.5→3.75s, tuned to Blood Surge — whiff a dash and the surge lapses, dropping Fan of Knives to baseline: the ~1600 ceiling was FREE surge uptime, now it's earned. Warlock Shadowbolt 3→1 mp (bolt-spam sustainable over a 5-min fight; the pool was mis-sized to ~50s). Void Shadowbolt drops its base mult — the burst rides the crush-crit stack instead, keeping Void ~5% under wind mage as the price for crushing bosses.
- **Ch3 boss HP retune (round 45):** audit vs the ch2 curve found the first-pass ch3 anchors 2-3x light on HP (2700/3300/5800 at L17/19/22 — ALL below Nullwarden's 8000 at L16, and the Sexton's physres 30 is lower than the Warden's 55, so no resist compensation either). Damage was textbook (58/66/88 rides the +5.5%/level curve with the same ~+13% finale premium Nullwarden carries) — untouched. HP re-anchored to the TTK budget with gear-inclusive dps (~12%/level: nullwarden 8000/40s at L16 → 224/281/395 dps at L17/19/22): Sexton **5400** (25s opener; small discount — the shambler swarm diverts dps), Vess **8000** (30s mid; inverse-telegraph downtime is real fight-time), Varo **14000** (40s finale less a censer-heal allowance — the censers ARE effective HP the player can cut short). Needs a playtest pass at story level to confirm the mechanics-heavy fights land inside budget.
- **Boss gems (round 44, player-designed):** bosses join the gem economy (was elite-only). FIRST clear of a chapter: every boss guarantees a **3-gem bundle** (`BOSS_GEMS_FIRST_CLEAR`; the drop rolls before `completed_<ch>` is set, so the first run's final boss counts). Replays: one gem per kill by chance, scaling with the boss's LEVEL — `Balance.boss_gem_chance()`: 1/25 floor at L5 and below (`BOSS_GEM_CHANCE_MIN`), linear climb, **guaranteed at L40+** (`BOSS_GEM_CAP_LEVEL`) — the socket progression path IS farming midgame bosses. Gem level rolls reuse the elite Lv2 chance (35%). Curve pinned by test (floor/monotone/cap).
- **No free immunity + endgame stat gate (round 43):** Shadow Dash's i-frame is REMOVED entirely (was 0.5→0.35s; now 0.0 — `_dash_strike` takes an `iframe` param, assassin passes 0): a short-cd dash with immunity was too abusable once the refund made it semi-spammable. The dodge is the MOVEMENT; only Death Mark's 0.8s all-in commit grants immunity (mage Blink keeps its stock 0.3s — different cd class). Passive text updated. **Gear gate:** lifesteal and combo never roll below B grade — random pool AND shape personality stats (the Wand's built-in combo, the Tome's lifesteal); S-set pieces untouched. Gems already B+ only (gem slots F-C: 0). PLANNED (player): mid/end-game bosses get conditional healing-reduction mechanics (zones/hits that cut healing X%) — the systemic check on all-in lifesteal builds. Gem economy note: gems drop ONLY from elites (guaranteed 1/kill, 35% Lv2) — a player skipping elite rooms sees zero gems; sockets are pure elite-hunting payoff.
- **Surge band set by the player (round 42):** 12-26% (`SURGE_LS_FLOOR` 0.12 + `SURGE_LS_SCALE` 0.14). Roster lifesteal for reference: warlock 5% permanent (+15% for 5s on Dark Pact = 20% peak, costs 12% max HP); paladin 4% permanent; warrior 15% only during Berserk (8s / 40s); assassin 0% baseline — the surge IS his lifesteal, melee-earned, 4s at a time; mage/archer none (ward/Second-Wind classes). The assassin's 26% peak stays the roster's highest burst-heal rate but the shortest-lived and the only one gated on landing a melee cut while LOW.
- **Safety trims on the spammable dash (round 41):** the refund made the dash semi-spammable, so the safety riding on each cast shrank to match — i-frame **0.5→0.35s** (`DASH_IFRAME`; ult's 0.8s untouched, archer tumble untouched) and the surge lifesteal CEILING **32→27%** (`SURGE_LS_SCALE` 0.18→0.13; the 14% full-health floor stays — the desperation curve flattens, the healthy-connect baseline doesn't move). Player asked for the surge's upper end before naming a number: it was 0.14 + 0.18 x missing-hp = 32% cap.
- **Depth-tiered rider (round 40, player-designed):** the dash-stab's flat 0.65x ignored HOW the needle was threaded. Now the rider pays by DEPTH: inside the old 105px corridor (no bonus range needed) the cut lands **1.0x** (`DASH_STAB_NEAR_MULT`); only the far 105-150px bonus-reach graze takes the 0.65x discount (`DASH_STAB_MULT`). The blood surge stays FULL at every depth — the discount is on damage, never on the engine. Standing stab raised again 1.1→**1.2x** (`STAB_MULT`): melee premium ~25% over surged knives. Ladder: standing 4.0 > surged knives 3.2 > dash-through 1.2+1.0 > far graze 0.65 > chip 1.6 atk/s.
- **The standing cut pays (round 39):** with the refund making the dash-stab semi-spammable, the two cuts needed separating: the STANDING stab — planting feet at blade range, the riskiest act in the kit — pays a premium (`STAB_MULT` 0.9→1.1, ~3.7 atk/s spammed), while the dash's proc'd stab takes the matching trim (`DASH_STAB_MULT` 0.9→0.65; dash package 1.2+0.65=1.85x per connect on the refund-cheapened cd). Payout ladder now: standing melee 3.7 > surged knives 3.2 > unsurged chip 1.6 atk/s — risk strictly orders reward. Dash tooltip updated ("a lighter stab + the full blood surge").
- **Fixed execution (round 38):** the dance (round 37) covers sustained damage, but the identity piece — burst someone down and exit — sat on too long a timer. Death Mark **36→30s**, with a caveat: the cd is **FIXED** — `ability_cd` returns the authored value for the assassin ult, immune to haste (`cdr`) and any future cd talents. At its numbers (2.7x true + 5s +50% mark) the execution gets disgusting below 30s; the authored cd IS the floor, by design and by regression test (cd asserted unchanged at 45% cdr). Tooltip carries the rule ("no haste can hurry the execution").
- **The dance is the engine (round 37, player options 1+3):** playtest verdict — viable and balanced, but "doesn't play like an assassin": knife chip paid ~87% of stab dps on boss hitboxes, so the rational loop was stand-and-chip with occasional dashes. Two levers, one package: (1) **dash refund** — a CONNECTING dash-stab refunds `Balance.DASH_REFUND` (35%) of the cooldown (whiffs pay full price; base 3.6→~2.3s on connect, ~1s at full build): going in is FREQUENT; (2) **earned knives** — baseline per-knife 0.26→0.16 (`KNIFE_MULT`; poison bloom 0.34→0.21), but while the stab surge runs the fan bites DOUBLE (`KNIFE_SURGE_MULT` 2.0, snapshotted at throw): going in is MANDATORY. The loop: dash in → cut (surge) → dash out → spend the 4s window on empowered knives → repeat. Knife cd stays 0.3s (the spam feel is the point; only the unearned baseline drops) and the cadence lock is untouched. Ult stays 36s — the event, not the rotation (option 2 rejected: scheduled safety is forgiveness-budget, and it trades burst for sustained — the opposite of the class). Regression: connecting dash must refund; surged/unsurged dart mult ratio must be 2.0.
- **Dash cut marks + razor lines (round 36):** the dash's stab rider was invisible on its victims — now every enemy it touches wears the cut: `_cut_flash` stamps one diagonal slashline on each rider victim, and a FULL pass-through (55px damage lane + rider) crosses the second stroke into an X on the body — graze = one cut, commitment = the X (screenshot-verified; scoped to the assassin's dash, mage Blink shares `_dash_strike` and stays clean). Lines thinned again on playtest ("not thin enough"): stab y 0.8→0.5, dart 0.5→0.32.
- **Blade cadence (round 35):** playtest — Stab (0.3s) and Fan of Knives (0.3s) were independent cooldowns, so point-blank you could WEAVE both: ~5.6 atk/s (vs the intended ~3.0) with every hit feeding the surge lifesteal — stand-on-the-boss immortality. Fix: the two spammables share a lockout — casting either floors the twin's cooldown to the fresh cast's (haste-aware, in `use_ability`). One hand, one blade: spam either, never both. The intended loop is untouched (stab in close ↔ knives from range are PHASES, not a stack); melee-phase dps returns to budget. Regression test: casting a1 locks a3 and vice versa.
- **Death Mark: the shadow execution (round 34, player-designed from the LoL reference):** the ult is now a CINEMATIC. Cast: darkness falls, the prey is MARKED — a floating red X (two crossed slashlines, bobbing) rides its head for 5s while it takes +50% damage (`vuln_time`). Then two living-shadow copies of the assassin (player-sprite ghosts, void-purple) dash THROUGH the prey from opposite diagonals, converging in an X — each pass rips an execution slash (red slashline + white core) and lands 0.7x TRUE damage, 0.16s apart. Finally the assassin himself appears on the FAR side of the prey and drives the killing stab back through it — the real `_melee_arc` stab (1.3x true), not a bolt-on flash. Shadow theme's under-30% finisher unchanged; poison mark-rot unchanged; blood rides via `blood_amp`. The rising skull is gone — the X IS the mark. Slashline slimming (playtest: "too fat"): stab y-scale 1.5→0.8, dart 0.8→0.5, execution slashes 0.9/0.45. Total ult budget unchanged (0.7+0.7+1.3 = 2.7x true + mark).
- **The slashline (round 33, player reference art):** the assassin's entire cut language is ONE shape — a solid white blade sliver with needle points at both ends, belly at ~65% (procedural `Art.tex("slashline")`, 96×14, solid core + 1px AA edge). White is the base; theme variants only tint it. Stab = the sliver thrusting along the strike line (0.06s thrust, 0.06s solid hold, 0.07s gone — brief and striking, no glow stack). Fan of Knives = the SAME sliver in motion (glow and trail removed; just clean lines flying). Death Mark flurry = two crossed slivers per hit, red stroke + white core. All three verified in rig screenshots against the reference. Rig lesson: capture frame counts are NOT wall-clock (fps varies per run) — bracket short-lived FX with multiple rapid shots, and put the dummy at real engagement range for projectile flights.
- **Blood pays in damage, not healing (round 32):** playtest — a blood assassin could STAND STILL spamming Fan of Knives and out-regen a same-level Warden Null (blood a3's 2% heal/hit at 3.3 casts/s × pierced multi-hits was a second sustain engine stacked on the base surge). Rule: the assassin's sustain lives in the BASE kit (stab surge) and nowhere else — every blood `heal`/`flurry_heal` fx is REMOVED. Blood's replacement gimmick mirrors the base kit: the base surge turns missing health into LIFESTEAL; blood now turns it into DAMAGE — `blood_amp`: hits bite up to +40% harder at death's door (ult +80%), scaling linearly with missing HP. One resource (your blood), two payouts; the desperation engine gets a damage ceiling instead of an immortality loop. With real sustain in the kit, 25% base evasion was double forgiveness — **eva 0.25→0.15**. And the dash-stab earns BONUS range: the graze corridor widens 105→150px (vs the standing stab's 118px reach) — striking in stride reaches deeper than planting your feet, making the dash-in the premium way to land the surge.
- **Manaless assassin + screenshot-verified FX (round 31):** assassin is now MANALESS (League-style) — all ability mp costs 0, HUD hides the mana bar/readout via a class-level `manaless` flag (flag lives at the CLASS root, not inside the passive dict: passive entries are numerically summed in `recalc()`, and a bool there was a float+bool crash — recalc now type-guards to float/int). FX judged by LOOKING, not by code: a windowed screenshot rig (`game/shot_kit.gd`, dev tool) boots the real game, casts each assassin skill on an immortal dummy at 2.4x zoom and saves PNGs at animation peaks. Verdicts and fixes — **Stab**: the 0.12s soft-glow streak was invisible on any backdrop; replaced with the dart NEEDLE sprite stretched along the stab line (hard steel edge, dark fringes, thrusts forward 0.07s then fades 0.15s) over a dark contrast under-glow — reads as the quick-draw sword. **Fan of Knives**: darts were a blown-out white cluster; steel-blue tint + a small snappy impact ring per hit so connects register even when flight is too short to see. **Death Mark**: the flurry was invisible (single 0.12s sliver per hit); now each hit rips a big crossing X (red stroke + white-hot core, 0.24s), hits at 0.13s cadence (three DISTINCT rips, inside the 0.8s i-frame), and the screen darkness holds 0.8s so the whole execution happens in the dark. Doctrine: **ability FX ship only after being seen in a screenshot** — code-side tuning of invisible effects is guesswork.
- **Quick-draw sword + honest mobility (round 30):** the round-18 Stab slide-step is REMOVED — 55px of free movement on a 0.3s cadence was infinite mobility and quietly game-breaking (kept only while the assassin was weak). Mobility now lives where it costs something: **Shadow Dash base cd 6.0→3.6s** — with the tree's Exsanguinate (−25%) and the 45% haste cap the floor is **1.49s**, the player-specified target; you BUILD toward the dance. Compensation for the lost slide: Stab reach 84→118px — lore upgrade, the assassin quick-draws a long blade now. Fan of Knives finally got its **dedicated dart sprite** (16px needle, hot tip, no tumble) replacing the stubby knife art that read as a stick regardless of tuning.
- **Dash graze corridor (round 29, player-designed):** the dash's DAMAGE lane stays 55px, but the stab rider reaches 105px — a skilled assassin threads the needle NEXT to the boss (never entering the body), lands stab + surge on the graze, and kites out already healing. The mastery loop: graze-pass past the swing → surge up → knife spam drains it → repeat. Full commitment (through the body) still pays more (dash damage + stab); the graze is the safer, lower-damage skill line.
- **Surge floor raise (round 28):** the stab surge's +5% floor was noise at full health — a healthy assassin's connects earned nothing felt. Floor tripled: `0.14 + 0.18 x missing-hp` (+14% at full → ~+32% near death). The desperation scaling stays; the baseline now matters in every trade.
- **Plate trim (round 27):** warrior and paladin verified SOLID post-rounds-19/21/22/24 — slightly too solid. ~10% durability trim via hp growth only (warrior hp_lvl 21→19, paladin 19→17; ≈8-9% less pool at L42) — the flat DR and resists stay untouched because they ARE the plate identity; the pool is the tuning fat.
- **Dash carries the knife + dart feel (round 26):** Shadow Dash now STABS every enemy passed in stride — dash damage (1.2x) + a stab hit (0.9x) + the blood surge per connect (shared `_grant_stab_surge`; the dash animation overrides the stab slide) — dashing IN is rewarded with the full package. Fan of Knives de-glowsticked: knives fly POINT-FIRST (no tumble — the spin read as thrown sticks), blade stretched along the flight line, dimmer/smaller glow, longer thinner streak, 36% faster (760px/s; bloom 660), sharper throw sound (pitch 1.55), damage 0.22→0.26 per knife (bloom 0.3→0.34).
- **Hybrid assassin (round 25, player-designed):** verdicts — mage/archer/warlock GOOD; melees still struggle; assassin's core contradiction named: paper-thin yet his damage lives at point-blank. Redesign to a ranged/melee hybrid with a desperation engine: **Fan of Knives spams at Stab cadence** (cd 2.8→0.3s, mp 2, per-knife mult 0.7→0.22, poison bloom 1.0→0.3 — safe CHIP at ~25-50% of stab dps on realistic boss connects; the killing still happens in close), and **a CONNECTING Stab surges lifesteal for 4s, scaling with MISSING health** (+5% at full → +30% near death). The intended ceiling, verbatim from the playtest: dive in low, cut for the surge, ult through the answer (0.8s i-frame), dash out, and heal it all back through knife spam from range. High risk, high reward, huge skill ceiling — the surge makes low health a RESOURCE. Watch items: bloom-mist spam visuals at 3.3 casts/s; knife chip vs archer identity overlap (knives are chip, arrows are a build — distinct for now).
- **Warrior breath + re-engage (round 24):** warrior damage "solid now" but he "feels awkward to execute". Concrete ask delivered: Second Wind keys, AMPED vs the paladin's (untouched 3s → +4%/s vs paladin's +3%/s — the warrior has no lifesteal to lean on). Awkwardness diagnosis (hypothesis, verify): same disease the paladin had — the slowest class (speed 250) with a stationary bread-and-butter walks back in after every dodge; his only closer, Shield Bash, sat on a 6s cd. Bash 6→4.5s makes the charge rotational: dodge → bash back in (with the 1.3s stun buying the trade window). If the awkwardness persists after this, the next suspects are Cleave's swing feel/timing or Whirlwind's 8s cd — need the player to name what specifically feels off. *(Second Wind keys later REMOVED in round 48 — Grit replaces them.)*
- **Ranged power-budget doctrine + mage sustain (round 23):** playtest — the mage ran OUT OF MANA mid-boss, and Firebolt carries nearly all real damage because a kiting ranged class can rarely connect its short-range skills. Doctrine (player-articulated, now canon): **damage budget on a ranged class's dash or short-range skill is phantom budget — those slots must carry UTILITY instead (healing, mobility, buffs)**. Applied: Frost Nova mp 20→15 and now restores **20% of MISSING health and mana** on cast (desperation-scaled; ~every 7s = the occasional sustain and the mana-out fix in one), and Firebolt — the skill that actually connects — carries the budget (mult 1.1→1.2, wind twins 0.7→0.75, on top of round 20's 0.45s cadence). Blink already follows the doctrine (ward utility, incidental damage).
- **Paladin closes the gap (round 22):** playtest — the paladin is the only melee with zero mobility: dodge a telegraph and you walk back in while the boss resets. Fixes: **Judgment leaps** — when the target stands beyond arm's reach (95-300px), the cast leaps the paladin onto them before the hammer falls (dodge out, leap back in; no new button, the bread-and-butter IS the gap-closer). And the round-14 Second Wind keys are reused: untouched for 3s → +3%/s regen on top of his 1%/s base (half the archer's rate — he has lifesteal too) — sustain for the disengaged beats where lifesteal earns nothing. *(Second Wind keys later REMOVED in round 48 — Conviction replaces them; the leap gained its own 5s cd.)*
- **Plate DR + void pact inversion (round 21):** the melee/mage shortfall verdict WAS post-round-19 plate (misread as untested), so the reserve lever is pulled: `flat_dr` passive — flat damage reduction applied AFTER resists (immune to the res curve's saturation; skips TRUE damage per doctrine) — Warrior 15%, Paladin 12%, plate-exclusive. Stacked with round 19: a L42 boss hit into the warrior now lands at ~35-38% of its raw value. Separately, **Void Dark Pact was anti-design** (a self-centered PULL dragging enemies onto a squishy ranged caster): inverted to an outward space-collapse — knockback 340 + slow, a panic-peel button that keeps the void identity. Mage was already re-armed in round 20 (Firebolt cadence + Meteor 44s) — bench him with those before more.
- **Ult cadence + variant polish (round 20):** ult cooldowns −20% across the board (Berserk/Arrow Storm/Chains 50→40, Meteor 55→44, Death Mark 45→36) except the reference warlock at −10% (Void Rift 55→50) — ults become rotational fight-beats, not once-a-fight events, which disproportionately helps the classes whose ult carries them (assassin especially: the 0.8s-i-frame commit every 36s). Archer variants: Hunt is the benchmark spec; Storm echo 0.40→0.45 + splash 0.25→0.30, Venom dot 0.30→0.38 + slow 0.25→0.30 close the intra-class gap. Mage Firebolt 0.5→0.45s (+11% sustained on top of the top growth line). Melee/mage "still falling short" reports predate the round-19 plate landing — benchmark WITH the new plate + rotational ults before the reserve levers (flat plate damage reduction; melee ability mults) get pulled.
- **Archer +10% (round 19b):** playtest follow-up — archer damage still a notch short after the round-15/17 bumps: atk 11.0/2.85 → 12.0/3.15 (+10.4% at L42 before gear).
- **Plate that plates (round 19):** playtest — warrior damage "decent" but both plate classes "feel like paper" vs the L42 boss. Math agreed: warrior's 60 physres = only 33% on the saturating res curve → ~200+ per boss hit into ~1050 hp at full melee exposure, with 1%/s regen (~10 hp/s) as noise. Tank budget moved to where tanks live: Warrior physres 60→80, magres 30→45, regen 1→1.5%/s, hp_lvl 18→21 (damage untouched); Paladin physres/magres 45→60/60, lifesteal 3→4%, regen 0.8→1%/s, hp_lvl 17→19, AND atk 14.3/4.0→15.0/4.3 (his damage still lagged). Rough L42 result: warrior ~1200 hp at ~55-60% phys reduction = 8-10 connected boss hits + 18 hp/s regen — a wall that still loses to sloppy play; paladin slightly softer, heals harder, hits as hard as the warrior. NEXT LEVER if still soft: a flat "plated" damage-taken reduction (multiplicative after res) as a plate-exclusive passive — held in reserve to avoid double-tuning.
- **Assassin dance kit (round 18):** Blood survives the L42 benchmark but landing damage stayed too risky — melee range IS the disproportionate cost of the archetype. Tools + numbers, all identity-true: Stab slides a short 55px step in the held move direction per cast (0.3s cadence = constant weaving; shares one _move_dir() input helper with the mover) and its arc mult rose 0.8→0.9; Shadow Dash 7→6s (more i-frame passes); **Fan of Knives 4.5→2.8s, damage untouched** (the ranged poke goes rotational — damage while repositioning, per playtest suggestion); Death Mark grants a 0.8s i-frame while blinking onto the target (longer than the dash's 0.5s — the commit button). Verdict pending the next benchmark; if damage still lags, one atk_lvl notch is the remaining lever. *(The slide-step was later REMOVED in round 30.)*
- **Class balance doctrine (agreed post-round-17, no numbers moved):** every class splits one constant budget between FORGIVENESS (sustain/mitigation/range — passive safety that works through mistakes) and PAYOUT (damage/burst). Sustain is the strongest currency (it converts time into safety at zero attention cost), so high-forgiveness kits (warlock, paladin, warrior) sit at or slightly below the payout line; execution kits (mage, assassin) earn ~10-15% above it AT THEIR SKILL CEILING, never more — mastery is paid, others aren't invalidated. Two rules that follow: (1) execution classes' payout edge should live in BASE/ability multipliers (constant ratio at every level), NOT in growth (atk_lvl edges compound and outscale late game — round 17's growth-based edge is flagged for conversion if late-game testing shows divergence); (2) their survival tools must stay binary, timing-gated and rate-limited (ward, dash i-frames) — floors stay low, ceilings pay. Future extension candidates: perfect-play rewards (i-framing THROUGH a telegraph refunds dash cd; a consumed ward refunds mana) — raise the ceiling by paying skill, never the floor by padding health. Forgiveness classes never get these; that is their trade.
- **Convergence, second pass (round 17):** per-class L42 verdicts — Warlock "golden standard: challenging, doable, engaging" → FROZEN as the reference kit; Archer "doable but tedious, damage a little short" → 10.5/2.7 → 11.0/2.85; Mage "needs godly kiting" → glass cannon now has the highest growth (13.0/3.4 → 13.5/3.7) AND Blink 6.0 → 4.5s cd (more wards, more mobility — the ward cadence was the real wall, one absorb per 6s vs boss hits every ~1.5s); Assassin "bottom of the barrel" → 14.0/4.1 → 14.5/4.5 (highest melee growth — the squishiest melee must hit hardest), regen 1 → 1.2%/s, and **Shadow Dash gains Tumble-style i-frames** (hurt_cd 0.5 — dashing THROUGH the swing is the assassin answer to the melee uptime tax); Warrior 15.0/4.0 → 15.5/4.3 and Paladin 14.0/3.8 → 14.3/4.0 (untested this round; same pattern, verify next). Calibration rule stands: every class benchmarks against the warlock's L42 A-gear nullwarden clear. *(Dash i-frames later REMOVED in round 43.)*
- **Six-kit convergence (round 16):** playtest verdict — warlock was the only class on the boss line; warrior/paladin "lack damage" (the melee uptime tax vs longer, harder boss fights), assassin's 20% dodge is unreliable RNG with weak regen, mage has no sustain and sub-par damage. Philosophy: the warlock's L42-A-gear clear IS the calibration (round 13), so five come UP and warlock shaves ~10% to meet just below it. Changes: Warrior atk 14.5/3.7→15.0/4.0; Paladin 13.5/3.5→14.0/3.8; Assassin 13.5/3.8→14.0/4.1 + eva 20→25% + regen 0.6→1%/s (reliability over RNG); Mage 12.5/3.1→13.0/3.4 + **Arcane Ward** (Blink shields — the next hit within 4s fully absorbed; one absorb per Blink, 6s cd — the round-14 candidate, skill-timed sustain, not lifesteal); Warlock Shadowbolt 0.5→0.55s + lifesteal 6→5%. Every class now has identity-true sustain: regen (war), leech+regen (pal), evasion+regen (sin), leech+pact (lock), Second Wind (archer), Blink ward (mage).
- **Class-aware loot + archer damage (round 15):** playtest — archer damage lagged the warlock (both ranged, but the warlock never paid the archer's round-3 tax and carries the hex amp), and gear rolled dead stats (MagPen on phys classes; archers looting Tomes). Fixes: (1) archer atk 10→10.5, growth 2.5→2.7, Quick Shot 0.4→0.36s (attack-speed identity, ~+20% single-target — parity with warlock counting his vuln amp); (2) `Items.CLASS_WEAPONS` — weapon DROPS come only from the receiving class's arsenal (warrior Blade/Edge/Claymore, archer Bow/Crossbow, assassin Fang/Kunai, mage Staff/Wand, paladin Hammer/Blade, warlock Tome/Wand; anything still equippable from the bag); (3) rolled substat pools drop the OTHER damage type's penetration (phys never rolls MagPen, magic never rolls PhysPen; neutral stats stay shared). Chest and shop paths both pass the class already.
- **Class sustain parity without universal lifesteal (round 14):** playtest — the L42 A-gear parity nullwarden is beatable on warlock (6% lifesteal passive) but NOT on archer (pure-offense passive, zero recovery). Universal lifesteal rejected (flattens identity). Fix: **Archer — Second Wind**: untouched for 4s → +2.5% max HP/s until next hit taken (`sw_delay`/`sw_regen` passive keys; a connected hit resets the clock). Kiting discipline IS the sustain — deeply archer. Round 14b: first cut (4s/2.5%) was too weak — lifesteal is a RATE ON DPS (~35-50 hp/s for a L42 warlock) and dwarfs a flat trickle. Now 3s/6%/s ≈ ~42 hp/s while spaced: matches the leech when played well, drops to zero when tagged (the warlock heals THROUGH mistakes; the archer heals by not making them). Sustain audit: Warrior regen 1%/s, Paladin lifesteal+regen, Assassin evasion+regen, Warlock lifesteal+Pact, Archer Second Wind — **Mage remains the one sustainless class** (burst + Blink + range as defense); watch in the next boss playtest, candidate fix if needed: a Blink-timed absorb ward, NOT lifesteal.
- **Boss curve recalibrated to the gear timeline (round 13):** playtest — L42 nullwarden vs L42 full-A-gear warlock was "a bit too difficult"; it felt like S gear was required, but S only starts dropping L40-70. Diagnosis: boss damage GROWTH (round 11's rescaled 7.7%/level, and round 12's staged edge on top) still outpaced the player's ~5.5%/level, so the parity gap silently widened with level. Recalibration: the skill tilt is the CONSTANT `BOSS_DMG_MULT = 1.2` (felt equally at every level); boss damage growth is pinned to the player curve (`BOSS_DMG_GROWTH = 0.055`, replacing the compounding edge). Gear rule now explicit: **at-level bosses must be beatable in the act's top drop grade (A before L40); S-tier is comfort and difficulty-tier headroom.** L42 parity hit: ~345 plain / ~415-465 with multipliers vs ~820 A-gear HP — two connected hits kill, dodging decides, no S required.
- **Bosses out-curve the player (round 12):** feedback — post-round-11 Warden Null's HP is "perfectly in tune" but his damage still lacks, and endgame bosses SHOULD have a slightly better power curve than the player so skill, not stats, wins parity fights. Two knobs, damage only (boss HP stays on the parity curve): `Balance.BOSS_DMG_MULT = 1.2` (bosses hit above their tables at any level, stacking with ENEMY_DMG_MULT) and `Balance.BOSS_DMG_GROWTH_EDGE = 0.01` (+1%/level on top of the rescaled growth — bosses gain ~2-3%/level on the player). Resulting feel: Fangmaw at L5 bites for ~26% of a squishy (charge ~36%); Warden Null at-anchor slams for ~26-30%; at L38 parity a boss hit costs ~60-75% — two connected hits kill, dodging decides. Mobs unchanged (their damage was called right at round 10). *(The growth edge was replaced by the round-13 pinned curve.)*
- **Growth matched to the player curve (round 11):** trigger — a L38 nullwarden ONE-SHOT a L38 full-S-gear player (55 x 1.14^22 = ~985 dmg vs ~900-1000 hp), while the same boss was trivial at low level. Root cause: compounded hp_g/dmg_g (~0.08-0.15/level) were tuned for GAP walls but also run along the at-level axis, where player power only grows ~5-6%/level — parity collapsed far from a monster's anchor. Fix: `Balance.GROWTH_SCALE = 0.55` rescales every authored growth rate inside enemy_stats_at (all chapters, codex-honest): effective growth ~4.5-8%/level, so at-level parity holds at ANY level. Gaps still bite: +10 = ~2x dmg (~2 mistakes), +20 Nightmare = ~3x with the round-10 dmg mult on top (near one-shot for squishies). This SUPERSEDES round 4's "+10 = one mistake" absolutism — that goal was mathematically incompatible with at-level parity across the leveling range. The autotest now guards BOTH directions (+10 must be >=1.8x dmg and <=3.5x).
- **Threat pass (round 10):** trigger — a L10 C-gear warlock facetanked a L16 Warden Null for ~6s and outran everything. Four levers, tuned together and deliberately conservative (they stack with round 9's HP): (1) `Balance.ENEMY_DMG_MULT = 1.3` — ALL monster damage, applied in enemy_stats_at so the codex stays honest (the codex monster rows now display live at-anchor stats, not raw table values); (2) ch1–2 boss speeds +~15% (Fangmaw 140→160, Morwen 105→120, Vargoth 115→132, Stormwarden 150→170, Choir Mother 105→120, Warden Null 85→100) — the player still outruns them, but kiting costs attention; (3) attack cadence/wind-ups trimmed ~10% (mob windup 0.3→0.27, bites, boss telegraph delays) — ch1–2 only, ch3's WIP untouched; (4) **no free full heal on level-up** — recalc keeps your hp/mp FRACTION as pools grow, so potions and merchants matter. Bonus fix: Vargoth's blade storm dealt a HARDCODED 30 damage at any level — now dmg×1.3, scaling like everything else (round-6 rule). Playtest the feel: every knob is one line.
- **Boss TTK budget (round 9):** trigger — Fangmaw died in <10s to a C-gear, zero-talent warlock. Root cause: boss HP pools predated gear grades/themes/skill tree and were exempted from the round-4 mob HP doubling ("already long fights" — no longer true; every class at L5 deals ~55–85 effective DPS). Rule: at level with modest gear a boss survives **~25s (chapter opener) / ~30s (mid) / ~40s (finale)** of realistic DPS — retune pools whenever player power moves. Ch1: Fangmaw 460→1200, Morwen 620→2200, Vargoth 1000→4200. Ch2: Stormwarden 750→2000, Choir Mother 1000→3200, Warden Null 2400→8000. Damage untouched (round-4 contact rule stands). Warlock verdict: NOT broken — Hex's guaranteed 3s EXPOSE on a 7s cd ≈ +21% net, strongest of six kits but within variance; watch it once fights actually last.

## Structural rounds (2–8) — systems that came out of pacing

- Rooms grow to 2–3 screens; more packs per room; aggro per-pack.
- TTK at level parity ~doubles (mob HP tune, not damage). Gold/potion scarcity so merchants matter.
- **Exponential monster growth, no level-gap rule (round 4):** hp/dmg growth COMPOUNDS (`base × (1+g)^Δ`). At-level balance untouched; +10 = a wall of honest codex stats (~1 mistake dead; ~2 at +5). No hidden multipliers anywhere. Boss base damage retuned up (contact ≈ 20–25% of a squishy's at-level HP; god runs stay possible). Rewards LINEAR per level — no farm spiral. (Trigger: naked Lv4 mage soloed a Lv14 boss.) *(The "+10 = one mistake" absolutism was superseded by round 11's GROWTH_SCALE.)*
- **Fixed chapter XP totals (round 4):** kill XP only from authored packs + bosses; event spawns/boss summons pay zero. Completed chapters pay NO XP on replay — farm gold/gear, never levels. XP curve assumes side rooms; skipping leaves you under-leveled.
- **Act loot ceilings (round 4):** Ch1 caps at C, Ch2 at A, S = Act 3/endgame. Rolls above the cap collapse to it. Ch1 was implicitly balanced around lucky A/S drops — the cap + mob retune is deliberate pressure; verify in playtest.
- **Level parity is the XP budget (round 5):** Ch1 trash paid ~1.6× its level plan (full clear hit L15 vs the L12 boss; "outleveling by 4 is unfair"). Trash XP cut ~45% (wolf 12→7, spider 14→8, cultist 20→11, skeleton 24→13, zombie 15→8): full clear now tracks mob levels ±1, hits bosses AT level (L5 Fangmaw / L8 Morwen / enter Vargoth L11, ding 12 on kill); spine-only ~1 under. Ch2 was already on budget (L8 Choir Mother, L16 Warden Null). Rule for new chapters: sum authored pack XP through the 30+22·lvl curve; land each boss room at boss level.
- **Attributes: 1 point/level, not 5 (round 5):** same cadence as skill points. 5/level banked 30+ by L10 and silently added ~1.5 levels of ATK per level. Per-point ratios unchanged.
- **No downscaling — listed level is a MINIMUM (round 6):** (trigger: "L20 mage solos 5 L36 Warden Nulls") endgame boss in Ch1 arrives stats-as-is; early boss at endgame scales UP (stays reusable). Ch2 bosses re-anchored at story levels 8/10/16 (was 14/22/32 — floors had gutted them: story Warden Null hit for 12, softer than his trash). Rewards re-anchored; chapter XP unchanged.
- **Every substat scales with level (round 6):** monsters carry an attribute BUILD (`attrs` pts/level above anchor via `MONSTER_ATTR_SCALE`: STR→pen/critres, AGI→dex/crit/eva, INT→magpen/magres, VIT→res/critres); archetype default when unauthored (brute STR / skirmisher AGI / caster INT; bosses invest 2× trash). Boss dmg growth raised to ~hp growth (0.13–0.14). Enemy hits resolve through the SAME Stats.resolve as player hits (their crit/pen/dex vs your critres/res/eva); telegraphs/hazards stay plain — dodge by moving.
- **Talent points buy substats 1:1 (round 6):** PhysRes/MagRes/CritRes/DEX/PhysPen/MagPen (Classes.SUBSTAT_SCALE) besides class-scaled STR/AGI/INT/VIT. Combo deliberately NOT purchasable (bought cooldown-skip would snowball).
- **Room sizes vary (round 6):** quiet types (social, dead_end, resonance, merchant, elite arenas) shrink their walled playable area (`Balance.SMALL_ROOM_INSET`); corridors connect doors to cell edges; camera clamps to the playable rect.
- **ELITES (round 6):** promoted monster = ~4× HP, 1.5× dmg, +res/critres, 1.3× sprite, gold ring. Seeded per character: ~30% of social rooms hold a lone elite instead of a wanderer (wanderer moves in after the kill); ~18% of combat rooms promote one pack member. ZERO XP; loot piñata: 3× gold, guaranteed gem, guaranteed silver/gold chest, ~30% reset stone, ~15% bag. LATER CHAPTERS may spawn several at once — single-elite numbers are Ch1–2 tuning, not the system's shape.
- **Bags & consumables (round 6):** the bag = carried capacity for everything unequipped; gear, GEM STACKS and consumables share slots (F15/E20/D25/C35/B50/A70/S100, `Items.BAG_SLOTS`; +15 per tier with the inventory rework → F30…S115). Gems stack one slot per stat+level; fitting an existing stack is always free (round 7 — the hoard relief valve). One bag; bigger loot upgrades in place, smaller → gold; elites are the source. Overflow pickups auto-sell; internal gem machinery never jams on capacity. Inventory = WoW-style slot grid (gear icons, gem stacks click-×3-to-synthesize, consumables click-to-use, dark free squares). First consumable: **Stone of Unlearning** (elite ~30%) — refunds every allocated talent point.
- **Resonance is visible (round 6):** Stats tab shows number + band + plain explanation (haggle prices, gated dialogue, NPC reactions). The "feel it first" rule held for Phase 1; real players immediately asked what the stat was.
- **Dev/rogue boss kills never touch the story (round 7):** only zone-spawned (`story_boss`) bosses drive quests/gates/dialogue/`boss_done`/endings; dev/test spawns route to `game.on_rogue_boss_died` — rewards only. (Trigger: a spare dev Vargoth died in the village and "won" Ch1.) Multi-boss music stays at its peak until the last boss falls.
- **Mailbox & ground overflow (round 8):** bag-full pickups DROP at your feet (tracked pickups; standing on one retries the claim) — nothing silently sells, and the shop refuses purchases into a full bag. At chapter end (victory or replay/advance teardown) unclaimed ground loot mails itself: subject "Dropped Loot", no body; the letter shows an inventory-style item grid + Claim (partial claims leave the rest). Claimed letters stay until deleted; unclaimed mail expires after `Balance.MAIL_EXPIRY_DAYS` (30). Mailbox lives in the pause menu with an unread badge; `game.send_mail(subject, body, items)` doubles as the dev/event-gift API. **Clock cheating:** players roll the OS clock to farm timed rewards — `game.trusted_now()` is a persisted monotonic anchor (never goes backwards; rolling forward only accelerates your OWN expiries). Never grant time-based anything from raw OS time.
- **Resonance surfacing v2 (round 8):** a HUD line right under Combat Rating — golden with sparkle particles when positive (shinier/busier as it climbs), ink-black on pale outline when negative, and a pulse animation on every change (bright for gains, violet for losses). Shard choices made in quiet (non-combat) rooms now pay a token reward on ANY resonance change, up or down (gold; + a chest at |Δ|≥5) — the shard rewards conviction, not virtue. The codex gained ELITES, BAGS and CONSUMABLES entries. NPC idle emote bubbles read the band too (`IDLE_EMOTES`): hearts and song when steady, wary …/?/! when tempted.
- **Combat readability** (standing feedback): visible cooldown/resource numbers; melee risk compensation via class passives.
