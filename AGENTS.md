# Crownless — working practices (all agents)

**The working practices live in [`CLAUDE.md`](CLAUDE.md) — the single source of truth.**

This file is intentionally just a pointer. The practices (mobile policy, code
layout, testing order, GDScript traps, multi-agent etiquette, asset sourcing,
PixelLab workflow) used to be duplicated here verbatim, which meant every edit
had to be made twice and the two copies drifted. Read `CLAUDE.md` instead —
whatever agent you are, that is the authoritative file for how work is done in
this repo.
