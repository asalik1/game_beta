# NPC painted ground contact

Factory NPC cast shadows now join the current visible painted edge of the body. The anchor follows the current sprite frame, flips, offset and transform, so breathing no longer separates the body from its projected cast shadow. The sampled alpha threshold excludes faint export fringe. Per-texture/frame measurements cache only coordinates; replacing the texture clears that cache.

This is an opt-in factory NPC path. Default hero/enemy cast shadows and scenery prop shadows retain their existing behavior. No artwork was regenerated or edited. The faint contact ellipse is unchanged; the painted-edge proxy is not an anatomical foot measurement.

Visual acceptance is scoped to the NPC cast join and unchanged behavior elsewhere. Full prop views still show the green animated tree elevated above its broad cast; capital views still show the Clerk Voss prompt crossing character heads, and the right-side scenery Read prompt can brush the hero helmet. These existing placement concerns remain follow-up work, not solved or approved overall by this checkpoint.

Desktop quick/full 147/227 and mobile quick 147 pass, with import/compile, scoped five-source synchronization and strict preflight. Fresh baseline retains 59 diagnostic checks; desktop and host-rendered mobile strict grounding each pass 109 checks, including eight known-mask cases. All 59 fresh originals have completed per-image original-resolution review; 36 native close-ups are separately retained as auxiliary evidence. Default prop/scenery each passes 187 exact checks with raw full-bounds/centering evidence; capital NPC context regressions pass.

Run `shot.bat prop_shadows --npc-contact --npc-grounding-strict --timeout=300` in a fresh isolated APPDATA. Omit the strict flag only for the old-source diagnostic; it checks capture/setup prerequisites and records the separation, without waiving strict geometry failures. The five focused originals show the three affected NPC assets at normal/close scale and a later live breath sample. Eight independent known-mask controls exercise frame changes/revisit, horizontal/vertical flips, noncentered offset, rotation/skew/scale and texture replacement. Run default `prop_shadows` and `capital_arrival` separately for scenery, live animation, interaction and readable NPC/HUD context. Mobile evidence renders mobile source on the host using Compatibility; it is not physical-device testing.

The fresh baseline and historical five-image diagnostic remain preserved, along with raw DeepSeek output, rejected revisions and the original helper UID archive. The strict review requires coherent body/cast contact, complete body/carried items and no visual regressions; numerical geometry alone is insufficient. The ordinary full suite supplies existing network/domain regressions; this change makes no new ENet or authority claim.

Coverage is limited to the tested factory assets and Sprite2D controls. Conversation-facing assets, region/Atlas fallback, mutation of texture pixels in place and physical-device behavior remain untested. Project import descriptors and helper UIDs are pinned independently; mobile synchronization never copies UID sidecars. Existing shutdown-only renderer diagnostics remain visible in logs under the unchanged runner policy, so passing gates are not an assertion of error-free shutdown logs.

Exact source, assets, receipts, original/auxiliary hashes, review manifests and final commit state are recorded in `build/qa/session-sept17/npc-grounding-checkpoint-validation.json`. Root records the serialized commit after reviewing the documentation and explicit fifteen-path scope.

Earlier default prop captures were rejected even though their crop intersections passed: normal camera framing overrode the manual fixture position and clipped the intended subject. The repaired QA gives the camera a temporary owned stationary parent, restores its original ownership/settings, and requires complete raw viewport bounds plus centered camera/unchanged limits for every measured capture. Old fixed1 evidence remains preserved and contributes no final default visual acceptance.
