Reproduce this 8-frame back-view walk EXACTLY, fixing ONE equipment detail. This
is a surgical edit -- NOT a redesign and NOT a new walk.

Reference image 1 is the CURRENT 8-frame back-view walk strip. Its leg cycle, body,
tabard, hammer and everything else are CORRECT -- reproduce every frame as drawn.
Its ONE defect: the kite shield on his left side is rendered as a DARK BROWN
surface in the frames where it shows.

Reference image 2 is his idle from the same angle and shows the CORRECT shield:
silver-grey face with a clearly visible GOLD CROSS, peeking out on his left side.
In EVERY frame where the shield is visible, repaint it to exactly that silver-and-
gold-cross look, same size and position as reference 1 drew it. Change nothing
else -- same legs, same palette, same brightness everywhere else.

Produce ONE image: a horizontal row of EXACTLY 8 figures, equal-width cells, all
feet on one shared ground line, constant body height, torso near each cell's
horizontal center, green margins on all sides -- no figure touching any image edge.
Flat solid #00ff00 GREEN background, nothing else in the image. Generate ONE image
and SAVE it to disk at the exact path
C:\Users\asali\Projects\MMO\art_src\stride_pilot_2026-08-27\paladin_bn_shieldfix\paladin_bn_shieldfix_row.png,
then stop.

In your final message report, frame by frame: is the shield now silver with the
gold cross, and does everything else match reference 1 exactly.
