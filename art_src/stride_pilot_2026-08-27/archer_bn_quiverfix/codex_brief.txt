Reproduce this 6-frame back-view walk EXACTLY, fixing ONE equipment detail. This is
a surgical edit -- NOT a redesign and NOT a new walk.

Reference image 1 is the CURRENT 6-frame back-view walk strip. Its leg cycle, cape
motion, body proportions, colors and everything else are CORRECT -- reproduce every
frame as drawn. Its ONE defect: the quiver on her back is slung LOW and DIAGONAL
across the mid-back.

Reference image 2 is her idle from the same angle and shows the CORRECT placement:
the quiver sits VERTICAL and HIGH on her back, arrows visible at the top beside her
head, strap over the shoulder. In EVERY frame, reposition the quiver (and its
arrows) to exactly that idle placement. The bow stays in the same hand as reference
1. Change nothing else -- same legs, same cape, same palette, same brightness.

Produce ONE image: a horizontal row of EXACTLY 6 figures, equal-width cells, all
feet on one shared ground line, constant body height, green margins on all sides --
no figure touching any image edge. Flat solid #00ff00 GREEN background, nothing
else in the image. Generate ONE image and SAVE it to disk at the exact path
C:\Users\asali\Projects\MMO\art_src\stride_pilot_2026-08-27\archer_bn_quiverfix\archer_bn_quiverfix_row.png,
then stop.

In your final message report, frame by frame: does the quiver sit vertical-high as
in reference 2, and do the legs match reference 1 exactly.
