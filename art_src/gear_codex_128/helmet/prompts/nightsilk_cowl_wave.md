# Nightsilk Cowl family wave

Generator: built-in ImageGen using the shared flat `#00ff00` chroma-key workflow. Untouched source PNGs are retained under `../generated/`; approved RGBA masters are retained under `../alpha/`.

## `h_nightsilk_cowl`

Neutral assassin family master: one empty standalone midnight-blue and black silk cowl with a gently swept crown, deep black face opening, layered collar folds, and a restrained dark-silver crescent brow clasp. It is a restrained wearable with no magic. This candidate was accepted after alpha inspection; its key field was removed with soft matte + despill.

Every source prompt carries the shared contract: one centered empty cowl, no person/head/face/body/mannequin/floor/text/duplicate prop or shadow, and a uniform green chroma field with no green in the item.

## 2026-08-08 continuation

`h_nightsilk_cowl_A`, `h_nightsilk_cowl_B`, and `h_nightsilk_cowl_S` each
used one separate built-in ImageGen call. The accepted results are one empty
blue-black silk cowl with a hollow opening, onyx fastening, and progressively
richer midnight-silk/dark-metal construction. Exact chroma sources and
approved alpha masters are stored by key in `../generated/` and `../alpha/`.
Border auto-key, soft matte (12/220), despill, and the non-installing
128px/32px builder passed for every key. Runtime and `mobile/` remain
untouched.
