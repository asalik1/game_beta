# Studded Hood continuation wave

## `h_studded_hood_A`

- Source: exactly one built-in ImageGen output, retained untouched as
  `../generated/h_studded_hood_A.png`; approved master:
  `../alpha/h_studded_hood_A.png`.
- Prompt: one A-grade empty Studded Hood: forest-green layered leather, an
  empty dark face cavity, fitted brow band, short shoulderless cowl edge,
  bronze stud rows and leaf-shaped rivets. Exotic panels, richer relief, dark
  verdigris fittings, contained amber glints and non-readable stitching make
  it exceptional. It prohibited people, heads, faces, bodies, mannequins,
  floor/shadow, text, duplicates, and magenta in the object on `#ff00ff`.
- Extraction/QA: border auto-key `#fb04f9`, soft matte, thresholds 12/220,
  despill. Transparent corners, exactly one empty hood, and a readable 128px
  plus hard-alpha 32px builder candidate were visually confirmed. No rejection
  or retry; runtime/mobile untouched.

## `h_studded_hood_B`

- Source: exactly one built-in ImageGen output, retained untouched as
  `../generated/h_studded_hood_B.png`; approved master:
  `../alpha/h_studded_hood_B.png`.
- Prompt: one B-grade empty Studded Hood retaining green leather, empty face
  cavity, brow band, short cowl, bronze stud rows and leaf rivets, with neat
  layered panels, fine stud spacing, restrained ornament, one amber glint and
  non-readable stitching. It prohibited people, heads, faces, bodies,
  mannequins, floor/shadow, text, duplicates, and magenta in the object on
  `#ff00ff`.
- Extraction/QA: border auto-key `#fb03fa`, soft matte, thresholds 12/220,
  despill. Transparent corners, exactly one empty hood, and readable 128px and
  hard-alpha 32px candidates were visually confirmed. No rejection or retry;
  runtime/mobile untouched.

## `h_studded_hood_S`

- Source: exactly one built-in ImageGen output, retained untouched as
  `../generated/h_studded_hood_S.png`; approved master:
  `../alpha/h_studded_hood_S.png`.
- Prompt: one S-grade empty Studded Hood retaining green leather, empty face
  cavity, brow band, short cowl, bronze stud rows and leaf rivets. Legendary
  construction adds richest layered leather, sculpted brow relief, finest dark
  verdigris fittings, dense non-readable stitchwork and two compact amber
  glints. It prohibited people, heads, faces, bodies, mannequins, floor/shadow,
  text, duplicates, and magenta in the object on `#ff00ff`.
- Extraction/QA: border auto-key `#fb04f9`, soft matte, thresholds 12/220,
  despill. Transparent corners, exactly one empty hood, and readable 128px and
  hard-alpha 32px candidates were visually confirmed. No rejection or retry;
  runtime/mobile untouched.
