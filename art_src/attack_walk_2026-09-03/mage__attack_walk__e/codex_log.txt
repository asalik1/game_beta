codex.exe : Reading prompt from stdin...
At line:7 char:14
+ ...    $brief | & $codex exec -C $d -s workspace-write --skip-git-repo-ch ...
+                 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (Reading prompt from stdin...:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
OpenAI Codex v0.152.0
--------
workdir: 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: high
reasoning summaries: none
session id: 01a0674d-4655-7b60-8aa1-58c8c943d7a8
--------
user
Redraw this EXACT walk cycle (reference 1) with one change: the character fires
its basic attack once during the stride.

Reference 1 is the character's own 7-frame walk, in profile, facing RIGHT, frame by frame. Reproduce
it EXACTLY: the same 7 frames, the same leg cycle and stride, the same crossing,
the same garment, the same held equipment in the same hand, the same body height
and width, the same palette. The legs are already correct -- do not redesign them.

THE ONLY CHANGE: the FREE hand (not the staff hand) thrusts forward at chest height, palm open, as if pushing a bolt 
away. Spread the motion across the 7 frames so it starts,
peaks around the middle and returns by the end, while the legs keep walking
through their unchanged cycle.

THE CHARACTER (unchanged from reference 1): a slender woman in a deep-blue hooded mage's robe with gold trim and a 
gold sash, a long dark staff with a glowing blue-white crystal head held in one hand, dark boots below a CLOSED 
floor-length hem.

Hard requirements:
- The feet stay PLANTED on the shared ground line exactly as reference 1 has them;
  the body does not slide sideways within its cell.
- The robe stays CLOSED and floor-length: no slit, no exposed legs, no bare
  skin. The walk reads through the BOOTS alternating below the hem and the
  hem swaying -- exactly as reference 1 shows it.
- Draw NO projectile, arrow, knife, flame, bolt, spark or muzzle flash anywhere.
  The game spawns the projectile itself; anything drawn here would double it.
- Same body height and width in every frame; no frame is a copy of another.

Produce ONE image: a horizontal row of EXACTLY 7 figures, evenly spaced, all feet
on one shared ground line, each body near its cell's horizontal centre, green
margins all round -- no figure touching an image edge or its neighbour. EVERY FIGURE
IS THE SAME SIZE; if they will not fit at full size, make the image WIDER. Flat solid
#00ff00 GREEN background, nothing else in the image.
Generate ONE image and SAVE it to disk at the exact path
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e\ma
ge_attack_walk_e_row.png
then stop.

In your final message, report for each frame: which leg leads, and where the firing
hand is. Confirm no projectile was drawn and the hem stayed closed.


codex
IΓÇÖm using the image-generation skill to preserve the supplied sprite sheet while changing only the free-hand attack 
motion. IΓÇÖll first load the repoΓÇÖs art rules and the skill instructions, then generate and verify the saved 
7-frame row.
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 
..\\..\\..\\..\\..\\CLAUDE.md; Get-Content -Raw 'C:\\Users\\asali\\.codex\\skills\\.system\\imagegen\\SKILL.md'" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 325ms:
Get-Content : Cannot find path 'C:\Users\asali\Projects\MMO\.claude\CLAUDE.md' because it does not exist.
At line:2 char:1
+ Get-Content -Raw ..\..\..\..\..\CLAUDE.md; Get-Content -Raw 'C:\Users ...
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : ObjectNotFound: (C:\Users\asali\...laude\CLAUDE.md:String) [Get-Content], ItemNotFoundEx 
   ception
    + FullyQualifiedErrorId : PathNotFound,Microsoft.PowerShell.Commands.GetContentCommand
 
---
name: "imagegen"
description: "Generate or edit raster images when the task benefits from AI-created bitmap visuals such as photos, 
illustrations, textures, sprites, mockups, or transparent-background cutouts. Use when Codex should create a brand-new 
image, transform an existing image, or derive visual variants from references, and the output should be a bitmap asset 
rather than repo-native code or vector. Do not use when the task is better handled by editing existing 
SVG/vector/code-native assets, extending an established icon or logo system, or building the visual directly in 
HTML/CSS/canvas."
---

# Image Generation Skill

Generates or edits images for the current project (for example website assets, game assets, UI mockups, product 
mockups, wireframes, logo design, photorealistic images, or infographics).

## Top-level modes and rules

This skill has exactly two top-level modes:

- **Default built-in tool mode (preferred):** built-in `image_gen` tool for image generation, editing, and 
transparent-image requests. Does not require `OPENAI_API_KEY`.
- **Fallback CLI mode:** `scripts/image_gen.py` CLI. Use when the user explicitly asks for or confirms the 
CLI/API/model path. Requires `OPENAI_API_KEY`.

Within CLI fallback, the CLI exposes three subcommands:

- `generate`
- `edit`
- `generate-batch`

Rules:
- Use the built-in `image_gen` tool by default for normal image generation and editing requests.
- Do not switch to CLI fallback for ordinary quality, size, or file-path control.
- For transparent images, ask built-in `image_gen` for a transparent background and preserve the generated alpha.
- Never silently switch from built-in `image_gen` or CLI `gpt-image-2` to CLI `gpt-image-1.5`; ask the user first 
unless they explicitly requested `gpt-image-1.5`.
- The word `batch` by itself does not mean CLI fallback. If the user asks for many assets or says to batch-generate 
assets without explicitly asking for CLI/API/model controls, stay on the built-in path and issue one built-in call per 
requested asset or variant.
- If the built-in tool fails or is unavailable, tell the user the CLI fallback exists and that it requires 
`OPENAI_API_KEY`. Proceed only if the user explicitly asks for that fallback.
- If the user explicitly asks for CLI mode, use the bundled `scripts/image_gen.py` workflow. Do not create one-off SDK 
runners.
- Never modify `scripts/image_gen.py`. If something is missing, ask the user before doing anything else.

Built-in save-path policy:
- In built-in tool mode, Codex saves generated images under `$CODEX_HOME/*` by default.
- Do not describe or rely on OS temp as the default built-in destination.
- Do not describe or rely on a destination-path argument (if any) on the built-in `image_gen` tool. If a specific 
location is needed, generate first and then move or copy the selected output from `$CODEX_HOME/generated_images/...`.
- Save-path precedence in built-in mode:
  1. If the user names a destination, move or copy the selected output there.
  2. If the image is meant for the current project, move or copy the final selected image into the workspace before 
finishing.
  3. If the image is only for preview or brainstorming, render it inline; the underlying file can remain at the 
default `$CODEX_HOME/*` path.
- Never leave a project-referenced asset only at the default `$CODEX_HOME/*` path.
- Do not overwrite an existing asset unless the user explicitly asked for replacement; otherwise create a sibling 
versioned filename such as `hero-v2.png` or `item-icon-edited.png`.

Shared prompt guidance for both modes lives in `references/prompting.md` and `references/sample-prompts.md`.

Fallback-only docs/resources for CLI mode:
- `references/cli.md`
- `references/image-api.md`
- `references/codex-network.md`
- `scripts/image_gen.py`

## When to use
- Generate a new image (concept art, product shot, cover, website hero)
- Generate a new image using one or more reference images for style, composition, or mood
- Edit an existing image (inpainting, lighting or weather transformations, background replacement, object removal, 
compositing, transparent background)
- Produce many assets or variants for one task

## When not to use
- Extending or matching an existing SVG/vector icon set, logo system, or illustration library inside the repo
- Creating simple shapes, diagrams, wireframes, or icons that are better produced directly in SVG, HTML/CSS, or canvas
- Making a small project-local asset edit when the source file already exists in an editable native format
- Any task where the user clearly wants deterministic code-native output instead of a generated bitmap

## Decision tree

Think about two separate questions:

1. **Intent:** is this a new image or an edit of an existing image?
2. **Execution strategy:** is this one asset or many assets/variants?

Intent:
- If the user wants to modify an existing image while preserving parts of it, treat the request as **edit**.
- If the user provides images only as references for style, composition, mood, or subject guidance, treat the request 
as **generate**.
- If the user provides no images, treat the request as **generate**.

Built-in edit semantics:
- Built-in edit mode is for images already visible in the conversation context, such as attached images or images 
generated earlier in the thread.
- If the user wants to edit a local image file with the built-in tool, first load it with built-in `view_image` tool 
so the image is visible in the conversation context, then proceed with the built-in edit flow.
- Do not promise arbitrary filesystem-path editing through the built-in tool.
- If a local file still needs direct file-path control, masks, or other explicit CLI-only parameters, use the explicit 
CLI fallback only when the user asks for it.
- For edits, preserve invariants aggressively and save non-destructively by default.

Execution strategy:
- In the built-in default path, produce many assets or variants by issuing one `image_gen` call per requested asset or 
variant.
- In the CLI fallback path, use the CLI `generate-batch` subcommand only when the user explicitly chose CLI mode and 
needs many prompts/assets.
- For many distinct assets, do not use `n` as a substitute for separate prompts. `n` is for variants of one prompt; 
distinct assets need distinct built-in calls or distinct CLI `generate-batch` jobs.

Assume the user wants a new image unless they clearly ask to change an existing one.

## Workflow
1. Decide the top-level mode: built-in by default, including transparent-output requests; fallback CLI only if 
explicitly requested or confirmed.
2. Decide the intent: `generate` or `edit`.
3. Decide whether the output is preview-only or meant to be consumed by the current project.
4. Decide the execution strategy: single asset vs repeated built-in calls vs CLI `generate-batch`.
5. Collect inputs up front: prompt(s), exact text (verbatim), constraints/avoid list, and any input images.
6. For every input image, label its role explicitly:
   - reference image
   - edit target
   - supporting insert/style/compositing input
7. If the edit target is only on the local filesystem and you are staying on the built-in path, inspect it with 
`view_image` first so the image is available in conversation context.
8. If the user asked for a photo, illustration, sprite, product image, banner, or other explicitly raster-style asset, 
use `image_gen` rather than substituting SVG/HTML/CSS placeholders. If the request is for an icon, logo, or UI graphic 
that should match existing repo-native SVG/vector/code assets, prefer editing those directly instead.
9. Augment the prompt based on specificity:
   - If the user's prompt is already specific and detailed, normalize it into a clear spec without adding creative 
requirements.
   - If the user's prompt is generic, add tasteful augmentation only when it materially improves output quality.
10. Use the built-in `image_gen` tool by default.
11. For transparent-output requests, ask built-in `image_gen` for a transparent background and preserve the generated 
alpha channel.
12. Inspect outputs and validate: subject, style, composition, text accuracy, and invariants/avoid items.
13. Iterate with a single targeted change, then re-check.
14. For preview-only work, render the image inline; the underlying file may remain at the default 
`$CODEX_HOME/generated_images/...` path.
15. For project-bound work, move or copy the selected artifact into the workspace and update any consuming code or 
references. Never leave a project-referenced asset only at the default `$CODEX_HOME/generated_images/...` path.
16. For batches or multi-asset requests, persist every requested deliverable final in the workspace unless the user 
explicitly asked to keep outputs preview-only. Discarded variants do not need to be kept unless requested.
17. If the user explicitly chooses or confirms the CLI fallback, then use the fallback-only docs for model, quality, 
size, `input_fidelity`, masks, output format, output paths, and network setup.
18. Always report the final saved path(s) for any workspace-bound asset(s), plus the final prompt or prompt set and 
whether the built-in tool or fallback CLI mode was used.

## Transparent image requests

Ask built-in `image_gen` for a genuinely transparent background and preserve its alpha.

## Prompt augmentation

Reformat user prompts into a structured, production-oriented spec. Make the user's goal clearer and more actionable, 
but do not blindly add detail.

Treat this as prompt-shaping guidance, not a closed schema. Use only the lines that help, and add a short extra 
labeled line when it materially improves clarity.

### Specificity policy

Use the user's prompt specificity to decide how much augmentation is appropriate:

- If the prompt is already specific and detailed, preserve that specificity and only normalize/structure it.
- If the prompt is generic, you may add tasteful augmentation when it will materially improve the result.

Allowed augmentations:
- composition or framing hints
- polish level or intended-use hints
- practical layout guidance
- reasonable scene concreteness that supports the stated request

Not allowed augmentations:
- extra characters or objects that are not implied by the request
- brand names, slogans, palettes, or narrative beats that are not implied
- arbitrary side-specific placement unless the surrounding layout supports it

## Use-case taxonomy (exact slugs)

Classify each request into one of these buckets and keep the slug consistent across prompts and references.

Generate:
- photorealistic-natural ├óΓé¼ΓÇ¥ candid/editorial lifestyle scenes with real texture and natural lighting.
- product-mockup ├óΓé¼ΓÇ¥ product/packaging shots, catalog imagery, merch concepts.
- ui-mockup ├óΓé¼ΓÇ¥ app/web interface mockups and wireframes; specify the desired fidelity.
- infographic-diagram ├óΓé¼ΓÇ¥ diagrams/infographics with structured layout and text.
- scientific-educational ├óΓé¼ΓÇ¥ classroom explainers, scientific diagrams, and learning visuals with required labels 
and accuracy constraints.
- ads-marketing ├óΓé¼ΓÇ¥ campaign concepts and ad creatives with audience, brand position, scene, and exact 
tagline/copy.
- productivity-visual ├óΓé¼ΓÇ¥ slide, chart, workflow, and data-heavy business visuals.
- logo-brand ├óΓé¼ΓÇ¥ logo/mark exploration, vector-friendly.
- illustration-story ├óΓé¼ΓÇ¥ comics, children├óΓé¼Γäós book art, narrative scenes.
- stylized-concept ├óΓé¼ΓÇ¥ style-driven concept art, 3D/stylized renders.
- historical-scene ├óΓé¼ΓÇ¥ period-accurate/world-knowledge scenes.

Edit:
- text-localization ├óΓé¼ΓÇ¥ translate/replace in-image text, preserve layout.
- identity-preserve ├óΓé¼ΓÇ¥ try-on, person-in-scene; lock face/body/pose.
- precise-object-edit ├óΓé¼ΓÇ¥ remove/replace a specific element (including interior swaps).
- lighting-weather ├óΓé¼ΓÇ¥ time-of-day/season/atmosphere changes only.
- background-extraction ├óΓé¼ΓÇ¥ transparent background / clean cutout. Ask built-in `image_gen` for actual 
transparency.
- style-transfer ├óΓé¼ΓÇ¥ apply reference style while changing subject/scene.
- compositing ├óΓé¼ΓÇ¥ multi-image insert/merge with matched lighting/perspective.
- sketch-to-render ├óΓé¼ΓÇ¥ drawing/line art to photoreal render.

## Shared prompt schema

Use the following labeled spec as shared prompt scaffolding for both top-level modes:

```text
Use case: <taxonomy slug>
Asset type: <where the asset will be used>
Primary request: <user's main prompt>
Input images: <Image 1: role; Image 2: role> (optional)
Scene/backdrop: <environment>
Subject: <main subject>
Style/medium: <photo/illustration/3D/etc>
Composition/framing: <wide/close/top-down; placement>
Lighting/mood: <lighting + mood>
Color palette: <palette notes>
Materials/textures: <surface details>
Text (verbatim): "<exact text>"
Constraints: <must keep/must avoid>
Avoid: <negative constraints>
```

Notes:
- `Asset type` and `Input images` are prompt scaffolding, not dedicated CLI flags.
- `Scene/backdrop` refers to the visual setting. It is not the same as the fallback CLI `background` parameter, which 
controls output transparency behavior.
- Fallback-only execution notes such as `Quality:`, `Input fidelity:`, masks, output format, and output paths belong 
in the CLI path only. Do not treat them as built-in `image_gen` tool arguments.

Augmentation rules:
- Keep it short.
- Add only the details needed to improve the prompt materially.
- For edits, explicitly list invariants (`change only X; keep Y unchanged`).
- If any critical detail is missing and blocks success, ask a question; otherwise proceed.

## Examples

### Generation example (hero image)
```text
Use case: product-mockup
Asset type: landing page hero
Primary request: a minimal hero image of a ceramic coffee mug
Style/medium: clean product photography
Composition/framing: wide composition with usable negative space for page copy if needed
Lighting/mood: soft studio lighting
Constraints: no logos, no text, no watermark
```

### Edit example (invariants)
```text
Use case: precise-object-edit
Asset type: product photo background replacement
Primary request: replace only the background with a warm sunset gradient
Constraints: change only the background; keep the product and its edges unchanged; no text; no watermark
```

## Prompting best practices
- Structure prompt as scene/backdrop -> subject -> details -> constraints.
- Include intended use (ad, UI mock, infographic) to set the mode and polish level.
- Use camera/composition language for photorealism.
- Only use SVG/vector stand-ins when the user explicitly asked for vector output or a non-image placeholder.
- Quote exact text and specify typography + placement.
- For tricky words, spell them letter-by-letter and require verbatim rendering.
- For multi-image inputs, reference images by index and describe how they should be used.
- For edits, repeat invariants every iteration to reduce drift.
- Iterate with single-change follow-ups.
- If the prompt is generic, add only the extra detail that will materially help.
- If the prompt is already detailed, normalize it instead of expanding it.
- For CLI fallback only, see `references/cli.md` and `references/image-api.md` for model, `quality`, `input_fidelity`, 
masks, output format, and output-path guidance.
- For transparent images, ask built-in `image_gen` for actual transparency and preserve its alpha.

More principles shared by both modes: `references/prompting.md`.
Copy/paste specs shared by both modes: `references/sample-prompts.md`.

## Guidance by asset type
Asset-type templates (website assets, game assets, wireframes, logo) are consolidated in 
`references/sample-prompts.md`.

## gpt-image-2 guidance for CLI fallback

The fallback CLI defaults to `gpt-image-2`.

- Use `gpt-image-2` for new CLI/API workflows unless the user confirms a different model.
- CLI `gpt-image-2` does not support `background=transparent`; ask before using `gpt-image-1.5` unless the user 
explicitly requested that model.
- `gpt-image-2` always uses high fidelity for image inputs; do not set `input_fidelity` with this model.
- `gpt-image-2` supports `quality` values `low`, `medium`, `high`, and `auto`.
- Use `quality low` for fast drafts, thumbnails, and quick iterations. Use `medium`, `high`, or `auto` for final 
assets, dense text, diagrams, identity-sensitive edits, or high-resolution outputs.
- Square images are typically fastest to generate. Use `1024x1024` for fast square drafts.
- If the user asks for 4K-style output, use `3840x2160` for landscape or `2160x3840` for portrait.
- `gpt-image-2` size may be `auto` or `WIDTHxHEIGHT` if all constraints hold: max edge `<= 3840px`, both edges 
multiples of `16px`, long-to-short ratio `<= 3:1`, total pixels between `655,360` and `8,294,400`.

Popular `gpt-image-2` sizes:
- `1024x1024` square
- `1536x1024` landscape
- `1024x1536` portrait
- `2048x2048` 2K square
- `2048x1152` 2K landscape
- `3840x2160` 4K landscape
- `2160x3840` 4K portrait
- `auto`

## Fallback CLI mode only

### Temp and output conventions
These conventions apply only to the CLI fallback. They do not describe built-in `image_gen` output behavior.
- Use `tmp/imagegen/` for intermediate files (for example JSONL batches); delete them when done.
- Write final artifacts under `output/imagegen/`.
- Use `--out` or `--out-dir` to control output paths; keep filenames stable and descriptive.

### Dependencies
Prefer `uv` for dependency management in this repo.

Required Python package:
```bash
uv pip install openai
```

Optional for image inspection and downscaling:
```bash
uv pip install pillow
```

Portability note:
- If you are using the installed skill outside this repo, install dependencies into that environment with its package 
manager.
- In uv-managed environments, `uv pip install ...` remains the preferred path.

### Environment
- `OPENAI_API_KEY` must be set for live API calls.
- Do not ask the user for `OPENAI_API_KEY` when using the built-in `image_gen` tool.
- Never ask the user to paste the full key in chat. Ask them to set it locally and confirm when ready.

If the key is missing, give the user these steps:
1. Create an API key in the OpenAI platform UI: https://platform.openai.com/api-keys
2. Set `OPENAI_API_KEY` as an environment variable in their system.
3. Offer to guide them through setting the environment variable for their OS/shell if needed.

If installation is not possible in this environment, tell the user which dependency is missing and how to install it 
into their active environment.

### Script-mode notes
- CLI commands + examples: `references/cli.md`
- API parameter quick reference: `references/image-api.md`
- Network approvals / sandbox settings for CLI mode: `references/codex-network.md`

## Reference map
- `references/prompting.md`: shared prompting principles for both modes.
- `references/sample-prompts.md`: shared copy/paste prompt recipes for both modes.
- `references/cli.md`: fallback-only CLI usage via `scripts/image_gen.py`.
- `references/image-api.md`: fallback-only API/CLI parameter reference.
- `references/codex-network.md`: fallback-only network/sandbox troubleshooting for CLI mode.
- `scripts/image_gen.py`: fallback-only CLI implementation. Use only when the user explicitly chooses or confirms CLI 
mode.


exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw ..\\..\\..\\CLAUDE.md; 
Get-Content -Raw 'C:\\Users\\asali\\.codex\\skills\\.system\\imagegen\\references\\prompting.md'" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 229ms:
# Crownless ├óΓé¼ΓÇ¥ working practices (all agents)

## Mobile version ├óΓé¼ΓÇ¥ kept in sync (unfrozen 2026-07-15)
- `mobile/` (repo root) holds the iOS/Android version: a snapshot fork of `game/` plus a small, fixed set of mobile 
deltas (Mobile renderer, touch HUD, export presets ├óΓé¼ΓÇ¥ the exact list lives in `mobile/README.md`). Deployment is 
proven end to end ├óΓé¼ΓÇ¥ GitHub Actions **"Mobile builds"** produces a signed-debug APK and an unsigned iOS `.ipa`; 
both install/run on-device (sideload).
- **Policy change (was "frozen, do not touch"):** keep `mobile/game/` in sync with `game/`. `game/` is still the 
**source of truth** ├óΓé¼ΓÇ¥ never edit `mobile/` to fix a desktop issue; fix it in `game/` and re-sync. To re-sync: 
re-copy `game/` over `mobile/game/`, re-apply the mobile deltas from `mobile/README.md`, then run the compile gate + 
`test_quick` against `mobile/game` before committing. Keep the delta list small and current.

## Code layout (see CODING_GUIDELINES.md ├é┬º38)
- Tuning knobs ├óΓÇáΓÇÖ `balance.gd`; data tables stay in domain files (classes/items/story). Never inline a bare 
tuning number.
- Docs split (2026-07-06): balance/pacing round narratives go in `BALANCE_HISTORY.md` (newest at TOP of the tuning 
list); `DESIGN.md` holds only current decisions + distilled standing rules ├óΓé¼ΓÇ¥ never append round-by-round 
history there.
- Big node classes are inheritance CHAINS (verbatim moves, calls flow derived├óΓÇáΓÇÖbase, all vars in the base layer):
  - Game: `game_base` (state/flags/convo/lookups/fx) ├óΓÇá┬É `game_world` (graph/rooms/walls/spawning/gates) ├óΓÇá┬É 
`game_flow` (deaths/loot/chapters/settings/terrain events) ├óΓÇá┬É `game.gd` (boot + per-frame).
  - Player: `player_core` (state/stats/gear/progression) ├óΓÇá┬É `player_combat` (targeting/hit/shared juice + shared 
primitives) ├óΓÇá┬É `player_kit_{warrior,archer,mage,assassin,paladin,warlock}` (one file per class: `_use_<class>` 
dispatch + its abilities) ├óΓÇá┬É `player.gd` (dispatch/survival/per-frame). Primitives shared by several kits 
(`_dash_strike`, `_melee_arc`, `_mist`, `_beam_fx`, `_grant_stab_surge`) stay in `player_combat` ├óΓé¼ΓÇ¥ calls only 
flow derived├óΓÇáΓÇÖbase.
  - Tests: `tests/test_base` (helpers) ├óΓÇá┬É `tests/test_ch1` ├óΓÇá┬É `tests/test_ch2` ├óΓÇá┬É `autotest.gd` (entry 
+ systems tier).
- Self-contained UI screens = static modules in `scripts/ui/` (dev_panel, codex) taking the Menus instance.
- New `class_name` script ├óΓÇáΓÇÖ run `--import` before any headless run.
- The in-game **codex** (`scripts/ui/codex.gd`) is the player-facing reference for monsters/bosses/terrains/gear 
├óΓé¼ΓÇ¥ it goes stale silently. Whenever you add player-facing content or a feature, ask whether the codex should 
reflect it, and if so update it in the same change. It reads `Story.ALL_ENEMIES`/data tables directly, but a few 
hand-maintained parallel lists still gate it (e.g. `menus.gd` `BOSS_KINDS` decides monster-vs-boss bucketing) ├óΓé¼ΓÇ¥ 
update those too, or a new boss lands in the wrong bucket. Layout since 2026-08-15 = rail of sections 
(`UICodex.SECTIONS`) + filter chips + ledger/detail; a new section is one SECTIONS row + a `_rows`/`_detail` (or page) 
builder, and every legacy `open_codex("<tab>")` id routes through `_resolve()` ├óΓé¼ΓÇ¥ extend that, never fork the 
panel. Reference prose lives under Field notes (`NOTE_PAGES`), not above a shelf.

## Testing ├óΓé¼ΓÇ¥ ALWAYS in this order
1. Compile gate first, every time (`test_quick.bat`/`test.bat` run `check_compile.gd` first). Never invoke the test 
scene directly: one parse error anywhere makes the suite script fail to compile and the headless engine idles FOREVER 
(a zombie that looks like a slow run). The gate catches it in ~3s with the real error.
2. Iterate with `test_quick.bat` (~15s): gate ├óΓÇáΓÇÖ boot ├óΓÇáΓÇÖ one class kit ├óΓÇáΓÇÖ systems ├óΓÇáΓÇÖ UI smoke 
├óΓÇáΓÇÖ pause menu.
3. `test.bat` (full suite, minutes; plays both chapters end to end) must be green before staging.

## Agent toolkit (the map: `tools/INDEX.md` ├óΓé¼ΓÇ¥ check it before writing a helper script)
- `preflight.bat` ├óΓé¼ΓÇ¥ the traps below, mechanized: stale/forgotten `--import`, codex/BOSS_KINDS staleness, 
unregistered content modules, diff-scoped balance-number + CONNECT_DEFERRED lints, new shot rig not `extends ShotRig`. 
Run it before staging; every finding prints its one-line fix.
- `python tools/safe_commit.py -m "msg" <your paths>` ├óΓé¼ΓÇ¥ the serialized-commit etiquette as a guardrail: 
path-scoped commit that lists sibling-staged work instead of swallowing it.
- `python tools/sync_mobile.py` ├óΓé¼ΓÇ¥ mobile drift report; `--apply [--gate]` performs the re-sync ritual from 
`mobile/README.md`.
- **Headless Codex (`codex exec`, the default generated-art lane): calling guide = 
`C:\Users\asali\Projects\CODEX_HEADLESS.md` (Projects root, shared by every project on this box: binary, call, all 
flags, machine limits) + `tools/CODEX_HEADLESS.md` (the MMO layer: runner, drivers, vet/install).** The binary's 
install dir is re-hashed on every app update ├óΓé¼ΓÇ¥ resolve it (`python tools/codex_bin.py` / `Resolve-Codex` in 
`run_codex_batch.ps1`), never hard-code it; prompt goes on STDIN; on this shared box run image batches serial + 
RAM-gated (`run_codex_batch.ps1 -MaxParallel 1 -MinFreeGB 1.3`), never beside a Godot suite (a parallel batch + suite 
OOM-crashed the host 2026-08-17).
- `python tools/art/verify_art.py <base>` ├óΓé¼ΓÇ¥ post-install sprite checks (strip geometry, 8-dir completeness, 
green-bleed, stale import, and in-cell content gates: anchor drift/ghost chunks/edge cuts/clip-vs-idle body scale + 
**HEROBODY** hero frame-0 scale-lock shrink).
  - **HEROBODY vs CLIPSCALE (added 2026-08-23):** CLIPSCALE models the ENEMY renderer (median body / cell). HEROES 
render differently ├óΓé¼ΓÇ¥ `player_core._measure_hero_frame` locks a clip's on-screen scale to FRAME 0's CONTENT bbox 
and plays the whole clip at it, so a clip whose REST pose already raises a weapon (frame-0 content > body) renders the 
body small for the entire clip (the archer-multishot 0.85├âΓÇö shrink CLIPSCALE structurally cannot see). HEROBODY 
flags a hero action clip whose frame-0 body renders <0.90├âΓÇö the idle's. A neutral rest frame reads ~1.0 and a legit 
crouch/lunge still starts neutral, so both are spared (0 FP across the 6 base classes + 5 skins).
  - **The eyes-only drift class has NO geometric gate ├óΓé¼ΓÇ¥ the net is a VISUAL QA artifact + agent review.** 
Face-fidelity drift (a fatter/blurrier face in action frames), a shield/armor detail that morphs frame-to-frame, a 
cape that flips sides ├óΓé¼ΓÇ¥ all pass every geometric check; the *drawing* is inconsistent. verify_art cannot catch 
these and a sharpness/consistency proxy false-positives on real motion. So a skin/class regen MUST emit a per-clip 
contact sheet (idle + action peak frames, feet-aligned + face crops) and an AGENT must review it before install. An AI 
reviewer catches "her face got fatter"; a number can't. Don't rely on the owner's eyes as the first line. **The full 
reusable workflow ├óΓé¼ΓÇ¥ dedup-scope ├óΓÇáΓÇÖ one review agent per subject over EVERY clip├âΓÇödir├âΓÇöframe 
├óΓÇáΓÇÖ orchestrator verifies each flag ├óΓé¼ΓÇ¥ is `tools/art/DRIFT_AUDIT.md` (covers heroes/skins AND the boss/mob 
adaptation: enemy render path = CLIPSCALE not HEROBODY).** Established 2026-08-24; the known drift catalogue 
(fire-on-move clips are the systemic weak point; un-regenerated clips carry the face-softening family; 
single-direction off-model art) lives there.
  - **WARN triage ├óΓé¼ΓÇ¥ FAIL is the bar, not WARN (investigated 2026-08-23).** The six BASE heroes throw a large, 
standing pile of WARNs that are NOT defects ├óΓé¼ΓÇ¥ the gate has no way to whitelist intentional content, so it flags 
it every run. All three benign categories, confirmed by rendering the flagged pixels on grass + overlaying them on the 
sprite:
    - **BLEED** on base heroes ├óΓÇ░┬á green keying rim. It's soft-alpha FX (the archer's cast/ult IS green 
Arrow-Storm magic; every class's ult/cast auras + glows) plus green-CLOTHING edges (the archer's green cloak). The 
BLEED counter is colour-blind (any `0<a<255` pixel), so green *content* inflates it; a band-normalize/upscale 
multiplies the same FX/cloak edge pixels and the raw count rises with zero new bleed. The real-bug discriminator is a 
green RIM hugging the silhouette (`G>max(R,B)+20` massed on the body outline) ├óΓé¼ΓÇ¥ on the base heroes that rim is 
absent; the green sits on the FX cloud and the cloak, and on a grass background there's no halo.
    - **STRAY** on base heroes = FX props the game does NOT re-spawn: the warlock's floating flaming SKULL (his 
signature, every frame), the warrior's flaming-sword blade/trail, the archer's green cast-motes. Distinct from a baked 
*projectile* (which the STRAY rule is really aimed at) ├óΓé¼ΓÇ¥ leave these.
    - **EDGECUT** on melee swings (`attack2`/`attackb`/`attackc` for warrior/assassin/paladin) = the weapon reaching 
the cell edge at the extreme of the arc, not a clipped body.
    - Baseline as of 2026-08-23: base heroes = **0 FAIL, ~261 WARN** (208 BLEED / 28 STRAY / 25 EDGECUT); the 5 
in-scope elite skins = clean `VERIFY OK`. Don't re-investigate or "fix" these WARN categories on the base heroes 
├óΓé¼ΓÇ¥ only act on a NEW **FAIL**, or a WARN that leaves these three buckets (e.g. a real body green-rim, a 
butchered/PARTIAL frame, an anchor/scale/FEETSLIDE hit).
- **Screenshot rigs: never write a standalone one (2026-08-15).** Twenty-three `game/shot_*.gd` files are copies of 
the same 25-line boot/`_shot`/quit boilerplate, none with a watchdog, muted only by convention. New rig = `extends 
ShotRig` (`scripts/dev/shot_rig.gd`: `boot()`, `shot()`, sim-clock waits, `arg()`/`flag()`, Master-bus mute, 
`--timeout=N` watchdog) + `_ready()` of steps + `finish()`; run it ├óΓé¼ΓÇ¥ and every legacy rig ├óΓé¼ΓÇ¥ through 
`shot.bat <rig> [--timeout=N] [rig args]`, which injects `--audio-driver Dummy`, compile-gates the rig script 
(check_compile's default walk skips `game/` root: a parse error there = a window that idles forever), and hard-kills a 
hung engine. `preflight` FAILs a new rig that doesn't extend it and WARNs on an edited legacy one (convert on touch). 
Worked example: `shot_fx_series.gd`; the map: `tools/INDEX.md` "In-engine shot rigs".
- **Windowed shot rigs (`shot_*.tscn`, `qa_skins`, anything not `--headless`) run MUTED: always pass `--audio-driver 
Dummy`.** They boot the real game with music + SFX on the owner's shared machine; an unmuted rig blares through his 
speakers mid-session (owner ruling 2026-08-15). Headless suites are already silent.

## GDScript traps (each has bitten us) ├óΓé¼ΓÇ¥ review-time deep list: `CODING_GUIDELINES.md` ├é┬º38
- `var x := obj.method()` on a loosely-typed obj (or any Variant expression, e.g. `Dictionary.get`) = PARSE ERROR 
"cannot infer type". Annotate: `var x: float = ...`.
- New `class_name` ├óΓÇáΓÇÖ `--import` first or headless silently hangs.
- Labels inside HBoxContainer collapse to one char per line without `custom_minimum_size`.
- Multiline lambda: following call args must sit on the SAME line as the lambda's last statement.
- Tests waiting on timed effects must poll wall-clock (`await create_timer(0.2)`) ├óΓé¼ΓÇ¥ frames race ahead headless.
- Autotest sections SNAPSHOT + RESTORE shared state (flags, resonance, standings); never `.clear()`. Two corollaries, 
both learned from one flaky capital run (2026-07-26):
  - **Restore on the FAILURE paths too.** `_fail` only QUEUES `quit(1)` ├óΓé¼ΓÇ¥ the suite keeps running to the end of 
the frame, so the next section still executes and reads whatever the failed one left behind. A mid-city failure 
stranded `chapter_id == "capital"`, where `shop_markup` answers a flat 1.0, and the road-markup section then reported 
a second, entirely bogus failure. Put the assertions in a helper and the restore in its caller, so all ~40 exits pass 
through it.
  - **Never assert against state an earlier section accumulated.** A content-tier section inherits a whole campaign of 
loot, gold, resonance and bag capacity ├óΓé¼ΓÇ¥ all of it loot-luck. The capital's lapidary quest asserted its kit 
landed in the bag, and a full bag legally mails the kit instead: pass or fail came down to whether a bag DROPPED that 
run. Control the precondition (lend a bag, pin the shard) or the assert is a coin flip.
- canvas_item shader `fragment()`: `COLOR` arrives PREMULTIPLIED (modulate ├âΓÇö TEXTURE sample). Writing `COLOR = 
result * COLOR` multiplies the texture in twice ├óΓé¼ΓÇ¥ everything darkens and "brighten" shaders read as darken 
(phantom_charge was bitten; owner saw "only gets darker"). Reuse ONLY `COLOR.a` (silhouette.gdshader convention).
- `Environment.adjustment_contrast` with `viewport/hdr_2d` runs in LINEAR light: it pivots on 0.5, and a dark floor 
sits near 0 there ├óΓé¼ΓÇ¥ contrast 1.06 HALVED the keep floor's brightness (gameplay-polish rig 2026-08-18). Leave 
contrast at 1.0; use saturation/brightness only, and judge any whole-frame grade in-game, never from the numbers.
- Spawning an Area2D + CollisionShape2D (Chest/Pickup/Projectile) from inside a `body_entered`/`area_entered` handler 
= non-fatal "Can't change this state while flushing queries" engine error (`area_set_shape_disabled`) ├óΓé¼ΓÇ¥ the new 
shape's enter-tree hits the physics-flush guard. Connect physics signals with `CONNECT_DEFERRED` or `call_deferred` 
the spawn (chest open + projectile ricochet were bitten).
- `draw_colored_polygon` on a SLIVER (near-collinear points) = random non-fatal "Invalid polygon data, triangulation 
failed" spam: the triangulator picks winding from a float32 shoelace over RAW coords, so a sliver's sign is rounding 
noise at world-scale coordinates ├óΓé¼ΓÇ¥ coordinate-dependent coin flip, NOT deterministic per shape (knife-trail 
fold-back miters were bitten). Pre-check `Geometry2D.triangulate_polygon(p).is_empty()` and skip ├óΓé¼ΓÇ¥ the exact 
C++ path behind the error, and everything it rejects is sub-pixel (the `_poly` guard in player_combat.gd).
- **In co-op, the tree pause is NOT an input gate.** `request_pause(true)` NO-OPS online (`game_base.gd`, ├é┬º5.4 
├óΓé¼ΓÇ¥ a shared world never pauses), so anything leaning on the pause to stop input/processing keeps running in a 
session. Menus, dialogue and choice prompts all pause solo and none of them pause online. Gate on the OVERLAY STATE 
instead ├óΓé¼ΓÇ¥ `menus.is_open()` / `hud.dialogue_active` / `hud.choices_active`, the guard `game.gd`'s tap-to-talk 
already uses. The touch HUD had no `process_mode` override and only checked `state == ST_PLAYING` (there is no 
`ST_MENU`), so its joystick ├óΓé¼ΓÇ¥ the whole left half of the screen ├óΓé¼ΓÇ¥ stayed live UNDER every menu and a 
drag walked your hero mid-fight. Note the shape of the miss: it was invisible in BOTH cheap tests (solo-on-phone 
pauses; co-op-on-desktop has no touch HUD) and only existed in the mobile ├âΓÇö co-op cross-product. When a feature is 
platform- or session-conditional, ask what its gate is in the OTHER combination.

## Machine RAM is tight (~10GB) ├óΓé¼ΓÇ¥ tool-process hygiene (2026-08-23)
- This box has ~9.8GB physical RAM and the BASELINE load already fills it: Claude Code + its MCP servers ├óΓÇ░╦å 3GB 
(22 procs ├óΓé¼ΓÇ¥ MCP servers orphan on restart; 3 duplicate `pixelforge-mcp` copies seen), browser ├óΓÇ░╦å 1.2GB, 
Windows (svchost + Defender + compression) ├óΓÇ░╦å 1.4GB. That leaves ~1GB free ├óΓé¼ΓÇ¥ nothing has headroom. This is 
WHY image batches run serial + RAM-gated and never beside a Godot suite. But be precise about WHAT is heavy: **a 
single `codex exec` image gen is API-side and costs only ~250MB local (measured 2026-08-24) ├óΓé¼ΓÇ¥ it is NOT a 
machine-killer and a single serial job runs safely even at ~1GB free.** The RAM risk is PARALLELISM (several gens at 
once) ├âΓÇö a co-resident **Godot suite** (each Godot instance is the real GB-scale hog). So `-MaxParallel 1 
-MinFreeGB 1.3` guards against *stacking* jobs and firing next to a suite ├óΓé¼ΓÇ¥ don't read it as "Codex is a spike" 
and refuse a single gated gen. (PixelLab local footprint unmeasured; don't assume it's the same.) A Claude restart 
clears orphaned MCP servers; closing browser tabs frees ~1GB.
- **Prefer the Bash tool over PowerShell.** PowerShell tool calls can leave lingering `powershell.exe` processes (7 
orphans spanning 2 days seen, ├óΓÇ░╦å0.45GB). Use Bash for process/file ops (`taskkill //F //PID <n>` cleans strays); 
reserve PowerShell for genuine cmdlet needs, and consolidate into ONE call rather than many.

## Multi-agent etiquette
- Task boards (`CH2_TASKS.md` pattern): one owner per task, claim before starting; content lands as NEW modules under 
`game/scripts/content/` (format: `scripts/content/README.md`) + one registration line in `Story.CONTENT_MODULES`.
- Stage with `git add` after a green full suite. No commits unless the user asks.
- **Commits are SERIALIZED (2026-07-07):** a commit sweeps the WHOLE index, and sibling agents stage concurrently 
├óΓé¼ΓÇ¥ so immediately before committing, run `git status` and look at what's actually staged. If the index holds 
another agent's work, either fold it into an accurate combined message or stop and say so ├óΓé¼ΓÇ¥ never commit a 
message that describes only your slice. (Trigger: an itemization pass got swallowed into a commit labeled "cover + 
boot flow" by exactly this race.)
- Commit messages: NO author/co-author trailers (no `Co-Authored-By`, no attribution lines) ├óΓé¼ΓÇ¥ this project 
keeps commits authorless; credit lives in a separate credits file.
- Autotest additions: content modules use the marked CONTENT-MODULE TEST HOOK (one func at file end + one call line); 
never edit existing sections.

## Asset sourcing
- **Allowed ├óΓé¼ΓÇ¥ and that's the whole list:** ONLY these two, or anything we generate ourselves (must be up to 
par: semantic fit + reads clean in-game).
  1. **CC0 / public domain** ├óΓé¼ΓÇ¥ zero obligations, ideal.
  2. **CC-BY / MIT / Apache / OFL** ├óΓé¼ΓÇ¥ fine; ship the attribution file (we already do, in 
`assets/*/CREDITS.txt`).
  No pre-approval needed for these. Source from anywhere ├óΓé¼ΓÇ¥ OpenGameArt, Kenney, itch.io, the Dungeon Crawl 
tileset (CC0), etc.
- **Everything else is off-limits** ├óΓé¼ΓÇ¥ do not use, don't even ask: share-alike (CC-BY-SA, GPL), non-commercial 
(CC-NC), proprietary/paid asset-store packs, and anything with no stated license (default = all rights reserved). This 
game ships commercially on Steam; these licenses either forbid selling it or can force us to open-source the whole 
project.
- Install flow: probe/download, and LOOK at the image (or listen to the audio) before installing; overrides drop into 
`assets/sounds|music|sprites/` by name (a PNG/wav there overrides the procedural version of the same name).
- Audio taste: semantic fit over quality; no melodic-jingle chiptunes for ults/terrains; never human grunts on casts; 
never touch `ult_mage.wav` or the village/story/boss synth tracks.

## Icon fidelity ├óΓé¼ΓÇ¥ authored resolution ├óΓÇ░┬á render size (owner ruling 2026-08-21; agents keep tripping on 
this)
- Player-facing icon families are **DUAL-RES**: a high-fidelity master for the codex/detail views + a downscaled 
gameplay copy for the bag. The **128px painterly master is the FIDELITY BAR** ├óΓé¼ΓÇ¥ match it, not a flat/pixel 
style, unless the owner says otherwise. Reference tiers: **gear** = `assets/icons/codex/<shape>_<grade>.png` @128px 
(codex, via `Art.codex_item_icon`) + `assets/icons/<name>.png` @32px (bag); **class splashes** ~235px authored 
├óΓÇáΓÇÖ render ~100px in-game (~2.35├âΓÇö ├óΓé¼ΓÇ¥ the owner's "renders nicely" benchmark).
- **THE TRAP:** authoring a family at **32px only**. It looks fine in a 32px bag slot, so it passes a quick check 
├óΓé¼ΓÇ¥ but every larger surface (codex ~28├óΓé¼ΓÇ£128px, the detail popover header 40px) UPSCALES it to mush, and it 
reads as low-fidelity beside the 128px gear. **Gems shipped 32px-only (2026-07-30) and were the 2026-08-20 
visual-review's one solid asset catch for exactly this.** When you regen an icon family: author at the 128px gear bar, 
store dual-res, and load the 128 in large contexts. Verify the fidelity numbers (stored res vs on-screen render px) 
BEFORE designing ├óΓé¼ΓÇ¥ the owner asks for this by name.
- **This generalises past icons to ALL world/character/codex art (owner ruling 2026-08-24): a master should be 
├óΓÇ░┬Ñ2├âΓÇö its on-screen render size.** The auditor is `tools/art/fidelity_audit.py` + `game/fidelity_dump.gd`; the 
current findings + prioritised re-art list live in **`FIDELITY_AUDIT.md`** (root). Bar = the base classes (render 99px 
from 235px = 2.35├âΓÇö). Standing findings: the **5 enhanced-base skins PASS (2.37├âΓÇö)** but the **18 legacy skins 
are ~1.2├âΓÇö** (the planned regen quantified); trees/large props + one npc (`pilgrims_schism`) + `choir_censer` (a 
boss-summoned add) are the other real re-art targets. Unplaced+low-fi mobs get DROPPED, not re-arted (`bat`/`direbat` 
gone 2026-08-24).

## World props ├óΓé¼ΓÇ¥ style + motion contract (owner ruling 2026-08-18, after a day of "shifts / dims / cartoonish" 
flags from play; review-time checklist: `CODING_GUIDELINES.md` ├é┬º40; the polish effort's board = `POLISH_TASKS.md`, 
its generation lane = `tools/art/style_unify/`)
- **One style.** Every world prop matches the cast: painterly, soft-shaded, NO black outlines, muted palette, top-down 
three-quarter, light top-left. A hi-res canvas is NOT a pass ├óΓé¼ΓÇ¥ a 1024px cartoon is still a cartoon (cactus2, 
tree_teal2, grave_cross all passed the resolution audit). Regen briefs name a painterly SIBLING as the style ref and 
the old asset only as the SUBJECT (silhouette/footprint/colour identity); judge the result on a contact sheet BESIDE 
its siblings (`scratchpad`-style sheet, or `tools/art/asset_gallery.py`), never against the asset it replaces.
- **Animate in place.** Frames never translate the object. Rigid props (furnaces, wells, shrines, rigs, pools) keep a 
byte-stable silhouette ├óΓé¼ΓÇ¥ colour-only motion (glow pulse, ember flicker), bbox/baseline/centre drift 0; only 
fire, cloth and foliage may change silhouette, and then anchored on frame 0's baseline. Wind sway is an ALLOW-list of 
vegetation (`_wind_scenery`), never "everything that isn't a building". Hazard pools stay put 
(`Balance.HAZARD_POOLS_DRIFT`). Anything that swings/pendulums needs authored frames with proper easing or no 
animation at all ├óΓé¼ΓÇ¥ a 4-frame swing reads as fake.
- **Dim, don't strobe.** A prop should look STILL at a glance and alive on a second look. Luminance swing ├óΓÇ░┬ñ 
10-12% for glows/pulses (`derive_prop_anim.py --amp 0.10`), ├óΓÇ░┬ñ 25% for open fire, never on the whole sprite 
(bright/warm pixels only). The 0.35 default that looked "visible" on a contact sheet strobes in-game.
- **Gate every strip you touch:** `tools/art/audit_prop_anims.py` (frames/canvas/per-frame drift + amp) + 
`verify_art.py`; a NEW rigid animated prop goes into autotest's `full_prop_anims` contract (frame == static canvas, 
baseline ├óΓÇ░┬ñ1 / centre ├óΓÇ░┬ñ2). Contact sheets and metrics do not replace watching the loop: run it in a shot 
rig at 1x (`shot.bat polish --gif`) before calling it done ├óΓé¼ΓÇ¥ a subtle loop that looks static on a sheet is 
correct.

## Hero skins ├óΓé¼ΓÇ¥ what is in scope vs OFF-LIMITS (owner ruling 2026-08-22)
- **In scope for polish: ONLY the 5 ENHANCED-BASE designs**, wired as elite skins purely for in-game preview: warrior 
`emberbound_heir`, assassin `erased_name`, archer `severed_thread`, warlock `ledgerbound`, mage `blighted_healer`. 
These are the enhanced base-class designs ├óΓé¼ΓÇ¥ fix/re-anchor/despur/frame-edit them freely.
- **OFF-LIMITS ├óΓé¼ΓÇ¥ do NOT touch, regen, re-anchor, despur, frame-edit, or "fix":** every OTHER hero skin. That is 
all of `game/assets/sprites/skins/mythic/` (dreadknight, stormforged, eclipse_knight, fallen_arbiter, blade_dancer, 
phantom/phantom_awakened, voidwraith, eldritch_herald, ├óΓé¼┬ª) **and** the real skins that live in `skins/elite/` 
alongside the enhanced-base five (dreadknight, frostfall_ranger, void_weaver, hellfire_inquisitor, eclipse_knight, 
blade_dancer, ├óΓé¼┬ª). A blanket art-sweep must EXCLUDE these ├óΓé¼ΓÇ¥ an auto-scan of `assets/sprites/` will hit 
them; scope it to the five, or to bosses/mobs, explicitly.
- **Why:** a full REGEN pass over all the real skins is planned (owner 2026-08-22, "for later"). Until that pass, 
leave them exactly as committed ├óΓé¼ΓÇ¥ a piecemeal fix now is wasted work the regen overwrites, and it drifts the 
design.
- **Known-defect note:** the real skins carry the same feet-slide artifact the enhanced-base + bosses/mobs were fixed 
for (2026-08-22), so `verify_art` will emit `FEETSLIDE` WARNs on them. That is EXPECTED and intentionally left for the 
regen pass ├óΓé¼ΓÇ¥ do not "fix" it.

## Required per-class animation clips beyond the standard set (so a regen knows what to produce) ├óΓé¼ΓÇ¥ 2026-08-23
Some abilities need EXTRA clips on top of anim/walk/attack/attack2/dash/cast/ult/death. A class or skin regen MUST 
produce these or the ability silently falls back to a worse pose. Both are art-driven (no strip = the fallback), so 
they're easy to forget ├óΓé¼ΓÇ¥ hence this manifest.

- **Fire-on-move (`<base>_attack_walk[_<dir>]`)** ├óΓé¼ΓÇ¥ a walk cycle that fires the basic mid-stride, so spamming a 
low-cd RANGED basic WHILE MOVING doesn't lock the standing pose sliding over the floor (`player.gd` `walk_fire`; a 
mid-cycle re-cast lets the stride finish). Needed by the ranged spammables ONLY:
  - mage **Firebolt** (a1=attack), warlock **Shadowbolt** (a1=attack), archer **Quick Shot** (a1=attack), assassin 
**Fan of Knives** (a3=attack2).
  - NOT the melee 3-way basic ├óΓé¼ΓÇ¥ the assassin STAB keeps its attack/attackb/attackc alternation while moving 
(gated by `slot=="a1" and _clips.has("attackb")`, which only excludes the assassin).
  - **HOW TO GENERATE (the technique that finally worked, owner 2026-08-23): it's the WALK clip + a firing arm 
├óΓé¼ΓÇ¥ do NOT author a walk from scratch.** Give the character's EXISTING `<base>_walk_<dir>` strip as the image ref 
and instruct: "reproduce this exact walk ├óΓé¼ΓÇ¥ same leg cycle/stride/crossing, same garment ├óΓé¼ΓÇ¥ change ONLY 
the free hand to fire the basic once." A from-scratch walk brief (however detailed the per-frame boot storyboard) kept 
gliding or failing to cross the legs; referencing the real walk carries its crossing gait for free. Keep the brief 
SHORT ├óΓé¼ΓÇ¥ the walk ref IS the leg spec.
  - Contract: 8-frame walk+fire; feet PLANTED (no in-cell slide ├óΓÇáΓÇÖ FEETSLIDE); the garment stays CLOSED and 
design-accurate (NO slit, NO exposed legs/bare skin ├óΓé¼ΓÇ¥ the walk reads through the BOOTS alternating below the 
hem + the hem swaying, exactly as the walk clip does); NO baked projectile/flame on the sprite (the game spawns it 
├óΓé¼ΓÇ¥ a small warm spark shares the mage's skin/gold palette and is un-maskable by colour except the bright-yellow 
core, so keep it out at gen time). Author S/E/N; E├óΓÇáΓÇÖSE/NE; **left MIRRORS right** (W/NW/SW = mirror of E); flat 
= S. Build lane: `scratchpad/attack_walk_codex.py`.
  - **`attack_walk_b` ├óΓé¼ΓÇ¥ the alternate variant (owner 2026-08-23).** Fire-on-move ALTERNATES `attack_walk 
├óΓÇáΓÇ¥ attack_walk_b` each fresh moving-basic cycle (player.gd `_walkfire_swing`), like the melee 
attack├óΓÇáΓÇ¥attackb, so spamming while moving doesn't replay one clip. Produce it with the SAME walk reference 
├óΓé¼ΓÇ¥ only the FIRE motion differs from the primary (warlock: two-hand shove vs one-hand thrust; mage: free-hand 
rising flick vs level thrust, staff hand kept clearly separate; assassin: sidearm flick vs overhand throw). **NOT the 
archer** (owner 2026-08-23): a bow draw has ONE proper form, so the archer has no `_b` ├óΓé¼ΓÇ¥ it plays the primary 
every cycle. When a `_b` would confuse the silhouette (e.g. the mage's casting hand reading as the staff hand), keep 
the two hands clearly separate or skip the variant. Build lane: `attack_walk_codex.py ├óΓé¼┬ª --b`. Art-driven: no 
`_b` strip = the primary plays every cycle.

- **Melee swing alternation (`attackb`, and `attackc` for enhanced variants)** ├óΓé¼ΓÇ¥ melee basics cycle multiple 
authored swings per cast (`player_core.gd` `_alt_basic_clip`: attack├óΓÇáΓÇÖattackb├óΓÇáΓÇÖattackc) so Cleave / Smite 
/ Stab don't replay one motion. Which classes ship which:
  - **warrior, paladin, assassin** carry `attackb` (2-way minimum). Ranged classes (mage/archer/warlock) have only 
`attack`.
  - **3-way (`attackc`)**: base **paladin**, warrior **emberbound_heir**, assassin **erased_name** (owner 2026-08-22 
├óΓé¼ΓÇ¥ the enhanced variants). `attackc` is a DISTINCT 3rd motion (a rising uppercut), not a recolor of 
attack/attackb. autotest `_test_swing_alternation` expects 3-way when a body ships an attackc.

## Generated art tool authorization
- **NEVER assume PixelLab is authorized. PixelLab may be used only when the owner explicitly authorizes PixelLab for 
the specific task.** Existing PixelLab assets, PixelLab helper scripts, character metadata, or a matching PixelLab 
workflow elsewhere in the repo do not grant permission for a new generation.
- When an agent has a built-in image-generation/editing tool (for example, ChatGPT/Codex image generation), that 
built-in tool is the default for generated art unless the owner explicitly requests or authorizes another generator.
- This rule applies even when another generator appears technically better suited. If the built-in tool cannot meet 
the requirement, stop and ask the owner; do not silently switch tools or spend external generation credits.

### Codex built-in ImageGen walk-animation observations
- For bipedal characters, the built-in ImageGen tool often fails to preserve a fluid left/right leg cycle. A real 
same-colored leg crossing over the other in one passing frame can create a convincing walking illusion; keep that 
crossing to one or at most two frames rather than making every frame crossed.
- If ImageGen repeats nearly identical crossed poses, give each cell an explicit foot-position storyboard instead of 
only asking for "alternating legs": use a wide split, a tight crossover, a second crossover with a different spread 
and the opposite leg emerging, then the opposite wide split or a subtle toe-off. State that frames 1, 3, and 4 must 
have visibly different lower-body silhouettes. (Tried on the Wild Fang Raider 2026-08-14 and again 08-15: the 4-cell 
2x2 kept coming back with the SAME leg leading in both stride cells and the body ~11% short ├óΓé¼ΓÇ¥ the owner still 
called the walk bad.)
- **What actually fixed the Wild Fang Raider walk (2026-08-15): ONE ROW of EIGHT figures, each frame labelled with its 
gait phase** (near-foot contact / load / far leg passes / reach, then the same four for the other leg ├óΓé¼ΓÇ¥ the 
pipeline doc's "legs do not alternate" remedy). The row came back with 8 distinct poses, constant body height and one 
ground line; installed as an 8-frame strip (the engine's strip reader is frame-count agnostic ├óΓé¼ΓÇ¥ width/height 
├óΓé¼ΓÇ¥ so 8 is fine; at the 2x walk clock the loop runs 0.67 s). Prefer this over 4-cell 2x2 for any bipedal walk. 
Ask Codex to report leg-lead per frame in its final message; it is a candid critic of its own output.
- Every ImageGen-keyed strip carried a dark-green antialias rim (`build_mob_walk_repairs.remove_green` now despills 
the 2px edge band); a strip that "looks fine" on a dark contact sheet can still ring green over grass ├óΓé¼ΓÇ¥ check 
`G > max(R,B)+20` counts on the rim, not just the semi-alpha BLEED gate.
- The tool is generally more reliable for non-bipedal walks (for example, spiders and wolves), and for characters 
whose locomotion does not expose a conventional leg cycle: robes can communicate motion through cloth flutter, and 
floating characters can use body/cloth drift.
- For bipedal sheets, prompt for fixed torso/pelvis anchors, a shared foot baseline, compact stride, 
held-weapon/upper-body continuity, and explicit negative constraints against recoloring legs, detached feet, 
duplicated frames, and high-knee marching. Inspect the generated sheet before installation and run 
`tools/art/verify_art.py <base>` afterward; anchor drift can still remain even when the sheet looks visually centered.

## PixelLab characters ├óΓé¼ΓÇ¥ regenerating an EXISTING one (deep dive: `tools/art/PIXELLAB_BOSS_EDITS.md`)
- **Authorization gate:** this section applies only after the owner has explicitly authorized PixelLab for the current 
task. It is not permission to choose PixelLab.
- **RULE: before you regenerate, re-roll, or tweak an existing PixelLab character (boss/hero/skin), ALWAYS read its 
export metadata FIRST.** Download the zip (`curl -H "Authorization: Bearer $PIXELLAB_SECRET" 
https://api.pixellab.ai/mcp/characters/<id>/download -o out.zip`) and read `metadata.json` ├óΓé¼ΓÇ¥ it stores the 
ORIGINAL creation `prompt` (plus size, `template_id`, view). Regenerate from that EXACT prompt and change ONLY the 
clause the owner flagged.
- **Never invent a fresh description from a screenshot or memory** ├óΓé¼ΓÇ¥ it silently drifts the design 
(proportions, palette, silhouette) even when the fix was one word. `get_character` does NOT return the creation 
prompt; the zip's `metadata.json` is the only source of truth.

## PixelLab characters ├óΓé¼ΓÇ¥ new generation workflow (prompt-craft cases: `tools/art/PIXELLAB_PROMPT_LESSONS.md`)
- **CHECK ALL 8 ROTATIONS before generating animations.** After a character body is created, visually inspect every 
direction (S, SE, E, NE, N, NW, W, SW) for consistency ├óΓé¼ΓÇ¥ same proportions, same gear, same silhouette. v3 mode 
frequently produces inconsistent north/west directions (wrong body shape, missing equipment, different outfit). If any 
direction is broken, re-roll the body first. Generating animations on a broken body wastes hundreds of generations.
- **Heroes use v3 mode (NOT pro).** The gold-standard assassin and warlock were both v3. Pro mode produces stocky 
proportions for visible-face characters. The `style: -, -, -` field in `get_character` appears in BOTH v3 and pro 
├óΓé¼ΓÇ¥ it cannot distinguish modes.
- **Standard mode is useless for proportions testing.** Its template skeleton produces chibi proportions regardless of 
description. Only useful for testing color palette and design elements.
- **Description formula (proven on assassin):** archetype first ├óΓÇáΓÇÖ every piece of gear described precisely 
├óΓÇáΓÇÖ body type ("lean and gaunt" / "lean and tall") ├óΓÇáΓÇÖ art mood ("muted dark palette, somber dark fantasy") 
├óΓÇáΓÇÖ explicit negatives ("no hood", "no smoke"). Leave no room for the model to improvise.
- Worked example (2026-07-12): owner said Vess should have "no smoke from mouth". The metadata prompt literally 
contained `faint blue-white keening light at the mouth` ├óΓé¼ΓÇ¥ the fix was deleting that one clause from the 
original prompt, not writing a new banshee description.

# Prompting best practices

These prompting principles are shared by both top-level modes of the skill:
- built-in `image_gen` tool (default)
- explicit `scripts/image_gen.py` CLI fallback

This file is about prompt structure, specificity, and iteration. Fallback-only execution controls such as `quality`, 
`input_fidelity`, masks, output format, and output paths live in the fallback docs.

## Contents
- [Structure](#structure)
- [Specificity policy](#specificity-policy)
- [Allowed and disallowed augmentation](#allowed-and-disallowed-augmentation)
- [Composition and layout](#composition-and-layout)
- [Constraints and invariants](#constraints-and-invariants)
- [Text in images](#text-in-images)
- [Input images and references](#input-images-and-references)
- [Iterate deliberately](#iterate-deliberately)
- [Transparent images](#transparent-images)
- [Fallback-only execution controls](#fallback-only-execution-controls)
- [Use-case tips](#use-case-tips)
- [Where to find copy/paste recipes](#where-to-find-copypaste-recipes)

## Structure
- Use a consistent order: scene/backdrop -> subject -> key details -> constraints -> output intent.
- Include intended use (ad, UI mock, infographic) to set the level of polish.
- For complex requests, use short labeled lines instead of one long paragraph.

## Specificity policy
- If the user prompt is already specific and detailed, normalize it into a clean spec without adding creative 
requirements.
- If the prompt is generic, you may add tasteful detail when it materially improves the output.
- Treat examples in `sample-prompts.md` as fully-authored recipes, not as the default amount of augmentation to add to 
every request.
- For photorealism, include `photorealistic` directly when that is the goal, plus concrete real-world texture such as 
pores, wrinkles, fabric wear, material grain, or imperfect everyday detail.

## Allowed and disallowed augmentation

Allowed augmentation for generic prompts:
- composition and framing cues
- intended-use or polish-level hints
- practical layout guidance
- reasonable scene concreteness that supports the request

Do not add:
- extra characters, props, or objects that are not implied
- brand palettes, slogans, or story beats that are not implied
- arbitrary side-specific placement unless the surrounding layout supports it

## Composition and layout
- Specify framing and viewpoint (close-up, wide, top-down) and placement only when it materially helps.
- Call out negative space if the asset clearly needs room for UI or copy.
- Avoid making left/right layout decisions unless the user or surrounding layout supports them.
- For people, describe body framing, scale, gaze, and object interactions when they matter (`full body visible`, 
`looking down at the book`, `hands naturally gripping the handlebars`).

## Constraints and invariants
- State what must not change (`keep background unchanged`).
- For edits, say `change only X; keep Y unchanged` and repeat invariants on every iteration to reduce drift.

## Text in images
- Put literal text in quotes or ALL CAPS and specify typography (font style, size, color, placement).
- Spell uncommon words letter-by-letter if accuracy matters.
- For in-image copy, require verbatim rendering and no extra characters.
- In CLI fallback mode, use `medium` or `high` quality for small text, dense infographics, data-heavy slides, 
multi-font layouts, legends, axes, and footnotes.

## Input images and references
- Do not assume that every provided image is an edit target.
- Label each image by index and role (`Image 1: edit target`, `Image 2: style reference`).
- If the user provides images for style, composition, or mood guidance and does not ask to modify them, treat the 
request as generation with references.
- If the user asks to preserve an existing image while changing specific parts, treat the request as an edit.
- For compositing, describe how the images interact (`place the subject from Image 2 into Image 1`).

## Iterate deliberately
- Start with a clean base prompt, then make small single-change edits.
- Re-specify critical constraints when you iterate.
- Prefer one targeted follow-up at a time over rewriting the whole prompt.

## Transparent images
- Ask built-in `image_gen` for a genuinely transparent background and preserve its alpha.

## Fallback-only execution controls
- `quality`, `input_fidelity`, explicit masks, output format, and output paths are fallback-only execution controls.
- Do not assume they are built-in `image_gen` tool arguments.
- If the user explicitly chooses CLI fallback, see `references/cli.md` and `references/image-api.md` for those 
controls.
- In CLI fallback mode, `gpt-image-2` is the default. It supports `quality=low|medium|high|auto`; use `low` for fast 
drafts and thumbnails, and move to `medium`, `high`, or `auto` for final assets.
- `gpt-image-2` always uses high fidelity for image inputs, so do not set `input_fidelity` with that model.
- CLI `gpt-image-2` does not support `background=transparent`; ask before using `gpt-image-1.5` unless the user 
explicitly requested that model.
- If the user asks for 4K-style output with `gpt-image-2`, use `3840x2160` for landscape or `2160x3840` for portrait.

## Use-case tips
Generate:
- photorealistic-natural: Prompt as if a real photo is captured in the moment; use photography language (lens, 
lighting, framing); call for real texture; avoid over-stylized polish unless requested.
- product-mockup: Describe the product/packaging and materials; ensure clean silhouette and label clarity; if in-image 
text is needed, require verbatim rendering and specify typography.
- ui-mockup: Describe the target fidelity first (shippable mockup or low-fi wireframe), then focus on layout, 
hierarchy, and practical UI elements; avoid concept-art language.
- infographic-diagram: Define the audience and layout flow; label parts explicitly; require verbatim text; prefer 
higher quality in CLI mode for dense labels.
- logo-brand: Keep it simple and scalable; ask for a strong silhouette and balanced negative space; avoid decorative 
flourishes unless requested.
- ads-marketing: Write like a creative brief; include brand positioning, audience, desired vibe, scene, and exact 
tagline if text must appear.
- productivity-visual: Name the exact artifact (slide, chart, workflow diagram), define the canvas and hierarchy, 
provide real labels/data, and ask for readable typography and polished spacing.
- scientific-educational: Define audience, lesson objective, required labels, scientific constraints, arrows, and 
scan-friendly whitespace.
- illustration-story: Define panels or scene beats; keep each action concrete.
- stylized-concept: Specify style cues, material finish, and rendering approach (3D, painterly, clay) without 
inventing new story elements.
- historical-scene: State the location/date and required period accuracy; constrain clothing, props, and environment 
to match the era.

Edit:
- text-localization: Change only the text; preserve layout, typography, spacing, and hierarchy; no extra words or 
reflow unless needed.
- identity-preserve: Lock identity (face, body, pose, hair, expression); change only the specified elements; match 
lighting and shadows.
- precise-object-edit: Specify exactly what to remove/replace; preserve surrounding texture and lighting; keep 
everything else unchanged.
- lighting-weather: Change only environmental conditions (light, shadows, atmosphere, precipitation); keep geometry, 
framing, and subject identity.
- background-extraction: Request a clean cutout on a genuinely transparent background; preserve fine edges and label 
text; no halos or restyling.
- style-transfer: Specify style cues to preserve (palette, texture, brushwork) and what must change; add `no extra 
elements` to prevent drift.
- compositing: Reference inputs by index; specify what moves where; match lighting, perspective, and scale; keep the 
base framing unchanged.
- sketch-to-render: Preserve layout, proportions, and perspective; choose materials and lighting that support the 
supplied sketch without adding new elements.

## Where to find copy/paste recipes
For copy/paste prompt specs (examples only), see `references/sample-prompts.md`. This file focuses on principles, 
specificity, and iteration patterns.


exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "(Get-Content ..\\..\\..\\CLAUDE.md).Count" 
in C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 232ms:
128

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content ..\\..\\..\\CLAUDE.md | 
Select-Object -First 64" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 318ms:
# Crownless ├óΓé¼ΓÇ¥ working practices (all agents)

## Mobile version ├óΓé¼ΓÇ¥ kept in sync (unfrozen 2026-07-15)
- `mobile/` (repo root) holds the iOS/Android version: a snapshot fork of `game/` plus a small, fixed set of mobile 
deltas (Mobile renderer, touch HUD, export presets ├óΓé¼ΓÇ¥ the exact list lives in `mobile/README.md`). Deployment is 
proven end to end ├óΓé¼ΓÇ¥ GitHub Actions **"Mobile builds"** produces a signed-debug APK and an unsigned iOS `.ipa`; 
both install/run on-device (sideload).
- **Policy change (was "frozen, do not touch"):** keep `mobile/game/` in sync with `game/`. `game/` is still the 
**source of truth** ├óΓé¼ΓÇ¥ never edit `mobile/` to fix a desktop issue; fix it in `game/` and re-sync. To re-sync: 
re-copy `game/` over `mobile/game/`, re-apply the mobile deltas from `mobile/README.md`, then run the compile gate + 
`test_quick` against `mobile/game` before committing. Keep the delta list small and current.

## Code layout (see CODING_GUIDELINES.md ├é┬º38)
- Tuning knobs ├óΓÇáΓÇÖ `balance.gd`; data tables stay in domain files (classes/items/story). Never inline a bare 
tuning number.
- Docs split (2026-07-06): balance/pacing round narratives go in `BALANCE_HISTORY.md` (newest at TOP of the tuning 
list); `DESIGN.md` holds only current decisions + distilled standing rules ├óΓé¼ΓÇ¥ never append round-by-round 
history there.
- Big node classes are inheritance CHAINS (verbatim moves, calls flow derived├óΓÇáΓÇÖbase, all vars in the base layer):
  - Game: `game_base` (state/flags/convo/lookups/fx) ├óΓÇá┬É `game_world` (graph/rooms/walls/spawning/gates) ├óΓÇá┬É 
`game_flow` (deaths/loot/chapters/settings/terrain events) ├óΓÇá┬É `game.gd` (boot + per-frame).
  - Player: `player_core` (state/stats/gear/progression) ├óΓÇá┬É `player_combat` (targeting/hit/shared juice + shared 
primitives) ├óΓÇá┬É `player_kit_{warrior,archer,mage,assassin,paladin,warlock}` (one file per class: `_use_<class>` 
dispatch + its abilities) ├óΓÇá┬É `player.gd` (dispatch/survival/per-frame). Primitives shared by several kits 
(`_dash_strike`, `_melee_arc`, `_mist`, `_beam_fx`, `_grant_stab_surge`) stay in `player_combat` ├óΓé¼ΓÇ¥ calls only 
flow derived├óΓÇáΓÇÖbase.
  - Tests: `tests/test_base` (helpers) ├óΓÇá┬É `tests/test_ch1` ├óΓÇá┬É `tests/test_ch2` ├óΓÇá┬É `autotest.gd` (entry 
+ systems tier).
- Self-contained UI screens = static modules in `scripts/ui/` (dev_panel, codex) taking the Menus instance.
- New `class_name` script ├óΓÇáΓÇÖ run `--import` before any headless run.
- The in-game **codex** (`scripts/ui/codex.gd`) is the player-facing reference for monsters/bosses/terrains/gear 
├óΓé¼ΓÇ¥ it goes stale silently. Whenever you add player-facing content or a feature, ask whether the codex should 
reflect it, and if so update it in the same change. It reads `Story.ALL_ENEMIES`/data tables directly, but a few 
hand-maintained parallel lists still gate it (e.g. `menus.gd` `BOSS_KINDS` decides monster-vs-boss bucketing) ├óΓé¼ΓÇ¥ 
update those too, or a new boss lands in the wrong bucket. Layout since 2026-08-15 = rail of sections 
(`UICodex.SECTIONS`) + filter chips + ledger/detail; a new section is one SECTIONS row + a `_rows`/`_detail` (or page) 
builder, and every legacy `open_codex("<tab>")` id routes through `_resolve()` ├óΓé¼ΓÇ¥ extend that, never fork the 
panel. Reference prose lives under Field notes (`NOTE_PAGES`), not above a shelf.

## Testing ├óΓé¼ΓÇ¥ ALWAYS in this order
1. Compile gate first, every time (`test_quick.bat`/`test.bat` run `check_compile.gd` first). Never invoke the test 
scene directly: one parse error anywhere makes the suite script fail to compile and the headless engine idles FOREVER 
(a zombie that looks like a slow run). The gate catches it in ~3s with the real error.
2. Iterate with `test_quick.bat` (~15s): gate ├óΓÇáΓÇÖ boot ├óΓÇáΓÇÖ one class kit ├óΓÇáΓÇÖ systems ├óΓÇáΓÇÖ UI smoke 
├óΓÇáΓÇÖ pause menu.
3. `test.bat` (full suite, minutes; plays both chapters end to end) must be green before staging.

## Agent toolkit (the map: `tools/INDEX.md` ├óΓé¼ΓÇ¥ check it before writing a helper script)
- `preflight.bat` ├óΓé¼ΓÇ¥ the traps below, mechanized: stale/forgotten `--import`, codex/BOSS_KINDS staleness, 
unregistered content modules, diff-scoped balance-number + CONNECT_DEFERRED lints, new shot rig not `extends ShotRig`. 
Run it before staging; every finding prints its one-line fix.
- `python tools/safe_commit.py -m "msg" <your paths>` ├óΓé¼ΓÇ¥ the serialized-commit etiquette as a guardrail: 
path-scoped commit that lists sibling-staged work instead of swallowing it.
- `python tools/sync_mobile.py` ├óΓé¼ΓÇ¥ mobile drift report; `--apply [--gate]` performs the re-sync ritual from 
`mobile/README.md`.
- **Headless Codex (`codex exec`, the default generated-art lane): calling guide = 
`C:\Users\asali\Projects\CODEX_HEADLESS.md` (Projects root, shared by every project on this box: binary, call, all 
flags, machine limits) + `tools/CODEX_HEADLESS.md` (the MMO layer: runner, drivers, vet/install).** The binary's 
install dir is re-hashed on every app update ├óΓé¼ΓÇ¥ resolve it (`python tools/codex_bin.py` / `Resolve-Codex` in 
`run_codex_batch.ps1`), never hard-code it; prompt goes on STDIN; on this shared box run image batches serial + 
RAM-gated (`run_codex_batch.ps1 -MaxParallel 1 -MinFreeGB 1.3`), never beside a Godot suite (a parallel batch + suite 
OOM-crashed the host 2026-08-17).
- `python tools/art/verify_art.py <base>` ├óΓé¼ΓÇ¥ post-install sprite checks (strip geometry, 8-dir completeness, 
green-bleed, stale import, and in-cell content gates: anchor drift/ghost chunks/edge cuts/clip-vs-idle body scale + 
**HEROBODY** hero frame-0 scale-lock shrink).
  - **HEROBODY vs CLIPSCALE (added 2026-08-23):** CLIPSCALE models the ENEMY renderer (median body / cell). HEROES 
render differently ├óΓé¼ΓÇ¥ `player_core._measure_hero_frame` locks a clip's on-screen scale to FRAME 0's CONTENT bbox 
and plays the whole clip at it, so a clip whose REST pose already raises a weapon (frame-0 content > body) renders the 
body small for the entire clip (the archer-multishot 0.85├âΓÇö shrink CLIPSCALE structurally cannot see). HEROBODY 
flags a hero action clip whose frame-0 body renders <0.90├âΓÇö the idle's. A neutral rest frame reads ~1.0 and a legit 
crouch/lunge still starts neutral, so both are spared (0 FP across the 6 base classes + 5 skins).
  - **The eyes-only drift class has NO geometric gate ├óΓé¼ΓÇ¥ the net is a VISUAL QA artifact + agent review.** 
Face-fidelity drift (a fatter/blurrier face in action frames), a shield/armor detail that morphs frame-to-frame, a 
cape that flips sides ├óΓé¼ΓÇ¥ all pass every geometric check; the *drawing* is inconsistent. verify_art cannot catch 
these and a sharpness/consistency proxy false-positives on real motion. So a skin/class regen MUST emit a per-clip 
contact sheet (idle + action peak frames, feet-aligned + face crops) and an AGENT must review it before install. An AI 
reviewer catches "her face got fatter"; a number can't. Don't rely on the owner's eyes as the first line. **The full 
reusable workflow ├óΓé¼ΓÇ¥ dedup-scope ├óΓÇáΓÇÖ one review agent per subject over EVERY clip├âΓÇödir├âΓÇöframe 
├óΓÇáΓÇÖ orchestrator verifies each flag ├óΓé¼ΓÇ¥ is `tools/art/DRIFT_AUDIT.md` (covers heroes/skins AND the boss/mob 
adaptation: enemy render path = CLIPSCALE not HEROBODY).** Established 2026-08-24; the known drift catalogue 
(fire-on-move clips are the systemic weak point; un-regenerated clips carry the face-softening family; 
single-direction off-model art) lives there.
  - **WARN triage ├óΓé¼ΓÇ¥ FAIL is the bar, not WARN (investigated 2026-08-23).** The six BASE heroes throw a large, 
standing pile of WARNs that are NOT defects ├óΓé¼ΓÇ¥ the gate has no way to whitelist intentional content, so it flags 
it every run. All three benign categories, confirmed by rendering the flagged pixels on grass + overlaying them on the 
sprite:
    - **BLEED** on base heroes ├óΓÇ░┬á green keying rim. It's soft-alpha FX (the archer's cast/ult IS green 
Arrow-Storm magic; every class's ult/cast auras + glows) plus green-CLOTHING edges (the archer's green cloak). The 
BLEED counter is colour-blind (any `0<a<255` pixel), so green *content* inflates it; a band-normalize/upscale 
multiplies the same FX/cloak edge pixels and the raw count rises with zero new bleed. The real-bug discriminator is a 
green RIM hugging the silhouette (`G>max(R,B)+20` massed on the body outline) ├óΓé¼ΓÇ¥ on the base heroes that rim is 
absent; the green sits on the FX cloud and the cloak, and on a grass background there's no halo.
    - **STRAY** on base heroes = FX props the game does NOT re-spawn: the warlock's floating flaming SKULL (his 
signature, every frame), the warrior's flaming-sword blade/trail, the archer's green cast-motes. Distinct from a baked 
*projectile* (which the STRAY rule is really aimed at) ├óΓé¼ΓÇ¥ leave these.
    - **EDGECUT** on melee swings (`attack2`/`attackb`/`attackc` for warrior/assassin/paladin) = the weapon reaching 
the cell edge at the extreme of the arc, not a clipped body.
    - Baseline as of 2026-08-23: base heroes = **0 FAIL, ~261 WARN** (208 BLEED / 28 STRAY / 25 EDGECUT); the 5 
in-scope elite skins = clean `VERIFY OK`. Don't re-investigate or "fix" these WARN categories on the base heroes 
├óΓé¼ΓÇ¥ only act on a NEW **FAIL**, or a WARN that leaves these three buckets (e.g. a real body green-rim, a 
butchered/PARTIAL frame, an anchor/scale/FEETSLIDE hit).
- **Screenshot rigs: never write a standalone one (2026-08-15).** Twenty-three `game/shot_*.gd` files are copies of 
the same 25-line boot/`_shot`/quit boilerplate, none with a watchdog, muted only by convention. New rig = `extends 
ShotRig` (`scripts/dev/shot_rig.gd`: `boot()`, `shot()`, sim-clock waits, `arg()`/`flag()`, Master-bus mute, 
`--timeout=N` watchdog) + `_ready()` of steps + `finish()`; run it ├óΓé¼ΓÇ¥ and every legacy rig ├óΓé¼ΓÇ¥ through 
`shot.bat <rig> [--timeout=N] [rig args]`, which injects `--audio-driver Dummy`, compile-gates the rig script 
(check_compile's default walk skips `game/` root: a parse error there = a window that idles forever), and hard-kills a 
hung engine. `preflight` FAILs a new rig that doesn't extend it and WARNs on an edited legacy one (convert on touch). 
Worked example: `shot_fx_series.gd`; the map: `tools/INDEX.md` "In-engine shot rigs".
- **Windowed shot rigs (`shot_*.tscn`, `qa_skins`, anything not `--headless`) run MUTED: always pass `--audio-driver 
Dummy`.** They boot the real game with music + SFX on the owner's shared machine; an unmuted rig blares through his 
speakers mid-session (owner ruling 2026-08-15). Headless suites are already silent.

## GDScript traps (each has bitten us) ├óΓé¼ΓÇ¥ review-time deep list: `CODING_GUIDELINES.md` ├é┬º38
- `var x := obj.method()` on a loosely-typed obj (or any Variant expression, e.g. `Dictionary.get`) = PARSE ERROR 
"cannot infer type". Annotate: `var x: float = ...`.
- New `class_name` ├óΓÇáΓÇÖ `--import` first or headless silently hangs.
- Labels inside HBoxContainer collapse to one char per line without `custom_minimum_size`.
- Multiline lambda: following call args must sit on the SAME line as the lambda's last statement.
- Tests waiting on timed effects must poll wall-clock (`await create_timer(0.2)`) ├óΓé¼ΓÇ¥ frames race ahead headless.
- Autotest sections SNAPSHOT + RESTORE shared state (flags, resonance, standings); never `.clear()`. Two corollaries, 
both learned from one flaky capital run (2026-07-26):
  - **Restore on the FAILURE paths too.** `_fail` only QUEUES `quit(1)` ├óΓé¼ΓÇ¥ the suite keeps running to the end of 
the frame, so the next section still executes and reads whatever the failed one left behind. A mid-city failure 
stranded `chapter_id == "capital"`, where `shop_markup` answers a flat 1.0, and the road-markup section then reported 
a second, entirely bogus failure. Put the assertions in a helper and the restore in its caller, so all ~40 exits pass 
through it.
  - **Never assert against state an earlier section accumulated.** A content-tier section inherits a whole campaign of 
loot, gold, resonance and bag capacity ├óΓé¼ΓÇ¥ all of it loot-luck. The capital's lapidary quest asserted its kit 
landed in the bag, and a full bag legally mails the kit instead: pass or fail came down to whether a bag DROPPED that 
run. Control the precondition (lend a bag, pin the shard) or the assert is a coin flip.
- canvas_item shader `fragment()`: `COLOR` arrives PREMULTIPLIED (modulate ├âΓÇö TEXTURE sample). Writing `COLOR = 
result * COLOR` multiplies the texture in twice ├óΓé¼ΓÇ¥ everything darkens and "brighten" shaders read as darken 
(phantom_charge was bitten; owner saw "only gets darker"). Reuse ONLY `COLOR.a` (silhouette.gdshader convention).
- `Environment.adjustment_contrast` with `viewport/hdr_2d` runs in LINEAR light: it pivots on 0.5, and a dark floor 
sits near 0 there ├óΓé¼ΓÇ¥ contrast 1.06 HALVED the keep floor's brightness (gameplay-polish rig 2026-08-18). Leave 
contrast at 1.0; use saturation/brightness only, and judge any whole-frame grade in-game, never from the numbers.
- Spawning an Area2D + CollisionShape2D (Chest/Pickup/Projectile) from inside a `body_entered`/`area_entered` handler 
= non-fatal "Can't change this state while flushing queries" engine error (`area_set_shape_disabled`) ├óΓé¼ΓÇ¥ the new 
shape's enter-tree hits the physics-flush guard. Connect physics signals with `CONNECT_DEFERRED` or `call_deferred` 
the spawn (chest open + projectile ricochet were bitten).
- `draw_colored_polygon` on a SLIVER (near-collinear points) = random non-fatal "Invalid polygon data, triangulation 
failed" spam: the triangulator picks winding from a float32 shoelace over RAW coords, so a sliver's sign is rounding 
noise at world-scale coordinates ├óΓé¼ΓÇ¥ coordinate-dependent coin flip, NOT deterministic per shape (knife-trail 
fold-back miters were bitten). Pre-check `Geometry2D.triangulate_polygon(p).is_empty()` and skip ├óΓé¼ΓÇ¥ the exact 
C++ path behind the error, and everything it rejects is sub-pixel (the `_poly` guard in player_combat.gd).
- **In co-op, the tree pause is NOT an input gate.** `request_pause(true)` NO-OPS online (`game_base.gd`, ├é┬º5.4 
├óΓé¼ΓÇ¥ a shared world never pauses), so anything leaning on the pause to stop input/processing keeps running in a 
session. Menus, dialogue and choice prompts all pause solo and none of them pause online. Gate on the OVERLAY STATE 
instead ├óΓé¼ΓÇ¥ `menus.is_open()` / `hud.dialogue_active` / `hud.choices_active`, the guard `game.gd`'s tap-to-talk 
already uses. The touch HUD had no `process_mode` override and only checked `state == ST_PLAYING` (there is no 
`ST_MENU`), so its joystick ├óΓé¼ΓÇ¥ the whole left half of the screen ├óΓé¼ΓÇ¥ stayed live UNDER every menu and a 
drag walked your hero mid-fight. Note the shape of the miss: it was invisible in BOTH cheap tests (solo-on-phone 
pauses; co-op-on-desktop has no touch HUD) and only existed in the mobile ├âΓÇö co-op cross-product. When a feature is 
platform- or session-conditional, ask what its gate is in the OTHER combination.

## Machine RAM is tight (~10GB) ├óΓé¼ΓÇ¥ tool-process hygiene (2026-08-23)
- This box has ~9.8GB physical RAM and the BASELINE load already fills it: Claude Code + its MCP servers ├óΓÇ░╦å 3GB 
(22 procs ├óΓé¼ΓÇ¥ MCP servers orphan on restart; 3 duplicate `pixelforge-mcp` copies seen), browser ├óΓÇ░╦å 1.2GB, 
Windows (svchost + Defender + compression) ├óΓÇ░╦å 1.4GB. That leaves ~1GB free ├óΓé¼ΓÇ¥ nothing has headroom. This is 
WHY image batches run serial + RAM-gated and never beside a Godot suite. But be precise about WHAT is heavy: **a 
single `codex exec` image gen is API-side and costs only ~250MB local (measured 2026-08-24) ├óΓé¼ΓÇ¥ it is NOT a 
machine-killer and a single serial job runs safely even at ~1GB free.** The RAM risk is PARALLELISM (several gens at 
once) ├âΓÇö a co-resident **Godot suite** (each Godot instance is the real GB-scale hog). So `-MaxParallel 1 
-MinFreeGB 1.3` guards against *stacking* jobs and firing next to a suite ├óΓé¼ΓÇ¥ don't read it as "Codex is a spike" 
and refuse a single gated gen. (PixelLab local footprint unmeasured; don't assume it's the same.) A Claude restart 
clears orphaned MCP servers; closing browser tabs frees ~1GB.
- **Prefer the Bash tool over PowerShell.** PowerShell tool calls can leave lingering `powershell.exe` processes (7 
orphans spanning 2 days seen, ├óΓÇ░╦å0.45GB). Use Bash for process/file ops (`taskkill //F //PID <n>` cleans strays); 
reserve PowerShell for genuine cmdlet needs, and consolidate into ONE call rather than many.

## Multi-agent etiquette
- Task boards (`CH2_TASKS.md` pattern): one owner per task, claim before starting; content lands as NEW modules under 
`game/scripts/content/` (format: `scripts/content/README.md`) + one registration line in `Story.CONTENT_MODULES`.
- Stage with `git add` after a green full suite. No commits unless the user asks.
- **Commits are SERIALIZED (2026-07-07):** a commit sweeps the WHOLE index, and sibling agents stage concurrently 
├óΓé¼ΓÇ¥ so immediately before committing, run `git status` and look at what's actually staged. If the index holds 
another agent's work, either fold it into an accurate combined message or stop and say so ├óΓé¼ΓÇ¥ never commit a 
message that describes only your slice. (Trigger: an itemization pass got swallowed into a commit labeled "cover + 
boot flow" by exactly this race.)
- Commit messages: NO author/co-author trailers (no `Co-Authored-By`, no attribution lines) ├óΓé¼ΓÇ¥ this project 
keeps commits authorless; credit lives in a separate credits file.
- Autotest additions: content modules use the marked CONTENT-MODULE TEST HOOK (one func at file end + one call line); 
never edit existing sections.


exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content ..\\..\\..\\CLAUDE.md | 
Select-Object -Skip 64" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 309ms:
## Asset sourcing
- **Allowed ├óΓé¼ΓÇ¥ and that's the whole list:** ONLY these two, or anything we generate ourselves (must be up to 
par: semantic fit + reads clean in-game).
  1. **CC0 / public domain** ├óΓé¼ΓÇ¥ zero obligations, ideal.
  2. **CC-BY / MIT / Apache / OFL** ├óΓé¼ΓÇ¥ fine; ship the attribution file (we already do, in 
`assets/*/CREDITS.txt`).
  No pre-approval needed for these. Source from anywhere ├óΓé¼ΓÇ¥ OpenGameArt, Kenney, itch.io, the Dungeon Crawl 
tileset (CC0), etc.
- **Everything else is off-limits** ├óΓé¼ΓÇ¥ do not use, don't even ask: share-alike (CC-BY-SA, GPL), non-commercial 
(CC-NC), proprietary/paid asset-store packs, and anything with no stated license (default = all rights reserved). This 
game ships commercially on Steam; these licenses either forbid selling it or can force us to open-source the whole 
project.
- Install flow: probe/download, and LOOK at the image (or listen to the audio) before installing; overrides drop into 
`assets/sounds|music|sprites/` by name (a PNG/wav there overrides the procedural version of the same name).
- Audio taste: semantic fit over quality; no melodic-jingle chiptunes for ults/terrains; never human grunts on casts; 
never touch `ult_mage.wav` or the village/story/boss synth tracks.

## Icon fidelity ├óΓé¼ΓÇ¥ authored resolution ├óΓÇ░┬á render size (owner ruling 2026-08-21; agents keep tripping on 
this)
- Player-facing icon families are **DUAL-RES**: a high-fidelity master for the codex/detail views + a downscaled 
gameplay copy for the bag. The **128px painterly master is the FIDELITY BAR** ├óΓé¼ΓÇ¥ match it, not a flat/pixel 
style, unless the owner says otherwise. Reference tiers: **gear** = `assets/icons/codex/<shape>_<grade>.png` @128px 
(codex, via `Art.codex_item_icon`) + `assets/icons/<name>.png` @32px (bag); **class splashes** ~235px authored 
├óΓÇáΓÇÖ render ~100px in-game (~2.35├âΓÇö ├óΓé¼ΓÇ¥ the owner's "renders nicely" benchmark).
- **THE TRAP:** authoring a family at **32px only**. It looks fine in a 32px bag slot, so it passes a quick check 
├óΓé¼ΓÇ¥ but every larger surface (codex ~28├óΓé¼ΓÇ£128px, the detail popover header 40px) UPSCALES it to mush, and it 
reads as low-fidelity beside the 128px gear. **Gems shipped 32px-only (2026-07-30) and were the 2026-08-20 
visual-review's one solid asset catch for exactly this.** When you regen an icon family: author at the 128px gear bar, 
store dual-res, and load the 128 in large contexts. Verify the fidelity numbers (stored res vs on-screen render px) 
BEFORE designing ├óΓé¼ΓÇ¥ the owner asks for this by name.
- **This generalises past icons to ALL world/character/codex art (owner ruling 2026-08-24): a master should be 
├óΓÇ░┬Ñ2├âΓÇö its on-screen render size.** The auditor is `tools/art/fidelity_audit.py` + `game/fidelity_dump.gd`; the 
current findings + prioritised re-art list live in **`FIDELITY_AUDIT.md`** (root). Bar = the base classes (render 99px 
from 235px = 2.35├âΓÇö). Standing findings: the **5 enhanced-base skins PASS (2.37├âΓÇö)** but the **18 legacy skins 
are ~1.2├âΓÇö** (the planned regen quantified); trees/large props + one npc (`pilgrims_schism`) + `choir_censer` (a 
boss-summoned add) are the other real re-art targets. Unplaced+low-fi mobs get DROPPED, not re-arted (`bat`/`direbat` 
gone 2026-08-24).

## World props ├óΓé¼ΓÇ¥ style + motion contract (owner ruling 2026-08-18, after a day of "shifts / dims / cartoonish" 
flags from play; review-time checklist: `CODING_GUIDELINES.md` ├é┬º40; the polish effort's board = `POLISH_TASKS.md`, 
its generation lane = `tools/art/style_unify/`)
- **One style.** Every world prop matches the cast: painterly, soft-shaded, NO black outlines, muted palette, top-down 
three-quarter, light top-left. A hi-res canvas is NOT a pass ├óΓé¼ΓÇ¥ a 1024px cartoon is still a cartoon (cactus2, 
tree_teal2, grave_cross all passed the resolution audit). Regen briefs name a painterly SIBLING as the style ref and 
the old asset only as the SUBJECT (silhouette/footprint/colour identity); judge the result on a contact sheet BESIDE 
its siblings (`scratchpad`-style sheet, or `tools/art/asset_gallery.py`), never against the asset it replaces.
- **Animate in place.** Frames never translate the object. Rigid props (furnaces, wells, shrines, rigs, pools) keep a 
byte-stable silhouette ├óΓé¼ΓÇ¥ colour-only motion (glow pulse, ember flicker), bbox/baseline/centre drift 0; only 
fire, cloth and foliage may change silhouette, and then anchored on frame 0's baseline. Wind sway is an ALLOW-list of 
vegetation (`_wind_scenery`), never "everything that isn't a building". Hazard pools stay put 
(`Balance.HAZARD_POOLS_DRIFT`). Anything that swings/pendulums needs authored frames with proper easing or no 
animation at all ├óΓé¼ΓÇ¥ a 4-frame swing reads as fake.
- **Dim, don't strobe.** A prop should look STILL at a glance and alive on a second look. Luminance swing ├óΓÇ░┬ñ 
10-12% for glows/pulses (`derive_prop_anim.py --amp 0.10`), ├óΓÇ░┬ñ 25% for open fire, never on the whole sprite 
(bright/warm pixels only). The 0.35 default that looked "visible" on a contact sheet strobes in-game.
- **Gate every strip you touch:** `tools/art/audit_prop_anims.py` (frames/canvas/per-frame drift + amp) + 
`verify_art.py`; a NEW rigid animated prop goes into autotest's `full_prop_anims` contract (frame == static canvas, 
baseline ├óΓÇ░┬ñ1 / centre ├óΓÇ░┬ñ2). Contact sheets and metrics do not replace watching the loop: run it in a shot 
rig at 1x (`shot.bat polish --gif`) before calling it done ├óΓé¼ΓÇ¥ a subtle loop that looks static on a sheet is 
correct.

## Hero skins ├óΓé¼ΓÇ¥ what is in scope vs OFF-LIMITS (owner ruling 2026-08-22)
- **In scope for polish: ONLY the 5 ENHANCED-BASE designs**, wired as elite skins purely for in-game preview: warrior 
`emberbound_heir`, assassin `erased_name`, archer `severed_thread`, warlock `ledgerbound`, mage `blighted_healer`. 
These are the enhanced base-class designs ├óΓé¼ΓÇ¥ fix/re-anchor/despur/frame-edit them freely.
- **OFF-LIMITS ├óΓé¼ΓÇ¥ do NOT touch, regen, re-anchor, despur, frame-edit, or "fix":** every OTHER hero skin. That is 
all of `game/assets/sprites/skins/mythic/` (dreadknight, stormforged, eclipse_knight, fallen_arbiter, blade_dancer, 
phantom/phantom_awakened, voidwraith, eldritch_herald, ├óΓé¼┬ª) **and** the real skins that live in `skins/elite/` 
alongside the enhanced-base five (dreadknight, frostfall_ranger, void_weaver, hellfire_inquisitor, eclipse_knight, 
blade_dancer, ├óΓé¼┬ª). A blanket art-sweep must EXCLUDE these ├óΓé¼ΓÇ¥ an auto-scan of `assets/sprites/` will hit 
them; scope it to the five, or to bosses/mobs, explicitly.
- **Why:** a full REGEN pass over all the real skins is planned (owner 2026-08-22, "for later"). Until that pass, 
leave them exactly as committed ├óΓé¼ΓÇ¥ a piecemeal fix now is wasted work the regen overwrites, and it drifts the 
design.
- **Known-defect note:** the real skins carry the same feet-slide artifact the enhanced-base + bosses/mobs were fixed 
for (2026-08-22), so `verify_art` will emit `FEETSLIDE` WARNs on them. That is EXPECTED and intentionally left for the 
regen pass ├óΓé¼ΓÇ¥ do not "fix" it.

## Required per-class animation clips beyond the standard set (so a regen knows what to produce) ├óΓé¼ΓÇ¥ 2026-08-23
Some abilities need EXTRA clips on top of anim/walk/attack/attack2/dash/cast/ult/death. A class or skin regen MUST 
produce these or the ability silently falls back to a worse pose. Both are art-driven (no strip = the fallback), so 
they're easy to forget ├óΓé¼ΓÇ¥ hence this manifest.

- **Fire-on-move (`<base>_attack_walk[_<dir>]`)** ├óΓé¼ΓÇ¥ a walk cycle that fires the basic mid-stride, so spamming a 
low-cd RANGED basic WHILE MOVING doesn't lock the standing pose sliding over the floor (`player.gd` `walk_fire`; a 
mid-cycle re-cast lets the stride finish). Needed by the ranged spammables ONLY:
  - mage **Firebolt** (a1=attack), warlock **Shadowbolt** (a1=attack), archer **Quick Shot** (a1=attack), assassin 
**Fan of Knives** (a3=attack2).
  - NOT the melee 3-way basic ├óΓé¼ΓÇ¥ the assassin STAB keeps its attack/attackb/attackc alternation while moving 
(gated by `slot=="a1" and _clips.has("attackb")`, which only excludes the assassin).
  - **HOW TO GENERATE (the technique that finally worked, owner 2026-08-23): it's the WALK clip + a firing arm 
├óΓé¼ΓÇ¥ do NOT author a walk from scratch.** Give the character's EXISTING `<base>_walk_<dir>` strip as the image ref 
and instruct: "reproduce this exact walk ├óΓé¼ΓÇ¥ same leg cycle/stride/crossing, same garment ├óΓé¼ΓÇ¥ change ONLY 
the free hand to fire the basic once." A from-scratch walk brief (however detailed the per-frame boot storyboard) kept 
gliding or failing to cross the legs; referencing the real walk carries its crossing gait for free. Keep the brief 
SHORT ├óΓé¼ΓÇ¥ the walk ref IS the leg spec.
  - Contract: 8-frame walk+fire; feet PLANTED (no in-cell slide ├óΓÇáΓÇÖ FEETSLIDE); the garment stays CLOSED and 
design-accurate (NO slit, NO exposed legs/bare skin ├óΓé¼ΓÇ¥ the walk reads through the BOOTS alternating below the 
hem + the hem swaying, exactly as the walk clip does); NO baked projectile/flame on the sprite (the game spawns it 
├óΓé¼ΓÇ¥ a small warm spark shares the mage's skin/gold palette and is un-maskable by colour except the bright-yellow 
core, so keep it out at gen time). Author S/E/N; E├óΓÇáΓÇÖSE/NE; **left MIRRORS right** (W/NW/SW = mirror of E); flat 
= S. Build lane: `scratchpad/attack_walk_codex.py`.
  - **`attack_walk_b` ├óΓé¼ΓÇ¥ the alternate variant (owner 2026-08-23).** Fire-on-move ALTERNATES `attack_walk 
├óΓÇáΓÇ¥ attack_walk_b` each fresh moving-basic cycle (player.gd `_walkfire_swing`), like the melee 
attack├óΓÇáΓÇ¥attackb, so spamming while moving doesn't replay one clip. Produce it with the SAME walk reference 
├óΓé¼ΓÇ¥ only the FIRE motion differs from the primary (warlock: two-hand shove vs one-hand thrust; mage: free-hand 
rising flick vs level thrust, staff hand kept clearly separate; assassin: sidearm flick vs overhand throw). **NOT the 
archer** (owner 2026-08-23): a bow draw has ONE proper form, so the archer has no `_b` ├óΓé¼ΓÇ¥ it plays the primary 
every cycle. When a `_b` would confuse the silhouette (e.g. the mage's casting hand reading as the staff hand), keep 
the two hands clearly separate or skip the variant. Build lane: `attack_walk_codex.py ├óΓé¼┬ª --b`. Art-driven: no 
`_b` strip = the primary plays every cycle.

- **Melee swing alternation (`attackb`, and `attackc` for enhanced variants)** ├óΓé¼ΓÇ¥ melee basics cycle multiple 
authored swings per cast (`player_core.gd` `_alt_basic_clip`: attack├óΓÇáΓÇÖattackb├óΓÇáΓÇÖattackc) so Cleave / Smite 
/ Stab don't replay one motion. Which classes ship which:
  - **warrior, paladin, assassin** carry `attackb` (2-way minimum). Ranged classes (mage/archer/warlock) have only 
`attack`.
  - **3-way (`attackc`)**: base **paladin**, warrior **emberbound_heir**, assassin **erased_name** (owner 2026-08-22 
├óΓé¼ΓÇ¥ the enhanced variants). `attackc` is a DISTINCT 3rd motion (a rising uppercut), not a recolor of 
attack/attackb. autotest `_test_swing_alternation` expects 3-way when a body ships an attackc.

## Generated art tool authorization
- **NEVER assume PixelLab is authorized. PixelLab may be used only when the owner explicitly authorizes PixelLab for 
the specific task.** Existing PixelLab assets, PixelLab helper scripts, character metadata, or a matching PixelLab 
workflow elsewhere in the repo do not grant permission for a new generation.
- When an agent has a built-in image-generation/editing tool (for example, ChatGPT/Codex image generation), that 
built-in tool is the default for generated art unless the owner explicitly requests or authorizes another generator.
- This rule applies even when another generator appears technically better suited. If the built-in tool cannot meet 
the requirement, stop and ask the owner; do not silently switch tools or spend external generation credits.

### Codex built-in ImageGen walk-animation observations
- For bipedal characters, the built-in ImageGen tool often fails to preserve a fluid left/right leg cycle. A real 
same-colored leg crossing over the other in one passing frame can create a convincing walking illusion; keep that 
crossing to one or at most two frames rather than making every frame crossed.
- If ImageGen repeats nearly identical crossed poses, give each cell an explicit foot-position storyboard instead of 
only asking for "alternating legs": use a wide split, a tight crossover, a second crossover with a different spread 
and the opposite leg emerging, then the opposite wide split or a subtle toe-off. State that frames 1, 3, and 4 must 
have visibly different lower-body silhouettes. (Tried on the Wild Fang Raider 2026-08-14 and again 08-15: the 4-cell 
2x2 kept coming back with the SAME leg leading in both stride cells and the body ~11% short ├óΓé¼ΓÇ¥ the owner still 
called the walk bad.)
- **What actually fixed the Wild Fang Raider walk (2026-08-15): ONE ROW of EIGHT figures, each frame labelled with its 
gait phase** (near-foot contact / load / far leg passes / reach, then the same four for the other leg ├óΓé¼ΓÇ¥ the 
pipeline doc's "legs do not alternate" remedy). The row came back with 8 distinct poses, constant body height and one 
ground line; installed as an 8-frame strip (the engine's strip reader is frame-count agnostic ├óΓé¼ΓÇ¥ width/height 
├óΓé¼ΓÇ¥ so 8 is fine; at the 2x walk clock the loop runs 0.67 s). Prefer this over 4-cell 2x2 for any bipedal walk. 
Ask Codex to report leg-lead per frame in its final message; it is a candid critic of its own output.
- Every ImageGen-keyed strip carried a dark-green antialias rim (`build_mob_walk_repairs.remove_green` now despills 
the 2px edge band); a strip that "looks fine" on a dark contact sheet can still ring green over grass ├óΓé¼ΓÇ¥ check 
`G > max(R,B)+20` counts on the rim, not just the semi-alpha BLEED gate.
- The tool is generally more reliable for non-bipedal walks (for example, spiders and wolves), and for characters 
whose locomotion does not expose a conventional leg cycle: robes can communicate motion through cloth flutter, and 
floating characters can use body/cloth drift.
- For bipedal sheets, prompt for fixed torso/pelvis anchors, a shared foot baseline, compact stride, 
held-weapon/upper-body continuity, and explicit negative constraints against recoloring legs, detached feet, 
duplicated frames, and high-knee marching. Inspect the generated sheet before installation and run 
`tools/art/verify_art.py <base>` afterward; anchor drift can still remain even when the sheet looks visually centered.

## PixelLab characters ├óΓé¼ΓÇ¥ regenerating an EXISTING one (deep dive: `tools/art/PIXELLAB_BOSS_EDITS.md`)
- **Authorization gate:** this section applies only after the owner has explicitly authorized PixelLab for the current 
task. It is not permission to choose PixelLab.
- **RULE: before you regenerate, re-roll, or tweak an existing PixelLab character (boss/hero/skin), ALWAYS read its 
export metadata FIRST.** Download the zip (`curl -H "Authorization: Bearer $PIXELLAB_SECRET" 
https://api.pixellab.ai/mcp/characters/<id>/download -o out.zip`) and read `metadata.json` ├óΓé¼ΓÇ¥ it stores the 
ORIGINAL creation `prompt` (plus size, `template_id`, view). Regenerate from that EXACT prompt and change ONLY the 
clause the owner flagged.
- **Never invent a fresh description from a screenshot or memory** ├óΓé¼ΓÇ¥ it silently drifts the design 
(proportions, palette, silhouette) even when the fix was one word. `get_character` does NOT return the creation 
prompt; the zip's `metadata.json` is the only source of truth.

## PixelLab characters ├óΓé¼ΓÇ¥ new generation workflow (prompt-craft cases: `tools/art/PIXELLAB_PROMPT_LESSONS.md`)
- **CHECK ALL 8 ROTATIONS before generating animations.** After a character body is created, visually inspect every 
direction (S, SE, E, NE, N, NW, W, SW) for consistency ├óΓé¼ΓÇ¥ same proportions, same gear, same silhouette. v3 mode 
frequently produces inconsistent north/west directions (wrong body shape, missing equipment, different outfit). If any 
direction is broken, re-roll the body first. Generating animations on a broken body wastes hundreds of generations.
- **Heroes use v3 mode (NOT pro).** The gold-standard assassin and warlock were both v3. Pro mode produces stocky 
proportions for visible-face characters. The `style: -, -, -` field in `get_character` appears in BOTH v3 and pro 
├óΓé¼ΓÇ¥ it cannot distinguish modes.
- **Standard mode is useless for proportions testing.** Its template skeleton produces chibi proportions regardless of 
description. Only useful for testing color palette and design elements.
- **Description formula (proven on assassin):** archetype first ├óΓÇáΓÇÖ every piece of gear described precisely 
├óΓÇáΓÇÖ body type ("lean and gaunt" / "lean and tall") ├óΓÇáΓÇÖ art mood ("muted dark palette, somber dark fantasy") 
├óΓÇáΓÇÖ explicit negatives ("no hood", "no smoke"). Leave no room for the model to improvise.
- Worked example (2026-07-12): owner said Vess should have "no smoke from mouth". The metadata prompt literally 
contained `faint blue-white keening light at the mouth` ├óΓé¼ΓÇ¥ the fix was deleting that one clause from the 
original prompt, not writing a new banshee description.

codex
The reference is a 1804├ù254 seven-cell strip with a stable baseline and generous green spacing. IΓÇÖm treating it as 
a precise edit target: the staff-side silhouette and the complete lower-body/hem pixels are locked; only the opposite 
arm progresses from rest to a chest-height open-palm thrust and back.
codex
The first pass captured the attack arc but drifted from the sourceΓÇÖs pixel scale and lower-body poses, so it is not 
acceptable. IΓÇÖm doing one focused correction using the original as the locked base and the draft only as a motion 
guide, with the original 1804├ù254 geometry explicitly required.
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "magick identify -verbose 'C:\\Users\\asali\\
.codex\\generated_images\\01a0674d-4655-7b60-8aa1-58c8c943d7a8\\exec-9a83bb34-a105-46e4-b519-452cd6d876e8.png' | 
Select-String 'Geometry|Colors:|Histogram' -Context 0,2" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 exited 1 in 855ms:
magick : The term 'magick' is not recognized as the name of a cmdlet, function, script file, or operable program. 
Check the spelling of the name, or if a path was included, verify that the path is correct and try again.
At line:2 char:1
+ magick identify -verbose 'C:\Users\asali\.codex\generated_images\01a0 ...
+ ~~~~~~
    + CategoryInfo          : ObjectNotFound: (magick:String) [], CommandNotFoundException
    + FullyQualifiedErrorId : CommandNotFoundException
 

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "python -c \"from PIL import Image; from 
collections import Counter; p=r'C:\\Users\\asali\\.codex\\generated_images\\01a0674d-4655-7b60-8aa1-58c8c943d7a8\\exec-
9a83bb34-a105-46e4-b519-452cd6d876e8.png'; im=Image.open(p).convert('RGB'); c=Counter(im.getdata()); print(im.size); 
print(c.most_common(8))\"" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 1074ms:
<string>:1: DeprecationWarning: Image.Image.getdata is deprecated and will be removed in Pillow 14 (2027-10-15). Use 
get_flattened_data instead.
(2046, 769)
[((5, 241, 18), 166989), ((5, 241, 17), 128092), ((4, 241, 17), 100360), ((5, 242, 18), 84756), ((5, 241, 19), 65114), 
((4, 240, 17), 51627), ((4, 241, 18), 49881), ((5, 242, 19), 49297)]

codex
The corrected draft still rendered the chroma background as near-green rather than literal `#00ff00`, which would fail 
the asset contract. IΓÇÖm checking the repoΓÇÖs existing attack-walk build lane now so the final strip can use its 
established normalization and geometry safeguards instead of leaving model-rendered chroma variation.
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg --files ..\\..\\.. | rg 
\"attack_walk_codex|tools[/\\\\]INDEX|verify_art\"" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 exited 1 in 847ms:
rg : The term 'rg' is not recognized as the name of a cmdlet, function, script file, or operable program. Check the 
spelling of the name, or if a path was included, verify that the path is correct and try again.
At line:2 char:1
+ rg --files ..\..\.. | rg "attack_walk_codex|tools[/\\]INDEX|verify_ar ...
+ ~~
    + CategoryInfo          : ObjectNotFound: (rg:String) [], CommandNotFoundException
    + FullyQualifiedErrorId : CommandNotFoundException
 

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-ChildItem ..\\..\\.. -Recurse -File 
-Filter 'attack_walk_codex.py' | Select-Object -ExpandProperty FullName; Get-ChildItem ..\\..\\..\\tools -File -Filter 
'INDEX.md' | Select-Object -ExpandProperty FullName; Get-ChildItem ..\\..\\..\\tools\\art -File -Filter 
'verify_art.py' | Select-Object -ExpandProperty FullName" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 1774ms:
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\verify_art.py

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -Path 
..\\..\\..\\tools\\INDEX.md -Pattern 'attack.walk|generated|art' -Context 2,8" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 616ms:

  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:4:about to write a helper script, 
check here first ΓÇö it probably exists.
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:5:Details live in each tool's 
`--help` / doc comment; deep pipelines have their
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:6:own docs (`tools/art/README.md`, 
`mobile/README.md`).
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:7:
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:8:Engine binary for every headless 
command: `tools\Godot_v4.4.1-stable_win64_console.exe`.
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:9:
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:10:## Gates & suites (run these, in 
this order ΓÇö CLAUDE.md "Testing")
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:11:
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:12:| tool | what it does |
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:13:|---|---|
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:14:| `preflight.bat` 
(`tools/preflight.py`) | mechanized trap checks: stale/missing `--import`, unregistered content modules, 
codex/BOSS_KINDS staleness, diff-scoped balance-number + CONNECT_DEFERRED lints. Run before staging; prints the fix 
per finding. `--fast` skips the engine data check. |
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:33:| `tools/sync_mobile.py` | game/ 
ΓåÆ mobile/game/ re-sync. Default = drift report (CRLF-blind, delta-aware); `--apply` re-copies + re-applies the 
README 
deltas (incl. the project.godot transform); `--apply --gate` then runs import + compile gate + quick suite on 
mobile/game. |
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:34:
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:35:## Art ΓÇö verify
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:36:
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:37:| tool | what it does |
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:38:|---|---|
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:39:| `tools/art/asset_gallery.py` (+ 
`game/asset_dump.gd`) | THE visual catalogue: walks every data table in the real engine, assembles each key's file 
family, measures it, and writes one HTML page showing every wired visual asset with a 0ΓÇô10 rating. Ratings live in 
`tools/art/asset_ratings.csv` (hand-managed, preserved across runs; the page can also rate inline and export it). 
`--sheets` writes labelled per-category contact sheets. Also reports art wired-but-absent and files nothing resolves. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:40:| `tools/art/verify_art.py` | 
post-install sprite checks for a base name: strip geometry (engine floors frames = w/h), 8-dir completeness, `*_dir` 
8┬╖K frame count, green-bleed semi-alpha, stale `--import`, plus content-geometry gates (2026-08-13) for defects 
INSIDE 
well-tiled cells ΓÇö ANCHOR (figure wanders ΓåÆ on-screen slide), GHOST (stray pose chunk in a cell), EDGECUT (limbs 
clipped at the frame cut), CLIPSCALE (clip plays smaller/larger than the idle body). Content gates are WARN-level 
judged lints; blobs/fliers/flames trip them legitimately. **STRIDE (2026-08-27)**: hero walk strips (flat/_e/_w) 
measure planted-contact backward sweep per cycle vs actual travel at hero speed ΓÇö marching-in-place walks (the whole 
legacy corpus, 0-30% coverage) WARN; `--stride-min 0.45` promotes to FAIL as the treadmill-regen install gate; eye 
check = `travel_gif.py`. Boss ability strips (`<base>_ability` + dedicated names) are swept too ΓÇö vocabulary parsed 
live from `play_action("...")` in boss.gd, gated as actions. `--all` sweeps the whole sprites dir (IMPORT+DIR8 only). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:41:| `tools/art/fidelity_audit.py` + 
`game/fidelity_dump.gd` | FIDELITY audit (2026-08-21, owner rule): a master sprite should be ΓëÑ2├ù its on-screen 
render 
size, or it looks soft (gems shipped 32pxΓåÆrendered ~40px = 1.0├ù, the review's catch; classes render ~99px from 
~235px 
art = 2.35├ù, the benchmark). Per asset `ratio = authored_px / rendered_screen_px`, flag `<2├ù`. Render formulas from 
the engine (├ùzoom 1.12): hero body `52┬╖1.7`, mob cell `scale┬╖1.7┬╖16`, boss cell `scale┬╖1.7┬╖16` (corrected 
2026-08-25 ΓÇö 
enemy.gd:312 applies CHAR_RENDER_SCALE to bosses too; "skips 1.7" was a stale comment), npc body 
`body_target┬╖1.7┬╖nsize` (no-target legacy NPCs: frame width at `3.0┬╖1.7┬╖nsize┬╖16`), critter `cell┬╖_scale`, prop 
width 
`SCENERY_RENDER_WIDTH` (family variants audited at the family width; boss-summoned adds counted as placed via def 
`summons` keys). classes/props/critters need no runtime data; mobs/bosses/npcs need each entity's live `scale` ΓåÆ 
`godot --headless --path game --script res://fidelity_dump.gd -- <out.json>` (run OFF the Codex box) then 
`fidelity_audit.py --entities <json> [--csv] [--only classes,props]`. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:42:| `tools/art/scan_key_rim.py` | 
folder-wide KEY-COLOUR RIM contamination sweep (2026-08-15): leftover magenta matte / bright-green chroma key 
surviving as a coloured ring on the silhouette edge (rock2's magenta line, the ImageGen mob green rim). Extensible 
`KEYS` dict; **`--pale-halo` (2026-08-26)** adds the COLOURLESS white-halo class the key list structurally misses 
(ring test: outermost 0-2px vs the 4-6px band inward ΓÇö a thin bright desaturated skin over darker content; caught 
the 
snow/green tree regen halos the BLEED counter is colour-blind to; JUDGED lint ΓëÑ0.25 ΓÇö pale-bodied subjects and 
wispy 
pale design strands false-positive, confirm on a grass composite; **2026-08-28**: also scores the SEMI-ALPHA fringe 
band ΓÇö the autumn trees' warm-tinted, mostly semi-alpha matte slipped the opaque-only ring + desat cap; fixer: 
`despill_pale_halo.py` below); per hit reports `rim` vs `interior` vs `key_share` and a verdict ΓÇö the discriminators 
that keep a genuinely purple/green sprite (mushroom, bush, purple projectile) out of the DEFECT bucket: contamination 
is edge-ONLY (interiorΓëê0) and a small share of the sprite. `--keys magenta,green --csv out.csv --montage 
suspects.png`. Reports only; fix with the despills in build_mob_walk_repairs/build_fx_strip (rim-BAND mask for props 
that legitimately carry the colour). Complements `verify_art.py` (per-base BLEED/geometry) with a cross-asset sweep. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:43:| 
`tools/art/despill_pale_halo.py` | the pale-halo FIXER (2026-08-28, autumn-tree catch): deterministic two-pass despill 
for the colourless white keying halo `scan_key_rim --pale-halo` flags ΓÇö halo-bright opaque-ring pixels retoned to 
the 
Gaussian-blurred local interior colour, the semi-alpha fringe fully recoloured to it (keeps the soft AA edge in the 
sprite's own palette), alphaΓëñ60 crumbs cut; `_anim` strips processed per cell (frame==static contract). The same 
recipe fixed snow/winter (fb604d2) and green (b4b2b1c) trees from a scratchpad ΓÇö now a tool because the class keeps 
recurring (autumn + bush3 + topiary were the third wave; their warm-tinted, mostly-semi-alpha fringe also forced the 
scanner's 2026-08-28 semi-alpha extension). JUDGED input: only run it on sprites CONFIRMED on a grass composite ΓÇö 
pale 
design content (teal moss, pale stone/bodies) scores like a halo and the despill would eat it. `python 
tools/art/despill_pale_halo.py <sprite.png> [...]`, then re-import + `verify_art` + `audit_prop_anims`. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:44:| `tools/art/gait_briefs.py` | 
(2026-09-03) writes the Codex STAGES for a mob/boss walk regen: reference 1 = the subject's own idle (identity), 
reference 2 = a GAIT DONOR walk rendered as a labelled row (`assassin_walk_e` for bipeds, `warrior_emberbound_heir` 
for heavy ones, `thorn_howler`/`spider_walk` for quads/arachnids, auto-flipped to the subject's native facing), plus a 
per-archetype leg-language paragraph ΓÇö biped / biped_front / biped_back / quadruped (a per-frame diagonal-trot 
storyboard) / arachnid / **glide** (robed casters + floaters: hem sway, hanging objects lagging, no leg cycle). On a 
BACK view it deliberately does NOT attach the outgoing strip: when the defect IS the view, handing it back re-seeds it 
(Vargoth's walk_n, three rounds). `--plan plan.json` (base/facing/arch/old/desc/frames/defect/heavy/extra), 
`--render-donors` for donor sheets. Feed `<root>/stages.txt` to run_codex_batch. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:45:| `tools/art/install_gait_row.py` 
| (2026-09-03) the generic rowΓåÆstrip installer behind that lane ΓÇö heroes, mobs AND bosses, frame-count agnostic 
(the 
row's N need not match the old strip's). Keys+despills, slices equal-width or at REAL gutters (`--frames N` to 
disambiguate), ONE row-wide scale from median body height, per-frame anchor (`--anchor torso|centroid|bbox`), feet pin 
to the old strip's line, slice-edge orphan sweep, tone match, `--out`/`--copy-out`/`--mirror-out`/`--backup-dir`. 
Guards learned the hard way: a SPECK cell is refused (a stray fleck sliced as its own frame shipped a 5px near-empty 
frame), and an anchor-relative WIDTH FIT (reach from the anchor, not bbox width) stops a tail being flat-cut at the 
cell edge ΓÇö floored at 5% because shrinking further trades a cropped tail for a body that pops smaller on move. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:46:| `tools/art/gait_row_sheet.py` | 
(2026-09-03) the LOOK step of that lane: OLD strip above NEW row at a shared scale, feet on one line, full-body and 
legs-crop ΓÇö the knee-articulation / passing-frame / alternation read that decides an install. `--all <stage_root>`. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:47:| `tools/art/despill_rim.py` | 
(2026-09-03) corpus scanner + fixer for the ImageGen green keying RING. `--scan <dir>` reports every sprite whose 
silhouette rim is green-dominant with an empty interior (halo, not design); the fixer touches ONLY the `--band` px 
next to transparency, so unlike `despill_keyed.py`'s global clamp it cannot dull an olive hide or a leaf cloak, and it 
refuses a sprite whose green is mostly interior. Swept 63 sprites on 2026-09-03 including 19 WARLOCK attack strips ΓÇö 
CLAUDE.md's BLEED triage had ruled base heroes carry no green rim, but that looked at cast/ult where the green IS the 
FX. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:48:| `tools/art/frame_fix.py` | 
(2026-09-03) the cheapest rung of the drift-fix taxonomy as a tool: `--drop-orphans` (erase detached non-body 
components in named cells), `--scale` about each cell's feet line, `--shift-cell N=DX,DY`, `--feet-pin`. Backs up into 
art_src/_backups and writes atomically. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:49:| `tools/art/palette_grade.py` | 
(2026-09-03) grade a style-outlier's saturation/value toward a house-palette sibling ΓÇö colour only, every pixel and 
edge in place, and the SAME transform on the `_anim` strip so a graded prop cannot drift frame to frame. `--mask 
green` keeps a tree's trunk. Prefer explicit `--sat/--val` over `--toward`: matching a reference's palette MEANS 
misfires when the two subjects differ in structure. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:50:| 
`tools/art/derive_light_sockets.py` | (2026-09-03) derives `"lights"` sockets for a STRUCTURE def from its own art ΓÇö 
flame cores clustered and converted through `_add_structure`'s placement math, so they can be re-derived after a 
repaint. Rejects GOLD trim (crowns/crests sit high in green and keep blue up; fire does not) and gives portals a cool 
colour via COOL_LIGHTS. Calibrated against torch_pillar's authored socket. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:51:| 
`tools/art/install_gait_walk.py` | (2026-08-28) installs a GAIT-TRANSFER walk batch (green-keyed Codex rows ΓåÆ engine 
strips): keys #00ff00, rim-band-ONLY despill (a global clamp dulls olive/green garments), TORSO-BAND ANCHORS each 
frame (gait-donor gens bake canvas travel ΓÇö 73px drift reads as wobble unless anchored), median-body scale + 
feet-line 
pin to the OLD strip's measured geometry, TONE-MATCHES each strip to the one it replaces (the brightness-shift drift 
class, owner catch 08-29 ΓÇö see DRIFT_AUDIT.md catalogue), writes mirrors (w/nw/swΓåÉe/ne/se) + flat=s. The 
severed_thread walk recipe; reuse for warrior/paladin rounds. Then --import + verify_art --stride-min 0.45 + gates. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:52:| `tools/art/travel_gif.py` | 
(2026-08-27) TRAVEL-GIF ΓÇö the foot-ground-coupling review artifact: renders a strip over checkered ground + 
world-anchored posts scrolling at REAL game speed (camera-follow, art on its own clock), i.e. exactly what the game 
shows. Exists because a marching-in-place walk looks PERFECT on every static loop (ba_gifs/contact sheets) ΓÇö the 
skate 
only exists under translation, which is how three review rounds passed walks whose feet cover ~15% of the ground 
(robotic-walk round 5). Two strips = stacked compare (old vs new). `--speed 250 --fps 9 --zoom 2`; out 
`~/Downloads/travel_gifs`. Pairs with verify_art's STRIDE metric (the number; this is the eye check). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:53:| `tools/art/ba_gifs.py` | 
(2026-08-26) BEFORE/AFTER review GIFs for changed sprites ΓÇö the visual-audit compiler: `--base <ref>` renders one 
side-by-side GIF per changed sprite (working tree vs the ref, same display height), auto-classified: strips LOOP side 
by side, statics get a labeled toggle (AFTER plays a matching `_anim` if one ships ΓÇö shows props that GAINED 
motion). 
`--names a,b,c` for explicit subjects; default out `~/Downloads/ba_gifs`. The 2026-08-25 fidelity pass was reviewed 
with it (111 GIFs). For change review, not color/tonemap judgement. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:54:| `tools/art/anim_sheet.py` | 
labeled per-clip contact sheets (8 facings ├ù frames, 1-based) ΓÇö the owner's QA format for animation review. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:55:| `game/shot_mobqa.gd` + 
`tools/art/mobqa_filmstrip.py` | in-engine mob clip QA (2026-08-15): the rig boots the real game, spawns a row of mobs 
(`-- --kinds a,b,c`), drives idle/walk/attack frame by frame through enemy.gd's own `_apply_strip` (body-cell 
reference, feet re-anchor, oversized action cells) and screenshots each; prints per strip the scale/offset + the feet 
row's screen y (must be identical across idle/walk/attack). The Python side crops the shots into one filmstrip PNG per 
mob. Catches what the sheets can't: a body that jumps on attack, a walk that vanishes (Vow Sentinel's blank walk), a 
clip at the wrong size. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:56:| 
`tools/art/build_mob_walk_repairs.py` | THE builder for the ImageGen mob repair masters in 
`art_src/Custom/MobWalkRepairs_2026-08-08/` (README there = per-mob history): green/magenta key + 2px edge despill, 
real-gutter splitting (`whole_subjects(n)`, per-row `four_grid_subjects_gutters`), scale/anchor normalizers 
(`normalize` bbox, `normalize_locked_motion` torso-locked, `normalize_attack` upper-body, `normalize_core_anchored` 
eroded-body for spiders/quadrupeds), refuses to write a transparent frame. Running `main()` re-installs EVERY strip 
ΓÇö 
to rebuild one, call its `install_*` with `SPRITES` pointed at a scratch dir, look, then copy. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:57:| `tools/art/dip_check.py` | 
flags a character whose weapon dips below its feet per direction (anchor trouble). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:58:| `tools/art/recenter_strip.py` | 
repairs square-cell strips whose figures were assembled OFF the frame grid (mob visibly slides side to side each 
loop): column-band segmentation reunites cross-boundary bleed, then each figure re-centres in its cell on the 
feet-band centroid. `--anchor head` for hovering casters whose hem sweeps (halo/hood band is the stable landmark; 
Morwen's Codex strips were quadrant-sliced from a 2x2 master, so frames 1/3 vs 2/4 sat 20-29px apart); `--grow` widens 
the square cell instead of clipping a wide cast. No `--apply` = audit only. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:59:| `tools/art/act1_brief_lib.py` | 
Act 1 boss Codex-regen brief library: per-boss identity (sprite/archetype) + a per-verb front-facing 4-frame 
storyboard library (~28 verbs: enrage/bolt/ring/summon/slam/beam/storm/lash/throw/blink/freeze/hymn/wail/rain/verdict/b
lade/pack/charge/melee/quench/piston/surface/toll/stab/shift/arc/splitΓÇª). `python act1_brief_lib.py <boss> <clip> 
<stage>` emits the brief + copies the boss's installed `_anim`/static as the identity reference. |
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:60:| 
`C:\Users\asali\Projects\CODEX_HEADLESS.md` (Projects root, all projects) + `tools/CODEX_HEADLESS.md` (MMO layer) | 
(2026-08-17) THE calling guide for headless Codex (`codex exec`). Root guide = machine-wide facts: binary resolution 
(the hashed install dir moves every app update), the canonical call (prompt on STDIN), what a run prints/returns 
(`-o`, `--json` event shapes, `--output-schema`, ~12.7k-token floor), the full `exec` flag table for the installed 
version, `resume/fork/review`, config defaults inherited, RAM/timing limits of batching on this shared box (serial + 
RAM-gated beside Godot), image_gen misfires, sandbox quirks. MMO layer = authorization/vet-install flow, the batch 
runner, brief generators/readers, prompt playbooks. Read both before writing any driver. |
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:61:| `tools/codex_bin.py` | 
(2026-08-17) resolve the installed `codex.exe` ΓÇö config.toml `CODEX_CLI_PATH` ΓåÆ newest 
`%LOCALAPPDATA%\OpenAI\Codex\bin\<hash>\codex.exe` ΓåÆ PATH. `python tools/codex_bin.py [--version]` prints it 
(`CODEX=$(python tools/codex_bin.py)`); importable `resolve()`. Never hard-code the hash. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:62:| `tools/art/batch_health.sh` | 
(2026-08-26) one-line health VERDICT for a running Codex batch: `OK-PROGRESSING` / `STALLED-GATE` / `DEAD-ORPHAN` / 
`DONE` from the runner log's CONTENT + newest-master age ΓÇö never poll the process table (an orphaned codex.exe reads 
as "still working" forever; a RAM-gated runner reads as alive while launching nothing). Doctrine: 
`tools/CODEX_HEADLESS.md` "Watching a running batch". |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:63:| `tools/art/run_codex_batch.ps1` 
| fire a batch of headless Codex image_gen jobs (one per staging dir: `codex_brief.txt` on stdin + `refs/*.png` as 
`-i`, `-o codex_result.md` + `codex_log.txt`) and wait for all, then print OK/FAIL per stage. Resolves the binary 
itself (2026-08-17; was a stale hard-coded hash). `-Stages "dir1,dir2,ΓÇª"`; `-MaxParallel N -MinFreeGB G` = the 
serial 
RAM-gated mode for the shared box (never parallel beside a Godot suite); `-TimeoutSec S` stops stragglers; `-ExtraArgs 
"ΓÇª"` appends flags (`-m`, `-c model_reasoning_effort=ΓÇª`, bypass). Defaults = legacy all-at-once. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:64:| 
`tools/art/build_act1_dirset.py` | assemble an 8-direction action set for one boss clip (owner: attacks need a 
facing). Builds S/N/E from generated masters (S falls back to the installed flat strip for a no-projectile swing), 
MIRRORS EΓåÆW, COPIES EΓåÆ{ne,se} and WΓåÆ{nw,sw}, each idle-aligned; writes `<sprite>_<clip>_<dir>.png` ├ù8 + a 
per-direction QA sheet; `--install` copies to game/. The named-action set lights up automatically via `Art.dir_set` 
ΓÇö 
no wiring. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:65:| `tools/art/qa_anim.py` | 
animation-consistency triage for any character (`<sprite>` or `--all`), catching what verify_art skips on ACTION 
clips: SLIDE (body shifts sideways as a weapon extends ΓÇö Korrag), SCALE (body renders smaller/larger than the idle 
ΓÇö 
Vargoth's blade), JUMP (rest body doesn't match the idle). Isolates the BODY by shape-matching the head band 
(weapon-independent) and measures SCALE at the action's ENGINE-proportional size (grown cells otherwise read ~0.72x 
small). `--montage out.png` renders flagged clips at TRUE ENGINE SCALE with a centre line ΓÇö the reliable confirm 
step. 
TRIAGE not a gate: over-flags back (n) views, floating/legless bodies (veyx), and heavy arms-up/effect poses 
(isolating a body from an arbitrary pose+weapon is genuinely hard) ΓÇö always confirm with the montage. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:66:| `tools/art/DRIFT_AUDIT.md` | 
(2026-08-24) THE visual generation-drift audit workflow ΓÇö the eyes-only net for what `verify_art`/`qa_anim` can't 
measure: face drift (fatter/blurrier/off-model), attire/equipment morphing (shield/cape/robe changing 
shape/colour/side), single-direction off-model art, garment violations, off-palette FX residue. Method = dedup-scope 
the unique dirs ΓåÆ fan out ONE review agent per subject over EVERY clip├ùdir├ùframe ΓåÆ orchestrator verifies each 
flag. 
Covers heroes/skins AND the boss/mob adaptation (enemy render path = CLIPSCALE not HEROBODY). Includes the known-drift 
catalogue + a reusable agent-prompt template + fix taxonomy (despill < frame-edit < single-strip regen < full-clip 
regen). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:67:| `tools/art/scan_drift.py` | 
pre-filter a directional wave for IDENTITY DRIFT by reading each job's own `codex_result.md` ("...a horned swordsman 
INSTEAD OF the hooded figure..."). Flags the ~5-10% that draw the wrong creature so a miss is one targeted re-roll, 
not a re-run. Over-flags minor rotation drift ΓåÆ still eyeball the flagged ones. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:68:| `tools/art/build_act1_boss.py` 
| build ALL of one Act-1 boss's strips from its generated 2x2 masters (idle `--self`, clips vs the built idle, preset 
`--anchor bbox --scale-ref area --valign hem`) + a combined `ALL_qa.png` contact sheet for review. Installs nothing. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:69:| 
`tools/art/install_act1_boss.py` | install a built Act-1 boss non-destructively (idleΓåÆ`<sprite>_anim_codex`, 
clipsΓåÆ`<sprite>_<clip>`) + archive masters/briefs/QA under `art_src/bosses_codex_wave1/<sprite>/`. Then wire 
art.gd/balance.gd by hand (4 dicts + BOSS_ACTION_FPS). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:70:| 
`tools/art/build_codex_2x2_strip.py` | Codex/ImageGen 2x2 animation master (TL,TR,BL,BR) ΓåÆ one square-cell 4-frame 
strip: cuts at the REAL gutters (widest empty band; midline when it qualifies) and refuses to slice a figure, 
normalizes body scale to a reference idle from ONE frame (`--scale-ref body|halo`, `--scale-frame N`; the old Morwen 
attack was drawn 15% smaller than her idle and shrank mid-cast), anchors every frame at the IDLE's own anchor position 
(`--anchor halo` = template-match the idle's crown/halo ΓÇö immune to motes or raised hands above the head; `hindfeet` 
= 
a quadruped's planted hind paws for howl/slam/roar/breath; `bbox` = body mass for a charge/pounce where every leg 
moves; `head`/`feet` bands otherwise), aligns hems (`--valign hem`), crowns (`top`) or master ROWS (`rows`: top row by 
f1's paws, bottom by f4's ΓÇö keeps an airborne f3 in the air) to the idle, `--floor-clip` flattens anything below the 
idle baseline (a mist pool would fool the engine's frame-0 feet line), cuts the vertical seam PER ROW (a lunge in one 
row closes the gutter the other row leaves open), grows the square cell (width OR height) when a frame would clip; 
writes a QA sheet with onion overlay. Stage first (`--out` in scratch), look, then install. Reproduce lines per boss: 
`art_src/bosses_codex_wave1/<boss>/README.md`. |
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:71:| `game/qa_skins.gd` | boots the 
real game once per class, equips every skin (base + awakened), screenshots. |
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:72:| `game/shot_gemcodex.gd` | 
(2026-08-15) boots the real game, equips S/A/B gear with every socket FILLED and shoots the inventory socket row + 
item panel (the "gems off-centre" repro), then walks EVERY codex section (legacy tab ids) and prints a density census 
(controls under the detail/page scroll + ledger row count + rows that fit) before screenshotting it, drives the boss 
route, and probes the interactions the suites don't (chip filter, search, row select, rail, bare reopen). Windowed ΓåÆ 
run with `--audio-driver Dummy`. Re-run after any codex layout change; numbers in 
`PROPOSALS/CODEX_LAYOUT/CODEX_LAYOUT.md`. |
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:73:| `game/dump_codex_data.gd` | 
(2026-08-15) headless `--script`: writes the codex's live tables (placed monsters/bosses with live stats + traits + 
mechanics, folk, terrains, curios, shapes, uniques, gems, achievements) as JSON plus the exact icons the codex draws 
(`codex_icons/`), so a layout mock can be built on real density and real sprites 
(`PROPOSALS/CODEX_LAYOUT/codex_mock.html` was). `-- <out.json>`. |
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:74:
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:75:## Art ΓÇö generate & install
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:76:
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:77:| tool | what it does |
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:78:|---|---|
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:79:| 
`tools/art/IMAGEGEN_SPRITE_PIPELINE.md` | zero-context, end-to-end built-in ImageGen playbook: lore/identity contract, 
prompts, timelines, source preservation, deterministic extraction, directional repairs, runtime wiring, visual QA, 
Godot tests, and mobile sync. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:80:| `tools/art/extract_sheet.py` | 
pre-keyed animation sheet ΓåÆ engine clip strips (the alpha-key/solidify/mirror/feet-anchor pipeline; see 
`tools/art/README.md`). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:81:| 
`tools/art/install_preservation_archer_walk_mirrors.py` | historical guarded Archer mirror installer; superseded by 
the owner's later direction-copy mapping. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:82:| 
`tools/art/install_preservation_owner_walk_copies.py` | exact owner-directed walk copies for Archer/Warlock plus the 
corrected seven-frame Warlock South install. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:83:| 
`tools/art/rebuild_preservation_archer_alpha.py` | rebuild Archer preservation idle/walk candidates with a 
border-connected key that preserves the green cape. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:84:| `tools/art/build_sprites.py` | 
rebuild every class sprite from source (per-class recipes codified), then re-import. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:85:| 
`tools/art/build_act1_directional_walks.py` | reviewed ImageGen sources -> grounded 8-direction Act 1 strips; prefers 
independent per-direction cycles when all eight exist, otherwise builds the reviewed 5x4 master. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:86:| 
`tools/art/enforce_act1_gait_alternation.py` | enforce and audit A/B lower-limb contact swaps while keeping generated 
upper bodies and low-hanging gear fixed. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:87:| `tools/art/build_fx_strip.py` | 
(2026-08-15) Codex/ImageGen FX contact sheet (chroma-keyed RGBA master, cols├ùrows) ΓåÆ one square-cell horizontal 
strip 
for `assets/sprites/fx/`. Finds cells per ROW by alpha column-bands assigned to the nearest nominal column (stray 
droplets rejoin their frame; a peak that bridges the gutter is split at the thinnest column), normalises the SET so 
the biggest frame's bbox = `--fill` of the cell, `--valign center|bottom|widest` (widest = anchor each frame's widest 
row ΓÇö a ground burst's ring equator = impact centre ΓÇö and print the sprite offset), `--despill magenta|green` 
(green 
also remaps yellow-green dust to tan), gamma 0.85, `--qa` dark+grey sheet. Built the poison cloud/splash/pool + 
meteor_impact + earth_slam strips; reproduce lines in `art_src/Custom/{PoisonMistFX,ImpactFX}_2026-08-15/README.md`. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:88:| `tools/art/build_gem_icons.py` 
| lore-authored 5├ù2 gem masters ΓåÆ 140 distinct 32px stat+level icons, hard-alpha QA sheet, desktop/mobile install. 
Cuts each gem as its own connected BLOB and re-centres it (2026-08-15: ImageGen ignores "equal spacing and centers" 
ΓÇö 
the old fixed-grid cut shipped off-centre/clipped icons with neighbour slivers; garnet even drew six gems on one row, 
see `ROW_DROPS`); `validate()` gates centring ┬▒1px + a clear 1px edge. FamilyΓåÆstat map must track `Items.GEM_STATS` 
keys (garnet became `hp_flat` 2026-08-07 and silently lost its art until this was caught). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:89:| 
`tools/art/build_gear_codex_icons.py` | approved transparent gear masters ΓåÆ 128px codex + separately optimized 32px 
gameplay candidates, exact 1,260-key coverage audit, dated backups and per-slot QA sheets. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:90:| `tools/art/upscale_hero.py` | 
rebuild a dark-class hero from the ChatGPT upscales (white-key, rescale, feet-anchor to original layout). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:91:| 
`tools/art/install_preservation_class_idle_walks.py` | guarded old-design Archer/Assassin/Warlock idle+walk candidate 
installer; validates 277px cells/180px bodies and archives the replaced runtime PNGs. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:92:| 
`tools/art/install_preservation_warrior_states.py` | (2026-08-16) guarded restore of the archived old-design Warrior 
`run`/`dash`/`ult`/`ultidle` (8 dirs + S alias) + flat `death` from 
`backup/warrior_base_pre_emberbound_heir_2026-07-31/` (manifest-verified) at the 180px frame-1 body ΓÇö the 37 
Emberbound Heir strips the 08-01 rollback missed (Berserk = ultΓåÆultidle/run, so the whole rage state showed the 
bare-arm Heir). ONE placement transform per strip (keeps the archived run bob + the ult's under-feet burst; per-frame 
bbox anchoring would hoist the body), a square cell per family (run 256 / dash 288 / ult 352 / ultidle 244 / death 288 
ΓÇö the engine grounds each strip from its own frame 1), hard alpha + 2px green-rim despill; audit + QA sheets/GIFs to 
`art_src/class_preservation_upscale_2026-08-01/warrior/states_old_normalized/`, `--apply` archives the replaced 
runtime under `.../runtime_pre_state_restore_2026-08-16/`. Sibling of `install_preservation_warrior_attacks.py`. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:93:| 
`tools/art/build_preservation_walk_candidate.py` | auto-detect authored figures from broad source gutters, refuse 
mismatched `--frames`, and build normalized preservation strips/QA. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:94:| 
`tools/art/correct_assassin_walk_hood_palette.py` | palette-only Assassin walk repair: rank-match generated f4-f6 
hood/scarf colors to accepted f1-f3 without changing silhouettes, alpha, weapons, or gait. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:95:| 
`tools/art/transplant_assassin_walk_hoods.py` | exact Assassin hood/face/scarf raster transplant from accepted f1-f3 
into generated f4-f6, aligned by the cyan eye while leaving the lower gait untouched. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:96:| 
`tools/art/pixellab_resize_assassin_east.py` | authorized candidate-only PixelLab Resize client for the untouched 
104px Assassin east walk; uses one union crop/source palette and targets a 180px body without touching runtime assets. 
|
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:97:| 
`tools/art/pixellab_add_assassin_second_dagger.py` | authorized candidate-only PixelLab animation edit for adding one 
coherent off-hand dagger across the phase-aligned resized Assassin east walk. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:98:| 
`tools/art/install_assassin_approved_east_walk_mapping.py` | install the approved two-dagger Assassin east walk to 
E/NE/SE and its framewise mirror to W/NW/SW, preserving north/south and archiving replaced strips. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:99:| 
`tools/art/install_assassin_idle_regen.py` | build/install the full-regeneration Assassin idle set from five generated 
masters plus exact W/SW/NW mirrors, with runtime backups and QA GIFs. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:100:| 
`tools/art/fix_warlock_ult_e_frames.py` | in-place repair of the warlock Hex (`ult` clip) EAST strip: frames 7-8 were 
authored facing WEST; rebuilds them from frame 6's east body + the authored beam/ring FX colour-masked, mirrored and 
re-seated on the hand (no regen). Backs up to `backup/warlock_ult_e_westframes_2026-08-16/` + before/after sheet; 
installs game/ + mobile/. The reusable pattern for "climax frames point the wrong way" (PIXELLAB_PROMPT_LESSONS rule 
10b). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:101:| 
`tools/art/build_pixellab_assassin_attack_review.py` | build candidate-only five-direction contact sheets/GIFs for the 
canonical PixelLab Assassin `attack` and `attack2` groups before resizing or installation. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:102:| 
`tools/art/pixellab_repair_assassin_attack_daggers.py` | authorized candidate-only PixelLab temporal edit for 
restoring exactly two short daggers in a selected one-to-four-frame Assassin attack segment. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:103:| 
`tools/art/install_char_anims.py` | PixelLab character download zip ΓåÆ installed 8-dir clip strips. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:104:| `tools/art/install_clip.py` | 
surgical per-clip strip installer (drift regens: replace ONE clip, touch nothing else). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:105:| 
`tools/art/install_row_strip.py` | (2026-08-27, gearlock lane) Codex ROW master (N figures on green, one image per 
clip so gear/face identity holds across frames) ΓåÆ engine strip: key+despill, tone-match to the OLD strip's palette, 
re-seat every frame to the old frame's body-height/feet/centroid (halla pattern), `--out` strips + `--mirror-out` 
flipped W-family (quadrant scheme), backups. Built for the paladin walk / archer idle coherence fixes 
(`art_src/gearlock_2026-08-27/`). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:106:| `tools/art/install_dirset.py` 
| assemble PixelLab per-direction exports into `<base>_<dir>.png` sets. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:107:| 
`tools/art/install_death_flat.py` | assemble a grounded single-facing death strip (the L/R-flip death convention). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:108:| `tools/art/install_ability.py` 
| add a boss's `<key>_ability` one-shot strip in the same format. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:109:| `tools/art/skin_install.py` | 
PixelLab 8 rotation stills ΓåÆ static skin sprite set. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:110:| 
`tools/art/install_env_asset.py` | environment art into the Track-D seams: ground tilesets, animated props 
(grid/square normalize + naming). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:111:| 
`tools/art/build_capital_water_anim.py` | generated 2├ù2 capital water storyboard + locked static landmark ΓåÆ 
geometry-stable horizontal animation strip (only blue/cyan water pixels may change). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:112:| 
`tools/art/build_capital_polish.py` | generated Crownfall furniture/hearth sources ΓåÆ normalized production props, 
plus 
integrated four-frame fire strips for every fire-bearing capital landmark (no nested flame decals). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:113:| 
`tools/art/build_terrain_prop_anims.py` | generated four-frame full-object terrain props ΓåÆ shared-crop, 
footprint-anchored static + `_anim` strips (fountains, furnaces, rifts, vents, conductor, sewer outfall; no motion 
stickers). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:114:| 
`tools/art/install_ground_field.py` + `floorgen_prompts.py` | (2026-08-17) crisp native-res FLOOR fields: 
`floorgen_prompts.py` writes palette-anchored (from `Art.GROUND`) Codex prompts for seamless tileable floors; 
`install_ground_field.py` downscales + pre-brightens (`--gamma`, counters the Forward+ sink) ΓåÆ 
`assets/sprites/ground_field_<kind>.png`, GPU-tiled by `game_world._apply_ground_field` (Polygon2D) at scale 1. 
Renderer: `Art.ground_field`/`ground_preview`. (2026-08-18) the stored tile edge is the FEATURE SCALE (1 texel = 1 
world px): `SIZE_BY_KIND` in the script pins stone/basalt/voidstone 256, crystalfloor 288, holystone 320, forest 400 
(else 512) so a cobble is a third-to-half of the 88px hero, not the hero's height. **`--prefix wall_field_`** installs 
the WALL twin: `wall_field_<kind>.png` (kind = `Terrains.wall_for` name, 128px seamless square) that 
`game_world._wall_dress`/`Art.wall_field` draw at 1:1 (with the face/shadow relief) instead of the 16px tile at 3x ΓÇö 
the 10 wall kinds shipped 2026-08-18 from a Codex batch (style ref = the matching floor field, colour ref = the old 
tile; edge crossfade for seams >10). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:115:| `tools/art/install_critter.py` 
| (2026-08-17) painterly Codex critter sheet ΓåÆ crisp pixel-art `critter_<kind>.png` animation strip: cuts N frames, 
aligns to a SHARED union bbox + uniform scale (per-frame trim would jitter the body), CENTER-anchored, posterize, 
`--alpha-cut` (low keeps thin bodies like a dragonfly abdomen). Ambience critters set hframes off it. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:116:| 
`tools/art/derive_prop_anim.py` | (2026-08-17) DERIVE a `<name>_anim.png` FROM a prop's existing static PNG (zero 
drift ΓÇö frame 0 = the static): `--motion pulse` (luminance-weighted glow breathe ΓÇö crystals/runes/molten), 
`flicker` 
(fire), `wave` (banner shear), `shimmer` (water). For GLOWING props only; mechanical props need real motion or stay 
static. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:117:| `tools/art/style_unify/` 
(README there) | (2026-08-18, `POLISH_TASKS.md`) the prop STYLE-UNIFY lane: `make_briefs.py` (repaint brief = 
painterly SIBLING as style ref + the old asset as SUBJECT), `rerolls.py` (description-led, no subject), 
`make_npc_briefs.py` (roster-benchmarked NPC bodies + the mill), `vet_sheet.py`/`green_check.py` (LOOK before 
install), `install_stage.py` (key ΓåÆ ΓêÆ14 % sat ΓåÆ `install_prop_hires`, game + mobile), `derive_stage.py` (rebuild 
`_anim` for animated statics at ┬º40c amps), `build_npcs.py` (Ivo 256┬▓ canvas, mill 384 override). 140+ props, 9 
NPCs, 
mill went through it (batches AΓÇôD). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:118:| 
`tools/art/audit_prop_anims.py` | (2026-08-18) the prop-strip GATE for CODING_GUIDELINES ┬º40b/c: per `_anim` strip 
ΓÇö 
frames, cell width (derived = static WxH, Codex = square), centroid drift, rigid-band OUTLINE drift in frame 0's row 
window (bottom 30 %, or the TOP band for hanging cloth), pulse % (glow Γëñ 12, `FIRE_PROPS` Γëñ 25, `ENERGY_PROPS` Γëñ 
18). 
`--fix`: a DERIVED strip (alpha identical across frames) is re-derived at `--amp`; an AUTHORED strip is re-anchored on 
its rigid band (scale + translate), band-locked if the outline still differs, silhouette-locked for `RIGID_MASK` ΓÇö 
never regenerated from its static. Exit 1 while anything is flagged. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:119:| 
`tools/art/install_prop_anim.py` | (2026-08-17) Codex 4-frame prop sheet ΓåÆ `<name>_anim.png` for PROCEDURAL props 
with 
no static (fires, fountains, storm array, chainrig swing): shared-bbox align, BOTTOM-anchored square cells, posterize. 
Anim replaces the procedural look. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:120:| 
`tools/art/build_terrain_art_fix.py` | `TERRAIN_ART_FIX_TASK.md` tier 1ΓÇô3 masters ΓåÆ 20 palette-controlled 
desktop/mobile replacements plus six registered full-object `_anim` strips. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:121:| 
`tools/art/build_material_icons.py` | generated crafting-material sources ΓåÆ 35 transparent 32x32 
Metal/Cloth/Bone/Reagent/Herb icons plus a labelled QA contact sheet. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:122:| 
`tools/art/build_capital_monumental.py` | generated Crownfall Crown Spire + connected city arcade sources ΓåÆ 
production 
architecture and an integrated four-frame gate-fire strip. |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:123:| `tools/art/clean_sprite.py` | 
FLUX/Pollinations render ΓåÆ clean pixel sprite (normalize). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:124:| `tools/art/polligen.py` / 
`tools/art/flux_draft.py` | free generation lanes (pollinations.ai textures/props / FLUX concept drafts ΓÇö note: HF 
inference is dead, see memory/ART docs). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:125:| `tools/art/pl_anim_ids.py` | 
print a PixelLab character's per-direction anim ids for a clip (frame-URL gotcha). |
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:126:| `tools/content/gen_capital.py` 
| regenerate `capital_hub.gd` (the 9-room, 3├ù3 Crownfall capital content module). |
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:127:| `gen_asset_manifest.py` | 
regenerate `game/assets/asset_manifest.json` (exports can't scan dirs; `export_all.bat` runs it). |
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:128:
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:129:## In-engine shot rigs 
(windowed, boot the real game, screenshot to disk)
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:130:
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:131:**Runner + base class 
(2026-08-15) ΓÇö use these for any new rig or run:**
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:132:
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:133:| tool | what it does |
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:147:SIMULATION-time offsets ΓÇö 
arrival / peak / lingering tail ΓÇö per class branch:
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:148:assassin poison wake+bloom, 
archer venom tumble, mage meteor (any theme),
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:149:warrior earth slams; `shot.bat 
fx_series --class=mage --theme=ice --terrain=keep`; `--ability=<slot>`
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:150:fires one slot generically 
(`--theme=none` = base kit), `--pin` freezes the dummy so an
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:151:impact lands ON it (layering 
proof), `--hazards` repaints the room through every
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:152:patch terrain and shoots the 
pools wide + close; prints paused/state/strip-frame
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:153:diagnostics per shot. Time by 
accumulated process
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:154:delta, never wall clock: the 
viewport readback in `shot()` costs ~0.1s, so
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:155:wall-clock offsets came due 
together and were shot in ONE frame) ┬╖
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:156:`shot_capgap` (2026-08-17, a 
`ShotRig`: capital PASSABILITY probe ΓÇö for every
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:157:Crownfall room with a north 
door, teleports the hero onto the door lane and
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:173:input path, road/magma/keep-wall 
walks) into `gif_<beat>/`; run with the runner's new
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:174:`--fixed-fps=30` so each frame 
is a deterministic 1/30 s; then
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:175:`python 
tools/art/gif_from_frames.py [--width 800] [--out ~/Downloads/crownless_polish_gifs]`
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:176:stitches 15 fps GIFs with one 
shared palette per GIF (owner review artefacts)) ┬╖
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:177:`tools/art/pixellab_resize_soft_c
lips.py` (2026-08-19, owner-authorized PixelLab:
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:178:upscales the OLD-generation hero 
action strips ΓÇö assassin/warlock/archer
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:179:run┬╖dash┬╖cast┬╖ult 8-dir sets 
+ 
the four flat deaths, 94 strips ΓÇö through
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:180:PixelLab /v2/resize exactly like 
`pixellab_resize_assassin_attack.py` did for
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:181:the crisp attack strips: per 
frame tight-crop ΓåÆ identity-prompt + runtime-
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:182:palette-locked redraw at the 180 
px (mage 202) body ΓåÆ paste back at original
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:183:coordinates in a 277 cell 
(auto-grows), baseline cell-22; `run` = spend
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:184:generations (resumable, frames 
cache), `install` = assemble + backup + write
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:185:game/+mobile/, `status` = 
progress) ┬╖
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:190:per-frame viewport readback 
drops the wall-clock rate to ~7 fps, fixed-fps makes
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:191:each frame exactly 1/30 s) into 
`user://shots/csdemo/<class>_<slot>/f###.png`;
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:192:then `python 
tools/art/build_csdemo.py [class ...]` ffmpeg-encodes them (crop to
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:193:the action, scale 640x396, 
libtheora q6, ~130-260 KB each) into
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:194:`game/assets/videos/csdemo_<class
>_<slot>.ogv` (+ mobile mirror) ΓÇö the class
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:195:selector's stage plays these 
full in-game takes (`menus._cs_play_demo`) instead
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:196:of the bare body clip). All live 
in
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:197:`game/`; run via `shot.bat 
<name>` (legacy ones too ΓÇö they just print no verdict
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:198:line) or by hand per their `.gd` 
docs.
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:199:
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:200:**ALWAYS run windowed rigs 
MUTED: `shot.bat` does it; by hand add `--audio-driver Dummy`**
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:218:| `export_all.bat` | rebuild 
Win/macOS/Linux into `executables\` (regenerates the asset manifest first). |
  C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:219:| `make_build.bat` | cut the 
friends co-op zip (Windows x86_64). |
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:220:- 
`art/pixellab_resize_assassin_attack.py` ΓÇö candidate-only PixelLab Resize pass for approved 8-frame Assassin attack 
directions; preserves original 212px motion coordinates while normalizing native-resolution output into 277px runtime 
cells.
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:221:- 
`art/build_assassin_stab_direction_set.py` ΓÇö assembles approved PixelLab Assassin Stab clips into a candidate-only 
8-direction set using the accepted side-copy/mirror policy and writes combined motion QA.
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:222:- 
`art/build_imagegen_attack_candidate.py` ΓÇö keys, losslessly slices, normalizes, and QA-renders one candidate-only 
ImageGen attack row without touching runtime sprites.
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:223:- 
`art/build_attack_regen_review.py` ΓÇö audits the 48 accepted Archer/Warlock/Warrior ImageGen attack strips and builds 
six consolidated all-direction review sheets/GIFs.
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:224:- `art/install_attack_regen.py` 
ΓÇö guarded dry-run/apply installer for the approved attack manifest; archives and atomically updates desktop/mobile 
runtime strips and South aliases.
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:225:- 
`art/stabilize_attack_anchors.py` ΓÇö reframe all approved attack candidates around a dense body-column X anchor in 
crop-safe 352px cells, eliminating weapon-padding side drift without resampling.
> C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\INDEX.md:226:- `art/alt_swing_pipeline.py` 
ΓÇö 
the melee swing-ALTERNATION lane (2026-08-16): `briefs <class>` stages Codex ImageGen briefs + refs for the alternate 
basic swing (`<class>_attackb_<dir>`), `build <class>` keys/slices/normalises/anchors the sources (component-based 
slicing ΓÇö a level sword's extent may interleave the neighbour's column, so it assigns islands to the nearest body 
instead of cutting on gutters; cell auto-grows to fit the arc), `assassin-assemble` lays out the PixelLab Resize 
outputs like the base stab (EΓåÆNE/SE, W side mirrored), `install <class> [--apply]` writes the 9 runtime files with a 
SHA-backed backup. `pixellab_resize_assassin_attack.py --runtime-cell N` grew out of it (a wide follow-through 
outgrows 277).



exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "python 
..\\..\\..\\tools\\art\\install_gait_row.py --help" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 865ms:
usage: install_gait_row.py [-h] --old OLD --row ROW [--out OUT]
                           [--mirror-out MIRROR_OUT] [--copy-out COPY_OUT]
                           [--frames FRAMES] [--gutters]
                           [--anchor {torso,centroid,bbox}] [--flip]
                           [--no-tone] [--no-orphans]
                           [--backup-dir BACKUP_DIR] [--dry] [--report]

Install a Codex gait ROW (N figures on flat #00ff00) as an engine walk strip
at the OUTGOING strip's geometry -- generic over heroes, mobs and bosses, and
frame-count agnostic (the row's N need not match the old strip's N; the engine
reads frames = width // height).

options:
  -h, --help            show this help message and exit
  --old OLD             outgoing strip (geometry + tone source)
  --row ROW             Codex row master on flat green
  --out OUT             strip destination(s)
  --mirror-out MIRROR_OUT
                        per-cell mirrored destination(s)
  --copy-out COPY_OUT   byte-copy destination(s)
  --frames FRAMES
  --gutters             slice at real gutters, not equal width
  --anchor {torso,centroid,bbox}
  --flip                mirror the row before install
  --no-tone
  --no-orphans
  --backup-dir BACKUP_DIR
  --dry
  --report

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "python -c \"from PIL import Image; from 
collections import Counter; import os; p=r'refs\\1_walk.png'; im=Image.open(p).convert('RGB'); 
c=Counter(im.getdata()); print(im.size); print(c.most_common(5))\"" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 666ms:
<string>:1: DeprecationWarning: Image.Image.getdata is deprecated and will be removed in Pillow 14 (2027-10-15). Use 
get_flattened_data instead.
(1804, 254)
[((0, 255, 0), 362647), ((222, 226, 213), 16862), ((2, 3, 2), 8509), ((135, 147, 154), 4463), ((101, 115, 124), 3716)]

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "python 
..\\..\\..\\tools\\art\\install_gait_row.py --old refs\\1_walk.png --row 'C:\\Users\\asali\\.codex\\generated_images\\0
1a0674d-4655-7b60-8aa1-58c8c943d7a8\\exec-9a83bb34-a105-46e4-b519-452cd6d876e8.png' --out normalized_candidate.png 
--frames 7 --anchor torso --no-tone --report" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 3098ms:
old 1_walk.png: cell 254 body 254 feet 253
  f0: body 254->253 px, feet 486->253, anchor x 153.2 -> cell centre
  f1: body 255->254 px, feet 487->253, anchor x 149.1 -> cell centre
  f2: body 255->254 px, feet 487->253, anchor x 149.0 -> cell centre
  f3: body 255->254 px, feet 487->253, anchor x 171.2 -> cell centre
  f4: body 255->254 px, feet 487->253, anchor x 170.5 -> cell centre
  f5: body 255->254 px, feet 487->253, anchor x 157.8 -> cell centre
  f6: body 256->254 px, feet 487->253, anchor x 125.9 -> cell centre
  row frames 7, scale 0.996, cell 254, body target 254, feet 253
wrote normalized_candidate.png (1778, 254)

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-ChildItem ..\\..\\..\\tools\\art -File 
| Where-Object { "'$_.Name -match '"'key|green|clean|strip' } | Select-Object -ExpandProperty Name" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 388ms:
audit_mob_strip_consistency.py
build_codex_2x2_strip.py
build_fx_strip.py
build_visual_cleanup.py
clean_sprite.py
despill_keyed.py
install_row_strip.py
mobqa_filmstrip.py
recenter_strip.py
revert_other_classes_to_old_strips.py
scan_key_rim.py

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "python 
..\\..\\..\\tools\\art\\despill_keyed.py --help" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "python 
..\\..\\..\\tools\\art\\clean_sprite.py --help" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "python 
..\\..\\..\\tools\\art\\install_row_strip.py --help" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 528ms:
 exited 1 in 860ms:
Traceback (most recent call last):
  File "C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\despill_keyed.py", line 45, in 
<module>
    despill(p)
    ~~~~~~~^^^
  File "C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\despill_keyed.py", line 21, in 
despill
    im = Image.open(path).convert("RGBA")
         ~~~~~~~~~~^^^^^^
  File "C:\Users\asali\AppData\Roaming\Python\Python314\site-packages\PIL\Image.py", line 3639, in open
    fp = builtins.open(filename, "rb")
FileNotFoundError: [Errno 2] No such file or directory: '--help'

 succeeded in 868ms:
usage: install_row_strip.py [-h] --row ROW --old OLD --frames FRAMES
                            [--out OUT] [--mirror-out MIRROR_OUT]
                            [--backup-dir BACKUP_DIR]
                            [--seat {perframe,uniform}]

Install a Codex row master as an engine strip, seated to the old strip's

options:
  -h, --help            show this help message and exit
  --row ROW             Codex row master (N figures on green)
  --old OLD             current installed strip (geometry + palette authority)
  --frames FRAMES
  --out OUT             strip destination (repeatable)
  --mirror-out MIRROR_OUT
                        destination for the horizontally flipped strip
                        (repeatable)
  --backup-dir BACKUP_DIR
  --seat {perframe,uniform}
                        perframe: match each frame to the old frame's geometry
                        (faithful re-draws of the same motion). uniform: one
                        scale + baseline from old frame 1, feet-center
                        anchored (re-choreographed clips whose poses no longer
                        correspond to the old frames)

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "python 
..\\..\\..\\tools\\art\\install_row_strip.py --row 'C:\\Users\\asali\\.codex\\generated_images\\01a0674d-4655-7b60-8aa1
-58c8c943d7a8\\exec-9a83bb34-a105-46e4-b519-452cd6d876e8.png' --old refs\\1_walk.png --frames 7 --out 
normalized_candidate2.png --seat perframe" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 2016ms:
note: old strip 1804x254 has 7 frames; installing 7
  f1: body 254px (old 254), feet y 253 (old 253), cx 126 (old 126)
  f2: body 254px (old 254), feet y 253 (old 253), cx 127 (old 126)
  f3: body 254px (old 254), feet y 253 (old 253), cx 126 (old 126)
  f4: body 254px (old 254), feet y 253 (old 253), cx 126 (old 126)
  f5: body 246px (old 254), feet y 253 (old 253), cx 128 (old 126)
  f6: body 254px (old 254), feet y 253 (old 253), cx 127 (old 126)
  f7: body 254px (old 254), feet y 253 (old 253), cx 126 (old 126)
wrote normalized_candidate2.png

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Copy-Item -LiteralPath 'refs\\1_walk.png' 
-Destination 'source_transparent.png'; python ..\\..\\..\\tools\\art\\despill_keyed.py source_transparent.png" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 982ms:
  source_transparent.png: green 362647 -> 0

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "python 
..\\..\\..\\tools\\art\\install_row_strip.py --row 'C:\\Users\\asali\\.codex\\generated_images\\01a0674d-4655-7b60-8aa1
-58c8c943d7a8\\exec-9a83bb34-a105-46e4-b519-452cd6d876e8.png' --old source_transparent.png --frames 7 --out 
normalized_candidate3.png --seat perframe" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 2010ms:
note: old strip 1804x254 has 7 frames; installing 7
  f1: body 254px (old 254), feet y 253 (old 253), cx 126 (old 126)
  f2: body 254px (old 254), feet y 253 (old 253), cx 127 (old 126)
  f3: body 254px (old 254), feet y 253 (old 253), cx 126 (old 126)
  f4: body 254px (old 254), feet y 253 (old 253), cx 126 (old 126)
  f5: body 246px (old 254), feet y 253 (old 253), cx 128 (old 126)
  f6: body 254px (old 254), feet y 253 (old 253), cx 127 (old 126)
  f7: body 254px (old 254), feet y 253 (old 253), cx 126 (old 126)
wrote normalized_candidate3.png

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "python -c \"from PIL import Image; 
im=Image.open('source_transparent.png').convert('RGBA'); print(im.getbbox()); print(im.getchannel('A').getbbox()); 
print(im.size); print([im.getpixel((0,y)) for y in (0,10,100,253)])\"" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 527ms:
(0, 0, 1804, 254)
(0, 0, 1804, 254)
(1804, 254)
[(0, 0, 0, 255), (0, 0, 0, 255), (0, 0, 0, 255), (0, 0, 0, 255)]

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw 
..\\..\\..\\tools\\art\\despill_keyed.py; Get-Content ..\\..\\..\\tools\\art\\install_row_strip.py | Select-Object 
-First 140" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 333ms:
#!/usr/bin/env python
"""Despill the dark/neon-green chroma rim that remove_chroma_key leaves on some
Codex masters, IN PLACE, before build_act1_dirset / build_codex_2x2_strip slice
them (those trust an already-keyed master and don't re-despill; a LANCZOS resize
would only spread the fringe). CLAUDE.md rule: clamp any visible pixel whose
green dominates toward max(R,B), and drop the faint green edge crumbs.

  python tools/art/despill_keyed.py <keyed.png> [<keyed.png> ...]

Reports the green-dominant pixel count before/after per file.
"""
import sys
import numpy as np
from PIL import Image

THR = 18          # green-dominance over max(r,b) to treat as spill
EDGE_A = 40       # alpha at/below this on a green-dominant pixel -> cut


def despill(path: str) -> None:
    im = Image.open(path).convert("RGBA")
    a = np.asarray(im).copy()
    r = a[..., 0].astype(np.int16)
    g = a[..., 1].astype(np.int16)
    b = a[..., 2].astype(np.int16)
    al = a[..., 3]
    dom = g - np.maximum(r, b)
    before = int(((dom > THR) & (al > 0)).sum())
    # clamp green on visible pixels
    spill = (dom > THR) & (al > 0)
    a[..., 1][spill] = np.maximum(r, b)[spill].astype(np.uint8)
    # cut faint green edge crumbs entirely
    crumbs = (dom > THR) & (al > 0) & (al <= EDGE_A)
    a[..., 3][crumbs] = 0
    im2 = Image.fromarray(a, "RGBA")
    a2 = np.asarray(im2)
    dom2 = a2[..., 1].astype(np.int16) - np.maximum(a2[..., 0], a2[..., 2]).astype(np.int16)
    after = int(((dom2 > THR) & (a2[..., 3] > 0)).sum())
    im2.save(path)
    print(f"  {path.split(chr(92))[-1]}: green {before} -> {after}")


if __name__ == "__main__":
    for p in sys.argv[1:]:
        despill(p)

#!/usr/bin/env python3
"""Install a Codex row master as an engine strip, seated to the old strip's
per-frame geometry.

The gearlock lane (2026-08-27, paladin walk / archer idle coherence fixes)
generates ONE image per clip: a horizontal row of N figures on flat #00ff00
green (single-image generation keeps gear/face identity across frames far
better than per-frame re-rolls -- the per-frame archer face restore drifted
into four different outfits). This tool turns that row into a drop-in strip:

  1. slice the row into N equal cells
  2. chroma-key the green + despill the 2px rim (G > max(R,B)+20 edge band)
  3. tone-match the new content to the OLD strip's palette (per-channel
     mean/std over solid pixels -- generated masters come back brighter than
     the installed, in-game-judged art)
  4. re-seat each frame to the OLD strip's frame-i content geometry: body
     bbox height, feet baseline, centroid-x (the halla re-seat pattern; keeps
     HEROBODY / anchors / scale-locks exactly as the accepted strip)
  5. write the strip to every --out; write the horizontally flipped strip to
     every --mirror-out (W-family = mirror of E, the quadrant scheme)

Old strips are backed up into --backup-dir before overwrite. After install:
run --import, then tools/art/verify_art.py <base>.

Usage:
  python tools/art/install_row_strip.py --row <master.png> --old <strip.png> \
      --frames 8 --out <a.png> [--out <b.png>] [--mirror-out <c.png>] \
      [--backup-dir <dir>]
"""
from __future__ import annotations

import argparse
import shutil
from pathlib import Path

import numpy as np
from PIL import Image

A_SOLID = 8


def key_green(arr: np.ndarray) -> np.ndarray:
    """Flat #00ff00-family background -> alpha 0, then despill the rim."""
    r = arr[..., 0].astype(np.int32)
    g = arr[..., 1].astype(np.int32)
    b = arr[..., 2].astype(np.int32)
    bg = (g >= 160) & (r <= 130) & (b <= 130) & (g > r + 60) & (g > b + 60)
    out = arr.copy()
    out[..., 3] = np.where(bg, 0, out[..., 3])
    # despill: green-dominant pixels within 2px of transparency get G clamped
    alpha = out[..., 3] > A_SOLID
    edge = alpha & ~_erode(alpha, 2)
    spill = edge & (g > np.maximum(r, b) + 20)
    out[..., 1] = np.where(spill, np.maximum(r, b).astype(arr.dtype), out[..., 1])
    return out


def _erode(mask: np.ndarray, iterations: int) -> np.ndarray:
    out = mask
    for _ in range(iterations):
        p = np.pad(out, 1, constant_values=False)
        out = p[1:-1, 1:-1] & p[:-2, 1:-1] & p[2:, 1:-1] & p[1:-1, :-2] & p[1:-1, 2:]
    return out


def tone_match(new: np.ndarray, old: np.ndarray) -> np.ndarray:
    """Per-channel mean/std match of new solid content to old solid content."""
    ns = new[..., 3] > 128
    os_ = old[..., 3] > 128
    if ns.sum() < 100 or os_.sum() < 100:
        return new
    out = new.astype(np.float64)
    for c in range(3):
        nv = out[..., c][ns]
        ov = old[..., c][os_].astype(np.float64)
        n_std = max(nv.std(), 1e-6)
        out[..., c][ns] = (nv - nv.mean()) * (ov.std() / n_std) + ov.mean()
    return np.clip(out, 0, 255).astype(np.uint8)


def _metrics(frame: np.ndarray) -> dict | None:
    a = frame[..., 3]
    ys, xs = np.where(a > A_SOLID)
    if not len(ys):
        return None
    return {
        "x0": int(xs.min()), "x1": int(xs.max()),
        "y0": int(ys.min()), "y1": int(ys.max()),
        "cx": float(xs.mean()),
        "bh": int(ys.max() - ys.min() + 1),
        "bw": int(xs.max() - xs.min() + 1),
    }


def seat(new_frame: np.ndarray, old_m: dict, cell: int) -> np.ndarray:
    """Scale new content to the old frame's bbox height, feet on the old
    baseline, centroid-x on the old centroid."""
    m = _metrics(new_frame)
    canvas = np.zeros((cell, cell, 4), dtype=np.uint8)
    if m is None:
        return canvas
    crop = new_frame[m["y0"]:m["y1"] + 1, m["x0"]:m["x1"] + 1]
    scale = old_m["bh"] / m["bh"]
    tw = max(1, round(m["bw"] * scale))
    th = max(1, round(m["bh"] * scale))
    if tw > cell:  # never wider than the cell
        s2 = cell / m["bw"]
        tw, th = cell, max(1, round(m["bh"] * s2))
    img = Image.fromarray(crop).resize((tw, th), Image.LANCZOS)
    scaled = np.asarray(img)
    sm = _metrics(scaled)
    if sm is None:
        return canvas
    # target: feet on old y1, centroid-x on old cx
    y_off = old_m["y1"] - sm["y1"]
    x_off = round(old_m["cx"] - sm["cx"])
    x0, y0 = x_off, y_off
    sx0, sy0 = max(0, -x0), max(0, -y0)
    dx0, dy0 = max(0, x0), max(0, y0)
    hh = min(scaled.shape[0] - sy0, cell - dy0)
    ww = min(scaled.shape[1] - sx0, cell - dx0)
    if hh > 0 and ww > 0:
        canvas[dy0:dy0 + hh, dx0:dx0 + ww] = scaled[sy0:sy0 + hh, sx0:sx0 + ww]
    return canvas


def despeck(frame: np.ndarray, min_px: int = 40) -> np.ndarray:
    """Clear tiny detached components (keying residue slivers) from one cell.
    The main figure is the largest component; anything else below min_px is
    noise from the chroma key, not content."""
    mask = frame[..., 3] > A_SOLID
    if not mask.any():
        return frame
    h, w = mask.shape
    lab = np.zeros((h, w), dtype=np.int32)
    nxt = 0
    sizes: dict[int, int] = {}
    stack: list[tuple[int, int]] = []
    for sy in range(h):
        for sx in range(w):

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "python -c \"import sys; 
sys.path.insert(0,r'..\\..\\..\\tools\\art'); import numpy as np; from PIL import Image; from install_row_strip import 
key_green; p='source_transparent.png'; a=np.asarray(Image.open(r'refs\\1_walk.png').convert('RGBA')); 
Image.fromarray(key_green(a),'RGBA').save(p)\"; python -c \"from PIL import Image; 
im=Image.open('source_transparent.png'); print(im.getchannel('A').getbbox())\"; python 
..\\..\\..\\tools\\art\\install_row_strip.py --row 'C:\\Users\\asali\\.codex\\generated_images\\01a0674d-4655-7b60-8aa1
-58c8c943d7a8\\exec-9a83bb34-a105-46e4-b519-452cd6d876e8.png' --old source_transparent.png --frames 7 --out 
normalized_candidate4.png --seat perframe" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 2867ms:
(57, 26, 1729, 228)
note: old strip 1804x254 has 7 frames; installing 7
  f1: body 201px (old 201), feet y 227 (old 227), cx 132 (old 131)
  f2: body 201px (old 201), feet y 227 (old 227), cx 130 (old 130)
  f3: body 201px (old 201), feet y 227 (old 227), cx 131 (old 130)
  f4: body 202px (old 202), feet y 227 (old 227), cx 132 (old 132)
  f5: body 201px (old 201), feet y 227 (old 227), cx 136 (old 136)
  f6: body 201px (old 201), feet y 227 (old 227), cx 139 (old 138)
  f7: body 201px (old 201), feet y 227 (old 227), cx 139 (old 139)
wrote normalized_candidate4.png

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "python -c \"from PIL import Image; 
im=Image.open(r'refs\\1_walk.png'); out=Image.new('RGB',(1804,508),(0,255,0)); 
out.paste(im.resize((1804,508),Image.Resampling.NEAREST)); out.save('ref_zoom.png')\"" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 564ms:
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "python -c \"import sys; 
sys.path.insert(0,r'..\\..\\..\\tools\\art'); import numpy as np; from PIL import Image; from install_row_strip import 
key_green,_metrics; p=r'C:\\Users\\asali\\.codex\\generated_images\\01a0674d-4655-7b60-8aa1-58c8c943d7a8\\exec-9a83bb34
-a105-46e4-b519-452cd6d876e8.png'; a=key_green(np.asarray(Image.open(p).convert('RGBA'))); print(a.shape); 
w=a.shape[1];
for i in range(7):
 x0=round(i*w/7); x1=round((i+1)*w/7); print(i+1,x0,x1,_metrics(a[:,x0:x1]))\"" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 1001ms:
(769, 2046, 4)
1 0 292 {'x0': 61, 'x1': 238, 'y0': 235, 'y1': 488, 'cx': 155.47479478943612, 'bh': 254, 'bw': 178}
2 292 585 {'x0': 42, 'x1': 240, 'y0': 234, 'y1': 489, 'cx': 141.505778052223, 'bh': 256, 'bw': 199}
3 585 877 {'x0': 33, 'x1': 259, 'y0': 235, 'y1': 489, 'cx': 136.5485843811479, 'bh': 255, 'bw': 227}
4 877 1169 {'x0': 53, 'x1': 291, 'y0': 234, 'y1': 489, 'cx': 157.21369524458837, 'bh': 256, 'bw': 239}
5 1169 1461 {'x0': 0, 'x1': 261, 'y0': 235, 'y1': 489, 'cx': 161.6846590909091, 'bh': 255, 'bw': 262}
6 1461 1754 {'x0': 62, 'x1': 236, 'y0': 234, 'y1': 489, 'cx': 157.34694611855716, 'bh': 256, 'bw': 175}
7 1754 2046 {'x0': 28, 'x1': 208, 'y0': 234, 'y1': 489, 'cx': 124.48030111792282, 'bh': 256, 'bw': 181}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "python -c \"import sys; 
sys.path.insert(0,r'..\\..\\..\\tools\\art'); import numpy as np; from PIL import Image; from install_row_strip import 
key_green; from scipy.ndimage import label,find_objects; p=r'C:\\Users\\asali\\.codex\\generated_images\\01a0674d-4655-
7b60-8aa1-58c8c943d7a8\\exec-9a83bb34-a105-46e4-b519-452cd6d876e8.png'; 
a=key_green(np.asarray(Image.open(p).convert('RGBA'))); lab,n=label(a[...,3]>8); comps=[];
for k,s in enumerate(find_objects(lab),1):
 z=(lab[s]==k).sum();
 if z>500: comps.append((z,(s[1].start,s[0].start,s[1].stop-1,s[0].stop-1)))
print(sorted(comps,reverse=True)[:12])\"" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 2122ms:
[(np.int64(23586), (930, 234, 1177, 489)), (np.int64(23382), (618, 235, 844, 489)), (np.int64(22762), (1231, 235, 
1430, 489)), (np.int64(22672), (334, 234, 532, 489)), (np.int64(22416), (61, 235, 238, 488)), (np.int64(22234), (1523, 
234, 1697, 489)), (np.int64(22184), (1782, 234, 1962, 489))]

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "python 
..\\..\\..\\tools\\art\\install_gait_row.py --old source_transparent.png --row 'C:\\Users\\asali\\.codex\\generated_ima
ges\\01a0674d-4655-7b60-8aa1-58c8c943d7a8\\exec-9a83bb34-a105-46e4-b519-452cd6d876e8.png' --out normalized_gutters.png 
--frames 7 --gutters --anchor torso --no-tone --report" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 2543ms:
old source_transparent.png: cell 254 body 201 feet 227
  f0: body 254->200 px, feet 385->227, anchor x 121.0 -> cell centre
  f1: body 255->201 px, feet 385->227, anchor x 122.8 -> cell centre
  f2: body 255->201 px, feet 385->227, anchor x 125.3 -> cell centre
  f3: body 255->201 px, feet 385->227, anchor x 128.9 -> cell centre
  f4: body 255->201 px, feet 385->227, anchor x 106.2 -> cell centre
  f5: body 255->201 px, feet 385->227, anchor x 111.9 -> cell centre
  f6: body 256->201 px, feet 385->227, anchor x 109.7 -> cell centre
  row frames 7, scale 0.788, cell 254, body target 201, feet 227
wrote normalized_gutters.png (1778, 254)

apply patch
patch: completed
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e\co
mpose_tmp.py
diff --git a/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py 
b/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py
new file mode 100644
index 0000000000000000000000000000000000000000..435b83caeb3678817f88014d7a97c0577b552b03
--- /dev/null
+++ b/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py
@@ -0,0 +1,60 @@
+from pathlib import Path
+
+from PIL import Image, ImageDraw
+
+
+ROOT = Path(__file__).resolve().parent
+SOURCE = ROOT / "refs" / "1_walk.png"
+DONOR = ROOT / "normalized_gutters.png"
+OUTPUT = ROOT / "mage_attack_walk_e_row.png"
+
+
+base = Image.open(SOURCE).convert("RGB")
+donor = Image.open(DONOR).convert("RGBA")
+out = base.copy()
+
+frames = 7
+source_w, source_h = base.size
+donor_cell = donor.width // frames
+
+# Frames 1 and 7 remain the exact source rest pose.  In frames 2-6, replace
+# only the free-arm quadrant.  The source's lower body (y >= 159) is never
+# touched, preserving every original hem and boot pixel.
+for frame in range(1, 6):
+    dst_left = round(frame * source_w / frames)
+    dst_right = round((frame + 1) * source_w / frames)
+    dst_pad = (dst_right - dst_left - donor_cell) // 2
+
+    # Remove the source's relaxed free sleeve/hand without opening the robe.
+    erase = Image.new("L", out.size, 0)
+    d = ImageDraw.Draw(erase)
+    x = dst_left + dst_pad
+    d.polygon(
+        [
+            (x + 139, 84),
+            (x + 157, 86),
+            (x + 169, 103),
+            (x + 181, 119),
+            (x + 181, 154),
+            (x + 158, 158),
+            (x + 149, 137),
+            (x + 146, 113),
+            (x + 138, 99),
+        ],
+        fill=255,
+    )
+    green = Image.new("RGB", out.size, (0, 255, 0))
+    out.paste(green, (0, 0), erase)
+
+    cell = donor.crop((frame * donor_cell, 0, (frame + 1) * donor_cell, source_h))
+    # Isolate the newly animated free arm, including only enough shoulder and
+    # side-torso pixels to make a clean sleeve join.
+    arm_gate = Image.new("L", cell.size, 0)
+    ImageDraw.Draw(arm_gate).rectangle((136, 70, donor_cell - 1, 158), fill=255)
+    arm_alpha = Image.composite(cell.getchannel("A"), Image.new("L", cell.size, 0), arm_gate)
+    arm = cell.copy()
+    arm.putalpha(arm_alpha)
+    out.paste(arm.convert("RGB"), (x, 0), arm_alpha)
+
+out.save(OUTPUT)
+print(OUTPUT)

diff --git a/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py 
b/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py
new file mode 100644
index 0000000000000000000000000000000000000000..435b83caeb3678817f88014d7a97c0577b552b03
--- /dev/null
+++ b/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py
@@ -0,0 +1,60 @@
+from pathlib import Path
+
+from PIL import Image, ImageDraw
+
+
+ROOT = Path(__file__).resolve().parent
+SOURCE = ROOT / "refs" / "1_walk.png"
+DONOR = ROOT / "normalized_gutters.png"
+OUTPUT = ROOT / "mage_attack_walk_e_row.png"
+
+
+base = Image.open(SOURCE).convert("RGB")
+donor = Image.open(DONOR).convert("RGBA")
+out = base.copy()
+
+frames = 7
+source_w, source_h = base.size
+donor_cell = donor.width // frames
+
+# Frames 1 and 7 remain the exact source rest pose.  In frames 2-6, replace
+# only the free-arm quadrant.  The source's lower body (y >= 159) is never
+# touched, preserving every original hem and boot pixel.
+for frame in range(1, 6):
+    dst_left = round(frame * source_w / frames)
+    dst_right = round((frame + 1) * source_w / frames)
+    dst_pad = (dst_right - dst_left - donor_cell) // 2
+
+    # Remove the source's relaxed free sleeve/hand without opening the robe.
+    erase = Image.new("L", out.size, 0)
+    d = ImageDraw.Draw(erase)
+    x = dst_left + dst_pad
+    d.polygon(
+        [
+            (x + 139, 84),
+            (x + 157, 86),
+            (x + 169, 103),
+            (x + 181, 119),
+            (x + 181, 154),
+            (x + 158, 158),
+            (x + 149, 137),
+            (x + 146, 113),
+            (x + 138, 99),
+        ],
+        fill=255,
+    )
+    green = Image.new("RGB", out.size, (0, 255, 0))
+    out.paste(green, (0, 0), erase)
+
+    cell = donor.crop((frame * donor_cell, 0, (frame + 1) * donor_cell, source_h))
+    # Isolate the newly animated free arm, including only enough shoulder and
+    # side-torso pixels to make a clean sleeve join.
+    arm_gate = Image.new("L", cell.size, 0)
+    ImageDraw.Draw(arm_gate).rectangle((136, 70, donor_cell - 1, 158), fill=255)
+    arm_alpha = Image.composite(cell.getchannel("A"), Image.new("L", cell.size, 0), arm_gate)
+    arm = cell.copy()
+    arm.putalpha(arm_alpha)
+    out.paste(arm.convert("RGB"), (x, 0), arm_alpha)
+
+out.save(OUTPUT)
+print(OUTPUT)

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'python compose_tmp.py' in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 651ms:
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e\ma
ge_attack_walk_e_row.png

diff --git a/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py 
b/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py
new file mode 100644
index 0000000000000000000000000000000000000000..435b83caeb3678817f88014d7a97c0577b552b03
--- /dev/null
+++ b/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py
@@ -0,0 +1,60 @@
+from pathlib import Path
+
+from PIL import Image, ImageDraw
+
+
+ROOT = Path(__file__).resolve().parent
+SOURCE = ROOT / "refs" / "1_walk.png"
+DONOR = ROOT / "normalized_gutters.png"
+OUTPUT = ROOT / "mage_attack_walk_e_row.png"
+
+
+base = Image.open(SOURCE).convert("RGB")
+donor = Image.open(DONOR).convert("RGBA")
+out = base.copy()
+
+frames = 7
+source_w, source_h = base.size
+donor_cell = donor.width // frames
+
+# Frames 1 and 7 remain the exact source rest pose.  In frames 2-6, replace
+# only the free-arm quadrant.  The source's lower body (y >= 159) is never
+# touched, preserving every original hem and boot pixel.
+for frame in range(1, 6):
+    dst_left = round(frame * source_w / frames)
+    dst_right = round((frame + 1) * source_w / frames)
+    dst_pad = (dst_right - dst_left - donor_cell) // 2
+
+    # Remove the source's relaxed free sleeve/hand without opening the robe.
+    erase = Image.new("L", out.size, 0)
+    d = ImageDraw.Draw(erase)
+    x = dst_left + dst_pad
+    d.polygon(
+        [
+            (x + 139, 84),
+            (x + 157, 86),
+            (x + 169, 103),
+            (x + 181, 119),
+            (x + 181, 154),
+            (x + 158, 158),
+            (x + 149, 137),
+            (x + 146, 113),
+            (x + 138, 99),
+        ],
+        fill=255,
+    )
+    green = Image.new("RGB", out.size, (0, 255, 0))
+    out.paste(green, (0, 0), erase)
+
+    cell = donor.crop((frame * donor_cell, 0, (frame + 1) * donor_cell, source_h))
+    # Isolate the newly animated free arm, including only enough shoulder and
+    # side-torso pixels to make a clean sleeve join.
+    arm_gate = Image.new("L", cell.size, 0)
+    ImageDraw.Draw(arm_gate).rectangle((136, 70, donor_cell - 1, 158), fill=255)
+    arm_alpha = Image.composite(cell.getchannel("A"), Image.new("L", cell.size, 0), arm_gate)
+    arm = cell.copy()
+    arm.putalpha(arm_alpha)
+    out.paste(arm.convert("RGB"), (x, 0), arm_alpha)
+
+out.save(OUTPUT)
+print(OUTPUT)

diff --git a/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py 
b/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py
new file mode 100644
index 0000000000000000000000000000000000000000..435b83caeb3678817f88014d7a97c0577b552b03
--- /dev/null
+++ b/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py
@@ -0,0 +1,60 @@
+from pathlib import Path
+
+from PIL import Image, ImageDraw
+
+
+ROOT = Path(__file__).resolve().parent
+SOURCE = ROOT / "refs" / "1_walk.png"
+DONOR = ROOT / "normalized_gutters.png"
+OUTPUT = ROOT / "mage_attack_walk_e_row.png"
+
+
+base = Image.open(SOURCE).convert("RGB")
+donor = Image.open(DONOR).convert("RGBA")
+out = base.copy()
+
+frames = 7
+source_w, source_h = base.size
+donor_cell = donor.width // frames
+
+# Frames 1 and 7 remain the exact source rest pose.  In frames 2-6, replace
+# only the free-arm quadrant.  The source's lower body (y >= 159) is never
+# touched, preserving every original hem and boot pixel.
+for frame in range(1, 6):
+    dst_left = round(frame * source_w / frames)
+    dst_right = round((frame + 1) * source_w / frames)
+    dst_pad = (dst_right - dst_left - donor_cell) // 2
+
+    # Remove the source's relaxed free sleeve/hand without opening the robe.
+    erase = Image.new("L", out.size, 0)
+    d = ImageDraw.Draw(erase)
+    x = dst_left + dst_pad
+    d.polygon(
+        [
+            (x + 139, 84),
+            (x + 157, 86),
+            (x + 169, 103),
+            (x + 181, 119),
+            (x + 181, 154),
+            (x + 158, 158),
+            (x + 149, 137),
+            (x + 146, 113),
+            (x + 138, 99),
+        ],
+        fill=255,
+    )
+    green = Image.new("RGB", out.size, (0, 255, 0))
+    out.paste(green, (0, 0), erase)
+
+    cell = donor.crop((frame * donor_cell, 0, (frame + 1) * donor_cell, source_h))
+    # Isolate the newly animated free arm, including only enough shoulder and
+    # side-torso pixels to make a clean sleeve join.
+    arm_gate = Image.new("L", cell.size, 0)
+    ImageDraw.Draw(arm_gate).rectangle((136, 70, donor_cell - 1, 158), fill=255)
+    arm_alpha = Image.composite(cell.getchannel("A"), Image.new("L", cell.size, 0), arm_gate)
+    arm = cell.copy()
+    arm.putalpha(arm_alpha)
+    out.paste(arm.convert("RGB"), (x, 0), arm_alpha)
+
+out.save(OUTPUT)
+print(OUTPUT)

codex
The composited strip now retains the original walk pixels from the waist down, including the full boot alternation and 
closed hem, while the attack hand is isolated to the upper-right quadrant. IΓÇÖm doing the final file checks now: 
exact chroma value, seven figures, unchanged ground line/lower body, and no detached attack effect.
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "python -c \"from PIL import 
Image,ImageDraw; im=Image.open(r'refs\\1_walk.png'); w,h=im.size; cells=[]
for i in range(7):
 a=round(i*w/7); b=round((i+1)*w/7); c=im.crop((a,0,b,h)).resize(((b-a)*3,h*3),Image.Resampling.NEAREST); 
cells.append(c)
out=Image.new('RGB',(258*3*4,254*3*2),(0,255,0));
for i,c in enumerate(cells): out.paste(c,((i%4)*258*3,(i//4)*254*3))
out.save('feet_review.png')\"" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 685ms:
diff --git a/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py 
b/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py
new file mode 100644
index 0000000000000000000000000000000000000000..435b83caeb3678817f88014d7a97c0577b552b03
--- /dev/null
+++ b/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py
@@ -0,0 +1,60 @@
+from pathlib import Path
+
+from PIL import Image, ImageDraw
+
+
+ROOT = Path(__file__).resolve().parent
+SOURCE = ROOT / "refs" / "1_walk.png"
+DONOR = ROOT / "normalized_gutters.png"
+OUTPUT = ROOT / "mage_attack_walk_e_row.png"
+
+
+base = Image.open(SOURCE).convert("RGB")
+donor = Image.open(DONOR).convert("RGBA")
+out = base.copy()
+
+frames = 7
+source_w, source_h = base.size
+donor_cell = donor.width // frames
+
+# Frames 1 and 7 remain the exact source rest pose.  In frames 2-6, replace
+# only the free-arm quadrant.  The source's lower body (y >= 159) is never
+# touched, preserving every original hem and boot pixel.
+for frame in range(1, 6):
+    dst_left = round(frame * source_w / frames)
+    dst_right = round((frame + 1) * source_w / frames)
+    dst_pad = (dst_right - dst_left - donor_cell) // 2
+
+    # Remove the source's relaxed free sleeve/hand without opening the robe.
+    erase = Image.new("L", out.size, 0)
+    d = ImageDraw.Draw(erase)
+    x = dst_left + dst_pad
+    d.polygon(
+        [
+            (x + 139, 84),
+            (x + 157, 86),
+            (x + 169, 103),
+            (x + 181, 119),
+            (x + 181, 154),
+            (x + 158, 158),
+            (x + 149, 137),
+            (x + 146, 113),
+            (x + 138, 99),
+        ],
+        fill=255,
+    )
+    green = Image.new("RGB", out.size, (0, 255, 0))
+    out.paste(green, (0, 0), erase)
+
+    cell = donor.crop((frame * donor_cell, 0, (frame + 1) * donor_cell, source_h))
+    # Isolate the newly animated free arm, including only enough shoulder and
+    # side-torso pixels to make a clean sleeve join.
+    arm_gate = Image.new("L", cell.size, 0)
+    ImageDraw.Draw(arm_gate).rectangle((136, 70, donor_cell - 1, 158), fill=255)
+    arm_alpha = Image.composite(cell.getchannel("A"), Image.new("L", cell.size, 0), arm_gate)
+    arm = cell.copy()
+    arm.putalpha(arm_alpha)
+    out.paste(arm.convert("RGB"), (x, 0), arm_alpha)
+
+out.save(OUTPUT)
+print(OUTPUT)

diff --git a/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py 
b/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py
new file mode 100644
index 0000000000000000000000000000000000000000..435b83caeb3678817f88014d7a97c0577b552b03
--- /dev/null
+++ b/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py
@@ -0,0 +1,60 @@
+from pathlib import Path
+
+from PIL import Image, ImageDraw
+
+
+ROOT = Path(__file__).resolve().parent
+SOURCE = ROOT / "refs" / "1_walk.png"
+DONOR = ROOT / "normalized_gutters.png"
+OUTPUT = ROOT / "mage_attack_walk_e_row.png"
+
+
+base = Image.open(SOURCE).convert("RGB")
+donor = Image.open(DONOR).convert("RGBA")
+out = base.copy()
+
+frames = 7
+source_w, source_h = base.size
+donor_cell = donor.width // frames
+
+# Frames 1 and 7 remain the exact source rest pose.  In frames 2-6, replace
+# only the free-arm quadrant.  The source's lower body (y >= 159) is never
+# touched, preserving every original hem and boot pixel.
+for frame in range(1, 6):
+    dst_left = round(frame * source_w / frames)
+    dst_right = round((frame + 1) * source_w / frames)
+    dst_pad = (dst_right - dst_left - donor_cell) // 2
+
+    # Remove the source's relaxed free sleeve/hand without opening the robe.
+    erase = Image.new("L", out.size, 0)
+    d = ImageDraw.Draw(erase)
+    x = dst_left + dst_pad
+    d.polygon(
+        [
+            (x + 139, 84),
+            (x + 157, 86),
+            (x + 169, 103),
+            (x + 181, 119),
+            (x + 181, 154),
+            (x + 158, 158),
+            (x + 149, 137),
+            (x + 146, 113),
+            (x + 138, 99),
+        ],
+        fill=255,
+    )
+    green = Image.new("RGB", out.size, (0, 255, 0))
+    out.paste(green, (0, 0), erase)
+
+    cell = donor.crop((frame * donor_cell, 0, (frame + 1) * donor_cell, source_h))
+    # Isolate the newly animated free arm, including only enough shoulder and
+    # side-torso pixels to make a clean sleeve join.
+    arm_gate = Image.new("L", cell.size, 0)
+    ImageDraw.Draw(arm_gate).rectangle((136, 70, donor_cell - 1, 158), fill=255)
+    arm_alpha = Image.composite(cell.getchannel("A"), Image.new("L", cell.size, 0), arm_gate)
+    arm = cell.copy()
+    arm.putalpha(arm_alpha)
+    out.paste(arm.convert("RGB"), (x, 0), arm_alpha)
+
+out.save(OUTPUT)
+print(OUTPUT)

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "python -c \"from PIL import Image; import 
numpy as np; from scipy.ndimage import label; p='mage_attack_walk_e_row.png'; ref='refs\\\\1_walk.png'; 
a=np.asarray(Image.open(p).convert('RGB')); b=np.asarray(Image.open(ref).convert('RGB')); 
print('size',a.shape[1],a.shape[0]); print('lower_body_changed_pixels',int(np.any(a[159:]"'!=b[159:],axis=2).sum())); 
print('"'exact_green_pixels',int(np.all(a==[0,255,0],axis=2).sum())); w=a.shape[1];
for i in range(7):
 x0=round(i*w/7); x1=round((i+1)*w/7); m=np.any(a[:,x0:x1]"'!=[0,255,0],axis=2); 
lab,n=label(m,structure=np.ones((3,3))); sizes=sorted([int((lab==k).sum()) for k in range(1,n+1)],reverse=True); 
print('"'frame',i+1,'components>=8',sum(s>=8 for s in sizes),'largest',sizes[:4])\"" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 2051ms:
size 1804 254
lower_body_changed_pixels 0
exact_green_pixels 359556
frame 1 components>=8 1 largest [14008]
frame 2 components>=8 1 largest [13683, 1, 1]
frame 3 components>=8 1 largest [14320]
frame 4 components>=8 1 largest [14119]
frame 5 components>=8 1 largest [13911, 1, 1, 1]
frame 6 components>=8 1 largest [14708]
frame 7 components>=8 1 largest [13906]

diff --git a/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py 
b/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py
new file mode 100644
index 0000000000000000000000000000000000000000..435b83caeb3678817f88014d7a97c0577b552b03
--- /dev/null
+++ b/art_src/attack_walk_2026-09-03/mage__attack_walk__e/compose_tmp.py
@@ -0,0 +1,60 @@
+from pathlib import Path
+
+from PIL import Image, ImageDraw
+
+
+ROOT = Path(__file__).resolve().parent
+SOURCE = ROOT / "refs" / "1_walk.png"
+DONOR = ROOT / "normalized_gutters.png"
+OUTPUT = ROOT / "mage_attack_walk_e_row.png"
+
+
+base = Image.open(SOURCE).convert("RGB")
+donor = Image.open(DONOR).convert("RGBA")
+out = base.copy()
+
+frames = 7
+source_w, source_h = base.size
+donor_cell = donor.width // frames
+
+# Frames 1 and 7 remain the exact source rest pose.  In frames 2-6, replace
+# only the free-arm quadrant.  The source's lower body (y >= 159) is never
+# touched, preserving every original hem and boot pixel.
+for frame in range(1, 6):
+    dst_left = round(frame * source_w / frames)
+    dst_right = round((frame + 1) * source_w / frames)
+    dst_pad = (dst_right - dst_left - donor_cell) // 2
+
+    # Remove the source's relaxed free sleeve/hand without opening the robe.
+    erase = Image.new("L", out.size, 0)
+    d = ImageDraw.Draw(erase)
+    x = dst_left + dst_pad
+    d.polygon(
+        [
+            (x + 139, 84),
+            (x + 157, 86),
+            (x + 169, 103),
+            (x + 181, 119),
+            (x + 181, 154),
+            (x + 158, 158),
+            (x + 149, 137),
+            (x + 146, 113),
+            (x + 138, 99),
+        ],
+        fill=255,
+    )
+    green = Image.new("RGB", out.size, (0, 255, 0))
+    out.paste(green, (0, 0), erase)
+
+    cell = donor.crop((frame * donor_cell, 0, (frame + 1) * donor_cell, source_h))
+    # Isolate the newly animated free arm, including only enough shoulder and
+    # side-torso pixels to make a clean sleeve join.
+    arm_gate = Image.new("L", cell.size, 0)
+    ImageDraw.Draw(arm_gate).rectangle((136, 70, donor_cell - 1, 158), fill=255)
+    arm_alpha = Image.composite(cell.getchannel("A"), Image.new("L", cell.size, 0), arm_gate)
+    arm = cell.copy()
+    arm.putalpha(arm_alpha)
+    out.paste(arm.convert("RGB"), (x, 0), arm_alpha)
+
+out.save(OUTPUT)
+print(OUTPUT)

apply patch
patch: completed
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e\co
mpose_tmp.py
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$taskRoot = (Resolve-Path '"'.').Path
"'$tempNames = @('"'normalized_candidate.png','normalized_candidate2.png','normalized_candidate3.png','normalized_candi
date4.png','normalized_gutters.png','source_transparent.png','ref_zoom.png','feet_review.png')
"'$resolved = foreach ($name in $tempNames) { $candidate = Join-Path $taskRoot $name; if (Test-Path -LiteralPath 
$candidate) { (Resolve-Path -LiteralPath $candidate).Path } }
$resolved | ForEach-Object { if (-not $_.StartsWith($taskRoot + [IO.Path]::DirectorySeparatorChar)) { throw "Refusing 
out-of-workspace path: $_" } }
$resolved | ForEach-Object { Remove-Item -LiteralPath $_ }
Get-Item -LiteralPath (Join-Path $taskRoot '"'mage_attack_walk_e_row.png') | Select-Object 
FullName,Length,LastWriteTime" in 
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e
 succeeded in 458ms:

FullName                                                                                                               
--------                                                                                                               
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e...



codex
Saved [mage_attack_walk_e_row.png](C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk
_2026-09-03\mage__attack_walk__e\mage_attack_walk_e_row.png).

- Frame 1: screen-right leg leads; hand resting.
- Frame 2: screen-right leg leads; hand lifting forward.
- Frame 3: screen-right leg leads; open palm extending.
- Frame 4: crossover; full chest-height open-palm thrust.
- Frame 5: screen-left leg leads; hand retracting.
- Frame 6: screen-left leg leads; hand nearly returned.
- Frame 7: screen-left leg closes the stride; hand resting.

No projectile or attack effect was drawn. The floor-length hem remains closed, and the original boots, gait, and 
ground line are preserved. Generated with the built-in image-editing workflow using the prompt to retain Reference 1 
exactly while changing only the free-hand attack arc.
tokens used
104,597
Saved [mage_attack_walk_e_row.png](C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\attack_walk_2026-09-03\mage__attack_walk__e\mage_attack_walk_e_row.png).

- Frame 1: screen-right leg leads; hand resting.
- Frame 2: screen-right leg leads; hand lifting forward.
- Frame 3: screen-right leg leads; open palm extending.
- Frame 4: crossover; full chest-height open-palm thrust.
- Frame 5: screen-left leg leads; hand retracting.
- Frame 6: screen-left leg leads; hand nearly returned.
- Frame 7: screen-left leg closes the stride; hand resting.

No projectile or attack effect was drawn. The floor-length hem remains closed, and the original boots, gait, and ground line are preserved. Generated with the built-in image-editing workflow using the prompt to retain Reference 1 exactly while changing only the free-hand attack arc.
