codex.exe : Reading prompt from stdin...
At line:7 char:14
+ ...    $brief | & $codex exec -C $d -s workspace-write --skip-git-repo-ch ...
+                 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (Reading prompt from stdin...:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
OpenAI Codex v0.153.0
--------
workdir: C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\warlock_aw_n_2026-09-05\warlock__atta
ck_walk__n
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: high
reasoning summaries: none
session id: 01a070c2-ab99-7d00-9ecb-c65724f7f194
--------
user
Redraw this EXACT walk cycle (reference 1) with one change: the character fires
its basic attack once during the stride.

Reference 1 is the character's own 6-frame walk, away from the viewer (back to camera), frame by frame. Reproduce
it EXACTLY: the same 6 frames, the same leg cycle and stride, the same crossing,
the same garment, the same held equipment in the same hand, the same body height
and width, the same palette. The legs are already correct -- do not redesign them.

THE ONLY CHANGE: the FREE hand thrusts forward at chest height, palm open and fingers splayed. Spread the motion 
across the 6 frames so it starts,
peaks around the middle and returns by the end, while the legs keep walking
through their unchanged cycle.

THE CHARACTER (unchanged from reference 1): a gaunt man in a heavy black hooded robe with a gold-edged collar and a 
red gem clasp, a small floating flaming skull companion beside his shoulder, dark boots below a CLOSED floor-length 
hem.

Hard requirements:
- The feet stay PLANTED on the shared ground line exactly as reference 1 has them;
  the body does not slide sideways within its cell.
- The robe stays CLOSED and floor-length: no slit, no exposed legs, no bare
  skin. The walk reads through the BOOTS alternating below the hem and the
  hem swaying -- exactly as reference 1 shows it.
- Draw NO projectile, arrow, knife, flame, bolt, spark or muzzle flash anywhere.
  The game spawns the projectile itself; anything drawn here would double it.
- Same body height and width in every frame; no frame is a copy of another.

Produce ONE image: a horizontal row of EXACTLY 6 figures, evenly spaced, all feet
on one shared ground line, each body near its cell's horizontal centre, green
margins all round -- no figure touching an image edge or its neighbour. EVERY FIGURE
IS THE SAME SIZE; if they will not fit at full size, make the image WIDER. Flat solid
#00ff00 GREEN background, nothing else in the image.
Generate ONE image and SAVE it to disk at the exact path
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\warlock_aw_n_2026-09-05\warlock__attack_walk__
n\warlock_attack_walk_n_row.png
then stop.

In your final message, report for each frame: which leg leads, and where the firing
hand is. Confirm no projectile was drawn and the hem stayed closed.


codex
IΓÇÖm using the image-generation workflow to preserve the six-frame walk sheet and alter only the free-hand attack 
motion. IΓÇÖll first read the projectΓÇÖs art rules, then generate and verify the saved PNG at the exact requested 
path.
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw -LiteralPath 'CLAUDE.md'; 
Get-Content -Raw -LiteralPath 'C:\\Users\\asali\\.codex\\skills\\.system\\imagegen\\SKILL.md'" in C:\Users\asali\Projec
ts\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\warlock_aw_n_2026-09-05\warlock__attack_walk__n
 succeeded in 950ms:
Get-Content : Cannot find path 'CLAUDE.md' because it does not exist.
At line:2 char:1
+ Get-Content -Raw -LiteralPath 'CLAUDE.md'; Get-Content -Raw -LiteralP ...
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : ObjectNotFound: (CLAUDE.md:String) [Get-Content], ItemNotFoundException
    + FullyQualifiedErrorId : PathNotFound,Microsoft.PowerShell.Commands.GetContentCommand
 
---
name: "imagegen"
description: "Generate or edit raster images when the task benefits from AI-created bitmap visuals such as photos, 
illustrations, textures, sprites, mockups, or transparent-background cutouts. Use when Codex should create a brand-new 
image, transform an existing image, or derive visual variants from references, and the output should be a bitmap asset 
rather than repo-native code or vector. Do not use when the task is better handled by editing existing 
SVG/vector/code-native assets, extending an established icon or logo system, or building the visual directly in 
HTML/CSS/canvas."
---

# Image Generation Skill

Generates or edits images for the current project (for example website assets, game assets, UI mockups, product 
mockups, wireframes, logo design, photorealistic images, or infographics).

## Top-level modes and rules

This skill has exactly two top-level modes:

- **Default built-in tool mode (preferred):** built-in `image_gen` tool for image generation, editing, and 
transparent-image requests. Does not require `OPENAI_API_KEY`.
- **Fallback CLI mode:** `scripts/image_gen.py` CLI. Use when the user explicitly asks for or confirms the 
CLI/API/model path. Requires `OPENAI_API_KEY`.

Within CLI fallback, the CLI exposes three subcommands:

- `generate`
- `edit`
- `generate-batch`

Rules:
- Use the built-in `image_gen` tool by default for normal image generation and editing requests.
- Do not switch to CLI fallback for ordinary quality, size, or file-path control.
- For transparent images, ask built-in `image_gen` for a transparent background and preserve the generated alpha.
- Never silently switch from built-in `image_gen` or CLI `gpt-image-2` to CLI `gpt-image-1.5`; ask the user first 
unless they explicitly requested `gpt-image-1.5`.
- The word `batch` by itself does not mean CLI fallback. If the user asks for many assets or says to batch-generate 
assets without explicitly asking for CLI/API/model controls, stay on the built-in path and issue one built-in call per 
requested asset or variant.
- If the built-in tool fails or is unavailable, tell the user the CLI fallback exists and that it requires 
`OPENAI_API_KEY`. Proceed only if the user explicitly asks for that fallback.
- If the user explicitly asks for CLI mode, use the bundled `scripts/image_gen.py` workflow. Do not create one-off SDK 
runners.
- Never modify `scripts/image_gen.py`. If something is missing, ask the user before doing anything else.

Built-in save-path policy:
- In built-in tool mode, Codex saves generated images under `$CODEX_HOME/*` by default.
- Do not describe or rely on OS temp as the default built-in destination.
- Do not describe or rely on a destination-path argument (if any) on the built-in `image_gen` tool. If a specific 
location is needed, generate first and then move or copy the selected output from `$CODEX_HOME/generated_images/...`.
- Save-path precedence in built-in mode:
  1. If the user names a destination, move or copy the selected output there.
  2. If the image is meant for the current project, move or copy the final selected image into the workspace before 
finishing.
  3. If the image is only for preview or brainstorming, render it inline; the underlying file can remain at the 
default `$CODEX_HOME/*` path.
- Never leave a project-referenced asset only at the default `$CODEX_HOME/*` path.
- Do not overwrite an existing asset unless the user explicitly asked for replacement; otherwise create a sibling 
versioned filename such as `hero-v2.png` or `item-icon-edited.png`.

Shared prompt guidance for both modes lives in `references/prompting.md` and `references/sample-prompts.md`.

Fallback-only docs/resources for CLI mode:
- `references/cli.md`
- `references/image-api.md`
- `references/codex-network.md`
- `scripts/image_gen.py`

## When to use
- Generate a new image (concept art, product shot, cover, website hero)
- Generate a new image using one or more reference images for style, composition, or mood
- Edit an existing image (inpainting, lighting or weather transformations, background replacement, object removal, 
compositing, transparent background)
- Produce many assets or variants for one task

## When not to use
- Extending or matching an existing SVG/vector icon set, logo system, or illustration library inside the repo
- Creating simple shapes, diagrams, wireframes, or icons that are better produced directly in SVG, HTML/CSS, or canvas
- Making a small project-local asset edit when the source file already exists in an editable native format
- Any task where the user clearly wants deterministic code-native output instead of a generated bitmap

## Decision tree

Think about two separate questions:

1. **Intent:** is this a new image or an edit of an existing image?
2. **Execution strategy:** is this one asset or many assets/variants?

Intent:
- If the user wants to modify an existing image while preserving parts of it, treat the request as **edit**.
- If the user provides images only as references for style, composition, mood, or subject guidance, treat the request 
as **generate**.
- If the user provides no images, treat the request as **generate**.

Built-in edit semantics:
- Built-in edit mode is for images already visible in the conversation context, such as attached images or images 
generated earlier in the thread.
- If the user wants to edit a local image file with the built-in tool, first load it with built-in `view_image` tool 
so the image is visible in the conversation context, then proceed with the built-in edit flow.
- Do not promise arbitrary filesystem-path editing through the built-in tool.
- If a local file still needs direct file-path control, masks, or other explicit CLI-only parameters, use the explicit 
CLI fallback only when the user asks for it.
- For edits, preserve invariants aggressively and save non-destructively by default.

Execution strategy:
- In the built-in default path, produce many assets or variants by issuing one `image_gen` call per requested asset or 
variant.
- In the CLI fallback path, use the CLI `generate-batch` subcommand only when the user explicitly chose CLI mode and 
needs many prompts/assets.
- For many distinct assets, do not use `n` as a substitute for separate prompts. `n` is for variants of one prompt; 
distinct assets need distinct built-in calls or distinct CLI `generate-batch` jobs.

Assume the user wants a new image unless they clearly ask to change an existing one.

## Workflow
1. Decide the top-level mode: built-in by default, including transparent-output requests; fallback CLI only if 
explicitly requested or confirmed.
2. Decide the intent: `generate` or `edit`.
3. Decide whether the output is preview-only or meant to be consumed by the current project.
4. Decide the execution strategy: single asset vs repeated built-in calls vs CLI `generate-batch`.
5. Collect inputs up front: prompt(s), exact text (verbatim), constraints/avoid list, and any input images.
6. For every input image, label its role explicitly:
   - reference image
   - edit target
   - supporting insert/style/compositing input
7. If the edit target is only on the local filesystem and you are staying on the built-in path, inspect it with 
`view_image` first so the image is available in conversation context.
8. If the user asked for a photo, illustration, sprite, product image, banner, or other explicitly raster-style asset, 
use `image_gen` rather than substituting SVG/HTML/CSS placeholders. If the request is for an icon, logo, or UI graphic 
that should match existing repo-native SVG/vector/code assets, prefer editing those directly instead.
9. Augment the prompt based on specificity:
   - If the user's prompt is already specific and detailed, normalize it into a clear spec without adding creative 
requirements.
   - If the user's prompt is generic, add tasteful augmentation only when it materially improves output quality.
10. Use the built-in `image_gen` tool by default.
11. For transparent-output requests, ask built-in `image_gen` for a transparent background and preserve the generated 
alpha channel.
12. Inspect outputs and validate: subject, style, composition, text accuracy, and invariants/avoid items.
13. Iterate with a single targeted change, then re-check.
14. For preview-only work, render the image inline; the underlying file may remain at the default 
`$CODEX_HOME/generated_images/...` path.
15. For project-bound work, move or copy the selected artifact into the workspace and update any consuming code or 
references. Never leave a project-referenced asset only at the default `$CODEX_HOME/generated_images/...` path.
16. For batches or multi-asset requests, persist every requested deliverable final in the workspace unless the user 
explicitly asked to keep outputs preview-only. Discarded variants do not need to be kept unless requested.
17. If the user explicitly chooses or confirms the CLI fallback, then use the fallback-only docs for model, quality, 
size, `input_fidelity`, masks, output format, output paths, and network setup.
18. Always report the final saved path(s) for any workspace-bound asset(s), plus the final prompt or prompt set and 
whether the built-in tool or fallback CLI mode was used.

## Transparent image requests

Ask built-in `image_gen` for a genuinely transparent background and preserve its alpha.

## Prompt augmentation

Reformat user prompts into a structured, production-oriented spec. Make the user's goal clearer and more actionable, 
but do not blindly add detail.

Treat this as prompt-shaping guidance, not a closed schema. Use only the lines that help, and add a short extra 
labeled line when it materially improves clarity.

### Specificity policy

Use the user's prompt specificity to decide how much augmentation is appropriate:

- If the prompt is already specific and detailed, preserve that specificity and only normalize/structure it.
- If the prompt is generic, you may add tasteful augmentation when it will materially improve the result.

Allowed augmentations:
- composition or framing hints
- polish level or intended-use hints
- practical layout guidance
- reasonable scene concreteness that supports the stated request

Not allowed augmentations:
- extra characters or objects that are not implied by the request
- brand names, slogans, palettes, or narrative beats that are not implied
- arbitrary side-specific placement unless the surrounding layout supports it

## Use-case taxonomy (exact slugs)

Classify each request into one of these buckets and keep the slug consistent across prompts and references.

Generate:
- photorealistic-natural ├óΓé¼ΓÇ¥ candid/editorial lifestyle scenes with real texture and natural lighting.
- product-mockup ├óΓé¼ΓÇ¥ product/packaging shots, catalog imagery, merch concepts.
- ui-mockup ├óΓé¼ΓÇ¥ app/web interface mockups and wireframes; specify the desired fidelity.
- infographic-diagram ├óΓé¼ΓÇ¥ diagrams/infographics with structured layout and text.
- scientific-educational ├óΓé¼ΓÇ¥ classroom explainers, scientific diagrams, and learning visuals with required labels 
and accuracy constraints.
- ads-marketing ├óΓé¼ΓÇ¥ campaign concepts and ad creatives with audience, brand position, scene, and exact 
tagline/copy.
- productivity-visual ├óΓé¼ΓÇ¥ slide, chart, workflow, and data-heavy business visuals.
- logo-brand ├óΓé¼ΓÇ¥ logo/mark exploration, vector-friendly.
- illustration-story ├óΓé¼ΓÇ¥ comics, children├óΓé¼Γäós book art, narrative scenes.
- stylized-concept ├óΓé¼ΓÇ¥ style-driven concept art, 3D/stylized renders.
- historical-scene ├óΓé¼ΓÇ¥ period-accurate/world-knowledge scenes.

Edit:
- text-localization ├óΓé¼ΓÇ¥ translate/replace in-image text, preserve layout.
- identity-preserve ├óΓé¼ΓÇ¥ try-on, person-in-scene; lock face/body/pose.
- precise-object-edit ├óΓé¼ΓÇ¥ remove/replace a specific element (including interior swaps).
- lighting-weather ├óΓé¼ΓÇ¥ time-of-day/season/atmosphere changes only.
- background-extraction ├óΓé¼ΓÇ¥ transparent background / clean cutout. Ask built-in `image_gen` for actual 
transparency.
- style-transfer ├óΓé¼ΓÇ¥ apply reference style while changing subject/scene.
- compositing ├óΓé¼ΓÇ¥ multi-image insert/merge with matched lighting/perspective.
- sketch-to-render ├óΓé¼ΓÇ¥ drawing/line art to photoreal render.

## Shared prompt schema

Use the following labeled spec as shared prompt scaffolding for both top-level modes:

```text
Use case: <taxonomy slug>
Asset type: <where the asset will be used>
Primary request: <user's main prompt>
Input images: <Image 1: role; Image 2: role> (optional)
Scene/backdrop: <environment>
Subject: <main subject>
Style/medium: <photo/illustration/3D/etc>
Composition/framing: <wide/close/top-down; placement>
Lighting/mood: <lighting + mood>
Color palette: <palette notes>
Materials/textures: <surface details>
Text (verbatim): "<exact text>"
Constraints: <must keep/must avoid>
Avoid: <negative constraints>
```

Notes:
- `Asset type` and `Input images` are prompt scaffolding, not dedicated CLI flags.
- `Scene/backdrop` refers to the visual setting. It is not the same as the fallback CLI `background` parameter, which 
controls output transparency behavior.
- Fallback-only execution notes such as `Quality:`, `Input fidelity:`, masks, output format, and output paths belong 
in the CLI path only. Do not treat them as built-in `image_gen` tool arguments.

Augmentation rules:
- Keep it short.
- Add only the details needed to improve the prompt materially.
- For edits, explicitly list invariants (`change only X; keep Y unchanged`).
- If any critical detail is missing and blocks success, ask a question; otherwise proceed.

## Examples

### Generation example (hero image)
```text
Use case: product-mockup
Asset type: landing page hero
Primary request: a minimal hero image of a ceramic coffee mug
Style/medium: clean product photography
Composition/framing: wide composition with usable negative space for page copy if needed
Lighting/mood: soft studio lighting
Constraints: no logos, no text, no watermark
```

### Edit example (invariants)
```text
Use case: precise-object-edit
Asset type: product photo background replacement
Primary request: replace only the background with a warm sunset gradient
Constraints: change only the background; keep the product and its edges unchanged; no text; no watermark
```

## Prompting best practices
- Structure prompt as scene/backdrop -> subject -> details -> constraints.
- Include intended use (ad, UI mock, infographic) to set the mode and polish level.
- Use camera/composition language for photorealism.
- Only use SVG/vector stand-ins when the user explicitly asked for vector output or a non-image placeholder.
- Quote exact text and specify typography + placement.
- For tricky words, spell them letter-by-letter and require verbatim rendering.
- For multi-image inputs, reference images by index and describe how they should be used.
- For edits, repeat invariants every iteration to reduce drift.
- Iterate with single-change follow-ups.
- If the prompt is generic, add only the extra detail that will materially help.
- If the prompt is already detailed, normalize it instead of expanding it.
- For CLI fallback only, see `references/cli.md` and `references/image-api.md` for model, `quality`, `input_fidelity`, 
masks, output format, and output-path guidance.
- For transparent images, ask built-in `image_gen` for actual transparency and preserve its alpha.

More principles shared by both modes: `references/prompting.md`.
Copy/paste specs shared by both modes: `references/sample-prompts.md`.

## Guidance by asset type
Asset-type templates (website assets, game assets, wireframes, logo) are consolidated in 
`references/sample-prompts.md`.

## gpt-image-2 guidance for CLI fallback

The fallback CLI defaults to `gpt-image-2`.

- Use `gpt-image-2` for new CLI/API workflows unless the user confirms a different model.
- CLI `gpt-image-2` does not support `background=transparent`; ask before using `gpt-image-1.5` unless the user 
explicitly requested that model.
- `gpt-image-2` always uses high fidelity for image inputs; do not set `input_fidelity` with this model.
- `gpt-image-2` supports `quality` values `low`, `medium`, `high`, and `auto`.
- Use `quality low` for fast drafts, thumbnails, and quick iterations. Use `medium`, `high`, or `auto` for final 
assets, dense text, diagrams, identity-sensitive edits, or high-resolution outputs.
- Square images are typically fastest to generate. Use `1024x1024` for fast square drafts.
- If the user asks for 4K-style output, use `3840x2160` for landscape or `2160x3840` for portrait.
- `gpt-image-2` size may be `auto` or `WIDTHxHEIGHT` if all constraints hold: max edge `<= 3840px`, both edges 
multiples of `16px`, long-to-short ratio `<= 3:1`, total pixels between `655,360` and `8,294,400`.

Popular `gpt-image-2` sizes:
- `1024x1024` square
- `1536x1024` landscape
- `1024x1536` portrait
- `2048x2048` 2K square
- `2048x1152` 2K landscape
- `3840x2160` 4K landscape
- `2160x3840` 4K portrait
- `auto`

## Fallback CLI mode only

### Temp and output conventions
These conventions apply only to the CLI fallback. They do not describe built-in `image_gen` output behavior.
- Use `tmp/imagegen/` for intermediate files (for example JSONL batches); delete them when done.
- Write final artifacts under `output/imagegen/`.
- Use `--out` or `--out-dir` to control output paths; keep filenames stable and descriptive.

### Dependencies
Prefer `uv` for dependency management in this repo.

Required Python package:
```bash
uv pip install openai
```

Optional for image inspection and downscaling:
```bash
uv pip install pillow
```

Portability note:
- If you are using the installed skill outside this repo, install dependencies into that environment with its package 
manager.
- In uv-managed environments, `uv pip install ...` remains the preferred path.

### Environment
- `OPENAI_API_KEY` must be set for live API calls.
- Do not ask the user for `OPENAI_API_KEY` when using the built-in `image_gen` tool.
- Never ask the user to paste the full key in chat. Ask them to set it locally and confirm when ready.

If the key is missing, give the user these steps:
1. Create an API key in the OpenAI platform UI: https://platform.openai.com/api-keys
2. Set `OPENAI_API_KEY` as an environment variable in their system.
3. Offer to guide them through setting the environment variable for their OS/shell if needed.

If installation is not possible in this environment, tell the user which dependency is missing and how to install it 
into their active environment.

### Script-mode notes
- CLI commands + examples: `references/cli.md`
- API parameter quick reference: `references/image-api.md`
- Network approvals / sandbox settings for CLI mode: `references/codex-network.md`

## Reference map
- `references/prompting.md`: shared prompting principles for both modes.
- `references/sample-prompts.md`: shared copy/paste prompt recipes for both modes.
- `references/cli.md`: fallback-only CLI usage via `scripts/image_gen.py`.
- `references/image-api.md`: fallback-only API/CLI parameter reference.
- `references/codex-network.md`: fallback-only network/sandbox troubleshooting for CLI mode.
- `scripts/image_gen.py`: fallback-only CLI implementation. Use only when the user explicitly chooses or confirms CLI 
mode.


exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$p = (Get-Location).Path; while ($true) { 
$candidate = Join-Path $p '"'CLAUDE.md'; if (Test-Path -LiteralPath "'$candidate) { Write-Output $candidate; 
Get-Content -Raw -LiteralPath $candidate; break }; $parent = Split-Path -Parent $p; if ($parent -eq $p -or 
[string]::IsNullOrEmpty($parent)) { break }; $p = $parent }' in C:\Users\asali\Projects\MMO\.claude\worktrees\epic-pani
ni-3fd8fd\art_src\warlock_aw_n_2026-09-05\warlock__attack_walk__n
 succeeded in 783ms:
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\CLAUDE.md
# Crownless ├óΓé¼ΓÇ¥ working practices (all agents)

## Mobile version ├óΓé¼ΓÇ¥ kept in sync (unfrozen 2026-07-15)
- `mobile/` (repo root) holds the iOS/Android version: a snapshot fork of `game/` plus a small, fixed set of mobile 
deltas (Mobile renderer, touch HUD, export presets ├óΓé¼ΓÇ¥ the exact list lives in `mobile/README.md`). Deployment is 
proven end to end ├óΓé¼ΓÇ¥ GitHub Actions **"Mobile builds"** produces a signed-debug APK and an unsigned iOS `.ipa`; 
both install/run on-device (sideload).
- **Policy change (was "frozen, do not touch"):** keep `mobile/game/` in sync with `game/`. `game/` is still the 
**source of truth** ├óΓé¼ΓÇ¥ never edit `mobile/` to fix a desktop issue; fix it in `game/` and re-sync. To re-sync: 
re-copy `game/` over `mobile/game/`, re-apply the mobile deltas from `mobile/README.md`, then run the compile gate + 
`test_quick` against `mobile/game` before committing. Keep the delta list small and current.

## Code layout (see CODING_GUIDELINES.md ├é┬º38)
- Tuning knobs ├óΓÇáΓÇÖ `balance.gd`; data tables stay in domain files (classes/items/story). Never inline a bare 
tuning number.
- Docs split (2026-07-06): balance/pacing round narratives go in `BALANCE_HISTORY.md` (newest at TOP of the tuning 
list); `DESIGN.md` holds only current decisions + distilled standing rules ├óΓé¼ΓÇ¥ never append round-by-round 
history there.
- Big node classes are inheritance CHAINS (verbatim moves, calls flow derived├óΓÇáΓÇÖbase, all vars in the base layer):
  - Game: `game_base` (state/flags/convo/lookups/fx) ├óΓÇá┬É `game_world` (graph/rooms/walls/spawning/gates) ├óΓÇá┬É 
`game_flow` (deaths/loot/chapters/settings/terrain events) ├óΓÇá┬É `game.gd` (boot + per-frame).
  - Player: `player_core` (state/stats/gear/progression) ├óΓÇá┬É `player_combat` (targeting/hit/shared juice + shared 
primitives) ├óΓÇá┬É `player_kit_{warrior,archer,mage,assassin,paladin,warlock}` (one file per class: `_use_<class>` 
dispatch + its abilities) ├óΓÇá┬É `player.gd` (dispatch/survival/per-frame). Primitives shared by several kits 
(`_dash_strike`, `_melee_arc`, `_mist`, `_beam_fx`, `_grant_stab_surge`) stay in `player_combat` ├óΓé¼ΓÇ¥ calls only 
flow derived├óΓÇáΓÇÖbase.
  - Tests: `tests/test_base` (helpers) ├óΓÇá┬É `tests/test_ch1` ├óΓÇá┬É `tests/test_ch2` ├óΓÇá┬É `autotest.gd` (entry 
+ systems tier).
- Self-contained UI screens = static modules in `scripts/ui/` (dev_panel, codex) taking the Menus instance.
- New `class_name` script ├óΓÇáΓÇÖ run `--import` before any headless run.
- The in-game **codex** (`scripts/ui/codex.gd`) is the player-facing reference for monsters/bosses/terrains/gear 
├óΓé¼ΓÇ¥ it goes stale silently. Whenever you add player-facing content or a feature, ask whether the codex should 
reflect it, and if so update it in the same change. It reads `Story.ALL_ENEMIES`/data tables directly, but a few 
hand-maintained parallel lists still gate it (e.g. `menus.gd` `BOSS_KINDS` decides monster-vs-boss bucketing) ├óΓé¼ΓÇ¥ 
update those too, or a new boss lands in the wrong bucket. Layout since 2026-08-15 = rail of sections 
(`UICodex.SECTIONS`) + filter chips + ledger/detail; a new section is one SECTIONS row + a `_rows`/`_detail` (or page) 
builder, and every legacy `open_codex("<tab>")` id routes through `_resolve()` ├óΓé¼ΓÇ¥ extend that, never fork the 
panel. Reference prose lives under Field notes (`NOTE_PAGES`), not above a shelf.

## Testing ├óΓé¼ΓÇ¥ ALWAYS in this order
1. Compile gate first, every time (`test_quick.bat`/`test.bat` run `check_compile.gd` first). Never invoke the test 
scene directly: one parse error anywhere makes the suite script fail to compile and the headless engine idles FOREVER 
(a zombie that looks like a slow run). The gate catches it in ~3s with the real error.
2. Iterate with `test_quick.bat` (~15s): gate ├óΓÇáΓÇÖ boot ├óΓÇáΓÇÖ one class kit ├óΓÇáΓÇÖ systems ├óΓÇáΓÇÖ UI smoke 
├óΓÇáΓÇÖ pause menu.
3. `test.bat` (full suite, minutes; plays both chapters end to end) must be green before staging.

## Agent toolkit (the map: `tools/INDEX.md` ├óΓé¼ΓÇ¥ check it before writing a helper script)
- `preflight.bat` ├óΓé¼ΓÇ¥ the traps below, mechanized: stale/forgotten `--import`, codex/BOSS_KINDS staleness, 
unregistered content modules, diff-scoped balance-number + CONNECT_DEFERRED lints, new shot rig not `extends ShotRig`. 
Run it before staging; every finding prints its one-line fix.
- `python tools/safe_commit.py -m "msg" <your paths>` ├óΓé¼ΓÇ¥ the serialized-commit etiquette as a guardrail: 
path-scoped commit that lists sibling-staged work instead of swallowing it.
- `python tools/sync_mobile.py` ├óΓé¼ΓÇ¥ mobile drift report; `--apply [--gate]` performs the re-sync ritual from 
`mobile/README.md`.
- **Headless Codex (`codex exec`, the default generated-art lane): calling guide = 
`C:\Users\asali\Projects\CODEX_HEADLESS.md` (Projects root, shared by every project on this box: binary, call, all 
flags, machine limits) + `tools/CODEX_HEADLESS.md` (the MMO layer: runner, drivers, vet/install).** The binary's 
install dir is re-hashed on every app update ├óΓé¼ΓÇ¥ resolve it (`python tools/codex_bin.py` / `Resolve-Codex` in 
`run_codex_batch.ps1`), never hard-code it; prompt goes on STDIN; on this shared box run image batches serial + 
RAM-gated (`run_codex_batch.ps1 -MaxParallel 1 -MinFreeGB 1.3`), never beside a Godot suite (a parallel batch + suite 
OOM-crashed the host 2026-08-17).
- `python tools/art/verify_art.py <base>` ├óΓé¼ΓÇ¥ post-install sprite checks (strip geometry, 8-dir completeness, 
green-bleed, stale import, and in-cell content gates: anchor drift/ghost chunks/edge cuts/clip-vs-idle body scale + 
**HEROBODY** hero frame-0 scale-lock shrink).
  - **HEROBODY vs CLIPSCALE (added 2026-08-23):** CLIPSCALE models the ENEMY renderer (median body / cell). HEROES 
render differently ├óΓé¼ΓÇ¥ `player_core._measure_hero_frame` locks a clip's on-screen scale to FRAME 0's CONTENT bbox 
and plays the whole clip at it, so a clip whose REST pose already raises a weapon (frame-0 content > body) renders the 
body small for the entire clip (the archer-multishot 0.85├âΓÇö shrink CLIPSCALE structurally cannot see). HEROBODY 
flags a hero action clip whose frame-0 body renders <0.90├âΓÇö the idle's. A neutral rest frame reads ~1.0 and a legit 
crouch/lunge still starts neutral, so both are spared (0 FP across the 6 base classes + 5 skins).
  - **The eyes-only drift class has NO geometric gate ├óΓé¼ΓÇ¥ the net is a VISUAL QA artifact + agent review.** 
Face-fidelity drift (a fatter/blurrier face in action frames), a shield/armor detail that morphs frame-to-frame, a 
cape that flips sides ├óΓé¼ΓÇ¥ all pass every geometric check; the *drawing* is inconsistent. verify_art cannot catch 
these and a sharpness/consistency proxy false-positives on real motion. So a skin/class regen MUST emit a per-clip 
contact sheet (idle + action peak frames, feet-aligned + face crops) and an AGENT must review it before install. An AI 
reviewer catches "her face got fatter"; a number can't. Don't rely on the owner's eyes as the first line. **The full 
reusable workflow ├óΓé¼ΓÇ¥ dedup-scope ├óΓÇáΓÇÖ one review agent per subject over EVERY clip├âΓÇödir├âΓÇöframe 
├óΓÇáΓÇÖ orchestrator verifies each flag ├óΓé¼ΓÇ¥ is `tools/art/DRIFT_AUDIT.md` (covers heroes/skins AND the boss/mob 
adaptation: enemy render path = CLIPSCALE not HEROBODY).** Established 2026-08-24; the known drift catalogue 
(fire-on-move clips are the systemic weak point; un-regenerated clips carry the face-softening family; 
single-direction off-model art) lives there.
  - **WARN triage ├óΓé¼ΓÇ¥ FAIL is the bar, not WARN (investigated 2026-08-23).** The six BASE heroes throw a large, 
standing pile of WARNs that are NOT defects ├óΓé¼ΓÇ¥ the gate has no way to whitelist intentional content, so it flags 
it every run. All three benign categories, confirmed by rendering the flagged pixels on grass + overlaying them on the 
sprite:
    - **BLEED** on base heroes ├óΓÇ░┬á green keying rim. It's soft-alpha FX (the archer's cast/ult IS green 
Arrow-Storm magic; every class's ult/cast auras + glows) plus green-CLOTHING edges (the archer's green cloak). The 
BLEED counter is colour-blind (any `0<a<255` pixel), so green *content* inflates it; a band-normalize/upscale 
multiplies the same FX/cloak edge pixels and the raw count rises with zero new bleed. The real-bug discriminator is a 
green RIM hugging the silhouette (`G>max(R,B)+20` massed on the body outline) ├óΓé¼ΓÇ¥ on the base heroes that rim is 
absent; the green sits on the FX cloud and the cloak, and on a grass background there's no halo.
    - **STRAY** on base heroes = FX props the game does NOT re-spawn: the warlock's floating flaming SKULL (his 
signature, every frame), the warrior's flaming-sword blade/trail, the archer's green cast-motes. Distinct from a baked 
*projectile* (which the STRAY rule is really aimed at) ├óΓé¼ΓÇ¥ leave these.
    - **EDGECUT** on melee swings (`attack2`/`attackb`/`attackc` for warrior/assassin/paladin) = the weapon reaching 
the cell edge at the extreme of the arc, not a clipped body.
    - Baseline as of 2026-08-23: base heroes = **0 FAIL, ~261 WARN** (208 BLEED / 28 STRAY / 25 EDGECUT); the 5 
in-scope elite skins = clean `VERIFY OK`. Don't re-investigate or "fix" these WARN categories on the base heroes 
├óΓé¼ΓÇ¥ only act on a NEW **FAIL**, or a WARN that leaves these three buckets (e.g. a real body green-rim, a 
butchered/PARTIAL frame, an anchor/scale/FEETSLIDE hit).
    - **CORRECTION (2026-09-03): the "no green rim on base heroes" finding was scoped to cast/ult.** A corpus scan 
(`tools/art/despill_rim.py --scan`, rim-band green with an EMPTY interior = halo, not design) found a real #00FF00 
keying ring on all 19 `warlock_attack*`/`attack2*` strips ├óΓé¼ΓÇ¥ a design with no green in it at all ├óΓé¼ΓÇ¥ plus 
44 mobs/NPCs/props. All despilled; the scan now returns 0. Re-run the scan after any keyed install; the fixer is 
rim-band-only so it cannot dull green garments, and it refuses a sprite whose green is mostly interior.
- **Screenshot rigs: never write a standalone one (2026-08-15).** Twenty-three `game/shot_*.gd` files are copies of 
the same 25-line boot/`_shot`/quit boilerplate, none with a watchdog, muted only by convention. New rig = `extends 
ShotRig` (`scripts/dev/shot_rig.gd`: `boot()`, `shot()`, sim-clock waits, `arg()`/`flag()`, Master-bus mute, 
`--timeout=N` watchdog) + `_ready()` of steps + `finish()`; run it ├óΓé¼ΓÇ¥ and every legacy rig ├óΓé¼ΓÇ¥ through 
`shot.bat <rig> [--timeout=N] [rig args]`, which injects `--audio-driver Dummy`, compile-gates the rig script 
(check_compile's default walk skips `game/` root: a parse error there = a window that idles forever), and hard-kills a 
hung engine. `preflight` FAILs a new rig that doesn't extend it and WARNs on an edited legacy one (convert on touch). 
Worked example: `shot_fx_series.gd`; the map: `tools/INDEX.md` "In-engine shot rigs".
- **Windowed shot rigs (`shot_*.tscn`, `qa_skins`, anything not `--headless`) run MUTED: always pass `--audio-driver 
Dummy`.** They boot the real game with music + SFX on the owner's shared machine; an unmuted rig blares through his 
speakers mid-session (owner ruling 2026-08-15). Headless suites are already silent.

## GDScript traps (each has bitten us) ├óΓé¼ΓÇ¥ review-time deep list: `CODING_GUIDELINES.md` ├é┬º38
- `var x := obj.method()` on a loosely-typed obj (or any Variant expression, e.g. `Dictionary.get`) = PARSE ERROR 
"cannot infer type". Annotate: `var x: float = ...`.
- New `class_name` ├óΓÇáΓÇÖ `--import` first or headless silently hangs.
- Labels inside HBoxContainer collapse to one char per line without `custom_minimum_size`.
- Multiline lambda: following call args must sit on the SAME line as the lambda's last statement.
- Tests waiting on timed effects must poll wall-clock (`await create_timer(0.2)`) ├óΓé¼ΓÇ¥ frames race ahead headless.
- Autotest sections SNAPSHOT + RESTORE shared state (flags, resonance, standings); never `.clear()`. Two corollaries, 
both learned from one flaky capital run (2026-07-26):
  - **Restore on the FAILURE paths too.** `_fail` only QUEUES `quit(1)` ├óΓé¼ΓÇ¥ the suite keeps running to the end of 
the frame, so the next section still executes and reads whatever the failed one left behind. A mid-city failure 
stranded `chapter_id == "capital"`, where `shop_markup` answers a flat 1.0, and the road-markup section then reported 
a second, entirely bogus failure. Put the assertions in a helper and the restore in its caller, so all ~40 exits pass 
through it.
  - **Never assert against state an earlier section accumulated.** A content-tier section inherits a whole campaign of 
loot, gold, resonance and bag capacity ├óΓé¼ΓÇ¥ all of it loot-luck. The capital's lapidary quest asserted its kit 
landed in the bag, and a full bag legally mails the kit instead: pass or fail came down to whether a bag DROPPED that 
run. Control the precondition (lend a bag, pin the shard) or the assert is a coin flip.
- canvas_item shader `fragment()`: `COLOR` arrives PREMULTIPLIED (modulate ├âΓÇö TEXTURE sample). Writing `COLOR = 
result * COLOR` multiplies the texture in twice ├óΓé¼ΓÇ¥ everything darkens and "brighten" shaders read as darken 
(phantom_charge was bitten; owner saw "only gets darker"). Reuse ONLY `COLOR.a` (silhouette.gdshader convention).
- `Environment.adjustment_contrast` with `viewport/hdr_2d` runs in LINEAR light: it pivots on 0.5, and a dark floor 
sits near 0 there ├óΓé¼ΓÇ¥ contrast 1.06 HALVED the keep floor's brightness (gameplay-polish rig 2026-08-18). Leave 
contrast at 1.0; use saturation/brightness only, and judge any whole-frame grade in-game, never from the numbers.
- Spawning an Area2D + CollisionShape2D (Chest/Pickup/Projectile) from inside a `body_entered`/`area_entered` handler 
= non-fatal "Can't change this state while flushing queries" engine error (`area_set_shape_disabled`) ├óΓé¼ΓÇ¥ the new 
shape's enter-tree hits the physics-flush guard. Connect physics signals with `CONNECT_DEFERRED` or `call_deferred` 
the spawn (chest open + projectile ricochet were bitten).
- `draw_colored_polygon` on a SLIVER (near-collinear points) = random non-fatal "Invalid polygon data, triangulation 
failed" spam: the triangulator picks winding from a float32 shoelace over RAW coords, so a sliver's sign is rounding 
noise at world-scale coordinates ├óΓé¼ΓÇ¥ coordinate-dependent coin flip, NOT deterministic per shape (knife-trail 
fold-back miters were bitten). Pre-check `Geometry2D.triangulate_polygon(p).is_empty()` and skip ├óΓé¼ΓÇ¥ the exact 
C++ path behind the error, and everything it rejects is sub-pixel (the `_poly` guard in player_combat.gd).
- **In co-op, the tree pause is NOT an input gate.** `request_pause(true)` NO-OPS online (`game_base.gd`, ├é┬º5.4 
├óΓé¼ΓÇ¥ a shared world never pauses), so anything leaning on the pause to stop input/processing keeps running in a 
session. Menus, dialogue and choice prompts all pause solo and none of them pause online. Gate on the OVERLAY STATE 
instead ├óΓé¼ΓÇ¥ `menus.is_open()` / `hud.dialogue_active` / `hud.choices_active`, the guard `game.gd`'s tap-to-talk 
already uses. The touch HUD had no `process_mode` override and only checked `state == ST_PLAYING` (there is no 
`ST_MENU`), so its joystick ├óΓé¼ΓÇ¥ the whole left half of the screen ├óΓé¼ΓÇ¥ stayed live UNDER every menu and a 
drag walked your hero mid-fight. Note the shape of the miss: it was invisible in BOTH cheap tests (solo-on-phone 
pauses; co-op-on-desktop has no touch HUD) and only existed in the mobile ├âΓÇö co-op cross-product. When a feature is 
platform- or session-conditional, ask what its gate is in the OTHER combination.

## Machine RAM is tight (~10GB) ├óΓé¼ΓÇ¥ tool-process hygiene (2026-08-23)
- This box has ~9.8GB physical RAM and the BASELINE load already fills it: Claude Code + its MCP servers ├óΓÇ░╦å 3GB 
(22 procs ├óΓé¼ΓÇ¥ MCP servers orphan on restart; 3 duplicate `pixelforge-mcp` copies seen), browser ├óΓÇ░╦å 1.2GB, 
Windows (svchost + Defender + compression) ├óΓÇ░╦å 1.4GB. That leaves ~1GB free ├óΓé¼ΓÇ¥ nothing has headroom. This is 
WHY image batches run serial + RAM-gated and never beside a Godot suite. But be precise about WHAT is heavy: **a 
single `codex exec` image gen is API-side and costs only ~250MB local (measured 2026-08-24) ├óΓé¼ΓÇ¥ it is NOT a 
machine-killer and a single serial job runs safely even at ~1GB free.** The RAM risk is PARALLELISM (several gens at 
once) ├âΓÇö a co-resident **Godot suite** (each Godot instance is the real GB-scale hog). So `-MaxParallel 1 
-MinFreeGB 1.3` guards against *stacking* jobs and firing next to a suite ├óΓé¼ΓÇ¥ don't read it as "Codex is a spike" 
and refuse a single gated gen. (PixelLab local footprint unmeasured; don't assume it's the same.) A Claude restart 
clears orphaned MCP servers; closing browser tabs frees ~1GB.
- **Prefer the Bash tool over PowerShell.** PowerShell tool calls can leave lingering `powershell.exe` processes (7 
orphans spanning 2 days seen, ├óΓÇ░╦å0.45GB). Use Bash for process/file ops (`taskkill //F //PID <n>` cleans strays); 
reserve PowerShell for genuine cmdlet needs, and consolidate into ONE call rather than many.

## Multi-agent etiquette
- Task boards (`CH2_TASKS.md` pattern): one owner per task, claim before starting; content lands as NEW modules under 
`game/scripts/content/` (format: `scripts/content/README.md`) + one registration line in `Story.CONTENT_MODULES`.
- Stage with `git add` after a green full suite. No commits unless the user asks.
- **Commits are SERIALIZED (2026-07-07):** a commit sweeps the WHOLE index, and sibling agents stage concurrently 
├óΓé¼ΓÇ¥ so immediately before committing, run `git status` and look at what's actually staged. If the index holds 
another agent's work, either fold it into an accurate combined message or stop and say so ├óΓé¼ΓÇ¥ never commit a 
message that describes only your slice. (Trigger: an itemization pass got swallowed into a commit labeled "cover + 
boot flow" by exactly this race.)
- Commit messages: NO author/co-author trailers (no `Co-Authored-By`, no attribution lines) ├óΓé¼ΓÇ¥ this project 
keeps commits authorless; credit lives in a separate credits file.
- Autotest additions: content modules use the marked CONTENT-MODULE TEST HOOK (one func at file end + one call line); 
never edit existing sections.

## Asset sourcing
- **Allowed ├óΓé¼ΓÇ¥ and that's the whole list:** ONLY these two, or anything we generate ourselves (must be up to 
par: semantic fit + reads clean in-game).
  1. **CC0 / public domain** ├óΓé¼ΓÇ¥ zero obligations, ideal.
  2. **CC-BY / MIT / Apache / OFL** ├óΓé¼ΓÇ¥ fine; ship the attribution file (we already do, in 
`assets/*/CREDITS.txt`).
  No pre-approval needed for these. Source from anywhere ├óΓé¼ΓÇ¥ OpenGameArt, Kenney, itch.io, the Dungeon Crawl 
tileset (CC0), etc.
- **Everything else is off-limits** ├óΓé¼ΓÇ¥ do not use, don't even ask: share-alike (CC-BY-SA, GPL), non-commercial 
(CC-NC), proprietary/paid asset-store packs, and anything with no stated license (default = all rights reserved). This 
game ships commercially on Steam; these licenses either forbid selling it or can force us to open-source the whole 
project.
- Install flow: probe/download, and LOOK at the image (or listen to the audio) before installing; overrides drop into 
`assets/sounds|music|sprites/` by name (a PNG/wav there overrides the procedural version of the same name).
- Audio taste: semantic fit over quality; no melodic-jingle chiptunes for ults/terrains; never human grunts on casts; 
never touch `ult_mage.wav` or the village/story/boss synth tracks.

## Icon fidelity ├óΓé¼ΓÇ¥ authored resolution ├óΓÇ░┬á render size (owner ruling 2026-08-21; agents keep tripping on 
this)
- Player-facing icon families are **DUAL-RES**: a high-fidelity master for the codex/detail views + a downscaled 
gameplay copy for the bag. The **128px painterly master is the FIDELITY BAR** ├óΓé¼ΓÇ¥ match it, not a flat/pixel 
style, unless the owner says otherwise. Reference tiers: **gear** = `assets/icons/codex/<shape>_<grade>.png` @128px 
(codex, via `Art.codex_item_icon`) + `assets/icons/<name>.png` @32px (bag); **class splashes** ~235px authored 
├óΓÇáΓÇÖ render ~100px in-game (~2.35├âΓÇö ├óΓé¼ΓÇ¥ the owner's "renders nicely" benchmark).
- **THE TRAP:** authoring a family at **32px only**. It looks fine in a 32px bag slot, so it passes a quick check 
├óΓé¼ΓÇ¥ but every larger surface (codex ~28├óΓé¼ΓÇ£128px, the detail popover header 40px) UPSCALES it to mush, and it 
reads as low-fidelity beside the 128px gear. **Gems shipped 32px-only (2026-07-30) and were the 2026-08-20 
visual-review's one solid asset catch for exactly this.** When you regen an icon family: author at the 128px gear bar, 
store dual-res, and load the 128 in large contexts. Verify the fidelity numbers (stored res vs on-screen render px) 
BEFORE designing ├óΓé¼ΓÇ¥ the owner asks for this by name.
- **This generalises past icons to ALL world/character/codex art (owner ruling 2026-08-24): a master should be 
├óΓÇ░┬Ñ2├âΓÇö its on-screen render size.** The auditor is `tools/art/fidelity_audit.py` + `game/fidelity_dump.gd`; the 
current findings + prioritised re-art list live in **`FIDELITY_AUDIT.md`** (root). Bar = the base classes (render 99px 
from 235px = 2.35├âΓÇö). Standing findings: the **5 enhanced-base skins PASS (2.37├âΓÇö)** but the **18 legacy skins 
are ~1.2├âΓÇö** (the planned regen quantified); trees/large props + one npc (`pilgrims_schism`) + `choir_censer` (a 
boss-summoned add) are the other real re-art targets. Unplaced+low-fi mobs get DROPPED, not re-arted (`bat`/`direbat` 
gone 2026-08-24).

## World props ├óΓé¼ΓÇ¥ style + motion contract (owner ruling 2026-08-18, after a day of "shifts / dims / cartoonish" 
flags from play; review-time checklist: `CODING_GUIDELINES.md` ├é┬º40; the polish effort's board = `POLISH_TASKS.md`, 
its generation lane = `tools/art/style_unify/`)
- **One style.** Every world prop matches the cast: painterly, soft-shaded, NO black outlines, muted palette, top-down 
three-quarter, light top-left. A hi-res canvas is NOT a pass ├óΓé¼ΓÇ¥ a 1024px cartoon is still a cartoon (cactus2, 
tree_teal2, grave_cross all passed the resolution audit). Regen briefs name a painterly SIBLING as the style ref and 
the old asset only as the SUBJECT (silhouette/footprint/colour identity); judge the result on a contact sheet BESIDE 
its siblings (`scratchpad`-style sheet, or `tools/art/asset_gallery.py`), never against the asset it replaces.
- **Animate in place.** Frames never translate the object. Rigid props (furnaces, wells, shrines, rigs, pools) keep a 
byte-stable silhouette ├óΓé¼ΓÇ¥ colour-only motion (glow pulse, ember flicker), bbox/baseline/centre drift 0; only 
fire, cloth and foliage may change silhouette, and then anchored on frame 0's baseline. Wind sway is an ALLOW-list of 
vegetation (`_wind_scenery`), never "everything that isn't a building". Hazard pools stay put 
(`Balance.HAZARD_POOLS_DRIFT`). Anything that swings/pendulums needs authored frames with proper easing or no 
animation at all ├óΓé¼ΓÇ¥ a 4-frame swing reads as fake.
- **Dim, don't strobe.** A prop should look STILL at a glance and alive on a second look. Luminance swing ├óΓÇ░┬ñ 
10-12% for glows/pulses (`derive_prop_anim.py --amp 0.10`), ├óΓÇ░┬ñ 25% for open fire, never on the whole sprite 
(bright/warm pixels only). The 0.35 default that looked "visible" on a contact sheet strobes in-game.
- **Gate every strip you touch:** `tools/art/audit_prop_anims.py` (frames/canvas/per-frame drift + amp) + 
`verify_art.py`; a NEW rigid animated prop goes into autotest's `full_prop_anims` contract (frame == static canvas, 
baseline ├óΓÇ░┬ñ1 / centre ├óΓÇ░┬ñ2). Contact sheets and metrics do not replace watching the loop: run it in a shot 
rig at 1x (`shot.bat polish --gif`) before calling it done ├óΓé¼ΓÇ¥ a subtle loop that looks static on a sheet is 
correct.

## Hero skins ├óΓé¼ΓÇ¥ what is in scope vs OFF-LIMITS (owner ruling 2026-08-22)
- **In scope for polish: ONLY the 5 ENHANCED-BASE designs**, wired as elite skins purely for in-game preview: warrior 
`emberbound_heir`, assassin `erased_name`, archer `severed_thread`, warlock `ledgerbound`, mage `blighted_healer`. 
These are the enhanced base-class designs ├óΓé¼ΓÇ¥ fix/re-anchor/despur/frame-edit them freely.
- **OFF-LIMITS ├óΓé¼ΓÇ¥ do NOT touch, regen, re-anchor, despur, frame-edit, or "fix":** every OTHER hero skin. That is 
all of `game/assets/sprites/skins/mythic/` (dreadknight, stormforged, eclipse_knight, fallen_arbiter, blade_dancer, 
phantom/phantom_awakened, voidwraith, eldritch_herald, ├óΓé¼┬ª) **and** the real skins that live in `skins/elite/` 
alongside the enhanced-base five (dreadknight, frostfall_ranger, void_weaver, hellfire_inquisitor, eclipse_knight, 
blade_dancer, ├óΓé¼┬ª). A blanket art-sweep must EXCLUDE these ├óΓé¼ΓÇ¥ an auto-scan of `assets/sprites/` will hit 
them; scope it to the five, or to bosses/mobs, explicitly.
- **Why:** a full REGEN pass over all the real skins is planned (owner 2026-08-22, "for later"). Until that pass, 
leave them exactly as committed ├óΓé¼ΓÇ¥ a piecemeal fix now is wasted work the regen overwrites, and it drifts the 
design.
- **Known-defect note:** the real skins carry the same feet-slide artifact the enhanced-base + bosses/mobs were fixed 
for (2026-08-22), so `verify_art` will emit `FEETSLIDE` WARNs on them. That is EXPECTED and intentionally left for the 
regen pass ├óΓé¼ΓÇ¥ do not "fix" it.

## Required per-class animation clips beyond the standard set (so a regen knows what to produce) ├óΓé¼ΓÇ¥ 2026-08-23
Some abilities need EXTRA clips on top of anim/walk/attack/attack2/dash/cast/ult/death. A class or skin regen MUST 
produce these or the ability silently falls back to a worse pose. Both are art-driven (no strip = the fallback), so 
they're easy to forget ├óΓé¼ΓÇ¥ hence this manifest.

- **Fire-on-move (`<base>_attack_walk[_<dir>]`)** ├óΓé¼ΓÇ¥ a walk cycle that fires the basic mid-stride, so spamming a 
low-cd RANGED basic WHILE MOVING doesn't lock the standing pose sliding over the floor (`player.gd` `walk_fire`; a 
mid-cycle re-cast lets the stride finish). Needed by the ranged spammables ONLY:
  - mage **Firebolt** (a1=attack), warlock **Shadowbolt** (a1=attack), archer **Quick Shot** (a1=attack), assassin 
**Fan of Knives** (a3=attack2).
  - NOT the melee 3-way basic ├óΓé¼ΓÇ¥ the assassin STAB keeps its attack/attackb/attackc alternation while moving 
(gated by `slot=="a1" and _clips.has("attackb")`, which only excludes the assassin).
  - **HOW TO GENERATE (the technique that finally worked, owner 2026-08-23): it's the WALK clip + a firing arm 
├óΓé¼ΓÇ¥ do NOT author a walk from scratch.** Give the character's EXISTING `<base>_walk_<dir>` strip as the image ref 
and instruct: "reproduce this exact walk ├óΓé¼ΓÇ¥ same leg cycle/stride/crossing, same garment ├óΓé¼ΓÇ¥ change ONLY 
the free hand to fire the basic once." A from-scratch walk brief (however detailed the per-frame boot storyboard) kept 
gliding or failing to cross the legs; referencing the real walk carries its crossing gait for free. Keep the brief 
SHORT ├óΓé¼ΓÇ¥ the walk ref IS the leg spec.
  - Contract: 8-frame walk+fire; feet PLANTED (no in-cell slide ├óΓÇáΓÇÖ FEETSLIDE); the garment stays CLOSED and 
design-accurate (NO slit, NO exposed legs/bare skin ├óΓé¼ΓÇ¥ the walk reads through the BOOTS alternating below the 
hem + the hem swaying, exactly as the walk clip does); NO baked projectile/flame on the sprite (the game spawns it 
├óΓé¼ΓÇ¥ a small warm spark shares the mage's skin/gold palette and is un-maskable by colour except the bright-yellow 
core, so keep it out at gen time). Author S/E/N; E├óΓÇáΓÇÖSE/NE; **left MIRRORS right** (W/NW/SW = mirror of E); flat 
= S. Build lane: `scratchpad/attack_walk_codex.py`.
  - **`attack_walk_b` ├óΓé¼ΓÇ¥ the alternate variant (owner 2026-08-23).** Fire-on-move ALTERNATES `attack_walk 
├óΓÇáΓÇ¥ attack_walk_b` each fresh moving-basic cycle (player.gd `_walkfire_swing`), like the melee 
attack├óΓÇáΓÇ¥attackb, so spamming while moving doesn't replay one clip. Produce it with the SAME walk reference 
├óΓé¼ΓÇ¥ only the FIRE motion differs from the primary (warlock: two-hand shove vs one-hand thrust; mage: free-hand 
rising flick vs level thrust, staff hand kept clearly separate; assassin: sidearm flick vs overhand throw). **NOT the 
archer** (owner 2026-08-23): a bow draw has ONE proper form, so the archer has no `_b` ├óΓé¼ΓÇ¥ it plays the primary 
every cycle. When a `_b` would confuse the silhouette (e.g. the mage's casting hand reading as the staff hand), keep 
the two hands clearly separate or skip the variant. Build lane: `attack_walk_codex.py ├óΓé¼┬ª --b`. Art-driven: no 
`_b` strip = the primary plays every cycle.

- **Melee swing alternation (`attackb`, and `attackc` for enhanced variants)** ├óΓé¼ΓÇ¥ melee basics cycle multiple 
authored swings per cast (`player_core.gd` `_alt_basic_clip`: attack├óΓÇáΓÇÖattackb├óΓÇáΓÇÖattackc) so Cleave / Smite 
/ Stab don't replay one motion. Which classes ship which:
  - **warrior, paladin, assassin** carry `attackb` (2-way minimum). Ranged classes (mage/archer/warlock) have only 
`attack`.
  - **3-way (`attackc`)**: base **paladin**, warrior **emberbound_heir**, assassin **erased_name** (owner 2026-08-22 
├óΓé¼ΓÇ¥ the enhanced variants). `attackc` is a DISTINCT 3rd motion (a rising uppercut), not a recolor of 
attack/attackb. autotest `_test_swing_alternation` expects 3-way when a body ships an attackc.

## Generated art tool authorization
- **NEVER assume PixelLab is authorized. PixelLab may be used only when the owner explicitly authorizes PixelLab for 
the specific task.** Existing PixelLab assets, PixelLab helper scripts, character metadata, or a matching PixelLab 
workflow elsewhere in the repo do not grant permission for a new generation.
- When an agent has a built-in image-generation/editing tool (for example, ChatGPT/Codex image generation), that 
built-in tool is the default for generated art unless the owner explicitly requests or authorizes another generator.
- This rule applies even when another generator appears technically better suited. If the built-in tool cannot meet 
the requirement, stop and ask the owner; do not silently switch tools or spend external generation credits.

### Codex built-in ImageGen walk-animation observations
- For bipedal characters, the built-in ImageGen tool often fails to preserve a fluid left/right leg cycle. A real 
same-colored leg crossing over the other in one passing frame can create a convincing walking illusion; keep that 
crossing to one or at most two frames rather than making every frame crossed.
- If ImageGen repeats nearly identical crossed poses, give each cell an explicit foot-position storyboard instead of 
only asking for "alternating legs": use a wide split, a tight crossover, a second crossover with a different spread 
and the opposite leg emerging, then the opposite wide split or a subtle toe-off. State that frames 1, 3, and 4 must 
have visibly different lower-body silhouettes. (Tried on the Wild Fang Raider 2026-08-14 and again 08-15: the 4-cell 
2x2 kept coming back with the SAME leg leading in both stride cells and the body ~11% short ├óΓé¼ΓÇ¥ the owner still 
called the walk bad.)
- **What actually fixed the Wild Fang Raider walk (2026-08-15): ONE ROW of EIGHT figures, each frame labelled with its 
gait phase** (near-foot contact / load / far leg passes / reach, then the same four for the other leg ├óΓé¼ΓÇ¥ the 
pipeline doc's "legs do not alternate" remedy). The row came back with 8 distinct poses, constant body height and one 
ground line; installed as an 8-frame strip (the engine's strip reader is frame-count agnostic ├óΓé¼ΓÇ¥ width/height 
├óΓé¼ΓÇ¥ so 8 is fine; at the 2x walk clock the loop runs 0.67 s). Prefer this over 4-cell 2x2 for any bipedal walk. 
Ask Codex to report leg-lead per frame in its final message; it is a candid critic of its own output.
- **Write every identity description FROM the idle sheet, never from memory or an audit's paraphrase (2026-09-03).** 
Three of fifteen boss/mob regen rows drifted for exactly this: a skeleton got a shield the idle never carried (twice 
├óΓé¼ΓÇ¥ the pilot and the batch), the winged flame-skull echo came back as a plain hooded shroud, korrag lost his 
lightning arcs. The generator reproduced the prose faithfully; the prose was wrong. Look at `<base>_anim` frame 0 and 
list what is actually there (what is in each hand, what is NOT there) before writing the brief.
- **A BACK-view regen never gets the outgoing strip as a reference** when the defect IS the view (front art wearing 
the N name): handing it back re-seeds it. `gait_briefs.py` drops it automatically on `facing: n`; the identity ref 
stays the front idle with the front-only parts NAMED as forbidden.
- **Row briefs must say every figure is the SAME size** ("do not crowd two small figures into one cell; make the image 
wider"). Three rows came back with undersized figures crammed into one cell before the brief said so; 
`install_gait_row` refuses a speck cell rather than ship a blinking near-empty frame.
- **Cells ├óΓÇ░┬Ñ 900px: request a two-row GRID, not a row (2026-09-04).** A 6-figure row comes back ~2100px wide 
(~400px figures), so a 920/1084px boss cell forces a 1.4-2.2├âΓÇö upscale at install (stormmouth/nullwarden N shipped 
soft the first time). Ask for TWO ROWS of THREE ("each figure roughly 900px tall") and flatten with 
`tools/art/grid_to_row.py`; the same rows landed at ~870px, installed at 0.65-1.07├âΓÇö.
- **A facing column authored FRONT-ON (the base mage's whole E column, the warlock's 3/4-front walk E) is fixed with 
`tools/art/profile_clip_briefs.py`** (identity = front idle, view = a real profile sibling, action = the old clip). 
When a base WALK is regenerated, its `attack_walk_<dir>` was built ON the old walk (the walk-plus-firing-arm 
technique) and must be regenerated from the new one, or fire-on-move snaps to a different view mid-stride.
- Every ImageGen-keyed strip carried a dark-green antialias rim (`build_mob_walk_repairs.remove_green` now despills 
the 2px edge band); a strip that "looks fine" on a dark contact sheet can still ring green over grass ├óΓé¼ΓÇ¥ check 
`G > max(R,B)+20` counts on the rim, not just the semi-alpha BLEED gate.
- The tool is generally more reliable for non-bipedal walks (for example, spiders and wolves), and for characters 
whose locomotion does not expose a conventional leg cycle: robes can communicate motion through cloth flutter, and 
floating characters can use body/cloth drift.
- For bipedal sheets, prompt for fixed torso/pelvis anchors, a shared foot baseline, compact stride, 
held-weapon/upper-body continuity, and explicit negative constraints against recoloring legs, detached feet, 
duplicated frames, and high-knee marching. Inspect the generated sheet before installation and run 
`tools/art/verify_art.py <base>` afterward; anchor drift can still remain even when the sheet looks visually centered.

## PixelLab characters ├óΓé¼ΓÇ¥ regenerating an EXISTING one (deep dive: `tools/art/PIXELLAB_BOSS_EDITS.md`)
- **Authorization gate:** this section applies only after the owner has explicitly authorized PixelLab for the current 
task. It is not permission to choose PixelLab.
- **RULE: before you regenerate, re-roll, or tweak an existing PixelLab character (boss/hero/skin), ALWAYS read its 
export metadata FIRST.** Download the zip (`curl -H "Authorization: Bearer $PIXELLAB_SECRET" 
https://api.pixellab.ai/mcp/characters/<id>/download -o out.zip`) and read `metadata.json` ├óΓé¼ΓÇ¥ it stores the 
ORIGINAL creation `prompt` (plus size, `template_id`, view). Regenerate from that EXACT prompt and change ONLY the 
clause the owner flagged.
- **Never invent a fresh description from a screenshot or memory** ├óΓé¼ΓÇ¥ it silently drifts the design 
(proportions, palette, silhouette) even when the fix was one word. `get_character` does NOT return the creation 
prompt; the zip's `metadata.json` is the only source of truth.

## PixelLab characters ├óΓé¼ΓÇ¥ new generation workflow (prompt-craft cases: `tools/art/PIXELLAB_PROMPT_LESSONS.md`)
- **CHECK ALL 8 ROTATIONS before generating animations.** After a character body is created, visually inspect every 
direction (S, SE, E, NE, N, NW, W, SW) for consistency ├óΓé¼ΓÇ¥ same proportions, same gear, same silhouette. v3 mode 
frequently produces inconsistent north/west directions (wrong body shape, missing equipment, different outfit). If any 
direction is broken, re-roll the body first. Generating animations on a broken body wastes hundreds of generations.
- **Heroes use v3 mode (NOT pro).** The gold-standard assassin and warlock were both v3. Pro mode produces stocky 
proportions for visible-face characters. The `style: -, -, -` field in `get_character` appears in BOTH v3 and pro 
├óΓé¼ΓÇ¥ it cannot distinguish modes.
- **Standard mode is useless for proportions testing.** Its template skeleton produces chibi proportions regardless of 
description. Only useful for testing color palette and design elements.
- **Description formula (proven on assassin):** archetype first ├óΓÇáΓÇÖ every piece of gear described precisely 
├óΓÇáΓÇÖ body type ("lean and gaunt" / "lean and tall") ├óΓÇáΓÇÖ art mood ("muted dark palette, somber dark fantasy") 
├óΓÇáΓÇÖ explicit negatives ("no hood", "no smoke"). Leave no room for the model to improvise.
- Worked example (2026-07-12): owner said Vess should have "no smoke from mouth". The metadata prompt literally 
contained `faint blue-white keening light at the mouth` ├óΓé¼ΓÇ¥ the fix was deleting that one clause from the 
original prompt, not writing a new banshee description.


codex
The reference shows the held purple-flame implement on the image-left arm and the free arm on image-right; the skull 
remains beside the image-right shoulder. IΓÇÖm locking the gait, robe, equipment, silhouette scale, baseline, and 
palette, and changing only that image-right arm across the six poses.
codex
The first render preserved the six distinct gait phases and closed hem, but the peak arm read too sideways for a 
north-facing attack and the tool returned an oversized 3:1 canvas. IΓÇÖm correcting the arm to a foreshortened forward 
thrust, then IΓÇÖll format the six cells to the referenceΓÇÖs 1844├ù264 strip geometry without changing their poses.
codex
The depth direction now reads correctly, but the peak hand is too high and crowds the companion. IΓÇÖm lowering the 
active hand to chest level while preserving the corrected foreshortening and every other frame element.
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$src='"'C:\\Users\\asali\\.codex\\generated_
images\\01a070c2-ab99-7d00-9ecb-c65724f7f194\\exec-aaefc372-1025-4a8c-a815-148b63e81da9.png'; if (Get-Command magick 
-ErrorAction SilentlyContinue) { magick identify -verbose "'$src | Select-String -Pattern 
'"'Geometry:|Colorspace:|Type:' } else { Add-Type -AssemblyName System.Drawing; 
"'$img=[System.Drawing.Image]::FromFile($src); Write-Output ("Geometry: {0}x{1}" -f $img.Width,$img.Height); 
$img.Dispose() }; Get-Item -LiteralPath $src | Select-Object FullName,Length' in C:\Users\asali\Projects\MMO\.claude\wo
rktrees\epic-panini-3fd8fd\art_src\warlock_aw_n_2026-09-05\warlock__attack_walk__n
 succeeded in 2363ms:
Geometry: 2146x733

FullName                                                                                                               
--------                                                                                                               
C:\Users\asali\.codex\generated_images\01a070c2-ab99-7d00-9ecb-c65724f7f194\exec-aaefc372-1025-4a8c-a815-148b63e81da...



exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Add-Type -AssemblyName System.Drawing; $src=
'"'C:\\Users\\asali\\.codex\\generated_images\\01a070c2-ab99-7d00-9ecb-c65724f7f194\\exec-aaefc372-1025-4a8c-a815-148b6
3e81da9.png'; "'$bmp=[System.Drawing.Bitmap]::FromFile($src); 
$pts=@(@(0,0),@($bmp.Width-1,0),@(0,$bmp.Height-1),@($bmp.Width-1,$bmp.Height-1),@(100,100),@(1000,100)); foreach($p 
in $pts){$c=$bmp.GetPixel($p[0],$p[1]); Write-Output ("{0},{1}: #{2:X2}{3:X2}{4:X2}" -f $p[0],$p[1],$c.R,$c.G,$c.B)}; 
$bmp.Dispose()' in C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\warlock_aw_n_2026-09-05\war
lock__attack_walk__n
 succeeded in 1438ms:
Method invocation failed because [System.Object[]] does not contain a method named 'op_Subtraction'.
At line:2 char:216
+ ... File($src); $pts=@(@(0,0),@($bmp.Width-1,0),@(0,$bmp.Height-1),@($bmp ...
+                 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : InvalidOperation: (op_Subtraction:String) [], RuntimeException
    + FullyQualifiedErrorId : MethodNotFound
 

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Add-Type -AssemblyName System.Drawing; $src=
'"'C:\\Users\\asali\\.codex\\generated_images\\01a070c2-ab99-7d00-9ecb-c65724f7f194\\exec-aaefc372-1025-4a8c-a815-148b6
3e81da9.png'; "'$bmp=[System.Drawing.Bitmap]::FromFile($src); $w=[int]$bmp.Width; $h=[int]$bmp.Height; 
$pts=@(@(0,0),@(($w-1),0),@(0,($h-1)),@(($w-1),($h-1)),@(100,100),@(1000,100)); foreach($p in 
$pts){$c=$bmp.GetPixel([int]$p[0],[int]$p[1]); Write-Output ("{0},{1}: #{2:X2}{3:X2}{4:X2}" -f 
$p[0],$p[1],$c.R,$c.G,$c.B)}; $bmp.Dispose()' in C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_s
rc\warlock_aw_n_2026-09-05\warlock__attack_walk__n
 succeeded in 707ms:
0,0: #0EE20C
2145,0: #0DE609
0,732: #0AE30A
2145,732: #0CE807
100,100: #04E905
1000,100: #04EA05

codex
The pose sheet is now visually correct, but pixel inspection found the generatorΓÇÖs ΓÇ£solid greenΓÇ¥ background 
contains a faint green gradient rather than literal `#00ff00`. IΓÇÖm normalizing only background-colored pixels to 
exact `#00ff00`, then checking dimensions, figure count, margins, and the shared foot baseline before saving.
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n \"remove_green|#00ff00|00ff00|green 
background|chroma\" tools/art -g '*.py'" in C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd
 exited 1 in 1951ms:
rg : The term 'rg' is not recognized as the name of a cmdlet, function, script file, or operable program. Check the 
spelling of the name, or if a path was included, verify that the path is correct and try again.
At line:2 char:1
+ rg -n "remove_green|#00ff00|00ff00|green background|chroma" tools/art ...
+ ~~
    + CategoryInfo          : ObjectNotFound: (rg:String) [], CommandNotFoundException
    + FullyQualifiedErrorId : CommandNotFoundException
 

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-ChildItem -LiteralPath 'tools\\art' 
-Filter '*.py' -Recurse | Select-String -Pattern 'remove_green|#00ff00|00ff00|green background|chroma' | Select-Object 
-First 100 Path,LineNumber,Line" in C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd
 succeeded in 1100ms:

Path                                                                                                            LineNum
                                                                                                                    ber
----                                                                                                            -------
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\act1_brief_lib.py                        301
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\act1_brief_lib.py                        312
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\act1_brief_lib.py                        317
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\act1_brief_lib.py                        406
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\act1_brief_lib.py                        423
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\act1_brief_lib.py                        428
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\alt_swing_pipeline.py                    125
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\alt_swing_pipeline.py                    131
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\alt_swing_pipeline.py                    133
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\alt_swing_pipeline.py                    212
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\alt_swing_pipeline.py                    252
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\assemble_generated_walk_frames.py         16
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\assemble_generated_walk_frames.py         37
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\asset_gallery.py                          68
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\asset_gallery.py                         352
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\attack_walk_briefs.py                    143
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_act1_directional_walks.py           23
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_act1_directional_walks.py           73
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_act1_directional_walks.py           95
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_act2_sprites.py                     24
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_act2_sprites.py                    213
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_auroch_minotaur.py                   4
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_auroch_minotaur.py                  82
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_blighted_mage.py                    55
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_blighted_mage.py                   145
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_blighted_mage.py                   176
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_capital_monumental.py                3
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_capital_water_anim.py                4
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_codex_2x2_strip.py                  36
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_codex_2x2_strip.py                  60
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_codex_2x2_strip.py                  61
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_codex_2x2_strip.py                  63
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_codex_2x2_strip.py                 412
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_codex_2x2_strip.py                 420
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_corrective_remaining_walks.py       43
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_corrective_remaining_walks.py       67
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_corrective_remaining_walks.py      103
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_erased_name_assassin.py             26
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_erased_name_assassin.py             85
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_erased_name_assassin.py            229
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_fx_strip.py                          1
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_fx_strip.py                        188
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_gear_codex_icons.py                242
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_gem_icons.py                       103
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_gem_icons.py                       123
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_gem_icons.py                       129
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_gem_icons.py                       284
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_imagegen_attack_candidate.py         3
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_ledgerbound_warlock.py              68
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_ledgerbound_warlock.py             180
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_loot.py                             31
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_loot.py                             82
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_loot.py                            112
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_projectiles.py                  13
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_projectiles.py                  82
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_redesigns.py                    59
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_redesigns.py                    77
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_redesigns.py                   195
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_walk_repairs.py                 39
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_walk_repairs.py                 67
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_walk_repairs.py                 86
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_walk_repairs.py                 87
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_walk_repairs.py                518
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_walk_repairs.py                521
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_walk_repairs.py                531
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_walk_repairs.py                553
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_walk_repairs.py                587
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_walk_repairs.py                603
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_walk_repairs.py                623
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_walk_repairs.py                628
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_walk_repairs.py                633
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_walk_repairs.py                644
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_mob_walk_repairs.py                672
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_oathbound_paladin.py                 3
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_oathbound_paladin.py                48
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_oathbound_paladin.py               146
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_oathbound_paladin.py               164
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_pc_extra_mobs.py                     5
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_pc_extra_mobs.py                    36
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_pc_extra_mobs.py                   179
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_pc_extra_mobs.py                   196
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_pc_extra_mobs.py                   201
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_preservation_walk_candidate.py       3
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_preservation_walk_candidate.py      27
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_preservation_walk_candidate.py     276
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_severed_thread_archer.py            60
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_severed_thread_archer.py            94
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_severed_thread_archer.py           147
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_skin_family.py                     111
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_skin_family.py                     125
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_terrain_art_fix.py                  66
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_terrain_art_fix.py                  90
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_terrain_prop_anims.py                4
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_terrain_prop_anims.py              135
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_walk8.py                            42
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_walk_v3_candidates.py               26
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_walk_v3_candidates.py              130
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\build_warlock_unify.py                    50
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\clean_sprite.py                           41
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\clean_sprite.py                          103



exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw -LiteralPath 
'tools\\art\\build_imagegen_attack_candidate.py'; Get-Content -Raw -LiteralPath 
'tools\\art\\assemble_generated_walk_frames.py'; Get-Content -LiteralPath 'tools\\art\\build_mob_walk_repairs.py' | 
Select-Object -First 115" in C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd
 succeeded in 706ms:
"""Build one unwired ImageGen attack row and its QA proofs.

The generated source may use any flat chroma field.  Border-connected keying
protects intended costume colors, source gutters are validated losslessly,
and one frame-1-derived scale preserves authored recoil/crouch motion.
"""

from __future__ import annotations

import argparse
from pathlib import Path

import numpy as np
from PIL import Image

from build_ledgerbound_warlock import _hard_alpha
from build_preservation_walk_candidate import (
    _crop_frames,
    _infer_columns,
    _normalize,
    _write_cut_overlay,
    _write_qa,
)
from rebuild_preservation_archer_alpha import _connected_key


def main() -> None:
    parser = argparse.ArgumentParser()
    parser.add_argument("source", type=Path)
    parser.add_argument("output_dir", type=Path)
    parser.add_argument("--stem", required=True)
    parser.add_argument("--label", required=True)
    parser.add_argument("--frames", type=int, required=True)
    parser.add_argument("--fps", type=float, default=22.0)
    parser.add_argument("--body", type=int, default=180)
    parser.add_argument("--peak", type=int, required=True)
    args = parser.parse_args()

    args.output_dir.mkdir(parents=True, exist_ok=True)
    source = Image.open(args.source).convert("RGBA")
    keyed, key, removed = _connected_key(source)
    keyed = _hard_alpha(keyed)
    keyed.save(args.output_dir / f"{args.stem}_keyed.png")

    alpha = np.asarray(keyed.getchannel("A"), dtype=np.uint8) > 0
    detected = _infer_columns(alpha)
    if detected != args.frames:
        raise ValueError(
            f"expected {args.frames} authored figures but detected {detected}"
        )
    frames, edges, gutters = _crop_frames(keyed, detected)
    _write_cut_overlay(
        args.output_dir / f"{args.stem}_cuts.png", keyed, edges, gutters
    )
    normalized = _normalize(frames, args.body)

    strip = Image.new("RGBA", (277 * detected, 277), (0, 0, 0, 0))
    for index, frame in enumerate(normalized):
        strip.alpha_composite(frame, (index * 277, 0))
    strip.save(args.output_dir / f"{args.stem}_candidate.png", optimize=True)
    _write_qa(
        args.output_dir,
        args.stem,
        args.label,
        normalized,
        args.fps,
        args.peak,
    )

    semi_alpha = int(
        ((np.asarray(strip.getchannel("A")) > 0) & (np.asarray(strip.getchannel("A")) < 255)).sum()
    )
    if semi_alpha:
        raise ValueError(f"candidate contains {semi_alpha} semi-transparent pixels")
    if any(strip.getpixel(point)[3] != 0 for point in ((0, 0), (strip.width - 1, 0), (0, 276), (strip.width - 1, 
276))):
        raise ValueError("candidate corners are not transparent")
    heights = [frame.getbbox()[3] - frame.getbbox()[1] for frame in normalized]
    print(
        f"{args.stem}: key={key}, removed={removed}, frames={detected}, "
        f"gutters={gutters}, heights={heights}, cell=277, body={args.body}, "
        f"fps={args.fps:g}"
    )


if __name__ == "__main__":
    main()

"""Assemble individually generated walk frames without resynthesizing pixels."""

from __future__ import annotations

import argparse
import sys
from pathlib import Path

from PIL import Image, ImageDraw, ImageFont


TOOLS = Path(__file__).resolve().parent
if str(TOOLS) not in sys.path:
    sys.path.insert(0, str(TOOLS))

from build_ledgerbound_warlock import _hard_alpha, _remove_green  # noqa: E402


def _font(size: int) -> ImageFont.ImageFont:
    for path in (Path("C:/Windows/Fonts/seguisb.ttf"), Path("C:/Windows/Fonts/arialbd.ttf")):
        if path.exists():
            return ImageFont.truetype(str(path), size)
    return ImageFont.load_default()


def main() -> None:
    parser = argparse.ArgumentParser()
    parser.add_argument("output", type=Path)
    parser.add_argument("frames", nargs="+", type=Path)
    parser.add_argument("--qa-dir", type=Path, required=True)
    parser.add_argument("--label", required=True)
    parser.add_argument("--fps", type=float, default=9.0)
    parser.add_argument("--target-body", type=int, default=390)
    args = parser.parse_args()

    keyed = [
        _hard_alpha(_remove_green(Image.open(path).convert("RGBA")))
        for path in args.frames
    ]
    boxes = [frame.getbbox() for frame in keyed]
    if any(box is None for box in boxes):
        raise ValueError("empty generated frame")
    cell, baseline = 512, 468
    normalized: list[Image.Image] = []
    for index, (frame, box) in enumerate(zip(keyed, boxes, strict=True)):
        assert box is not None
        figure = frame.crop(box)
        scale = args.target_body / float(figure.height)
        size = (max(1, round(figure.width * scale)), max(1, round(figure.height * scale)))
        figure = _hard_alpha(figure.resize(size, Image.Resampling.LANCZOS))
        if figure.width > cell - 12 or figure.height > baseline - 6:
            raise ValueError(f"frame {index + 1} exceeds staging cell: {figure.size}")
        canvas = Image.new("RGBA", (cell, cell), (0, 0, 0, 0))
        canvas.alpha_composite(figure, ((cell - figure.width) // 2, baseline - figure.height))
        normalized.append(canvas)

    strip = Image.new("RGBA", (cell * len(normalized), cell), (0, 0, 0, 0))
    for index, frame in enumerate(normalized):
        strip.alpha_composite(frame, (index * cell, 0))
    args.output.parent.mkdir(parents=True, exist_ok=True)
    strip.save(args.output)

    args.qa_dir.mkdir(parents=True, exist_ok=True)
    preview, top = 220, 42
    contact = Image.new("RGBA", (preview * len(normalized), preview + top), (25, 28, 34, 255))
    draw = ImageDraw.Draw(contact)
    draw.text((10, 8), args.label, font=_font(18), fill=(255, 224, 126, 255))
    gif_frames: list[Image.Image] = []
    for index, frame in enumerate(normalized):
        shown = frame.resize((preview, preview), Image.Resampling.LANCZOS)
        contact.alpha_composite(shown, (index * preview, top))
        draw.text((index * preview + 6, top + 5), f"f{index + 1}", font=_font(14), fill=(255, 224, 126, 255))
        page = Image.new("RGBA", (preview, preview), (25, 28, 34, 255))
        page.alpha_composite(shown)
        gif_frames.append(page.convert("P", palette=Image.Palette.ADAPTIVE))
    stem = args.output.stem
    contact.save(args.qa_dir / f"{stem}_contact.png")
    gif_frames[0].save(
        args.qa_dir / f"{stem}.gif",
        save_all=True,
        append_images=gif_frames[1:],
        duration=round(1000.0 / args.fps),
        loop=0,
        disposal=2,
    )
    print(f"assembled {len(normalized)} frames -> {args.output}")


if __name__ == "__main__":
    main()

#!/usr/bin/env python3
"""Install the 2026-08-08 ImageGen mob locomotion repair pass.

The source masters are intentionally kept in ``art_src``.  This builder
removes the flat green screen, restores each live sprite's existing canvas,
scale, and ground anchor, installs only complete generated frames, and writes
compact QA contact sheets beside the masters.
"""

from __future__ import annotations

from pathlib import Path

import numpy as np
from PIL import Image, ImageDraw, ImageFilter


ROOT = Path(__file__).resolve().parents[2]
SOURCE = ROOT / "art_src" / "Custom" / "MobWalkRepairs_2026-08-08"
REFERENCES = SOURCE / "references"
SPRITES = ROOT / "game" / "assets" / "sprites"

IDLE_ONLY = (
    "static_caller", "rat_mage", "stormcult", "mummy", "skeleton_mage",
    "skeleton_warrior", "vale_mourner", "mummy_mage", "cold_pilgrim",
)

# Robe-covered locomotion uses a full-frame glide/breathing strip instead of
# visible steps. Null Acolyte keeps its existing static design but now gets the
# same readable whole-body motion treatment as the redesigned robe identities.
ROBE_MOTION = ("null_acolyte", *IDLE_ONLY)

# The same redesigned, floor-length identities need matching complete-frame
# attacks. Null Acolyte keeps its existing identity and attack art.
ROBE_ATTACKS = IDLE_ONLY

# Owner-reviewed attack replacements whose legacy strips contained neighboring
# frame fragments or moved the body core. Every master supplies four complete
# generated subjects separated by real chroma gutters.
CONSISTENCY_ATTACKS = (
    "fungus_heavy", "skeleton", "orc_rogue", "elf_ranger", "cultist",
    "bandit_scout", "grove_horror", "spider", "blightwolf",
    "duneprowler", "null_acolyte", "skeleton_rogue", "stone_broken",
    "storm_harrier", "rat_mage", "zombie", "winterfang", "bog_lurker",
    "fungus_long", "elf_druid", "vow_sentinel", "royal_knight",
)

WALKS = (
    "wolf", "cultist", "skeleton", "zombie", "blightwolf", "orc",
    "orc_rogue", "elf_ranger", "duneprowler", "deep_stalker",
    "casket_creeper", "stone_broken", "vent_skitter", "winterfang",
    "royal_knight", "bog_lurker", "elf_druid", "vow_sentinel",
    "bandit_scout", "fungus_long",
)


def reference_path(key: str, suffix: str = "") -> Path:
    """Use the frozen art_src metric when present, otherwise the live idle/anim.

    A few later repairs target shipped sprites whose original metric snapshot
    predates the walk-repair bundle. Falling back to the unchanged live asset
    keeps those repairs reproducible without inventing a second reference.
    """
    frozen = REFERENCES / f"{key}{suffix}.png"
    return frozen if frozen.exists() else SPRITES / f"{key}{suffix}.png"

def remove_green(path: Path) -> Image.Image:
    with Image.open(path) as opened:
        image = opened.convert("RGBA")
    rgba = np.asarray(image).copy()
    rgb = rgba[..., :3].astype(np.int16)
    border = np.concatenate((
        rgb[:12].reshape(-1, 3), rgb[-12:].reshape(-1, 3),
        rgb[:, :12].reshape(-1, 3), rgb[:, -12:].reshape(-1, 3),
    ))
    key = np.median(border, axis=0)
    distance = np.max(np.abs(rgb - key), axis=2)
    green_like = ((rgb[..., 1] - np.maximum(rgb[..., 0], rgb[..., 2]) > 34)
                  & (rgb[..., 1] > 110))
    magenta_like = ((np.minimum(rgb[..., 0], rgb[..., 2]) - rgb[..., 1] > 34)
                    & (np.maximum(rgb[..., 0], rgb[..., 2]) > 110))
    # Most masters use green; foliage-bearing subjects use magenta so their
    # real greens survive. Select despill from the sampled border key rather
    # than leaving magenta antialias pixels around those subjects.
    key_is_magenta = min(key[0], key[2]) - key[1] > 40
    chroma_like = magenta_like if key_is_magenta else green_like
    key_like = (distance < 34) | chroma_like
    rgba[..., 3][key_like] = 0
    rgba[..., 3] = np.where(rgba[..., 3] >= 96, 255, 0).astype(np.uint8)
    # Edge despill (2026-08-15). The antialiased rim of every keyed figure is
    # the body colour blended with the key: dark-GREEN pixels that survive the
    # threshold above and ring the silhouette in-game (Wildfang Raider's walk
    # carried ~2,900 of them; the PixelLab-era idle strips ~200). Only the
    # 2px band next to transparency is touched, so a subject's own greens
    # (Bog Lurker's olive) stay: rim pixels that are MOSTLY key are dropped,
    # the rest have the key channel clamped to the other two (classic despill).
    opaque = rgba[..., 3] > 0
    core = np.asarray(Image.fromarray((opaque * 255).astype(np.uint8))
                      .filter(ImageFilter.MinFilter(5))) > 0
    rim = opaque & ~core
    r, g, b = (rgba[..., i].astype(np.int16) for i in range(3))
    if key_is_magenta:
        spill = np.minimum(r, b) - g
    else:
        spill = g - np.maximum(r, b)
    rgba[..., 3][rim & (spill > 40)] = 0
    fix = rim & (spill > 8) & (rgba[..., 3] > 0)
    if key_is_magenta:
        cap = g[fix].clip(0, 255)
        rgba[..., 0][fix] = np.minimum(r[fix], cap).astype(np.uint8)
        rgba[..., 2][fix] = np.minimum(b[fix], cap).astype(np.uint8)
    else:
        rgba[..., 1][fix] = np.maximum(r, b)[fix].astype(np.uint8)
    return Image.fromarray(rgba.astype(np.uint8), "RGBA")


exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -Path 
'tools\\art\\build_preservation_walk_candidate.py' -Pattern '"'^def _infer_columns|''^def _crop_frames|''^def 
_normalize'"' -Context 0,80; Select-String -Path 'tools\\art\\build_ledgerbound_warlock.py' -Pattern '"'^def 
_hard_alpha|''^def _remove_green'"' -Context 0,70" in C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd
 succeeded in 788ms:

> tools\art\build_preservation_walk_candidate.py:79:def _infer_columns(alpha: np.ndarray) -> int:
  tools\art\build_preservation_walk_candidate.py:80:    """Count authored figures separated by broad empty vertical 
gutters."""
  tools\art\build_preservation_walk_candidate.py:81:
  tools\art\build_preservation_walk_candidate.py:82:    occupancy = alpha.any(axis=0)
  tools\art\build_preservation_walk_candidate.py:83:    used = np.flatnonzero(occupancy)
  tools\art\build_preservation_walk_candidate.py:84:    if used.size == 0:
  tools\art\build_preservation_walk_candidate.py:85:        raise ValueError("cannot infer frames from an empty keyed 
source")
  tools\art\build_preservation_walk_candidate.py:86:    left, right = int(used[0]), int(used[-1])
  tools\art\build_preservation_walk_candidate.py:87:    empty = ~occupancy[left : right + 1]
  tools\art\build_preservation_walk_candidate.py:88:    runs: list[tuple[int, int]] = []
  tools\art\build_preservation_walk_candidate.py:89:    start: int | None = None
  tools\art\build_preservation_walk_candidate.py:90:    for offset, is_empty in enumerate(empty):
  tools\art\build_preservation_walk_candidate.py:91:        if is_empty and start is None:
  tools\art\build_preservation_walk_candidate.py:92:            start = offset
  tools\art\build_preservation_walk_candidate.py:93:        elif not is_empty and start is not None:
  tools\art\build_preservation_walk_candidate.py:94:            runs.append((start + left, offset - 1 + left))
  tools\art\build_preservation_walk_candidate.py:95:            start = None
  tools\art\build_preservation_walk_candidate.py:96:    if start is not None:
  tools\art\build_preservation_walk_candidate.py:97:        runs.append((start + left, right))
  tools\art\build_preservation_walk_candidate.py:98:    # The preservation-source contract requires broad authored 
gutters. Tiny
  tools\art\build_preservation_walk_candidate.py:99:    # holes inside a silhouette are not separators; eight empty 
columns is the
  tools\art\build_preservation_walk_candidate.py:100:    # same safety floor used by _safe_edges.
  tools\art\build_preservation_walk_candidate.py:101:    separators = [run for run in runs if run[1] - run[0] + 1 >= 8]
  tools\art\build_preservation_walk_candidate.py:102:    columns = len(separators) + 1
  tools\art\build_preservation_walk_candidate.py:103:    if columns < 1:
  tools\art\build_preservation_walk_candidate.py:104:        raise ValueError("could not infer a positive frame count")
  tools\art\build_preservation_walk_candidate.py:105:    return columns
  tools\art\build_preservation_walk_candidate.py:106:
  tools\art\build_preservation_walk_candidate.py:107:
> tools\art\build_preservation_walk_candidate.py:108:def _crop_frames(
  tools\art\build_preservation_walk_candidate.py:109:    keyed: Image.Image, columns: int
  tools\art\build_preservation_walk_candidate.py:110:) -> tuple[list[Image.Image], list[int], list[int]]:
  tools\art\build_preservation_walk_candidate.py:111:    alpha = np.asarray(keyed.getchannel("A"), dtype=np.uint8) > 0
  tools\art\build_preservation_walk_candidate.py:112:    edges, gutters = _safe_edges(alpha, columns)
  tools\art\build_preservation_walk_candidate.py:113:    frames: list[Image.Image] = []
  tools\art\build_preservation_walk_candidate.py:114:    for index in range(columns):
  tools\art\build_preservation_walk_candidate.py:115:        frame = keyed.crop((edges[index], 0, edges[index + 1], 
keyed.height))
  tools\art\build_preservation_walk_candidate.py:116:        box = frame.getbbox()
  tools\art\build_preservation_walk_candidate.py:117:        if box is None:
  tools\art\build_preservation_walk_candidate.py:118:            raise ValueError(f"empty generated frame f{index + 
1}")
  tools\art\build_preservation_walk_candidate.py:119:        if box[0] <= 1 or box[2] >= frame.width - 1:
  tools\art\build_preservation_walk_candidate.py:120:            raise ValueError(f"frame f{index + 1} touches its 
horizontal cell edge")
  tools\art\build_preservation_walk_candidate.py:121:        frames.append(frame)
  tools\art\build_preservation_walk_candidate.py:122:    source_pixels = int(alpha.sum())
  tools\art\build_preservation_walk_candidate.py:123:    sliced_pixels = sum(
  tools\art\build_preservation_walk_candidate.py:124:        int((np.asarray(frame.getchannel("A"), dtype=np.uint8) > 
0).sum())
  tools\art\build_preservation_walk_candidate.py:125:        for frame in frames
  tools\art\build_preservation_walk_candidate.py:126:    )
  tools\art\build_preservation_walk_candidate.py:127:    if source_pixels != sliced_pixels:
  tools\art\build_preservation_walk_candidate.py:128:        raise ValueError(
  tools\art\build_preservation_walk_candidate.py:129:            f"non-lossless slice: source={source_pixels}, 
frames={sliced_pixels}"
  tools\art\build_preservation_walk_candidate.py:130:        )
  tools\art\build_preservation_walk_candidate.py:131:    return frames, edges, gutters
  tools\art\build_preservation_walk_candidate.py:132:
  tools\art\build_preservation_walk_candidate.py:133:
> tools\art\build_preservation_walk_candidate.py:134:def _normalize(frames: list[Image.Image], body: int) -> 
list[Image.Image]:
  tools\art\build_preservation_walk_candidate.py:135:    boxes = [frame.getbbox() for frame in frames]
  tools\art\build_preservation_walk_candidate.py:136:    if any(box is None for box in boxes):
  tools\art\build_preservation_walk_candidate.py:137:        raise ValueError("empty frame")
  tools\art\build_preservation_walk_candidate.py:138:    first = boxes[0]
  tools\art\build_preservation_walk_candidate.py:139:    assert first is not None
  tools\art\build_preservation_walk_candidate.py:140:    reference_height = first[3] - first[1]
  tools\art\build_preservation_walk_candidate.py:141:    scale = body / float(reference_height)
  tools\art\build_preservation_walk_candidate.py:142:    cell, baseline = 277, 255
  tools\art\build_preservation_walk_candidate.py:143:    normalized: list[Image.Image] = []
  tools\art\build_preservation_walk_candidate.py:144:    for index, (frame, box) in enumerate(zip(frames, boxes, 
strict=True)):
  tools\art\build_preservation_walk_candidate.py:145:        assert box is not None
  tools\art\build_preservation_walk_candidate.py:146:        figure = frame.crop(box)
  tools\art\build_preservation_walk_candidate.py:147:        size = (
  tools\art\build_preservation_walk_candidate.py:148:            max(1, round(figure.width * scale)),
  tools\art\build_preservation_walk_candidate.py:149:            max(1, round(figure.height * scale)),
  tools\art\build_preservation_walk_candidate.py:150:        )
  tools\art\build_preservation_walk_candidate.py:151:        figure = _hard_alpha(figure.resize(size, 
Image.Resampling.LANCZOS))
  tools\art\build_preservation_walk_candidate.py:152:        if figure.width > cell - 8 or figure.height > baseline - 
4:
  tools\art\build_preservation_walk_candidate.py:153:            raise ValueError(f"normalized f{index + 1} exceeds 
staging cell: {figure.size}")
  tools\art\build_preservation_walk_candidate.py:154:        canvas = Image.new("RGBA", (cell, cell), (0, 0, 0, 0))
  tools\art\build_preservation_walk_candidate.py:155:        canvas.alpha_composite(figure, ((cell - figure.width) // 
2, baseline - figure.height))
  tools\art\build_preservation_walk_candidate.py:156:        normalized.append(canvas)
  tools\art\build_preservation_walk_candidate.py:157:    return normalized
  tools\art\build_preservation_walk_candidate.py:158:
  tools\art\build_preservation_walk_candidate.py:159:
  tools\art\build_preservation_walk_candidate.py:160:def _shown(frame: Image.Image, body: int, cell: int) -> 
Image.Image:
  tools\art\build_preservation_walk_candidate.py:161:    box = frame.getbbox()
  tools\art\build_preservation_walk_candidate.py:162:    if box is None:
  tools\art\build_preservation_walk_candidate.py:163:        raise ValueError("empty normalized frame")
  tools\art\build_preservation_walk_candidate.py:164:    figure = frame.crop(box)
  tools\art\build_preservation_walk_candidate.py:165:    scale = body / float(figure.height)
  tools\art\build_preservation_walk_candidate.py:166:    figure = figure.resize(
  tools\art\build_preservation_walk_candidate.py:167:        (max(1, round(figure.width * scale)), body), 
Image.Resampling.LANCZOS
  tools\art\build_preservation_walk_candidate.py:168:    )
  tools\art\build_preservation_walk_candidate.py:169:    canvas = Image.new("RGBA", (cell, cell), (25, 28, 34, 255))
  tools\art\build_preservation_walk_candidate.py:170:    canvas.alpha_composite(figure, ((cell - figure.width) // 2, 
cell - figure.height - 10))
  tools\art\build_preservation_walk_candidate.py:171:    return canvas
  tools\art\build_preservation_walk_candidate.py:172:
  tools\art\build_preservation_walk_candidate.py:173:
  tools\art\build_preservation_walk_candidate.py:174:def _write_cut_overlay(
  tools\art\build_preservation_walk_candidate.py:175:    path: Path, keyed: Image.Image, edges: list[int], gutters: 
list[int]
  tools\art\build_preservation_walk_candidate.py:176:) -> None:
  tools\art\build_preservation_walk_candidate.py:177:    canvas = Image.new("RGBA", keyed.size, (25, 28, 34, 255))
  tools\art\build_preservation_walk_candidate.py:178:    canvas.alpha_composite(keyed)
  tools\art\build_preservation_walk_candidate.py:179:    draw = ImageDraw.Draw(canvas)
  tools\art\build_preservation_walk_candidate.py:180:    for index, edge in enumerate(edges[1:-1], start=1):
  tools\art\build_preservation_walk_candidate.py:181:        draw.line((edge, 0, edge, keyed.height - 1), fill=(255, 
72, 72, 255), width=2)
  tools\art\build_preservation_walk_candidate.py:182:        draw.text(
  tools\art\build_preservation_walk_candidate.py:183:            (max(2, edge - 32), 5),
  tools\art\build_preservation_walk_candidate.py:184:            f"{index}|{index + 1} g{gutters[index - 1]}",
  tools\art\build_preservation_walk_candidate.py:185:            font=_font(14),
  tools\art\build_preservation_walk_candidate.py:186:            fill=(255, 230, 120, 255),
  tools\art\build_preservation_walk_candidate.py:187:        )
  tools\art\build_preservation_walk_candidate.py:188:    canvas.save(path)
  tools\art\build_preservation_walk_candidate.py:189:
  tools\art\build_preservation_walk_candidate.py:190:
  tools\art\build_preservation_walk_candidate.py:191:def _write_qa(
  tools\art\build_preservation_walk_candidate.py:192:    output_dir: Path,
  tools\art\build_preservation_walk_candidate.py:193:    stem: str,
  tools\art\build_preservation_walk_candidate.py:194:    label: str,
  tools\art\build_preservation_walk_candidate.py:195:    frames: list[Image.Image],
  tools\art\build_preservation_walk_candidate.py:196:    fps: float,
  tools\art\build_preservation_walk_candidate.py:197:    opposite_contact: int,
  tools\art\build_preservation_walk_candidate.py:198:) -> None:
  tools\art\build_preservation_walk_candidate.py:199:    source_cell, header = 220, 42
  tools\art\build_preservation_walk_candidate.py:200:    contact = Image.new(
  tools\art\build_preservation_walk_candidate.py:201:        "RGBA", (source_cell * len(frames), source_cell + 
header), (25, 28, 34, 255)
  tools\art\build_preservation_walk_candidate.py:202:    )
  tools\art\build_preservation_walk_candidate.py:203:    draw = ImageDraw.Draw(contact)
  tools\art\build_preservation_walk_candidate.py:204:    draw.text((10, 8), label, font=_font(18), fill=(255, 224, 
126, 255))
  tools\art\build_preservation_walk_candidate.py:205:    source_pages: list[Image.Image] = []
  tools\art\build_preservation_walk_candidate.py:206:    actual_pages: list[Image.Image] = []
  tools\art\build_preservation_walk_candidate.py:207:    for index, frame in enumerate(frames):
  tools\art\build_preservation_walk_candidate.py:208:        shown = _shown(frame, 180, source_cell)
  tools\art\build_preservation_walk_candidate.py:209:        contact.alpha_composite(shown, (index * source_cell, 
header))
  tools\art\build_preservation_walk_candidate.py:210:        draw.text(
  tools\art\build_preservation_walk_candidate.py:211:            (index * source_cell + 7, header + 5),
  tools\art\build_preservation_walk_candidate.py:212:            f"f{index + 1}",
  tools\art\build_preservation_walk_candidate.py:213:            font=_font(14),
  tools\art\build_preservation_walk_candidate.py:214:            fill=(255, 224, 126, 255),
> tools\art\build_ledgerbound_warlock.py:68:def _remove_green(image: Image.Image) -> Image.Image:
  tools\art\build_ledgerbound_warlock.py:69:    """Hard-key ImageGen's green field and neutralize surviving green 
spill."""
  tools\art\build_ledgerbound_warlock.py:70:
  tools\art\build_ledgerbound_warlock.py:71:    arr = np.asarray(image.convert("RGBA"), dtype=np.uint8).copy()
  tools\art\build_ledgerbound_warlock.py:72:    rgb = arr[..., :3].astype(np.float32)
  tools\art\build_ledgerbound_warlock.py:73:    red, green, blue = rgb[..., 0], rgb[..., 1], rgb[..., 2]
  tools\art\build_ledgerbound_warlock.py:74:    keyed = (
  tools\art\build_ledgerbound_warlock.py:75:        (green > 72.0)
  tools\art\build_ledgerbound_warlock.py:76:        & (green > red * 1.14 + 12.0)
  tools\art\build_ledgerbound_warlock.py:77:        & (green > blue * 1.14 + 12.0)
  tools\art\build_ledgerbound_warlock.py:78:    )
  tools\art\build_ledgerbound_warlock.py:79:    arr[..., 3] = np.where(keyed, 0, 255).astype(np.uint8)
  tools\art\build_ledgerbound_warlock.py:80:    visible = ~keyed
  tools\art\build_ledgerbound_warlock.py:81:    neutral_green = np.maximum(arr[..., 0], arr[..., 2]).astype(np.uint16) 
+ 10
  tools\art\build_ledgerbound_warlock.py:82:    arr[..., 1] = np.where(
  tools\art\build_ledgerbound_warlock.py:83:        visible,
  tools\art\build_ledgerbound_warlock.py:84:        np.minimum(arr[..., 1].astype(np.uint16), neutral_green),
  tools\art\build_ledgerbound_warlock.py:85:        0,
  tools\art\build_ledgerbound_warlock.py:86:    ).astype(np.uint8)
  tools\art\build_ledgerbound_warlock.py:87:    arr[keyed, :3] = 0
  tools\art\build_ledgerbound_warlock.py:88:    return Image.fromarray(arr, "RGBA")
  tools\art\build_ledgerbound_warlock.py:89:
  tools\art\build_ledgerbound_warlock.py:90:
  tools\art\build_ledgerbound_warlock.py:91:def _valley_separators(
  tools\art\build_ledgerbound_warlock.py:92:    mask: np.ndarray,
  tools\art\build_ledgerbound_warlock.py:93:    count: int,
  tools\art\build_ledgerbound_warlock.py:94:    axis: int,
  tools\art\build_ledgerbound_warlock.py:95:    weight_power: float = 1.0,
  tools\art\build_ledgerbound_warlock.py:96:) -> list[int]:
  tools\art\build_ledgerbound_warlock.py:97:    """Place cuts in real empty gutters rather than nominal equal cells."""
  tools\art\build_ledgerbound_warlock.py:98:
  tools\art\build_ledgerbound_warlock.py:99:    extent = mask.shape[axis]
  tools\art\build_ledgerbound_warlock.py:100:    if count == 1:
  tools\art\build_ledgerbound_warlock.py:101:        return [0, extent]
  tools\art\build_ledgerbound_warlock.py:102:    projection = mask.sum(axis=1 - axis).astype(np.float64)
  tools\art\build_ledgerbound_warlock.py:103:    coords = np.arange(extent, dtype=np.float64)
  tools\art\build_ledgerbound_warlock.py:104:    weights = np.power(projection, weight_power)
  tools\art\build_ledgerbound_warlock.py:105:    total = weights.sum()
  tools\art\build_ledgerbound_warlock.py:106:    if total <= 0:
  tools\art\build_ledgerbound_warlock.py:107:        return [round(index * extent / count) for index in range(count + 
1)]
  tools\art\build_ledgerbound_warlock.py:108:
  tools\art\build_ledgerbound_warlock.py:109:    cumulative = np.cumsum(weights)
  tools\art\build_ledgerbound_warlock.py:110:    centers = np.array(
  tools\art\build_ledgerbound_warlock.py:111:        [
  tools\art\build_ledgerbound_warlock.py:112:            np.searchsorted(cumulative, total * (index + 0.5) / count)
  tools\art\build_ledgerbound_warlock.py:113:            for index in range(count)
  tools\art\build_ledgerbound_warlock.py:114:        ],
  tools\art\build_ledgerbound_warlock.py:115:        dtype=np.float64,
  tools\art\build_ledgerbound_warlock.py:116:    )
  tools\art\build_ledgerbound_warlock.py:117:    for _ in range(24):
  tools\art\build_ledgerbound_warlock.py:118:        assignment = np.argmin(
  tools\art\build_ledgerbound_warlock.py:119:            np.abs(coords[:, None] - centers[None, :]),
  tools\art\build_ledgerbound_warlock.py:120:            axis=1,
  tools\art\build_ledgerbound_warlock.py:121:        )
  tools\art\build_ledgerbound_warlock.py:122:        updated = centers.copy()
  tools\art\build_ledgerbound_warlock.py:123:        for index in range(count):
  tools\art\build_ledgerbound_warlock.py:124:            selected = assignment == index
  tools\art\build_ledgerbound_warlock.py:125:            weight = weights[selected].sum()
  tools\art\build_ledgerbound_warlock.py:126:            if weight > 0:
  tools\art\build_ledgerbound_warlock.py:127:                updated[index] = (
  tools\art\build_ledgerbound_warlock.py:128:                    coords[selected] * weights[selected]
  tools\art\build_ledgerbound_warlock.py:129:                ).sum() / weight
  tools\art\build_ledgerbound_warlock.py:130:        if np.max(np.abs(updated - centers)) < 0.05:
  tools\art\build_ledgerbound_warlock.py:131:            centers = updated
  tools\art\build_ledgerbound_warlock.py:132:            break
  tools\art\build_ledgerbound_warlock.py:133:        centers = updated
  tools\art\build_ledgerbound_warlock.py:134:
  tools\art\build_ledgerbound_warlock.py:135:    separators = [0]
  tools\art\build_ledgerbound_warlock.py:136:    for index in range(1, count):
  tools\art\build_ledgerbound_warlock.py:137:        target = round((centers[index - 1] + centers[index]) / 2.0)
  tools\art\build_ledgerbound_warlock.py:138:        lo = max(separators[-1] + 1, round(centers[index - 1]))
> tools\art\build_ledgerbound_warlock.py:336:def _hard_alpha(image: Image.Image) -> Image.Image:
  tools\art\build_ledgerbound_warlock.py:337:    arr = np.asarray(image.convert("RGBA"), dtype=np.uint8).copy()
  tools\art\build_ledgerbound_warlock.py:338:    opaque = arr[..., 3] > 24
  tools\art\build_ledgerbound_warlock.py:339:    arr[..., 3] = np.where(opaque, 255, 0).astype(np.uint8)
  tools\art\build_ledgerbound_warlock.py:340:    arr[~opaque, :3] = 0
  tools\art\build_ledgerbound_warlock.py:341:    return Image.fromarray(arr, "RGBA")
  tools\art\build_ledgerbound_warlock.py:342:
  tools\art\build_ledgerbound_warlock.py:343:
  tools\art\build_ledgerbound_warlock.py:344:def _normalize_clip(
  tools\art\build_ledgerbound_warlock.py:345:    directions: dict[str, list[Image.Image]],
  tools\art\build_ledgerbound_warlock.py:346:) -> dict[str, list[Image.Image]]:
  tools\art\build_ledgerbound_warlock.py:347:    scales: dict[str, float] = {}
  tools\art\build_ledgerbound_warlock.py:348:    for suffix, frames in directions.items():
  tools\art\build_ledgerbound_warlock.py:349:        bbox = frames[0].getbbox()
  tools\art\build_ledgerbound_warlock.py:350:        if bbox is None:
  tools\art\build_ledgerbound_warlock.py:351:            raise ValueError(f"empty first frame in {suffix}")
  tools\art\build_ledgerbound_warlock.py:352:        scales[suffix] = TARGET_STANDING_BODY / float(bbox[3] - bbox[1])
  tools\art\build_ledgerbound_warlock.py:353:
  tools\art\build_ledgerbound_warlock.py:354:    normalized: dict[str, list[Image.Image]] = {}
  tools\art\build_ledgerbound_warlock.py:355:    for suffix, frames in directions.items():
  tools\art\build_ledgerbound_warlock.py:356:        out_frames: list[Image.Image] = []
  tools\art\build_ledgerbound_warlock.py:357:        for index, frame in enumerate(frames):
  tools\art\build_ledgerbound_warlock.py:358:            bbox = frame.getbbox()
  tools\art\build_ledgerbound_warlock.py:359:            if bbox is None:
  tools\art\build_ledgerbound_warlock.py:360:                raise ValueError(f"empty frame {suffix}:{index}")
  tools\art\build_ledgerbound_warlock.py:361:            figure = frame.crop(bbox)
  tools\art\build_ledgerbound_warlock.py:362:            width = max(1, round(figure.width * scales[suffix]))
  tools\art\build_ledgerbound_warlock.py:363:            height = max(1, round(figure.height * scales[suffix]))
  tools\art\build_ledgerbound_warlock.py:364:            figure = _hard_alpha(
  tools\art\build_ledgerbound_warlock.py:365:                figure.resize((width, height), Image.Resampling.LANCZOS)
  tools\art\build_ledgerbound_warlock.py:366:            )
  tools\art\build_ledgerbound_warlock.py:367:            if width > STAGING_CELL - 8 or height > STAGING_BASELINE - 4:
  tools\art\build_ledgerbound_warlock.py:368:                raise ValueError(
  tools\art\build_ledgerbound_warlock.py:369:                    f"frame {suffix}:{index} does not fit staging cell: "
  tools\art\build_ledgerbound_warlock.py:370:                    f"{width}x{height}"
  tools\art\build_ledgerbound_warlock.py:371:                )
  tools\art\build_ledgerbound_warlock.py:372:            canvas = Image.new(
  tools\art\build_ledgerbound_warlock.py:373:                "RGBA", (STAGING_CELL, STAGING_CELL), (0, 0, 0, 0)
  tools\art\build_ledgerbound_warlock.py:374:            )
  tools\art\build_ledgerbound_warlock.py:375:            x = (STAGING_CELL - width) // 2
  tools\art\build_ledgerbound_warlock.py:376:            y = STAGING_BASELINE - height
  tools\art\build_ledgerbound_warlock.py:377:            canvas.alpha_composite(figure, (x, y))
  tools\art\build_ledgerbound_warlock.py:378:            out_frames.append(canvas)
  tools\art\build_ledgerbound_warlock.py:379:        normalized[suffix] = out_frames
  tools\art\build_ledgerbound_warlock.py:380:    return normalized
  tools\art\build_ledgerbound_warlock.py:381:
  tools\art\build_ledgerbound_warlock.py:382:
  tools\art\build_ledgerbound_warlock.py:383:def _expected_character_paths() -> set[Path]:
  tools\art\build_ledgerbound_warlock.py:384:    dirs = ("s", "se", "e", "ne", "n", "nw", "w", "sw")
  tools\art\build_ledgerbound_warlock.py:385:    expected = {OUT_DIR / "warlock.png", OUT_DIR / "warlock_death.png"}
  tools\art\build_ledgerbound_warlock.py:386:    for stem in ("anim", "walk", "run", "attack", "attack2", "cast", 
"ult"):
  tools\art\build_ledgerbound_warlock.py:387:        expected.add(OUT_DIR / f"warlock_{stem}.png")
  tools\art\build_ledgerbound_warlock.py:388:        expected.update(OUT_DIR / f"warlock_{stem}_{suffix}.png" for 
suffix in dirs)
  tools\art\build_ledgerbound_warlock.py:389:    return expected
  tools\art\build_ledgerbound_warlock.py:390:
  tools\art\build_ledgerbound_warlock.py:391:
  tools\art\build_ledgerbound_warlock.py:392:def _validate_output(cell: int) -> None:
  tools\art\build_ledgerbound_warlock.py:393:    expected = _expected_character_paths()
  tools\art\build_ledgerbound_warlock.py:394:    if len(expected) != 65:
  tools\art\build_ledgerbound_warlock.py:395:        raise AssertionError(f"internal contract error: {len(expected)} 
files")
  tools\art\build_ledgerbound_warlock.py:396:    missing = sorted(path for path in expected if not path.exists())
  tools\art\build_ledgerbound_warlock.py:397:    if missing:
  tools\art\build_ledgerbound_warlock.py:398:        raise ValueError(f"missing Warlock runtime files: {missing}")
  tools\art\build_ledgerbound_warlock.py:399:    for path in sorted(expected):
  tools\art\build_ledgerbound_warlock.py:400:        image = Image.open(path).convert("RGBA")
  tools\art\build_ledgerbound_warlock.py:401:        if image.height != cell or image.width % cell != 0:
  tools\art\build_ledgerbound_warlock.py:402:            raise ValueError(f"bad strip geometry: {path} {image.size}")
  tools\art\build_ledgerbound_warlock.py:403:        expected_frames = 1 if path.name == "warlock.png" else None
  tools\art\build_ledgerbound_warlock.py:404:        for stem, frames in CLIP_FRAMES.items():
  tools\art\build_ledgerbound_warlock.py:405:            if path.name == f"{stem}.png" or 
path.name.startswith(f"{stem}_"):
  tools\art\build_ledgerbound_warlock.py:406:                expected_frames = frames



exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-ChildItem -LiteralPath 'tools\\art' 
-Filter '*.py' -Recurse | Select-String -Pattern 'Image\\.new\\(.*\\(0, 255, 0|Image\\.new\\(.*\\(0,255,0|GREEN = 
\\(0, 255, 0' | Select-Object Path,LineNumber,Line" in C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd
 succeeded in 2993ms:

Path                                                                                             LineNumber Line       
----                                                                                             ---------- ----       
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\alt_swing_pipeline.py        214     bg =...
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\attack_walk_briefs.py         39 GREEN = ...
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\gait_briefs.py                45 GREEN = ...
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\grid_to_row.py                14 GREEN = ...
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\normalize_row.py              13 GREEN = ...
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\profile_clip_brief...         33 GREEN = ...
C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\tools\art\split_legacy_walk_...         50         ...



exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw -LiteralPath 
'tools\\art\\normalize_row.py'; Get-Content -LiteralPath 'tools\\art\\attack_walk_briefs.py' | Select-Object -First 
220" in C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd
 succeeded in 728ms:
#!/usr/bin/env python
"""Equalise figure heights in a Codex row (the "two small figures crammed into
one cell" defect): gutter-slice the green row, scale every figure to the
median figure height, re-lay them evenly on fresh green. Feed the result to
install_gait_row.

    python tools/art/normalize_row.py <row.png> <out.png> [--gap N]
"""
import argparse
import numpy as np
from PIL import Image

GREEN = (0, 255, 0, 255)


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("row")
    ap.add_argument("out")
    ap.add_argument("--gap", type=int, default=6, help="content runs closer than this merge")
    ap.add_argument("--pad", type=int, default=40)
    a = ap.parse_args()
    im = Image.open(a.row).convert("RGBA")
    arr = np.array(im)
    g = (arr[:, :, 1] > 180) & (arr[:, :, 0] < 110) & (arr[:, :, 2] < 110)
    fg = ~g & (arr[:, :, 3] > 0)
    cols = fg.any(axis=0)
    runs, on, s = [], False, 0
    for x, c in enumerate(cols):
        if c and not on:
            on, s = True, x
        elif not c and on:
            on = False
            runs.append([s, x])
    if on:
        runs.append([s, len(cols)])
    merged = []
    for r in runs:
        if merged and r[0] - merged[-1][1] < a.gap:
            merged[-1][1] = r[1]
        else:
            merged.append(r)
    merged = [r for r in merged if r[1] - r[0] > 12]
    figs = []
    for x0, x1 in merged:
        ys = np.nonzero(fg[:, x0:x1].any(axis=1))[0]
        figs.append(im.crop((x0, int(ys.min()), x1, int(ys.max()) + 1)))
    hs = sorted(f.height for f in figs)
    target = hs[len(hs) // 2]
    scaled = []
    for f in figs:
        k = target / f.height
        scaled.append(f.resize((max(1, round(f.width * k)), target), Image.LANCZOS) if abs(k - 1) > 0.02 else f)
    cell = max(f.width for f in scaled) + a.pad
    out = Image.new("RGBA", (cell * len(scaled) + a.pad, target + 2 * a.pad), GREEN)
    for i, f in enumerate(scaled):
        out.alpha_composite(f, (a.pad + i * cell + (cell - a.pad - f.width) // 2, a.pad))
    out.save(a.out)
    print(f"{len(figs)} figures, heights {[f.height for f in figs]} -> {target}; wrote {a.out} {out.size}")


if __name__ == "__main__":
    main()

#!/usr/bin/env python
"""FIRE-ON-MOVE (`<base>_attack_walk[_b]_<dir>`) briefs for the BASE classes.

`player.gd`'s walk_fire plays a walk cycle that fires the basic mid-stride, so
spamming a low-cooldown ranged basic WHILE MOVING does not lock the standing
pose sliding over the floor. It is art-driven: no strip, no feature. As of
2026-09-03 every one of the 72 attack_walk PNGs in the tree lives under
`skins/elite/` -- the six BASE classes ship none, so the feature has been dark
for every default character since it shipped.

THE TECHNIQUE (CLAUDE.md, owner 2026-08-23 -- the one that finally worked):
it is the WALK clip plus a firing arm. Do NOT author a walk from scratch; a
from-scratch brief, however detailed the storyboard, kept gliding or failing to
cross the legs. Hand over the character's OWN `<base>_walk_<dir>` strip and say
"reproduce this exact walk, change ONLY the free hand". Keep the brief SHORT --
the walk reference IS the leg specification.

    python tools/art/attack_walk_briefs.py <stage_root> [--classes mage,warlock] [--b]

Per CLAUDE.md's manifest the feature is wanted by the ranged spammables only:
mage Firebolt, warlock Shadowbolt, archer Quick Shot (a1) and assassin Fan of
Knives (a3). The archer gets NO `_b` variant (a bow draw has one proper form).
Author S/E/N; the installer copies E to SE/NE and mirrors it for W/NW/SW, and
flat = S.
"""
from __future__ import annotations

import argparse
import json
import shutil
import sys
from pathlib import Path

import numpy as np
from PIL import Image

ROOT = Path(__file__).resolve().parents[2]
SPR = ROOT / "game" / "assets" / "sprites"
GREEN = (0, 255, 0, 255)

ROBE = (
    "- The robe stays CLOSED and floor-length: no slit, no exposed legs, no bare\n"
    "  skin. The walk reads through the BOOTS alternating below the hem and the\n"
    "  hem swaying -- exactly as reference 1 shows it."
)
OUTFIT = (
    "- The outfit stays exactly as reference 1 shows it: same cloak/coat length,\n"
    "  same straps and buckles in the same places, no bare skin the reference\n"
    "  does not have."
)

# class -> (identity words, PRIMARY fire motion, _b motion or None, garment clause)
CLASSES = {
    "mage": (
        "a slender woman in a deep-blue hooded mage's robe with gold trim and a gold sash, "
        "a long dark staff with a glowing blue-white crystal head held in one hand, "
        "dark boots below a CLOSED floor-length hem",
        "the FREE hand (not the staff hand) thrusts forward at chest height, palm open, "
        "as if pushing a bolt away",
        "the FREE hand sweeps upward in a rising flick, fingers spread, ending above the "
        "shoulder -- keep it clearly separate from the staff hand so the two never read as one",
        ROBE,
    ),
    "warlock": (
        "a gaunt man in a heavy black hooded robe with a gold-edged collar and a red gem clasp, "
        "a small floating flaming skull companion beside his shoulder, "
        "dark boots below a CLOSED floor-length hem",
        "the FREE hand thrusts forward at chest height, palm open and fingers splayed",
        "BOTH hands shove forward together at chest height, palms out",
        ROBE,
    ),
    "archer": (
        "a woman in a green hooded cloak over brown leather armour, a quiver on her back, "
        "a wooden recurve bow held in one hand, dark boots",
        "the bow comes up and the free hand draws the string back to the cheek and releases -- "
        "NO arrow is drawn on the sprite",
        None,   # a bow draw has ONE proper form (owner 2026-08-23)
        OUTFIT,
    ),
    "assassin": (
        "a lean hooded figure in a dark layered coat with leather straps and buckles, "
        "a dagger at the belt, dark boots",
        "the free hand throws a knife overhand, arm coming over the shoulder and snapping "
        "forward -- NO knife is drawn in the air",
        "the free hand flicks a knife sidearm, arm sweeping low across the body -- "
        "NO knife is drawn in the air",
        OUTFIT,
    ),
}

VIEW = {"s": "toward the viewer", "e": "in profile, facing RIGHT",
        "n": "away from the viewer (back to camera)"}


def walk_row(base: str, facing: str, out: Path) -> int:
    """The character's own walk strip as a labelled row on flat green ├óΓé¼ΓÇ¥ the leg spec."""
    for cand in [f"{base}_walk_{facing}", f"{base}_walk"]:
        p = SPR / f"{cand}.png"
        if not p.exists():
            continue
        im = Image.open(p).convert("RGBA")
        c = im.height
        n = max(1, im.width // c)
        a = np.array(im)[:, :, 3] > 40
        ys = np.nonzero(a)[0]
        top, bot = int(ys.min()), int(ys.max())
        pad = 26
        row = Image.new("RGBA", (n * (c + pad) + pad, (bot - top + 1) + 2 * pad), GREEN)
        for f in range(n):
            row.alpha_composite(im.crop((f * c, top, (f + 1) * c, bot + 1)),
                                (pad + f * (c + pad), pad))
        row.save(out)
        return n
    raise SystemExit(f"no walk strip for {base} {facing}")


BRIEF = """Redraw this EXACT walk cycle (reference 1) with one change: the character fires
its basic attack once during the stride.

Reference 1 is the character's own {n}-frame walk, {view}, frame by frame. Reproduce
it EXACTLY: the same {n} frames, the same leg cycle and stride, the same crossing,
the same garment, the same held equipment in the same hand, the same body height
and width, the same palette. The legs are already correct -- do not redesign them.

THE ONLY CHANGE: {motion}. Spread the motion across the {n} frames so it starts,
peaks around the middle and returns by the end, while the legs keep walking
through their unchanged cycle.

THE CHARACTER (unchanged from reference 1): {ident}.

Hard requirements:
- The feet stay PLANTED on the shared ground line exactly as reference 1 has them;
  the body does not slide sideways within its cell.
{garment}
- Draw NO projectile, arrow, knife, flame, bolt, spark or muzzle flash anywhere.
  The game spawns the projectile itself; anything drawn here would double it.
- Same body height and width in every frame; no frame is a copy of another.

Produce ONE image: a horizontal row of EXACTLY {n} figures, evenly spaced, all feet
on one shared ground line, each body near its cell's horizontal centre, green
margins all round -- no figure touching an image edge or its neighbour. EVERY FIGURE
IS THE SAME SIZE; if they will not fit at full size, make the image WIDER. Flat solid
#00ff00 GREEN background, nothing else in the image.
Generate ONE image and SAVE it to disk at the exact path
{out}
then stop.

In your final message, report for each frame: which leg leads, and where the firing
hand is. Confirm no projectile was drawn and the hem stayed closed.
"""


def main() -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("stage_root")
    ap.add_argument("--classes", default="mage,warlock,archer,assassin")
    ap.add_argument("--facings", default="e,s,n")
    ap.add_argument("--b", action="store_true", help="the alternate _b variant instead")
    args = ap.parse_args()
    root = Path(args.stage_root)
    root.mkdir(parents=True, exist_ok=True)
    stages = []
    for base in [c.strip() for c in args.classes.split(",") if c.strip()]:
        ident, prim, alt, garment = CLASSES[base]
        motion = alt if args.b else prim
        if motion is None:
            print(f"skip {base}: no _b variant by design")
            continue
        clip = "attack_walk_b" if args.b else "attack_walk"
        for facing in [f.strip() for f in args.facings.split(",") if f.strip()]:
            stage = root / f"{base}__{clip}__{facing}"
            refs = stage / "refs"
            refs.mkdir(parents=True, exist_ok=True)
            n = walk_row(base, facing, refs / "1_walk.png")
            out = (stage / f"{base}_{clip}_{facing}_row.png").resolve()
            (stage / "codex_brief.txt").write_text(
                BRIEF.format(n=n, view=VIEW[facing], motion=motion, ident=ident,
                             garment=garment, out=out),
                encoding="utf-8")
            (stage / "job.json").write_text(json.dumps(
                {"base": base, "clip": clip, "facing": facing, "frames": n}, indent=1),
                encoding="utf-8")
            stages.append(str(stage.resolve()))
            print(f"stage {base} {clip} {facing} ({n} frames)")
    (root / "stages.txt").write_text("\n".join(stages) + "\n", encoding="utf-8")
    print(f"{len(stages)} stages -> {root / 'stages.txt'}")
    return 0


if __name__ == "__main__":
    sys.exit(main())

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -Path 
'tools\\art\\rebuild_preservation_archer_alpha.py' -Pattern '"'^def _connected_key'"' -Context 0,95; Add-Type 
-AssemblyName System.Drawing; "'$ref='"'art_src\\warlock_aw_n_2026-09-05\\warlock__attack_walk__n\\refs\\1_walk.png'; 
"'$bmp=[System.Drawing.Bitmap]::FromFile((Resolve-Path $ref)); Write-Output ("ref {0}x{1}" -f $bmp.Width,$bmp.Height); 
$bmp.Dispose()' in C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd
 succeeded in 1196ms:

> tools\art\rebuild_preservation_archer_alpha.py:66:def _connected_key(image: Image.Image) -> tuple[Image.Image, 
tuple[int, int, int], int]:
  tools\art\rebuild_preservation_archer_alpha.py:67:    rgba = np.asarray(image.convert("RGBA"), dtype=np.uint8).copy()
  tools\art\rebuild_preservation_archer_alpha.py:68:    rgb = rgba[..., :3].astype(np.float32)
  tools\art\rebuild_preservation_archer_alpha.py:69:    key = _border_key(image)
  tools\art\rebuild_preservation_archer_alpha.py:70:    key_max = float(key.max())
  tools\art\rebuild_preservation_archer_alpha.py:71:    key_channels = [
  tools\art\rebuild_preservation_archer_alpha.py:72:        index
  tools\art\rebuild_preservation_archer_alpha.py:73:        for index, value in enumerate(key)
  tools\art\rebuild_preservation_archer_alpha.py:74:        if value >= key_max - 32.0 and value >= 128.0
  tools\art\rebuild_preservation_archer_alpha.py:75:    ]
  tools\art\rebuild_preservation_archer_alpha.py:76:    other_channels = [index for index in range(3) if index not in 
key_channels]
  tools\art\rebuild_preservation_archer_alpha.py:77:    distance = np.max(np.abs(rgb - key.reshape((1, 1, 3))), axis=2)
  tools\art\rebuild_preservation_archer_alpha.py:78:    if key_channels:
  tools\art\rebuild_preservation_archer_alpha.py:79:        key_strength = np.min(rgb[..., key_channels], axis=2)
  tools\art\rebuild_preservation_archer_alpha.py:80:        other_strength = (
  tools\art\rebuild_preservation_archer_alpha.py:81:            np.max(rgb[..., other_channels], axis=2)
  tools\art\rebuild_preservation_archer_alpha.py:82:            if other_channels
  tools\art\rebuild_preservation_archer_alpha.py:83:            else np.zeros(rgb.shape[:2], dtype=np.float32)
  tools\art\rebuild_preservation_archer_alpha.py:84:        )
  tools\art\rebuild_preservation_archer_alpha.py:85:        dominance = (key_strength > 72.0) & (key_strength > 
other_strength + 24.0)
  tools\art\rebuild_preservation_archer_alpha.py:86:    else:
  tools\art\rebuild_preservation_archer_alpha.py:87:        dominance = np.zeros(rgb.shape[:2], dtype=bool)
  tools\art\rebuild_preservation_archer_alpha.py:88:    close_key = distance <= 84.0
  tools\art\rebuild_preservation_archer_alpha.py:89:
  tools\art\rebuild_preservation_archer_alpha.py:90:    # Flood only the key-like component connected to the outer 
field. Identical
  tools\art\rebuild_preservation_archer_alpha.py:91:    # green values enclosed by Archer's dark cape outline are 
never selected.
  tools\art\rebuild_preservation_archer_alpha.py:92:    mask = Image.fromarray(np.where(dominance, 255, 
0).astype(np.uint8), "L")
  tools\art\rebuild_preservation_archer_alpha.py:93:    draw = ImageDraw.Draw(mask)
  tools\art\rebuild_preservation_archer_alpha.py:94:    width, height = mask.size
  tools\art\rebuild_preservation_archer_alpha.py:95:    for point in ((0, 0), (width - 1, 0), (0, height - 1), (width 
- 1, height - 1)):
  tools\art\rebuild_preservation_archer_alpha.py:96:        if mask.getpixel(point) == 255:
  tools\art\rebuild_preservation_archer_alpha.py:97:            ImageDraw.floodfill(mask, point, 128, thresh=0)
  tools\art\rebuild_preservation_archer_alpha.py:98:    connected_background = np.asarray(mask, dtype=np.uint8) == 128
  tools\art\rebuild_preservation_archer_alpha.py:99:    # Exact/near-exact key pixels are background even when 
enclosed by a bow or
  tools\art\rebuild_preservation_archer_alpha.py:100:    # a gap between limbs. Broader green-dominant pixels are 
removed only when
  tools\art\rebuild_preservation_archer_alpha.py:101:    # connected to the outer field, which protects the darker 
green cape.
  tools\art\rebuild_preservation_archer_alpha.py:102:    background = close_key | connected_background
  tools\art\rebuild_preservation_archer_alpha.py:103:    if not all((background[0, 0], background[0, -1], 
background[-1, 0], background[-1, -1])):
  tools\art\rebuild_preservation_archer_alpha.py:104:        raise ValueError("connected-key removal did not reach 
every source corner")
  tools\art\rebuild_preservation_archer_alpha.py:105:
  tools\art\rebuild_preservation_archer_alpha.py:106:    rgba[..., 3] = np.where(background, 0, 255).astype(np.uint8)
  tools\art\rebuild_preservation_archer_alpha.py:107:    rgba[background, :3] = 0
  tools\art\rebuild_preservation_archer_alpha.py:108:    keyed = Image.fromarray(rgba, "RGBA")
  tools\art\rebuild_preservation_archer_alpha.py:109:    return keyed, tuple(int(round(value)) for value in key), 
int(background.sum())
  tools\art\rebuild_preservation_archer_alpha.py:110:
  tools\art\rebuild_preservation_archer_alpha.py:111:
  tools\art\rebuild_preservation_archer_alpha.py:112:def _write_candidate(
  tools\art\rebuild_preservation_archer_alpha.py:113:    output_dir: Path,
  tools\art\rebuild_preservation_archer_alpha.py:114:    stem: str,
  tools\art\rebuild_preservation_archer_alpha.py:115:    source: Path,
  tools\art\rebuild_preservation_archer_alpha.py:116:    frames_count: int,
  tools\art\rebuild_preservation_archer_alpha.py:117:    fps: float,
  tools\art\rebuild_preservation_archer_alpha.py:118:) -> None:
  tools\art\rebuild_preservation_archer_alpha.py:119:    keyed, key, removed = _connected_key(Image.open(source))
  tools\art\rebuild_preservation_archer_alpha.py:120:    keyed = _hard_alpha(keyed)
  tools\art\rebuild_preservation_archer_alpha.py:121:    keyed.save(output_dir / f"{stem}_keyed.png")
  tools\art\rebuild_preservation_archer_alpha.py:122:    frames, edges, gutters = _crop_frames(keyed, frames_count)
  tools\art\rebuild_preservation_archer_alpha.py:123:    _write_cut_overlay(output_dir / f"{stem}_cuts.png", keyed, 
edges, gutters)
  tools\art\rebuild_preservation_archer_alpha.py:124:    normalized = _normalize(frames, 180)
  tools\art\rebuild_preservation_archer_alpha.py:125:    strip = Image.new("RGBA", (277 * frames_count, 277), (0, 0, 
0, 0))
  tools\art\rebuild_preservation_archer_alpha.py:126:    for index, frame in enumerate(normalized):
  tools\art\rebuild_preservation_archer_alpha.py:127:        strip.alpha_composite(frame, (index * 277, 0))
  tools\art\rebuild_preservation_archer_alpha.py:128:    strip.save(output_dir / f"{stem}_candidate.png", 
optimize=True)
  tools\art\rebuild_preservation_archer_alpha.py:129:    _write_qa(
  tools\art\rebuild_preservation_archer_alpha.py:130:        output_dir,
  tools\art\rebuild_preservation_archer_alpha.py:131:        stem,
  tools\art\rebuild_preservation_archer_alpha.py:132:        f"Archer alpha-safe {'idle' if frames_count == 4 else 
'walk'}",
  tools\art\rebuild_preservation_archer_alpha.py:133:        normalized,
  tools\art\rebuild_preservation_archer_alpha.py:134:        fps,
  tools\art\rebuild_preservation_archer_alpha.py:135:        3 if frames_count == 4 else 4,
  tools\art\rebuild_preservation_archer_alpha.py:136:    )
  tools\art\rebuild_preservation_archer_alpha.py:137:    print(
  tools\art\rebuild_preservation_archer_alpha.py:138:        f"{stem}: key={key}, removed={removed}, 
frames={frames_count}, "
  tools\art\rebuild_preservation_archer_alpha.py:139:        f"gutters={gutters}, cell=277, body=180"
  tools\art\rebuild_preservation_archer_alpha.py:140:    )
  tools\art\rebuild_preservation_archer_alpha.py:141:
  tools\art\rebuild_preservation_archer_alpha.py:142:
  tools\art\rebuild_preservation_archer_alpha.py:143:def main() -> None:
  tools\art\rebuild_preservation_archer_alpha.py:144:    for folder, source_name, stem, frames_count, fps in JOBS:
  tools\art\rebuild_preservation_archer_alpha.py:145:        output_dir = ARCHER / folder
  tools\art\rebuild_preservation_archer_alpha.py:146:        source = output_dir / source_name
  tools\art\rebuild_preservation_archer_alpha.py:147:        if not source.is_file():
  tools\art\rebuild_preservation_archer_alpha.py:148:            raise FileNotFoundError(source)
  tools\art\rebuild_preservation_archer_alpha.py:149:        _write_candidate(output_dir, stem, source, frames_count, 
fps)
  tools\art\rebuild_preservation_archer_alpha.py:150:
  tools\art\rebuild_preservation_archer_alpha.py:151:
  tools\art\rebuild_preservation_archer_alpha.py:152:if __name__ == "__main__":
  tools\art\rebuild_preservation_archer_alpha.py:153:    main()
ref 1844x264



exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "@'
import sys
from pathlib import Path
from PIL import Image
sys.path.insert(0, str(Path('tools/art').resolve()))
from rebuild_preservation_archer_alpha import _connected_key
from build_preservation_walk_candidate import _crop_frames
from build_ledgerbound_warlock import _hard_alpha
src = Path(r'C:\\Users\\asali\\.codex\\generated_images\\01a070c2-ab99-7d00-9ecb-c65724f7f194\\exec-aaefc372-1025-4a8c-
a815-148b63e81da9.png')
out_path = Path(r'C:\\Users\\asali\\Projects\\MMO\\.claude\\worktrees\\epic-panini-3fd8fd\\art_src\\warlock_aw_n_2026-0
9-05\\warlock__attack_walk__n\\warlock_attack_walk_n_row.png')
keyed, key, removed = _connected_key(Image.open(src).convert('RGBA'))
keyed = _hard_alpha(keyed)
frames, edges, gutters = _crop_frames(keyed, 6)
boxes = [frame.getbbox() for frame in frames]
if any(box is None for box in boxes):
    raise ValueError('empty frame')
first = boxes[0]
scale = 205.0 / float(first[3] - first[1])
width, height, baseline = 1844, 264, 237
result = Image.new('RGBA', (width, height), (0, 255, 0, 255))
heights = []
widths = []
for i, (frame, box) in enumerate(zip(frames, boxes)):
    figure = frame.crop(box)
    size = (max(1, round(figure.width * scale)), max(1, round(figure.height * scale)))
    figure = _hard_alpha(figure.resize(size, Image.Resampling.LANCZOS))
    x0 = round(i * width / 6)
    x1 = round((i + 1) * width / 6)
    x = x0 + ((x1 - x0) - figure.width) // 2
    y = baseline - figure.height
    if x <= x0 or x + figure.width >= x1 or y <= 0 or baseline >= height:
        raise ValueError(f'frame {i + 1} does not fit: {figure.size} at {(x,y)} in {(x0,x1)}')
    result.alpha_composite(figure, (x, y))
    heights.append(figure.height)
    widths.append(figure.width)
out_path.parent.mkdir(parents=True, exist_ok=True)
result.convert('RGB').save(out_path, optimize=True)
print(f'key={key} removed={removed} gutters={gutters}')
print(f'wrote {out_path} size={result.size} baseline={baseline} heights={heights} widths={widths}')
'@ | python -" in C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd
 succeeded in 12343ms:
key=(4, 236, 4) removed=1379073 gutters=[168, 160, 161, 162, 170]
wrote C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\warlock_aw_n_2026-09-05\warlock__attack_
walk__n\warlock_attack_walk_n_row.png size=(1844, 264) baseline=237 heights=[205, 204, 205, 205, 205, 205] 
widths=[118, 118, 127, 126, 120, 120]

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "@'
from pathlib import Path
from PIL import Image
import numpy as np
p=Path(r'art_src\\warlock_aw_n_2026-09-05\\warlock__attack_walk__n\\warlock_attack_walk_n_row.png')
im=Image.open(p).convert('RGB')
a=np.asarray(im)
g=(a[:,:,0]==0)&(a[:,:,1]==255)&(a[:,:,2]==0)
fg=~g
cell_edges=[round(i*im.width/6) for i in range(7)]
boxes=[]
for i in range(6):
    ys,xs=np.nonzero(fg[:,cell_edges[i]:cell_edges[i+1]])
    boxes.append((cell_edges[i]+int(xs.min()),int(ys.min()),cell_edges[i]+int(xs.max())+1,int(ys.max())+1))
print('size', im.size, 'mode', im.mode, 'corners', [im.getpixel(q) for q in 
((0,0),(im.width-1,0),(0,im.height-1),(im.width-1,im.height-1))])
print('boxes', boxes)
print('bottoms', [b[3] for b in boxes], 'cell_margins', [(b[0]-cell_edges[i],cell_edges[i+1]-b[2]) for i,b in 
enumerate(boxes)])
print('top_margin_exact_green', bool(g[:30].all()), 'bottom_margin_exact_green', bool(g[237:].all()))
'@ | python -" in C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd
 succeeded in 1934ms:
size (1844, 264) mode RGB corners [(0, 255, 0), (0, 255, 0), (0, 255, 0), (0, 255, 0)]
boxes [(94, 32, 212, 237), (402, 33, 520, 237), (705, 32, 832, 237), (1012, 32, 1138, 237), (1323, 32, 1443, 237), 
(1630, 32, 1750, 237)]
bottoms [237, 237, 237, 237, 237, 237] cell_margins [(94, 95), (95, 95), (90, 90), (90, 91), (94, 94), (93, 94)]
top_margin_exact_green True bottom_margin_exact_green True

codex
Saved [warlock_attack_walk_n_row.png](C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\warlock_
aw_n_2026-09-05\warlock__attack_walk__n\warlock_attack_walk_n_row.png).

- Frame 1: right leg leads; free hand relaxed, beginning to lift.
- Frame 2: feet nearly even, left slightly leading; hand bent at chest.
- Frame 3: left leg leads; open hand thrusting forward, fingers splayed.
- Frame 4: left leg leads; firing hand at peak forward extension.
- Frame 5: left leg leads; hand retracting at chest.
- Frame 6: right leg leads; hand returned to rest.

No projectile or attack effect was drawn. The floor-length hem remains closed throughout. Final output is 1844├ù264 
with exact solid `#00ff00`, six centered figures, and a shared baseline.

Created with the built-in ImageGen edit workflow. Final prompt focused solely on lowering and foreshortening the 
free-hand thrust while preserving the referenced gait, equipment, companion, closed robe, and projectile-free sprite.
tokens used
76,826
Saved [warlock_attack_walk_n_row.png](C:\Users\asali\Projects\MMO\.claude\worktrees\epic-panini-3fd8fd\art_src\warlock_aw_n_2026-09-05\warlock__attack_walk__n\warlock_attack_walk_n_row.png).

- Frame 1: right leg leads; free hand relaxed, beginning to lift.
- Frame 2: feet nearly even, left slightly leading; hand bent at chest.
- Frame 3: left leg leads; open hand thrusting forward, fingers splayed.
- Frame 4: left leg leads; firing hand at peak forward extension.
- Frame 5: left leg leads; hand retracting at chest.
- Frame 6: right leg leads; hand returned to rest.

No projectile or attack effect was drawn. The floor-length hem remains closed throughout. Final output is 1844├ù264 with exact solid `#00ff00`, six centered figures, and a shared baseline.

Created with the built-in ImageGen edit workflow. Final prompt focused solely on lowering and foreshortening the free-hand thrust while preserving the referenced gait, equipment, companion, closed robe, and projectile-free sprite.
