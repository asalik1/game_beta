You are generating ONE game sprite animation master with your built-in image_gen tool. Read $CODEX_HOME/skills/.system/imagegen/SKILL.md first, then follow this brief exactly. Work only inside the current directory (the staging dir). Do NOT touch any file under C:\Users\asali\Projects\MMO.

One image is attached to this prompt:
- Image 1 = refs/01_style_fangmaw_frame.png : a STYLE reference only — another quadruped beast boss from THIS SAME GAME. Use it to bind the ART STYLE, the crisp dark-fantasy PIXEL-ART treatment, the muted desaturated palette, the SIDE-PROFILE camera angle, and the body scale. Match that pixel-art rendering EXACTLY. Do NOT copy its identity: the subject below is a DIFFERENT creature — do not give it Image 1's exposed bone ribs, back spikes, collar, chains, or red eyes.

TASK: generate exactly ONE new 2x2 source sheet for the IDLE (breathing loop) animation of the creature described below, with this image_gen prompt (adapt only if the tool's parameters require it; keep every constraint):

---
Use case: illustration-story
Asset type: high-resolution 4-frame game sprite animation source, to be cropped into a horizontal sprite strip. Square output canvas.
Input images: Image 1 is the STYLE, pixel-art treatment, palette, side-profile camera angle and body-scale reference only — NOT the identity.
Scene/backdrop: a perfectly flat uniform #00ff00 chroma-key background filling every pixel, including the seams between cells. No scenery, floor plane, ground, puddle, platform, cast shadow, contact shadow, gradient, vignette, border, grid, line, or panel divider.
Primary request: create exactly four separate equal square sprite cells as a clean 2x2 contact sheet in reading order top-left, top-right, bottom-left, bottom-right. The source will be cropped into a horizontal strip. All four cells have identical scale and framing; no cell may overlap another; no drawn separators; leave a broad band of flat green between the two columns and between the two rows.
Subject: THE FIRST HOWL — a colossal, ancient, primeval dire-wolf, the first thing that ever hunted, far larger and heavier than any living wolf. A full thick shaggy pelt of weathered grey and bone-white streaked with dried fen-mud; massive shoulders and a heavy lowered head; old scars, a torn ear; pale, flat eyes like moonlight on still water, NOT glowing. A low, coiled, watchful prowling stance, shown in strict SIDE PROFILE (facing left), all four legs visible, the whole beast reading instantly as a WOLF. The same character in every cell — same body, markings, proportions and palette.
Style/medium: crisp high-resolution dark-fantasy PIXEL ART with hard readable pixel clusters and a controlled muted palette, matching Image 1's treatment exactly; NOT painterly, NOT photorealistic, NOT 3D, NOT anime, NOT a smooth digital painting.
Composition/framing and ANCHOR RULE: the wolf is centred in each square cell in side profile, its body about 74% of the cell height, with generous green margin on every side. It stays in the SAME spot in every cell — its head and its four feet sit at the same coordinates in every frame; nothing slides sideways, the camera never moves. Only the parts named in the storyboard move.
IDLE (breathing loop) storyboard - four poses of THIS wolf, side profile:
 frame 1 (top-left) a restrained standing prowl idle: neutral rest, head low, weight coiled
 frame 2 (top-right) a slow inhale — ribcage and shoulders lift a few pixels, the shaggy pelt and tail settle
 frame 3 (bottom-left) neutral rest again with a faint sway, a small shift of the low-hanging belly fur and tail
 frame 4 (bottom-right) a slow exhale, everything settling back to the rest pose
Keep the pose, silhouette and palette continuous and centred — this is a slow breathing loop of a huge wolf at rest, not four different poses; nothing slides sideways; the legs stay planted.
Constraints: output only the four cells on green; no text, watermark, UI, extra characters, scenery, floor artifact, shadow, or separators. Do not use #00ff00 or any bright/pure green anywhere on the wolf. Keep the wolf identical across all four frames — same silhouette, markings and palette. Frames 1, 2, 3 and 4 must each differ subtly (breathing), never slide sideways.
---

AFTER generation:
1. Copy the raw generated PNG (do not resize it) to ./idle_master_2x2_v1.png in the current directory.
2. Run the skill's remove_chroma_key.py helper on it and write the keyed RGBA result to ./idle_master_2x2_v1_keyed.png (auto-key sampling, despill on).
3. Do NOT slice, do NOT install, do NOT write anywhere under the MMO project. The caller will slice + install.
4. In your final message report: the raw output size in pixels, whether all four cells were produced with a huge SIDE-PROFILE wolf in crisp pixel art (frame by frame), and any deviation from the brief you noticed (painterly instead of pixel-art, front-facing instead of side profile, the wolf copying Image 1's bone-ribs/spikes, poses sliding/resizing, an effect touching an edge, extra limbs, wrong facing, etc.). Be blunt about defects; do not soften them.

If image_gen is unavailable, say so and stop; do not fall back to another provider or the CLI.
